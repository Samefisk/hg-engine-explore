#include "../../include/overworld_wild_spawns_internal.h"
#include "../../include/overworld_wild_behavior_data.h"

#include "../../include/config.h"

#ifdef IMPLEMENT_OVERWORLD_WILD_SPAWNS

#include "../../include/constants/buttons.h"
#include "../../include/constants/file.h"
#include "../../include/constants/save.h"
#include "../../include/constants/sndseq.h"
#include "../../include/constants/species.h"
#include "../../include/map_events_internal.h"
#include "../../include/map_teleport.h"
#include "../../include/overlay.h"
#include "../../include/overworld_wild_helper.h"
#include "../../include/overworld_wild_movement.h"
#include "../../include/pokemon.h"
#include "../../include/pokemon_storage_system.h"
#include "../../include/save.h"
#include "../../include/script.h"
#include "../../include/sound.h"
#include "../../include/sprite.h"
#include "../../include/task.h"

#ifndef TYPE_NORMAL
#define TYPE_NORMAL 0
#endif
#ifndef TYPE_FIGHTING
#define TYPE_FIGHTING 1
#endif
#ifndef TYPE_FLYING
#define TYPE_FLYING 2
#endif
#ifndef TYPE_POISON
#define TYPE_POISON 3
#endif
#ifndef TYPE_GROUND
#define TYPE_GROUND 4
#endif
#ifndef TYPE_ROCK
#define TYPE_ROCK 5
#endif
#ifndef TYPE_BUG
#define TYPE_BUG 6
#endif
#ifndef TYPE_GHOST
#define TYPE_GHOST 7
#endif
#ifndef TYPE_STEEL
#define TYPE_STEEL 8
#endif
#ifndef TYPE_FAIRY
#define TYPE_FAIRY 9
#endif
#ifndef TYPE_FIRE
#define TYPE_FIRE 10
#endif
#ifndef TYPE_WATER
#define TYPE_WATER 11
#endif
#ifndef TYPE_GRASS
#define TYPE_GRASS 12
#endif
#ifndef TYPE_ELECTRIC
#define TYPE_ELECTRIC 13
#endif
#ifndef TYPE_PSYCHIC
#define TYPE_PSYCHIC 14
#endif
#ifndef TYPE_ICE
#define TYPE_ICE 15
#endif
#ifndef TYPE_DRAGON
#define TYPE_DRAGON 16
#endif
#ifndef TYPE_DARK
#define TYPE_DARK 17
#endif
#ifndef TYPE_TYPELESS
#define TYPE_TYPELESS 18
#endif
#ifndef TYPE_STELLAR
#define TYPE_STELLAR 19
#endif

void LONG_CALL GX_EngineAToggleLayers(int planes, int status);

typedef void (*OverworldWildMapObjectMovementFunc)(LocalMapObject *object);

#define OW_WILD_HEADBUTT_TREE_TOPS_MAX_LOCATIONS 512
#define OW_WILD_HEADBUTT_SPAWN_CHANCE_PERCENT 10
#define OW_WILD_HEADBUTT_REFILL_ATTEMPT_COOLDOWN 10
#define OW_WILD_REFILL_COOLDOWN_STEPS 1
#define OW_WILD_PLAYER_BALL_PERSONAL_RECORD_SIZE 28
#define OW_WILD_PLAYER_BALL_PERSONAL_TYPE_1_OFFSET 6
#define OW_WILD_PLAYER_BALL_PERSONAL_TYPE_2_OFFSET 7
#define OW_WILD_PLAYER_BALL_PERSONAL_CATCH_RATE_OFFSET 8
#define OW_WILD_PLAYER_BALL_CAPTURE_DESTINATION_PARTY (-1)
#define OW_WILD_PLAYER_BALL_CAPTURE_DESTINATION_NONE (-2)
#define OW_WILD_PLAYER_BALL_PC_STORAGE_SAVE_BLOCK 41
#define OW_WILD_FISHING_SPAWN_CHANCE_PERCENT 20
#define OW_WILD_FISHING_REFILL_ATTEMPT_COOLDOWN 4
#define OW_WILD_SPAWN_MIN_DISTANCE 4
#define OW_WILD_SPAWN_MAX_DISTANCE 8
#define OW_WILD_PLAYER_RELATIVE_SPAWN_MIN_DISTANCE 1
#define OW_WILD_PLAYER_RELATIVE_SPAWN_MAX_DISTANCE OW_WILD_SPAWN_MAX_DISTANCE
#define OW_WILD_DESPAWN_DISTANCE 14
#define OW_WILD_DESPAWN_CULL_DISTANCE (OW_WILD_DESPAWN_DISTANCE + 2)
#define OW_WILD_SPAWN_MIN_MON_DISTANCE 3
#define OW_WILD_AMBIENT_CRY_MIN_COOLDOWN_STEPS 48
#define OW_WILD_AMBIENT_CRY_RANDOM_COOLDOWN_STEPS 96
#define OW_WILD_AMBIENT_CRY_MAX_COOLDOWN_TICK 4
#define OW_WILD_OBJECT_ID_START 0xE0
#define OW_WILD_MANKEY_TREE_TOP_PROXY_OBJECT_ID_START 0xB0
#define OW_WILD_PHANTOM_TELEPORT_FLICKER_OBJECT_ID_START 0xC0
#define OW_WILD_PHANTOM_FLICKER_OBJECT_ID_START 0xD0
#define OW_WILD_FOLLOWER_OBJECT_ID 253
#define OW_WILD_VAR_SPECIAL_LAST_TALKED 0x800D
#define OW_WILD_FLEE_GRACE_STEPS 3
#define OW_WILD_TILE_ENCOUNTER_GRASS 2
#define OW_WILD_TILE_LONG_GRASS 3
#define OW_WILD_TILE_HEADBUTT 6
#define OW_WILD_TILE_LEDGE_EAST 56
#define OW_WILD_TILE_LEDGE_WEST 57
#define OW_WILD_TILE_LEDGE_NORTH 58
#define OW_WILD_TILE_LEDGE_SOUTH 59
#define OW_WILD_STEP_DIAGNOSTIC_ENTRY_ONLY 0
#define OW_WILD_STEP_DIAGNOSTIC_UPDATE_ONLY 0
#define OW_WILD_STEP_DIAGNOSTIC_DROP_STALE_ONLY 0
#define OW_WILD_STEP_DIAGNOSTIC_DESPAWN_ONLY 0
#define OW_WILD_STEP_DIAGNOSTIC_BATTLE_ONLY 0
#define OW_WILD_STEP_DIAGNOSTIC_SKIP_AMBIENT_CRY 1
#define OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_PARAM_TICK 1
#define OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_COORD_READ 1
#define OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_LOOK_COMMAND 1
#define OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_BLOCKED_CHECK 1
#define OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_WALK_COMMAND 1
#define OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_UPDATE_COMMAND 1
#define OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_BURST_UPDATE 0
#define OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK 1
#define OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_IDLE_OBJECT_MOVEMENT 1
#define OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_TRACE 0
#define OW_WILD_SPAWNER_PERF_DIAGNOSTICS 0
#define OW_WILD_SPAWNER_IMMEDIATE_AI_AFTER_COMMAND_COMPLETION 1
#define OW_WILD_SPAWNER_PROFILE_CACHE_FAST_HIT 1
#define OW_WILD_SPAWNER_SINGLE_PASS_WANDER_VALIDATION 1
#define OW_WILD_SPAWNER_MOVEMENT_DECISION_COOLDOWN 0
#define OW_WILD_SPAWNER_MOVEMENT_RANGE 32
#define OW_WILD_SPAWNER_MOVEMENT_BURST_UPDATE_STEPS 32
#define OW_WILD_SPAWNER_MOVEMENT_FRAME_TASK_PRIORITY 1300
#define OW_WILD_SPAWNER_MOVEMENT_FRAME_DECISION_INTERVAL 1
#define OW_WILD_SPAWNER_MOVEMENT_MAX_DIRECTIONS 4
#define OW_WILD_SPAWNER_UNTANGLE_MAX_DIRECTIONS 4
#define OW_WILD_SPAWNER_HOP_PLAN_MAX_DIRECTIONS 8
#define OW_WILD_SPAWNER_HOP_PLAN_MAX_HOPS 5
#define OW_WILD_SPAWNER_HOP_PLAN_NODE_COUNT 64
#define OW_WILD_SPAWNER_BATTLE_SETTLE_FRAMES 16
#define OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot) (1u << (slot))
#define OW_WILD_SPAWNER_FRAME_WORK_HEAVY_SHIFT 16
#define OW_WILD_SPAWNER_FRAME_WORK_GLOBAL 0x80000000
#define OW_WILD_PLAYER_STEP_MAINTENANCE_NONE 0
#define OW_WILD_PLAYER_STEP_MAINTENANCE_QUEUED 1
#define OW_WILD_PLAYER_STEP_MAINTENANCE_DESPAWN 2
#define OW_WILD_PLAYER_STEP_MAINTENANCE_REFILL 3
#define OW_WILD_PLAYER_STEP_MAINTENANCE_REVEAL 4
#define OW_WILD_PLAYER_STEP_MAINTENANCE_PHASE_MASK 0x0F
#define OW_WILD_PLAYER_STEP_MAINTENANCE_DIRTY 0x80
#define OW_WILD_SPAWNER_MOVEMENT_LOOK_UP_COMMAND 0x00
#define OW_WILD_SPAWNER_MOVEMENT_SPEED_1 1
#define OW_WILD_SPAWNER_MOVEMENT_SPEED_2 2
#define OW_WILD_SPAWNER_MOVEMENT_SPEED_3 3
#define OW_WILD_SPAWNER_MOVEMENT_SPEED_4 4
#define OW_WILD_SPAWNER_MOVEMENT_SPEED_DEFAULT 1
#define OW_WILD_SPAWNER_MOVEMENT_SPEED_1_COMMAND 0x08
#define OW_WILD_SPAWNER_MOVEMENT_SPEED_2_COMMAND 0x0C
#define OW_WILD_SPAWNER_MOVEMENT_SPEED_3_COMMAND 0x10
#define OW_WILD_SPAWNER_MOVEMENT_SPEED_4_COMMAND 0x14
#define OW_WILD_SPAWNER_MOVEMENT_SPEED_1_STEP_FRAMES 16
#define OW_WILD_SPAWNER_MOVEMENT_SPEED_2_STEP_FRAMES 8
#define OW_WILD_SPAWNER_MOVEMENT_SPEED_3_STEP_FRAMES 4
#define OW_WILD_SPAWNER_MOVEMENT_SPEED_4_STEP_FRAMES OW_WILD_SPAWNER_MOVEMENT_SPEED_3_STEP_FRAMES
#define OW_WILD_SPAWNER_MOVEMENT_SPEED_3_DECISION_INTERVAL 2
#define OW_WILD_SPAWNER_MOVEMENT_SPEED_4_DECISION_INTERVAL 1
#define OW_WILD_SPAWNER_REAL_FRAMES_PER_LOGICAL_TICK 2
#define OW_WILD_SPAWNER_REAL_FRAMES_TO_LOGICAL_TICKS(frames) \
    (((frames) + OW_WILD_SPAWNER_REAL_FRAMES_PER_LOGICAL_TICK - 1) \
        / OW_WILD_SPAWNER_REAL_FRAMES_PER_LOGICAL_TICK)
#define OW_WILD_SPAWNER_MOVEMENT_LEDGE_JUMP_COMMAND 0x38
#define OW_WILD_SPAWNER_CANOPY_HOPPER_JUMP_1_COMMAND 0x34
#define OW_WILD_SPAWNER_CANOPY_HOPPER_JUMP_2_COMMAND 0x38
#define OW_WILD_SPAWNER_CANOPY_HOPPER_TRAVEL_COMMAND OW_WILD_SPAWNER_CANOPY_HOPPER_JUMP_1_COMMAND
#define OW_WILD_SPAWNER_CANOPY_HOPPER_WAIT_JUMP_LEFT_2_COMMAND 0x5E
#define OW_WILD_SPAWNER_CANOPY_HOPPER_WAIT_JUMP_RIGHT_2_COMMAND 0x5F
#define OW_WILD_SPAWNER_CANOPY_HOPPER_LOCK_DIR_COMMAND 0x47
#define OW_WILD_SPAWNER_CANOPY_HOPPER_RELEASE_DIR_COMMAND 0x48
#define OW_WILD_SPAWNER_CANOPY_HOPPER_PARTNER_PREP_COMMAND 0x49
#define OW_WILD_SPAWNER_CANOPY_HOPPER_FREEZE_COMMAND 0x3E
#define OW_WILD_SPAWNER_CANOPY_HOPPER_PARTNER_RESTORE_COMMAND 0x4A
#define OW_WILD_SPAWNER_CANOPY_HOPPER_MOVEMENT_END_COMMAND 0xFE
#define OW_WILD_SPAWNER_CUSTOM_JUMP_COMMAND OW_WILD_SPAWNER_CANOPY_HOPPER_JUMP_1_COMMAND
#define OW_WILD_SPAWNER_CUSTOM_JUMP_DELTA_FX32 0x2000
#define OW_WILD_SPAWNER_CUSTOM_JUMP_ARC_HEIGHT_FX32 OW_WILD_SPAWNER_TILE_FX32
#define OW_WILD_SPAWNER_CUSTOM_JUMP_TYPE 3
#define OW_WILD_SPAWNER_CUSTOM_JUMP_ARC_TABLE 0
#define OW_WILD_SPAWNER_CUSTOM_JUMP_ARC_FULL 0x1000
#define OW_WILD_SPAWNER_CUSTOM_JUMP_FRAMES_PER_TILE 4
#define OW_WILD_SPAWNER_PIDGEY_MOVEMENT_SPEED 3
#define OW_WILD_SPAWNER_SPOT_RANGE 3
#define OW_WILD_SPAWNER_SWARM_MAX_EXTRA_SPAWNS 3
#define OW_WILD_SPAWNER_HELP_SPAWN_FRAME_GAP 1
#define OW_WILD_SPAWNER_CIRCLE_PLAYER_MAX_RADIUS 8
#define OW_WILD_SPAWNER_CLOSE_ALERT_RADIUS 1
#define OW_WILD_SPAWNER_SPOT_EMOTE_PARTNER_PREP_COMMAND 0x49
#define OW_WILD_SPAWNER_SPOT_EMOTE_JUMP_SITE_COMMAND 0x30
#define OW_WILD_SPAWNER_SPOT_EMOTE_FREEZE_COMMAND 0x3E
#define OW_WILD_SPAWNER_SPOT_EMOTE_PARTNER_RESTORE_COMMAND 0x4A
#define OW_WILD_SPAWNER_ALERT_TIME_AUTO 0
#define OW_WILD_SPAWNER_SPOT_EMOTE_FRAMES_PER_JUMP 64
#define OW_WILD_SPAWNER_SPOT_EMOTE_SPEECH_FRAMES 10
#define OW_WILD_SPAWNER_THROW_RESERVATION_DECISIONS 120
#define OW_WILD_SPAWNER_THROW_WINDUP_FRAMES 20
#define OW_WILD_SPAWNER_THROW_RECOVERY_FRAMES 60
#define OW_WILD_SPAWNER_SPOT_EMOTE_JUMPS_DEFAULT 1
#define OW_WILD_SPAWNER_SPOT_EMOTE_JUMPS_SPEED_3 2
#define OW_WILD_SPAWNER_SPOT_EMOTE_SE SEQ_SE_GS_UFO_JUMP
#define OW_WILD_SPAWNER_HOP_START_SE SEQ_SE_DP_DANSA
#define OW_WILD_SPAWNER_CANOPY_HOP_RUSTLE_SE SEQ_SE_DP_FPASA2
#define OW_WILD_SPAWNER_CANOPY_HOP_BALLOON_SE SEQ_SE_PL_BALLOON03
#define OW_WILD_SPAWNER_CANOPY_HOP_SOUND_VARIANTS 2
#define OW_WILD_SPAWNER_HOP_START_SE_SUPPRESS_FRAMES 4
#define OW_WILD_MAP_OBJECT_UNPAUSE_MOVEMENT ((OverworldWildMapObjectMovementFunc)0x0205F709)
#define OW_WILD_SPAWNER_TIRED_AFTER_STEPS 5
#define OW_WILD_SPAWNER_TIRED_USE_FOLLOWER_BUBBLE 0
#define OW_WILD_SPAWNER_TIRED_USE_DIRECT_BUBBLE_CREATOR 1
#define OW_WILD_SPAWNER_TIRED_STOP_BUBBLE_SE 0
#define OW_WILD_SPAWNER_TIRED_PLAY_COOLDOWN_SE 0
#define OW_WILD_SPAWNER_BUBBLE_ID_HEART 0
#define OW_WILD_SPAWNER_BUBBLE_ID_SMILE 1
#define OW_WILD_SPAWNER_BUBBLE_ID_ANGRY 2
#define OW_WILD_SPAWNER_BUBBLE_ID_SAD 3
#define OW_WILD_SPAWNER_BUBBLE_ID_MILDLY_HAPPY 4
#define OW_WILD_SPAWNER_BUBBLE_ID_DISAPPROVAL 5
#define OW_WILD_SPAWNER_BUBBLE_ID_MUSIC_NOTE 6
#define OW_WILD_SPAWNER_BUBBLE_ID_QUESTION_MARK 7
#define OW_WILD_SPAWNER_BUBBLE_ID_EXCLAMATION_MARK 8
#define OW_WILD_SPAWNER_BUBBLE_ID_WATER_DROPLET 9
#define OW_WILD_SPAWNER_BUBBLE_ID_DESPAIR 10
#define OW_WILD_SPAWNER_BUBBLE_ID_POISON 11
#define OW_WILD_SPAWNER_BUBBLE_ID_ELLIPSIS 12
#define OW_WILD_SPAWNER_BUBBLE_ID_SLEEP 13
#define OW_WILD_SPAWNER_TIRED_BUBBLE_ID_PROBE 0
#define OW_WILD_SPAWNER_TIRED_BUBBLE_ID_PROBE_MIN OW_WILD_SPAWNER_BUBBLE_ID_HEART
#define OW_WILD_SPAWNER_TIRED_BUBBLE_ID_PROBE_MAX OW_WILD_SPAWNER_BUBBLE_ID_SLEEP
#define OW_WILD_SPAWNER_TIRED_BUBBLE_ID OW_WILD_SPAWNER_BUBBLE_ID_WATER_DROPLET
#define OW_WILD_SPAWNER_TIRED_EMOTE_COMMAND 0x65
#define OW_WILD_SPAWNER_TIRED_EMOTE_FRAMES 96
#define OW_WILD_SPAWNER_TIRED_SPOT_COOLDOWN_FRAMES 180
#define OW_WILD_SPAWNER_FLEE_TIRED_REST_TIME 4
#define OW_WILD_SPAWNER_ASLEEP_REST_TIMER 255
#define OW_WILD_SPAWNER_TIRED_WANDER_PAUSE_FRAMES 24
#define OW_WILD_SPAWNER_TIRED_EMOTE_SE SEQ_SE_PL_BALLOON05
#define OW_WILD_SPAWNER_DEFAULT_MOVEMENT_PAUSE_FRAMES 32
#define OW_WILD_SPAWNER_MOVEMENT_TARGET_MAX 6
#define OW_WILD_SPAWNER_PHANTOM_STALK_TELEPORT_DISTANCE 5
#define OW_WILD_SPAWNER_PHANTOM_STALK_TELEPORT_MOVE_DISTANCE 6
#define OW_WILD_SPAWNER_PHANTOM_STALK_CHILL_TELEPORT_MIN_DISTANCE 3
#define OW_WILD_SPAWNER_PHANTOM_STALK_CHILL_TELEPORT_MAX_DISTANCE 6
#define OW_WILD_SPAWNER_PHANTOM_STALK_FLICKER_CHANCE_PERCENT 100
#define OW_WILD_SPAWNER_PHANTOM_STALK_FLICKER_FRAMES 18
#define OW_WILD_SPAWNER_PHANTOM_STALK_TELEPORT_FLICKER_FRAMES 18
#define OW_WILD_SPAWNER_PHANTOM_STALK_TELEPORT_MOVE_FRAMES (OW_WILD_SPAWNER_PHANTOM_STALK_TELEPORT_FLICKER_FRAMES / 2)
#define OW_WILD_SPAWNER_PHANTOM_STALK_TELEPORT_MOVE_PHASE_FRAMES 2
#define OW_WILD_SPAWNER_PHANTOM_STALK_POST_TELEPORT_COOLDOWN_FRAMES 30
#define OW_WILD_SPAWNER_PHANTOM_STALK_FAIL_COOLDOWN_FRAMES 8
#define OW_WILD_SPAWNER_PHANTOM_STALK_FLICKER_VISIBLE_FRAMES 5
#define OW_WILD_SPAWNER_PHANTOM_STALK_FLICKER_HIDDEN_FRAMES 2
#define OW_WILD_SPAWNER_PHANTOM_STALK_ACTIVE_VISIBLE_FRAMES 3
#define OW_WILD_SPAWNER_PHANTOM_STALK_ACTIVE_HIDDEN_FRAMES 2
#define OW_WILD_SPAWNER_PHANTOM_STALK_ACTIVE_FLICKER_FRAMES \
    (OW_WILD_SPAWNER_PHANTOM_STALK_ACTIVE_VISIBLE_FRAMES + OW_WILD_SPAWNER_PHANTOM_STALK_ACTIVE_HIDDEN_FRAMES)
#define OW_WILD_SPAWNER_PHANTOM_STALK_ACTIVE_REAL_FLICKER 0
#define OW_WILD_SPAWNER_PHANTOM_STALK_RANGE (OW_WILD_SPAWNER_MOVEMENT_RANGE * 2)
#define OW_WILD_SPAWNER_PREVIOUS_TILE_NONE -1
#define OW_WILD_SPAWNER_ONIX_RAM_ALERTNESS 14
#define OW_WILD_SPAWNER_ONIX_RAM_START_SPEED 2
#define OW_WILD_SPAWNER_ONIX_RAM_RANGE 16
#define OW_WILD_SPAWNER_ONIX_RAM_SPEED_UP_TILES 3
#define OW_WILD_SPAWNER_ONIX_RAM_MAX_SPEED OW_WILD_SPAWNER_MOVEMENT_SPEED_4
#define OW_WILD_SPAWNER_ONIX_RAM_CRASH_SE SEQ_SE_DP_WALL_HIT
#define OW_WILD_SPAWNER_ONIX_RAM_STEP_SE SEQ_SE_GS_IWAOTOSHI02
#define OW_WILD_SPAWNER_CANOPY_HOPPER_RANGE 16
#define OW_WILD_SPAWNER_CANOPY_SHIFT_SOUTH_LAND_ANCHORS 0
#define OW_WILD_SPAWNER_CANOPY_SOUTH_LAND_SHIFT_TILES 1
#define OW_WILD_SPAWNER_CANOPY_SHIFT_NORTH_LAND_ANCHORS 0
#define OW_WILD_SPAWNER_CANOPY_NORTH_LAND_SHIFT_TILES 1
#define OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_VISIBILITY_BASELINE 0
#define OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_SINGLE_JUMP_PROBE 0
#define OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_JUMP2_PROBE 0
#define OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_CHAINED_JUMP2_PROBE 0
#define OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_CHAINED_JUMP2_NO_MIDPOINT_NORMALIZE_PROBE 0
#define OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_CHAINED_JUMP2_LOGICAL_MIDPOINT_PROBE 0
#define OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_WRAPPED_JUMP2_PROBE 0
#define OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_WRAPPED_CHAINED_JUMP2_PROBE 0
#define OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_SINGLE_WRAPPER_CHAINED_JUMP2_PROBE 0
#define OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_PARTNER_PREPPED_INTERNAL_JUMP_PROBE 0
#define OW_WILD_SPAWNER_CUSTOM_JUMP_ALLOW_DIAGONAL 1
#define OW_WILD_SPAWNER_CUSTOM_JUMP_RENDER_OFFSET 1
#define OW_WILD_SPAWNER_CUSTOM_JUMP_RENDER_MAX_SECONDARY_TILES 8
#define OW_WILD_SPAWNER_CUSTOM_JUMP_SYNC_LOGICAL_SECONDARY 1
#define OW_WILD_SPAWNER_CUSTOM_JUMP_POST_RESTORE_FINALIZE 1
#define OW_WILD_SPAWNER_CUSTOM_JUMP_RENDER_SETTLE 1
#define OW_WILD_SPAWNER_CUSTOM_JUMP_POST_LANDING_RENDER_SETTLE 0
#define OW_WILD_SPAWNER_CUSTOM_JUMP_POST_LANDING_RENDER_SETTLE_FRAMES 12
#define OW_WILD_SPAWNER_CUSTOM_JUMP_POST_LANDING_RENDER_SETTLE_ENABLED \
    (OW_WILD_SPAWNER_CUSTOM_JUMP_RENDER_SETTLE \
        && OW_WILD_SPAWNER_CUSTOM_JUMP_POST_LANDING_RENDER_SETTLE \
        && OW_WILD_SPAWNER_CUSTOM_JUMP_POST_LANDING_RENDER_SETTLE_FRAMES != 0)
#define OW_WILD_SPAWNER_CUSTOM_JUMP_VISIBLE_LEGS 0
#define OW_WILD_SPAWNER_CUSTOM_JUMP_RAM_PROBE 0
#define OW_WILD_SPAWNER_CUSTOM_JUMP_RAM_PROBE_MIN_TILES 4
#define OW_WILD_SPAWNER_CUSTOM_JUMP_RAM_PROBE_KEYS \
    (PAD_BUTTON_L | PAD_BUTTON_R)
#define OW_WILD_SPAWNER_CANOPY_HOPPER_MIN_HOP_TILES 3
#define OW_WILD_SPAWNER_CANOPY_HOPPER_AMBUSH_RANGE 8
#define OW_WILD_SPAWNER_CANOPY_HOPPER_MAX_HOP_TILES 8
#define OW_WILD_SPAWNER_MANKEY_HEADBUTT_TREE_TOP_Y_OFFSET 1
#define OW_WILD_SPAWNER_MANKEY_HEADBUTT_TREE_TOP_PRIORITY_BITS 0x00000180
#define OW_WILD_SPAWNER_MANKEY_HEADBUTT_TREE_TOP_DRAW_CALLBACK \
    OverworldWildSpawns_MankeyTreeTopDrawWrapper
#define OW_WILD_SPAWNER_MANKEY_TREE_TOP_LAYER_PROBE_FRAMES 60
#define OW_WILD_SPAWNER_MANKEY_TREE_TOP_LAYER_MASK_2 2
#define OW_WILD_SPAWNER_MANKEY_TREE_TOP_LAYER_MASK_4 4
#define OW_WILD_SPAWNER_MANKEY_TREE_TOP_LAYER_MASK_8 8
#define OW_WILD_SPAWNER_MANKEY_TREE_TOP_LAYER_PROBE_PHASES 4
#define OW_WILD_SPAWNER_MANKEY_TREE_TOP_LAYER_PROBE_ENABLED 0
#define OW_WILD_SPAWNER_MANKEY_TREE_TOP_BUBBLE_PROBE_ENABLED 0
#define OW_WILD_SPAWNER_MANKEY_TREE_TOP_BUBBLE_PROBE_FRAMES 90
#define OW_WILD_SPAWNER_MANKEY_TREE_TOP_BUBBLE_PROBE_ID OW_WILD_SPAWNER_BUBBLE_ID_EXCLAMATION_MARK
#define OW_WILD_SPAWNER_MANKEY_TREE_TOP_EFFECT_MARKER_FRAMES 45
#define OW_WILD_SPAWNER_MANKEY_TREE_TOP_EFFECT_MARKER_ID OW_WILD_SPAWNER_BUBBLE_ID_EXCLAMATION_MARK
#define OW_WILD_SPAWNER_MANKEY_TREE_TOP_LATE_DRAW_EFFECT_ENABLED 0
#define OW_WILD_SPAWNER_MANKEY_TREE_TOP_RENDER_OVERRIDE_SAVE_ENABLED 0
#define OW_WILD_SPAWNER_MANKEY_TREE_TOP_PRIORITY_OVERRIDE_ENABLED 0
#define OW_WILD_SPAWNER_MANKEY_TREE_TOP_DRAW_CALLBACK_OVERRIDE_ENABLED 0
#define OW_WILD_SPAWNER_MANKEY_HEADBUTT_TREE_TOP_PATH_MAX_JUMPS 4
#define OW_WILD_SPAWNER_MANKEY_PATH_FAILURE_BASE_COOLDOWN_FRAMES 60
#define OW_WILD_SPAWNER_MANKEY_PATH_FAILURE_MAX_COOLDOWN_FRAMES 240
#define OW_WILD_SPAWNER_MANKEY_PATH_FAILURE_MAX_COUNT 4
#define OW_WILD_SPAWNER_CANOPY_HOPPER_TREE_ALERT_RADIUS 5
#define OW_WILD_SPAWNER_CANOPY_HOPPER_PRE_HOP_WAIT_FRAMES 30
#define OW_WILD_SPAWNER_CANOPY_HOPPER_SEGMENT_SETTLE_FRAMES 8
#define OW_WILD_SPAWNER_CANOPY_HOPPER_COOLDOWN_FRAMES 40
#define OW_WILD_SPAWNER_CUSTOM_JUMP_MIN_TILES OW_WILD_SPAWNER_CANOPY_HOPPER_MIN_HOP_TILES
#define OW_WILD_SPAWNER_CUSTOM_JUMP_MAX_TILES OW_WILD_SPAWNER_CANOPY_HOPPER_MAX_HOP_TILES
#define OW_WILD_SPAWNER_CANOPY_HOPPER_PATH_RADIUS (OW_WILD_SPAWNER_CANOPY_HOPPER_RANGE - 1)
#define OW_WILD_SPAWNER_CANOPY_HOPPER_PATH_DIAMETER (OW_WILD_SPAWNER_CANOPY_HOPPER_PATH_RADIUS * 2 + 1)
#define OW_WILD_SPAWNER_CANOPY_HOPPER_PATH_NODE_COUNT \
    (OW_WILD_SPAWNER_CANOPY_HOPPER_PATH_DIAMETER * OW_WILD_SPAWNER_CANOPY_HOPPER_PATH_DIAMETER)
#define OW_WILD_SPAWNER_CANOPY_HOPPER_PATH_WORD_COUNT \
    ((OW_WILD_SPAWNER_CANOPY_HOPPER_PATH_NODE_COUNT + 31) / 32)
#define OW_WILD_SPAWNER_CANOPY_HOPPER_MOVEMENT_LIST_WORDS_PER_SEGMENT 2
#define OW_WILD_SPAWNER_CANOPY_HOPPER_MOVEMENT_LIST_WRAPPER_WORDS 6
#define OW_WILD_SPAWNER_CANOPY_HOPPER_PATH_MAX_STEPS \
    ((OW_WILD_STAGED_HOP_MOVEMENT_LIST_WORDS - OW_WILD_SPAWNER_CANOPY_HOPPER_MOVEMENT_LIST_WRAPPER_WORDS - 2) / OW_WILD_SPAWNER_CANOPY_HOPPER_MOVEMENT_LIST_WORDS_PER_SEGMENT)
#define OW_WILD_SPAWNER_FX32_ONE (1 << 12)
#define OW_WILD_SPAWNER_TILE_FX32 (OW_WILD_SPAWNER_FX32_ONE * 16)
#define OW_WILD_SPAWNER_TILE_CENTER_FX32 (OW_WILD_SPAWNER_TILE_FX32 / 2)
#define OW_WILD_SPAWNER_TILE_CENTER_COORD_FX32(tile) \
    ((s32)(tile) * OW_WILD_SPAWNER_TILE_FX32 + OW_WILD_SPAWNER_TILE_CENTER_FX32)
#define OW_WILD_SPAWNER_RAM_CRASH_SHAKE_FRAMES 32
#define OW_WILD_SPAWNER_RAM_CRASH_SHAKE_FX32_AMPLITUDE (OW_WILD_SPAWNER_FX32_ONE / 8)
#define OW_WILD_SPAWNER_WAIT_JUMP_SITE_SET_BITS 0x00010004
#define OW_WILD_SPAWNER_WAIT_JUMP_SITE_CLEAR_BITS 0x00100000
#define OW_WILD_SPAWNER_CUSTOM_JUMP_OWNED_BITS \
    (BIT_JUMP_START | BIT_MOVE_START | MAPOBJECTFLAG_UNK13)
#define OW_WILD_SPAWNER_FOLLOWER_CREATE_SET_BITS 0x00002400
#define OW_WILD_SPAWNER_FOLLOWER_CREATE_CLEAR_BITS 0x00000180
#define OW_WILD_SPAWNER_FORCE_BEHAVIOR_TEST_SPECIES 0
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_TRACE \
    || OW_WILD_SPAWNER_CUSTOM_JUMP_RAM_PROBE
#define OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE volatile
#else
#define OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE
#endif
#define OW_WILD_SPAWNER_BUBBLE_ID_NONE 0xFF
#define OW_WILD_SPAWNER_SPOT_STATE_CHILL 0
#define OW_WILD_SPAWNER_SPOT_STATE_EMOTING 1
#define OW_WILD_SPAWNER_SPOT_STATE_ACTIVE 2
#define OW_WILD_SPAWNER_SPOT_STATE_TIRED 3
#define OW_WILD_SPAWNER_THROW_TARGET_NONE 0
#define OW_WILD_SPAWNER_THROW_TARGET_CARRIED_FLAG 0x80
#define OW_WILD_SPAWNER_THROW_TARGET_WINDUP_FLAG 0x40
#define OW_WILD_SPAWNER_THROW_TARGET_SLOT_MASK 0x3F
#define OW_WILD_SPAWNER_THROW_TARGET_ENCODE(slot) ((u8)((slot) + 1))
#define OW_WILD_SPAWNER_THROW_TARGET_ENCODE_CARRIED(slot) \
    ((u8)(OW_WILD_SPAWNER_THROW_TARGET_CARRIED_FLAG | OW_WILD_SPAWNER_THROW_TARGET_ENCODE(slot)))
#define OW_WILD_SPAWNER_THROW_TARGET_DECODE(value) \
    ((u8)(((value) & OW_WILD_SPAWNER_THROW_TARGET_SLOT_MASK) - 1))
#define OW_WILD_BEHAVIOR_KIND_NONE 0
#define OW_WILD_BEHAVIOR_KIND_IDLE 1
#define OW_WILD_BEHAVIOR_KIND_WANDER 2
#define OW_WILD_BEHAVIOR_KIND_CHASE 3
#define OW_WILD_BEHAVIOR_KIND_FLEE 4
#define OW_WILD_BEHAVIOR_KIND_PLAYFUL 5
#define OW_WILD_BEHAVIOR_KIND_RAM 6
#define OW_WILD_BEHAVIOR_KIND_HEADBUTT_TREE_HOP 7
#define OW_WILD_BEHAVIOR_KIND_ASLEEP 8
#define OW_WILD_BEHAVIOR_KIND_TIRED_EMOTE 10
#define OW_WILD_BEHAVIOR_KIND_NO_VISUAL 11
#define OW_WILD_BEHAVIOR_ALERT_STATE_NONE 0
#define OW_WILD_BEHAVIOR_ALERT_STATE_HOP 1
#define OW_WILD_BEHAVIOR_ALERT_STATE_SPEECH 2
#define OW_WILD_BEHAVIOR_ALERT_SPECIAL_NONE 0
#define OW_WILD_BEHAVIOR_ALERT_SPECIAL_CALL_FOR_HELP 1
#define OW_WILD_BEHAVIOR_ALERT_SPECIAL_PICKUP_THROW 2
#define OW_WILD_BEHAVIOR_BATTLE_TRIGGER_NONE 0
#define OW_WILD_BEHAVIOR_BATTLE_TRIGGER_CONTACT 1
#define OW_WILD_BEHAVIOR_BATTLE_TRIGGER_RAM_CRASH 2
#define OW_WILD_BEHAVIOR_JUMP_LEVEL_NONE 0
#define OW_WILD_BEHAVIOR_JUMP_LEVEL_DOWNHILL 1
#define OW_WILD_BEHAVIOR_JUMP_LEVEL_BOTH 2
#define OW_WILD_BEHAVIOR_PROFILE_DEFAULT 0
#define OW_WILD_BEHAVIOR_LIMIT_KEY_OVERRIDE_BASE OWBD_CLASS_PROFILE_COUNT
#define OW_WILD_BEHAVIOR_SPAWN_STATE_APPEAR 0
#define OW_WILD_BEHAVIOR_SPAWN_STATE_RUN_FROM_OFF_SCREEN 1
#define OW_WILD_BEHAVIOR_SPAWN_STATE_HOP_FROM_OFF_SCREEN 2
#define OW_WILD_BEHAVIOR_SPAWN_STATE_APPEAR_HOP 3
#define OW_WILD_BEHAVIOR_BOOL_NO 0
#define OW_WILD_BEHAVIOR_BOOL_YES 1
#define OW_WILD_BEHAVIOR_ALERT_RANGE_NONE 0
#define OW_WILD_BEHAVIOR_ALERT_RANGE_FACING_LINE 1
#define OW_WILD_BEHAVIOR_ALERT_RANGE_FACING_LINE_CLOSE_RADIUS 2
#define OW_WILD_BEHAVIOR_ALERT_RANGE_CARDINAL_LINE 3
#define OW_WILD_BEHAVIOR_ALERT_RANGE_RADIUS 4
#define OW_WILD_BEHAVIOR_ALERT_RANGE_TERRAIN_ONLY 5
#define OW_WILD_BEHAVIOR_ATTENTIVE_ACTION_NONE 0
#define OW_WILD_BEHAVIOR_ATTENTIVE_ACTION_APPROACH 1
#define OW_WILD_BEHAVIOR_ATTENTIVE_ACTION_FLEE 2
#define OW_WILD_BEHAVIOR_ATTENTIVE_ACTION_EMOTE 3
#define OW_WILD_BEHAVIOR_LOCOMOTION_NONE 0
#define OW_WILD_BEHAVIOR_LOCOMOTION_WANDER 1
#define OW_WILD_BEHAVIOR_LOCOMOTION_HOP 2
#define OW_WILD_BEHAVIOR_LOCOMOTION_RUN_FROM_OFF_SCREEN 3
#define OW_WILD_BEHAVIOR_LOCOMOTION_HOP_FROM_OFF_SCREEN 4
#define OW_WILD_BEHAVIOR_LOCOMOTION_RAM 5
#define OW_WILD_BEHAVIOR_LOCOMOTION_PHANTOM_TELEPORT 6
#define OW_WILD_BEHAVIOR_LOCOMOTION_APPEAR_HOP 7
#define OW_WILD_BEHAVIOR_TARGET_NONE 0
#define OW_WILD_BEHAVIOR_TARGET_RANDOM_NEARBY 1
#define OW_WILD_BEHAVIOR_TARGET_TOWARD_PLAYER 2
#define OW_WILD_BEHAVIOR_TARGET_AWAY_FROM_PLAYER 3
#define OW_WILD_BEHAVIOR_TARGET_TREE_TOP 4
#define OW_WILD_BEHAVIOR_TARGET_PLAYFUL_ORBIT 5
#define OW_WILD_BEHAVIOR_TARGET_PLAYER_FRONT 6
#define OW_WILD_BEHAVIOR_TARGET_PLAYER_CARDINAL_LINE 8
#define OW_WILD_BEHAVIOR_TARGET_CIRCLE_PLAYER 9
#define OW_WILD_BEHAVIOR_ALERT_LOGIC_NONE 0
#define OW_WILD_BEHAVIOR_ALERT_LOGIC_FACING_LINE 1
#define OW_WILD_BEHAVIOR_ALERT_LOGIC_FACING_LINE_CLOSE_RADIUS 2
#define OW_WILD_BEHAVIOR_ALERT_LOGIC_CARDINAL_LINE 3
#define OW_WILD_BEHAVIOR_ALERT_LOGIC_RADIUS 4
#define OW_WILD_BEHAVIOR_ALERT_LOGIC_TERRAIN_ONLY 5
#define OW_WILD_BEHAVIOR_REACTION_NONE 0
#define OW_WILD_BEHAVIOR_REACTION_CONTACT 1
#define OW_WILD_BEHAVIOR_REACTION_FLEE 2
#define OW_WILD_BEHAVIOR_REACTION_CRY 3
#define OW_WILD_BEHAVIOR_REACTION_EMOTE 4
#define OW_WILD_BEHAVIOR_REACTION_TIRED 5
#define OW_WILD_BEHAVIOR_MATCH_ANY_SPECIES SPECIES_NONE
#define OW_WILD_BEHAVIOR_MATCH_ANY_TERRAIN 0xFF
#define OW_WILD_BEHAVIOR_MATCH_ANY_CLASS 0xFF
#define OW_WILD_BEHAVIOR_MATCH_ANY_SHINY 0xFF
#define OW_WILD_BEHAVIOR_MATCH_CLASS_FORCED_ASLEEP 0xFD
#define OW_WILD_BEHAVIOR_MATCH_LEVEL_ANY 0
#define OW_WILD_BEHAVIOR_GROUP_NONE 0
#define OW_WILD_BEHAVIOR_GROUP_BABY (1u << 0)
#define OW_WILD_BEHAVIOR_GROUP_GHOST (1u << 1)
#define OW_WILD_BEHAVIOR_GROUP_TYPE_NORMAL (1u << 2)
#define OW_WILD_BEHAVIOR_GROUP_TYPE_FIGHTING (1u << 3)
#define OW_WILD_BEHAVIOR_GROUP_TYPE_FLYING (1u << 4)
#define OW_WILD_BEHAVIOR_GROUP_TYPE_POISON (1u << 5)
#define OW_WILD_BEHAVIOR_GROUP_TYPE_GROUND (1u << 6)
#define OW_WILD_BEHAVIOR_GROUP_TYPE_ROCK (1u << 7)
#define OW_WILD_BEHAVIOR_GROUP_TYPE_BUG (1u << 8)
#define OW_WILD_BEHAVIOR_GROUP_TYPE_GHOST (1u << 9)
#define OW_WILD_BEHAVIOR_GROUP_TYPE_STEEL (1u << 10)
#define OW_WILD_BEHAVIOR_GROUP_TYPE_FAIRY (1u << 11)
#define OW_WILD_BEHAVIOR_GROUP_TYPE_FIRE (1u << 12)
#define OW_WILD_BEHAVIOR_GROUP_TYPE_WATER (1u << 13)
#define OW_WILD_BEHAVIOR_GROUP_TYPE_GRASS (1u << 14)
#define OW_WILD_BEHAVIOR_GROUP_TYPE_ELECTRIC (1u << 15)
#define OW_WILD_BEHAVIOR_GROUP_TYPE_PSYCHIC (1u << 16)
#define OW_WILD_BEHAVIOR_GROUP_TYPE_ICE (1u << 17)
#define OW_WILD_BEHAVIOR_GROUP_TYPE_DRAGON (1u << 18)
#define OW_WILD_BEHAVIOR_GROUP_TYPE_DARK (1u << 19)
#define OW_WILD_BEHAVIOR_GROUP_TYPE_TYPELESS (1u << 20)
#define OW_WILD_BEHAVIOR_GROUP_TYPE_STELLAR (1u << 21)
#define OW_WILD_SPAWNER_SPOT_EMOTE_STEP_PARTNER_PREP 0
#define OW_WILD_SPAWNER_SPOT_EMOTE_STEP_JUMP 1
#define OW_WILD_SPAWNER_SPOT_EMOTE_STEP_FREEZE 2
#define OW_WILD_SPAWNER_SPOT_EMOTE_STEP_PARTNER_RESTORE 3
#define OW_WILD_SPAWNER_SPOT_EMOTE_STEP_DONE 4
#define OW_WILD_SPAWNER_SPOT_EMOTE_STEP_LOOK_FIRST 5
#define OW_WILD_SPAWNER_SPOT_EMOTE_STEP_LOOK_SECOND 6
#define OW_WILD_SPAWNER_SPOT_EMOTE_STEP_LOOK_RETURN 7
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_IDLE_OBJECT_MOVEMENT
#define OW_WILD_SPAWNER_MOVEMENT_OBJECT_MOVEMENT OW_WILD_MOVE_STOCK_IDLE
#else
#define OW_WILD_SPAWNER_MOVEMENT_OBJECT_MOVEMENT OW_WILD_MOVE_STOCK_WANDER
#endif
#define OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_NONE 0xFF
#define OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_UP 0
#define OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN 1
#define OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_LEFT 2
#define OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT 3
#define OW_WILD_SPAWNER_MOVEMENT_DISTANCE_STEP 1
#define OW_WILD_SPAWNER_MOVEMENT_DISTANCE_LEDGE_JUMP 2
#define OW_WILD_SPAWNER_SPAWN_RUN_MIN_TRAVEL_TILES 4
#define OW_WILD_SPAWNER_SPAWN_RUN_OFFSCREEN_MIN_DISTANCE \
    (OW_WILD_SPAWN_MAX_DISTANCE + OW_WILD_SPAWNER_SPAWN_RUN_MIN_TRAVEL_TILES)
#define OW_WILD_SPAWNER_SPAWN_RUN_OFFSCREEN_MAX_DISTANCE OW_WILD_DESPAWN_DISTANCE
#define OW_WILD_SPAWNER_SPAWN_RUN_LATERAL_SPREAD 4
#define OW_WILD_SPAWNER_SPAWN_RUN_START_ATTEMPTS 12
#define OW_WILD_SPAWNER_SPAWN_HOP_DISTANCE OW_WILD_SPAWNER_CUSTOM_JUMP_MAX_TILES
#define OW_WILD_UPDATE_DIAGNOSTIC_READ_ONLY 0
#define OW_WILD_UPDATE_DIAGNOSTIC_STATE_READ_ONLY 0
#define OW_WILD_UPDATE_DIAGNOSTIC_SETTER_ONLY 0
#define OW_WILD_UPDATE_DIAGNOSTIC_SKIP_CLEAR 1
#define OW_WILD_BEHAVIOR_ALLOWED_TILE_PACK(primary, secondary) \
    (((primary) & 0x0F) | (((secondary) & 0x0F) << 4))
#define OW_WILD_BEHAVIOR_ALLOWED_TILE_PRIMARY(packed) ((packed) & 0x0F)
#define OW_WILD_BEHAVIOR_ALLOWED_TILE_SECONDARY(packed) (((packed) >> 4) & 0x0F)
// Param 2 mirrors the follower palette metadata without switching to follower rendering.
#define OW_WILD_PAL_PARAM_SHINY 1
#define OW_WILD_PAL_PARAM_ENABLE 2
typedef struct OverworldWildBehaviorPrimitives {
    u8 spawnLocomotion;
    u8 chillLocomotion;
    u8 chillTarget;
    u8 alertLogic;
    u8 alertReaction;
    u8 attentiveLocomotion;
    u8 attentiveTarget;
    u8 activeReaction;
    u8 tiredLocomotion;
    u8 tiredTarget;
    u8 tiredReaction;
} OverworldWildBehaviorPrimitives;

typedef enum OverworldWildDirectionStepResult {
    OW_WILD_DIRECTION_STEP_BUSY,
    OW_WILD_DIRECTION_STEP_BLOCKED,
    OW_WILD_DIRECTION_STEP_STARTED,
} OverworldWildDirectionStepResult;

typedef struct OverworldWildDirectionStepContext {
    OverworldWildSpawnState *state;
    FieldSystem *fieldSystem;
    LocalMapObject *object;
    const OverworldWildBehaviorProfile *profile;
    const OverworldWildBehaviorPrimitives *primitives;
    u8 slot;
    u8 allowedTile;
    u8 jumpLevel;
    u8 avoidPreviousTile;
} OverworldWildDirectionStepContext;

typedef struct OverworldWildBehaviorSlotCache {
    OverworldWildBehaviorProfile profile;
    u16 species;
    u16 mapId;
    u8 form;
    u8 level;
    u8 terrain;
    u8 shiny;
    u8 behaviorClass;
    u8 valid;
} OverworldWildBehaviorSlotCache;

typedef struct OverworldWildOverlayRuntimeState {
    u8 movementCustomJumpPrepActive[OW_WILD_MAX_SPAWNS];
    u8 movementCustomJumpActive[OW_WILD_MAX_SPAWNS];
    LocalMapObject *movementEmotePartnerPrepObjects[OW_WILD_MAX_SPAWNS];
    s16 movementCustomJumpStartX[OW_WILD_MAX_SPAWNS];
    s16 movementCustomJumpStartY[OW_WILD_MAX_SPAWNS];
    s16 movementCustomJumpTargetX[OW_WILD_MAX_SPAWNS];
    s16 movementCustomJumpTargetY[OW_WILD_MAX_SPAWNS];
    s32 movementCustomJumpTargetBaseY[OW_WILD_MAX_SPAWNS];
    u16 movementCustomJumpFrameCounts[OW_WILD_MAX_SPAWNS];
    u16 movementCustomJumpElapsedFrames[OW_WILD_MAX_SPAWNS];
    u16 movementCustomJumpSpinElapsedFrames[OW_WILD_MAX_SPAWNS];
    u8 movementCustomJumpSpinSpeeds[OW_WILD_MAX_SPAWNS];
    u8 movementCustomJumpSpinTimers[OW_WILD_MAX_SPAWNS];
    u8 movementCustomJumpSpinSteps[OW_WILD_MAX_SPAWNS];
    s16 playerBallShadowTileX;
    s16 playerBallShadowTileY;
    u16 playerBallShadowGfxId;
    u8 playerBallShadowTrackingValid;
    u8 movementMankeyTreeTopCacheValid[OW_WILD_MAX_SPAWNS];
    u8 movementMankeyTreeTopCacheResult[OW_WILD_MAX_SPAWNS];
    s16 movementMankeyTreeTopCacheX[OW_WILD_MAX_SPAWNS];
    s16 movementMankeyTreeTopCacheY[OW_WILD_MAX_SPAWNS];
    u16 movementMankeyTreeTopCacheMapId[OW_WILD_MAX_SPAWNS];
    u8 movementMankeyTreeTopLandingExpected[OW_WILD_MAX_SPAWNS];
    u8 movementMankeyTreeTopSettled[OW_WILD_MAX_SPAWNS];
    s16 movementMankeyTreeTopSettledX[OW_WILD_MAX_SPAWNS];
    s16 movementMankeyTreeTopSettledY[OW_WILD_MAX_SPAWNS];
    u8 movementEmotePlayHopSound[OW_WILD_MAX_SPAWNS];
    u8 movementHopStartSoundSuppressFrames[OW_WILD_MAX_SPAWNS];
    u8 movementBehaviorLimitKeys[OW_WILD_MAX_SPAWNS];
    u8 playerBallCatchValues[OW_WILD_MAX_SPAWNS];
    u8 movementHelpSpawnParentSlotPlusOne;
    u8 movementHelpSpawnRemaining;
    u8 movementHelpSpawnDelay;
    u8 playerStepMaintenanceState;
    OverworldWildThrowState throwState;
    u16 movementFrameDrivenChillMask;
    u16 movementFrameDrivenActiveMask;
    u16 movementChillPhantomMask;
    u16 movementActivePhantomMask;
    OverworldWildBehaviorSlotCache *movementBehaviorSlotCaches;
    OverworldWildPresentationState spawnPresentations;
    OverworldWildDespawnTelemetry despawnTelemetry;
    OverworldWildResidentData *residentData;
    u16 movementNativeHeldMask;
    u8 movementIdleAiCursor;
    u8 movementNativeShadowRestorePending;
} OverworldWildOverlayRuntimeState;

#define OW_WILD_RUNTIME(state) ((OverworldWildOverlayRuntimeState *)((state)->movementRuntimeState))

typedef struct OverworldWildAlertPrimitiveMap {
    u8 logic;
    u8 reaction;
} OverworldWildAlertPrimitiveMap;

typedef struct OverworldWildMovementTarget {
    int x;
    int y;
} OverworldWildMovementTarget;

static const u8 sOverworldWildSpawnLocomotionBySpawnState[] = {
    OW_WILD_BEHAVIOR_LOCOMOTION_NONE,
    OW_WILD_BEHAVIOR_LOCOMOTION_RUN_FROM_OFF_SCREEN,
    OW_WILD_BEHAVIOR_LOCOMOTION_HOP_FROM_OFF_SCREEN,
    OW_WILD_BEHAVIOR_LOCOMOTION_APPEAR_HOP,
};

static const OverworldWildHelperOverlayEntry *OverworldWildSpawns_GetHelperOverlayEntry(void);

static u8 OverworldWildSpawns_CalculatePlayerBallShakes(
    OverworldWildSpawnState *state,
    int slot,
    u16 encounterGeneration)
{
    const OverworldWildHelperOverlayEntry *helperEntry;
    OverworldWildOverlayRuntimeState *runtime;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !state->spawns[slot].active
        || state->spawns[slot].encounterGeneration != encounterGeneration) {
        return 0;
    }
    runtime = OW_WILD_RUNTIME(state);
    if (runtime == NULL) {
        return 0;
    }
    helperEntry = OverworldWildSpawns_GetHelperOverlayEntry();
    if (helperEntry == NULL) {
        return 0;
    }
    return helperEntry->calculatePlayerBallShakes(
        runtime->playerBallCatchValues[slot]);
}

static u8 OverworldWildSpawns_GetPlayerBallCatchValue(u8 catchRate)
{
    return (u8)((catchRate + 2) / 3);
}

static int OverworldWildSpawns_FindCapturedPokemonDestination(
    FieldSystem *fieldSystem)
{
    struct Party *party;
    PCStorage *storage;
    int box;

    if (fieldSystem == NULL || fieldSystem->savedata == NULL) {
        return OW_WILD_PLAYER_BALL_CAPTURE_DESTINATION_NONE;
    }
    party = SaveData_GetPlayerPartyPtr(fieldSystem->savedata);
    if (party != NULL && party->count < party->maxPossibleCount) {
        return OW_WILD_PLAYER_BALL_CAPTURE_DESTINATION_PARTY;
    }
    storage = SaveArray_Get(
        fieldSystem->savedata,
        OW_WILD_PLAYER_BALL_PC_STORAGE_SAVE_BLOCK);
    if (storage == NULL) {
        return OW_WILD_PLAYER_BALL_CAPTURE_DESTINATION_NONE;
    }
    box = PCStorage_FindFirstBoxWithEmptySlot(storage);
    return box < NUM_PC_BOXES
        ? box
        : OW_WILD_PLAYER_BALL_CAPTURE_DESTINATION_NONE;
}

static const OverworldWildAlertPrimitiveMap sOverworldWildAlertPrimitivesByRange[] = {
    {OW_WILD_BEHAVIOR_ALERT_LOGIC_NONE, OW_WILD_BEHAVIOR_REACTION_NONE},
    {OW_WILD_BEHAVIOR_ALERT_LOGIC_FACING_LINE, OW_WILD_BEHAVIOR_REACTION_EMOTE},
    {OW_WILD_BEHAVIOR_ALERT_LOGIC_FACING_LINE_CLOSE_RADIUS, OW_WILD_BEHAVIOR_REACTION_EMOTE},
    {OW_WILD_BEHAVIOR_ALERT_LOGIC_CARDINAL_LINE, OW_WILD_BEHAVIOR_REACTION_EMOTE},
    {OW_WILD_BEHAVIOR_ALERT_LOGIC_RADIUS, OW_WILD_BEHAVIOR_REACTION_EMOTE},
    {OW_WILD_BEHAVIOR_ALERT_LOGIC_TERRAIN_ONLY, OW_WILD_BEHAVIOR_REACTION_EMOTE},
};

static OverworldWildBehaviorProfile OverworldWildSpawns_GetFallbackBehaviorProfile(void)
{
    OverworldWildBehaviorProfile profile = {
        OW_WILD_BEHAVIOR_KIND_WANDER,
        OW_WILD_BEHAVIOR_ALERT_STATE_SPEECH,
        OW_WILD_SPAWNER_BUBBLE_ID_QUESTION_MARK,
        OW_WILD_SPAWNER_SPOT_EMOTE_SPEECH_FRAMES,
        OW_WILD_SPAWNER_SPOT_RANGE,
        OW_WILD_BEHAVIOR_KIND_FLEE,
        1,
        OW_WILD_BEHAVIOR_KIND_NO_VISUAL,
        4,
        OW_WILD_SPAWNER_MOVEMENT_SPEED_DEFAULT,
        2,
        OW_WILD_SPAWNER_MOVEMENT_SPEED_DEFAULT,
        OW_WILD_SPAWNER_MOVEMENT_RANGE,
        OW_WILD_BEHAVIOR_JUMP_LEVEL_BOTH,
        OW_WILD_BEHAVIOR_PROFILE_DEFAULT,
        OW_WILD_BEHAVIOR_SPAWN_STATE_APPEAR_HOP,
        OW_WILD_BEHAVIOR_LOCOMOTION_WANDER,
        OW_WILD_BEHAVIOR_TARGET_NONE,
        OW_WILD_BEHAVIOR_ALERT_RANGE_FACING_LINE_CLOSE_RADIUS,
        OW_WILD_BEHAVIOR_ATTENTIVE_ACTION_FLEE,
        OW_WILD_BEHAVIOR_TARGET_AWAY_FROM_PLAYER,
        OW_WILD_BEHAVIOR_LOCOMOTION_WANDER,
        100,
        OW_WILD_SPAWN_DESTINATION_POOL,
        OW_WILD_BEHAVIOR_BATTLE_TRIGGER_NONE,
        OW_WILD_BEHAVIOR_LOCOMOTION_NONE,
        OW_WILD_BEHAVIOR_BOOL_YES,
        1,
        2,
        0,
        OW_WILD_SPAWNER_PHANTOM_STALK_TELEPORT_MOVE_FRAMES,
        OW_WILD_SPAWNER_PHANTOM_STALK_POST_TELEPORT_COOLDOWN_FRAMES,
        OW_WILD_BEHAVIOR_ALERT_SPECIAL_NONE,
        0,
        1,
        5,
        OW_WILD_SPAWNER_ONIX_RAM_SPEED_UP_TILES,
        OW_WILD_SPAWNER_ONIX_RAM_MAX_SPEED,
        OW_WILD_BEHAVIOR_CHAIN_PAUSE_ACTION_NONE,
        OW_WILD_BEHAVIOR_ALLOWED_TILE_LAND,
        OW_WILD_BEHAVIOR_ALLOWED_TILE_LAND,
        OW_WILD_BEHAVIOR_ALLOWED_TILE_LAND,
        OW_WILD_BEHAVIOR_ALLOWED_TILE_NONE,
        OW_WILD_BEHAVIOR_ALLOWED_TILE_NONE,
        OW_WILD_BEHAVIOR_ALLOWED_TILE_NONE,
        OW_WILD_BEHAVIOR_BOOL_YES,
        1,
        2,
        0,
        OW_WILD_SPAWNER_PHANTOM_STALK_TELEPORT_MOVE_FRAMES,
        OW_WILD_SPAWNER_PHANTOM_STALK_POST_TELEPORT_COOLDOWN_FRAMES,
        OW_WILD_SPAWNER_ONIX_RAM_SPEED_UP_TILES,
        OW_WILD_SPAWNER_ONIX_RAM_MAX_SPEED,
        OW_WILD_BEHAVIOR_BOOL_YES,
        1,
        2,
        0,
        OW_WILD_SPAWNER_PHANTOM_STALK_TELEPORT_MOVE_FRAMES,
        OW_WILD_SPAWNER_PHANTOM_STALK_POST_TELEPORT_COOLDOWN_FRAMES,
        OW_WILD_SPAWNER_ONIX_RAM_SPEED_UP_TILES,
        OW_WILD_SPAWNER_ONIX_RAM_MAX_SPEED,
        0,
        0,
        0,
        0,
        0,
        0,
        0,
        0,
        0,
    };

    return profile;
}

static void *sOverworldWildBehaviorDataBlob;
static BOOL sOverworldWildBehaviorDataLoadAttempted;
static u8 sOverworldWildHelperOverlayReady;

static OverworldWildSpawnState *sOverworldWildLastState;
static void OverworldWildSpawns_CleanupPresentationBeforeUnload(OverworldWildSpawnState *state);
static void OverworldWildSpawns_CleanupResidentTasks(void);
static const OverworldWildHelperOverlayEntry *OverworldWildSpawns_GetHelperOverlayEntry(void);

typedef struct OverworldWildBehaviorDataBlob {
    OverworldWildBehaviorDataBlobHeader header;
    OverworldWildBehaviorProfile classProfiles[OWBD_CLASS_PROFILE_COUNT];
    OverworldWildBehaviorClassRule classRules[OWBD_CLASS_RULE_COUNT];
    OverworldWildBehaviorSpeciesClassRule speciesClassRules[OWBD_SPECIES_CLASS_RULE_COUNT];
    OverworldWildBehaviorOverrideProfile overrideProfiles[OWBD_OVERRIDE_PROFILE_COUNT];
    u16 overrideMembers[OWBD_OVERRIDE_MEMBER_COUNT];
} OverworldWildBehaviorDataBlob;

static BOOL OverworldWildSpawns_CleanupResidentData(void)
{
    OverworldWildSpawnState *state = sOverworldWildLastState;
    BOOL helperLoaded = IsOverlayLoaded(OVERLAY_OVERWORLD_WILD_HELPER);

    if (!helperLoaded && sOverworldWildHelperOverlayReady) {
        return FALSE;
    }
    if (helperLoaded
        && !OVERWORLD_WILD_HELPER_OVERLAY_VALIDATE(
            sOverworldWildHelperOverlayReady
                ? OVERWORLD_WILD_HELPER_OWNED_BEHAVIOR
                : OVERWORLD_WILD_HELPER_VALIDATE_ONLY)) {
        return FALSE;
    }
    if (sOverworldWildHelperOverlayReady
        && !CanOverlayBeLoaded(OVERLAY_OVERWORLD_WILD_BEHAVIOR_DATA)) {
        return FALSE;
    }
    if (helperLoaded
        && !OVERWORLD_WILD_HELPER_OVERLAY_LIFECYCLE(
            OVERWORLD_WILD_HELPER_LIFECYCLE_PREPARE_CLEANUP,
            state != NULL
                ? state->movementFieldSystem
                : NULL)) {
        return FALSE;
    }
    OverworldWildSpawns_CleanupPresentationBeforeUnload(state);
    OverworldWildSpawns_CleanupResidentTasks();
    if (helperLoaded
        && !OVERWORLD_WILD_HELPER_OVERLAY_LIFECYCLE(
            sOverworldWildHelperOverlayReady
                ? OVERWORLD_WILD_HELPER_LIFECYCLE_FINISH_OWNED
                : OVERWORLD_WILD_HELPER_LIFECYCLE_FINISH_UNOWNED,
            NULL)) {
        return FALSE;
    }
    if (state != NULL && state->movementRuntimeState != NULL) {
        OverworldWildOverlayRuntimeState *runtime = OW_WILD_RUNTIME(state);

        sys_FreeMemoryEz(runtime->movementBehaviorSlotCaches);
        runtime->movementBehaviorSlotCaches = NULL;
        sys_FreeMemoryEz(runtime);
        state->movementRuntimeState = NULL;
    }
    sOverworldWildLastState = NULL;

    sys_FreeMemoryEz(sOverworldWildBehaviorDataBlob);
    sOverworldWildBehaviorDataBlob = NULL;

    sOverworldWildBehaviorDataLoadAttempted = FALSE;

    sOverworldWildHelperOverlayReady = 0;
    UnloadOverlayByID(OVERLAY_OVERWORLD_WILD_HELPER);
    return TRUE;
}

static BOOL OverworldWildSpawns_LoadCodeAddonBlob(u32 memberId, u32 expectedSize, void **blob, u32 *loadedSize)
{
    void *narc;
    u32 size;
    void *loadedBlob;

    narc = NARC_ctor(ARC_CODE_ADDONS, HEAPID_WORLD);
    if (narc == NULL) {
        return FALSE;
    }
    if (NARC_GetFileCount(narc) <= memberId) {
        NARC_dtor(narc);
        return FALSE;
    }

    size = NARC_GetMemberSize(narc, memberId);
    if (size == 0 || (expectedSize != 0 && size != expectedSize)) {
        NARC_dtor(narc);
        return FALSE;
    }

    loadedBlob = sys_AllocMemory(HEAPID_WORLD, size);
    if (loadedBlob == NULL) {
        NARC_dtor(narc);
        return FALSE;
    }

    NARC_ReadWholeMember(narc, memberId, loadedBlob);
    NARC_dtor(narc);
    *blob = loadedBlob;
    if (loadedSize != NULL) {
        *loadedSize = size;
    }
    return TRUE;
}

static BOOL OverworldWildSpawns_DecodeBehaviorDataBlob(void)
{
    const OverworldWildBehaviorDataBlob *blob;
    const OverworldWildBehaviorDataBlobHeader *header;

    blob = (const OverworldWildBehaviorDataBlob *)sOverworldWildBehaviorDataBlob;
    header = &blob->header;
    if (header->magic != OVERWORLD_WILD_BEHAVIOR_DATA_MAGIC
        || header->version != OVERWORLD_WILD_BEHAVIOR_DATA_VERSION
        || header->headerSize != sizeof(OverworldWildBehaviorDataBlobHeader)
        || header->blobSize != sizeof(OverworldWildBehaviorDataBlob)) {
        return FALSE;
    }

    return TRUE;
}

static const OverworldWildBehaviorDataBlob *OverworldWildSpawns_GetBehaviorDataBlob(void)
{
    if (!sOverworldWildBehaviorDataLoadAttempted) {
        sOverworldWildBehaviorDataLoadAttempted = TRUE;
        if (!OverworldWildSpawns_LoadCodeAddonBlob(
                CODE_ADDON_OVERWORLD_WILD_BEHAVIOR_DATA,
                sizeof(OverworldWildBehaviorDataBlob),
                &sOverworldWildBehaviorDataBlob,
                NULL)
            || !OverworldWildSpawns_DecodeBehaviorDataBlob()) {
            sys_FreeMemoryEz(sOverworldWildBehaviorDataBlob);
            sOverworldWildBehaviorDataBlob = NULL;
        }
    }

    return (const OverworldWildBehaviorDataBlob *)sOverworldWildBehaviorDataBlob;
}

typedef struct OverworldWildHeadbuttTreeTopScan {
    int centerX;
    int centerY;
    int radius;
    int minX;
    int maxX;
    int maxY;
    int nextX;
    int nextY;
    u16 emitted;
} OverworldWildHeadbuttTreeTopScan;

typedef struct OverworldWildCoordOffset {
    s8 dx;
    s8 dy;
} OverworldWildCoordOffset;

typedef struct OverworldWildFieldEffectDescriptor {
    u32 workSize;
    BOOL (*init)(void *effect, void *work);
    void (*destroy)(void *effect, void *work);
    void (*update)(void *effect, void *work);
    void (*render)(void *effect, void *work);
} OverworldWildFieldEffectDescriptor;

typedef struct OverworldWildMankeyTreeTopLateDrawEffectInit {
    OverworldWildSpawnState *state;
    FieldSystem *fieldSystem;
    LocalMapObject *object;
    int slot;
} OverworldWildMankeyTreeTopLateDrawEffectInit;

typedef struct OverworldWildMankeyTreeTopLateDrawEffectWork {
    OverworldWildSpawnState *state;
    FieldSystem *fieldSystem;
    LocalMapObject *object;
    int slot;
    u8 markerTimer;
} OverworldWildMankeyTreeTopLateDrawEffectWork;

typedef struct OverworldWildBehaviorHopValidationContext {
    OverworldWildSpawnState *state;
    FieldSystem *fieldSystem;
    int slot;
    u8 allowedTile;
    BOOL blockPlayerUnlessFinal;
} OverworldWildBehaviorHopValidationContext;

typedef struct OverworldWildSpawnPrepContext {
    OverworldWildSpawnState *state;
    FieldSystem *fieldSystem;
    OverworldWildSpawnTerrain terrain;
} OverworldWildSpawnPrepContext;

static BOOL OverworldWildSpawns_IsTileOccupiedByObject(FieldSystem *fieldSystem, int x, int y);
static const OverworldWildHelperOverlayEntry *OverworldWildSpawns_GetHelperOverlayEntry(void);
static BOOL OverworldWildSpawns_OverlayOnPlayerStep(
    FieldSystem *fieldSystem,
    OverworldWildSpawnState *state,
    OverworldWildResidentData *residentData);
static BOOL OverworldWildSpawns_OverlayOnPlayerFrame(
    FieldSystem *fieldSystem,
    OverworldWildSpawnState *state);
static void OverworldWildSpawns_OverlayOnFieldBusy(
    FieldSystem *fieldSystem,
    OverworldWildSpawnState *state,
    OverworldWildResidentData *residentData);
static BOOL OverworldWildSpawns_TickPlayerBallProjectile(
    FieldSystem *fieldSystem,
    OverworldWildSpawnState *state);
static void OverworldWildSpawns_TransitionPlayerBallProjectile(
    FieldSystem *fieldSystem,
    OverworldWildSpawnState *state,
    int command);
static LocalMapObject *OverworldWildSpawns_GetPlayerBallProjectileObject(void);
static BOOL OverworldWildSpawns_IsPlayerBallProjectileActive(void);
static BOOL OverworldWildSpawns_OverlayTryPrimeBattleFromTalk(
    FieldSystem *fieldSystem,
    OverworldWildSpawnState *state,
    LocalMapObject *talkedObject);
static int OverworldWildSpawns_FindBattleTalkSlot(
    FieldSystem *fieldSystem,
    OverworldWildSpawnState *state,
    LocalMapObject *talkedObject);
static u8 OverworldWildSpawns_OverlayCleanupPendingBattle(FieldSystem *fieldSystem, OverworldWildSpawnState *state, u16 battleResult);
static BOOL OverworldWildSpawns_CleanupResidentData(void);
static void OverworldWildSpawns_ResetSlotState(OverworldWildSpawnState *state, int slot, BOOL deleteAuxiliaryObjects);
static void OverworldWildSpawns_PrepareSlotForCapture(OverworldWildSpawnState *state, int slot);
static BOOL OverworldWildSpawns_TryStartSpawnRunStep(OverworldWildSpawnState *state, FieldSystem *fieldSystem, int slot);
static void OverworldWildSpawns_StartSpawnHop(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    int startX,
    int startY,
    int targetX,
    int targetY,
    u8 direction);
static BOOL OverworldWildSpawns_HandleFinishedSpawnRunMovementCommand(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot);
static BOOL OverworldWildSpawns_IsEnabledMap(FieldSystem *fieldSystem);
static BOOL OverworldWildSpawns_IsSurfBehavior(u8 behavior);
static BOOL OverworldWildSpawns_IsHeadbuttTreeTopLocation(FieldSystem *fieldSystem, int x, int y);
static BOOL OverworldWildSpawns_IsLandMapTile(FieldSystem *fieldSystem, int x, int y);
static BOOL OverworldWildSpawns_IsPlayerTile(FieldSystem *fieldSystem, int x, int y);
static BOOL OverworldWildSpawns_IsBehaviorAllowedMovementTile(
    FieldSystem *fieldSystem,
    u8 allowedTile,
    int x,
    int y);
static BOOL OverworldWildSpawns_IsBehaviorAllowedHopLandingTile(
    OverworldWildSpawnState *state,
    int slot,
    FieldSystem *fieldSystem,
    u8 allowedTile,
    int x,
    int y,
    int finalTargetX,
    int finalTargetY);
static BOOL OverworldWildSpawns_IsTileReservedByOtherWild(
    OverworldWildSpawnState *state,
    int movingSlot,
    int x,
    int y);
static BOOL OverworldWildSpawns_IsTileOccupiedByNonPlayerObject(
    FieldSystem *fieldSystem,
    LocalMapObject *ignoredObject,
    int x,
    int y);
static BOOL OverworldWildSpawns_TryStartBattle(OverworldWildSpawnState *state, FieldSystem *fieldSystem, BOOL decrementBattleGrace);
static BOOL OverworldWildSpawns_TryStartBattleForSlot(OverworldWildSpawnState *state, FieldSystem *fieldSystem, int slot);
static void OverworldWildSpawns_PrimePendingBattleForSlot(OverworldWildSpawnState *state, int slot);
static BOOL OverworldWildSpawns_QueueBattleForSlot(OverworldWildSpawnState *state, FieldSystem *fieldSystem, int slot);
static BOOL OverworldWildSpawns_TryStartBattleForSlotOrQueue(OverworldWildSpawnState *state, FieldSystem *fieldSystem, int slot);
static void OverworldWildSpawns_ResetPendingBattle(OverworldWildSpawnState *state);
#if OW_WILD_UPDATE_DIAGNOSTIC_SKIP_CLEAR
static void OverworldWildSpawns_ClearContextLite(OverworldWildSpawnState *state);
#endif
static BOOL OverworldWildSpawns_HasPendingBattle(OverworldWildSpawnState *state);
static BOOL OverworldWildSpawns_HasQueuedBattle(OverworldWildSpawnState *state);
static BOOL OverworldWildSpawns_TryStartQueuedBattle(OverworldWildSpawnState *state, FieldSystem *fieldSystem);
static BOOL OverworldWildSpawns_TryStartBattleFromAButton(OverworldWildSpawnState *state, FieldSystem *fieldSystem);
static BOOL OverworldWildSpawns_IsPlayerStableForBattle(FieldSystem *fieldSystem);
static BOOL OverworldWildSpawns_TryGetCustomJumpVector(
    int dx,
    int dy,
    u8 *direction,
    u8 *distance);
static int OverworldWildSpawns_GetCustomJumpDistance(int dx, int dy);
static BOOL OverworldWildSpawns_IsCustomJumpVectorShape(int dx, int dy);
#if !OW_WILD_STEP_DIAGNOSTIC_SKIP_AMBIENT_CRY || !OW_WILD_UPDATE_DIAGNOSTIC_SKIP_CLEAR
static void OverworldWildSpawns_ResetAmbientCryCooldown(OverworldWildSpawnState *state);
#endif
static u32 OverworldWildSpawns_GetSpriteID(u16 species, u8 form);
static BOOL OverworldWildSpawns_IsPresentationFieldContextCurrent(OverworldWildSpawnState *state, FieldSystem *fieldSystem);
static BOOL OverworldWildSpawns_IsMovementFieldContextCurrent(OverworldWildSpawnState *state, FieldSystem *fieldSystem);
static BOOL OverworldWildSpawns_IsFrameDrivenActiveLocomotion(u8 locomotion);
static u8 OverworldWildSpawns_GetBehaviorClassForSpawn(
    u16 species,
    u8 level,
    u8 terrain,
    u8 shiny);
static OverworldWildBehaviorProfile OverworldWildSpawns_GetBehaviorProfile(OverworldWildSpawnState *state, int slot);
static OverworldWildBehaviorProfile OverworldWildSpawns_GetMovementTickProfile(OverworldWildSpawnState *state, int slot);
static void OverworldWildSpawns_GetMovementTickProfileAndPrimitives(
    OverworldWildSpawnState *state,
    int slot,
    OverworldWildBehaviorProfile *profileOut,
    OverworldWildBehaviorPrimitives *primitivesOut);
static OverworldWildBehaviorPrimitives OverworldWildSpawns_ResolveBehaviorPrimitives(
    const OverworldWildBehaviorProfile *profile);
static BOOL OverworldWildSpawns_IsCanopyHopperTreeTopProfile(const OverworldWildBehaviorProfile *profile);
static void OverworldWildSpawns_ApplyMovementRange(LocalMapObject *object, u8 range);
static void OverworldWildSpawns_ApplyPokemonRenderParams(
    LocalMapObject *object,
    u16 species,
    u8 form,
    u32 spriteId,
    BOOL shiny);
static void OverworldWildSpawns_RevealNonPhantomObjects(OverworldWildSpawnState *state, FieldSystem *fieldSystem);
static void OverworldWildSpawns_SetObjectFacing(LocalMapObject *object, u8 direction);
static void OverworldWildSpawns_SetObjectTile(LocalMapObject *object, int x, int y);
static void OverworldWildSpawns_SetObjectLogicalTileOnly(LocalMapObject *object, int x, int y);
static void OverworldWildSpawns_SetObjectLandingTile(
    FieldSystem *fieldSystem,
    LocalMapObject *object,
    int x,
    int y);
static s32 OverworldWildSpawns_GetObjectGroundBaseYAt(LocalMapObject *object, int x, int y);
static void OverworldWildSpawns_ResetSlotSpotState(OverworldWildSpawnState *state, int slot);
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_PARAM_TICK
static void OverworldWildSpawns_FinishActivePresentationCommand(LocalMapObject *object);
static void OverworldWildSpawns_CancelSpotEmotePresentation(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object);
static void OverworldWildSpawns_ResetAllMovementCommands(
    OverworldWildSpawnState *state,
    BOOL clearObjectCommand,
    BOOL preserveMapHeaderState);
static void OverworldWildSpawns_ResetAllMovementStateOnly(
    OverworldWildSpawnState *state,
    BOOL clearObjectCommand,
    BOOL preserveMapHeaderState);
static void OverworldWildSpawns_DetachAllMovementStateOnContextLoss(
    OverworldWildSpawnState *state,
    BOOL preserveMapHeaderState);
static void OverworldWildSpawns_PrepareMapHeaderChange(
    OverworldWildSpawnState *state,
    OverworldWildMapHeaderChangeMode mode);
static void OverworldWildSpawns_StartTiredEmoteWithProfile(
    OverworldWildSpawnState *state,
    int slot,
    const OverworldWildBehaviorProfile *profileOverride,
    const OverworldWildBehaviorPrimitives *primitivesOverride);
static void OverworldWildSpawns_StartTiredEmote(OverworldWildSpawnState *state, int slot);
static void OverworldWildSpawns_StartHopStartSoundSuppression(OverworldWildSpawnState *state, int slot);
static BOOL OverworldWildSpawns_TryStartManualHopEmote(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object,
    u8 requiredState,
    u8 endState,
    u8 direction,
    u8 jumpCount,
    u8 emoteFrames,
    u8 bubbleId,
    BOOL showBubbleEachJump,
    BOOL playHopSound);
static BOOL OverworldWildSpawns_TryStartChainPauseAction(
    OverworldWildSpawnState *state,
    int slot,
    const OverworldWildBehaviorProfile *profile);
static BOOL OverworldWildSpawns_StartCarriedThrowTarget(
    OverworldWildSpawnState *state,
    int carrierSlot,
    int targetSlot,
    LocalMapObject *object);
static void OverworldWildSpawns_ClearStagedHopTarget(OverworldWildSpawnState *state, int slot);
static void OverworldWildSpawns_ClearSpawnRunState(OverworldWildSpawnState *state, int slot);
static void OverworldWildSpawns_ClearRamCrashMovementState(OverworldWildSpawnState *state, int slot);
static void OverworldWildSpawns_ShowBubble(LocalMapObject *object, u8 bubbleId);
static void OverworldWildSpawns_ClearPhantomFlickerObject(
    OverworldWildSpawnState *state,
    int slot,
    BOOL deleteObject);
static int OverworldWildSpawns_MovementDirectionDeltaX(u8 direction);
static int OverworldWildSpawns_MovementDirectionDeltaY(u8 direction);
static BOOL OverworldWildSpawns_TryGetClosestPlayerMovementTarget(
    FieldSystem *fieldSystem,
    int fromX,
    int fromY,
    int *targetX,
    int *targetY);
static BOOL OverworldWildSpawns_TryGetCanopyHopperPlayerFrontTarget(
    FieldSystem *fieldSystem,
    int *targetX,
    int *targetY);
static void OverworldWildSpawns_TrySpawnHelpChildren(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    const OverworldWildBehaviorProfile *profile);
static void OverworldWildSpawns_ClearQueuedHelpChildren(OverworldWildSpawnState *state);
static void OverworldWildSpawns_TickQueuedHelpChildren(OverworldWildSpawnState *state, FieldSystem *fieldSystem);
static BOOL OverworldWildSpawns_IsBehaviorLimitKeyAtOverworldLimit(
    OverworldWildSpawnState *state,
    u8 behaviorLimitKey,
    const OverworldWildBehaviorProfile *profile);
static void OverworldWildSpawns_TryStartPickupThrowAction(
    OverworldWildSpawnState *state,
    int slot);
static void OverworldWildSpawns_ApplyHelpChildSpawnState(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object);
static void OverworldWildSpawns_RevealPhantomStalker(OverworldWildSpawnState *state, int slot, LocalMapObject *object);
static void OverworldWildSpawns_ClearCanopyHopperVisualStateAtBoundary(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot);
static void OverworldWildSpawns_ClearStagedHopMovementListTask(
    OverworldWildSpawnState *state,
    int slot);
static void OverworldWildSpawns_UpdateMankeyTreeTopPriorityBits(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    LocalMapObject *object);
static BOOL OverworldWildSpawns_ContinuePendingStagedHop(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot);
static BOOL OverworldWildSpawns_TryStartRandomBehaviorHopCommand(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    const OverworldWildBehaviorProfile *profile);
static BOOL OverworldWildSpawns_TryStartDirectedBehaviorHopCommand(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    const OverworldWildBehaviorProfile *profile,
    int dx,
    int dy,
    const u8 *directions,
    int directionCount,
    BOOL stopOneHopAway);
static BOOL OverworldWildSpawns_TryStartPhantomTeleportMovementCommand(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    const u8 *directions,
    int directionCount,
    const OverworldWildBehaviorProfile *profile,
    const OverworldWildBehaviorPrimitives *primitives);
static BOOL OverworldWildSpawns_TryStartRamMovementCommand(
    const OverworldWildDirectionStepContext *stepContext);
static BOOL OverworldWildSpawns_TryStartRamMovementCommandFromDirections(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    const u8 *directions,
    int directionCount,
    const OverworldWildBehaviorProfile *profile,
    const OverworldWildBehaviorPrimitives *primitives);
static int OverworldWildSpawns_BuildChillWanderDirections(
    FieldSystem *fieldSystem,
    LocalMapObject *object,
    u8 allowedTile,
    u8 *directions);
static BOOL OverworldWildSpawns_StartPreparedCustomJumpCommand(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    LocalMapObject *object,
    u8 direction,
    u8 distance,
    int targetX,
    int targetY,
    const OverworldWildBehaviorProfile *profile,
    BOOL suppressHopStartSound);
static BOOL OverworldWildSpawns_InitHeadbuttTreeTopScan(
    FieldSystem *fieldSystem,
    OverworldWildHeadbuttTreeTopScan *scan,
    int centerX,
    int centerY,
    int radius);
static BOOL OverworldWildSpawns_NextHeadbuttTreeTopCandidate(
    FieldSystem *fieldSystem,
    OverworldWildHeadbuttTreeTopScan *scan,
    int *x,
    int *y);
static BOOL OverworldWildSpawns_IsHeadbuttTreeTopLocation(FieldSystem *fieldSystem, int x, int y);
static BOOL OverworldWildSpawns_HandleFinishedStagedHopMovementCommand(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    BOOL customJumpLandingFinalized);
static BOOL OverworldWildSpawns_StageHopTarget(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    LocalMapObject *object,
    const OverworldWildBehaviorProfile *profile,
    int targetX,
    int targetY,
    u8 direction,
    u8 distance,
    BOOL finishWithTired);
static BOOL OverworldWildSpawns_ExecutePendingStagedHop(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    LocalMapObject *object,
    const OverworldWildBehaviorProfile *profile);
#if OW_WILD_SPAWNER_CUSTOM_JUMP_RAM_PROBE
static BOOL OverworldWildSpawns_TryStartCustomJumpRamProbe(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem);
#else
#define OverworldWildSpawns_TryStartCustomJumpRamProbe(state, fieldSystem) FALSE
#endif
static void OverworldWildSpawns_DespawnFarMons(OverworldWildSpawnState *state, FieldSystem *fieldSystem);
static void OverworldWildSpawns_TryRefill(OverworldWildSpawnState *state, FieldSystem *fieldSystem);
static u16 OverworldWildSpawns_GetThrowParticipantMask(OverworldWildSpawnState *state);
static BOOL OverworldWildSpawns_TickCustomJumpRenderSettle(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot);
static void OverworldWildSpawns_ReconcileNativeShadow(
    FieldSystem *fieldSystem,
    LocalMapObject *object);
static void OverworldWildSpawns_ResetPlayerBallShadowTracking(
    OverworldWildSpawnState *state);
static void OverworldWildSpawns_SyncPlayerBallShadowObject(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    LocalMapObject *ballObject);
#if OW_WILD_SPAWNER_CUSTOM_JUMP_RAM_PROBE
static void OverworldWildSpawns_RecordCustomJumpRamObject(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object,
    int stage);
#else
#define OverworldWildSpawns_RecordCustomJumpRamObject(state, slot, object, stage) ((void)0)
#endif
static void OverworldWildSpawns_FinishPendingStagedHop(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object,
    const OverworldWildBehaviorProfile *profile);
static LocalMapObject *__attribute__((noinline)) OverworldWildSpawns_RecreateSpawnObjectAtTile(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    LocalMapObject *object,
    int x,
    int y);
static BOOL OverworldWildSpawns_TickStagedHopMovementListTask(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    BOOL *finished);
static void OverworldWildSpawns_RestoreMankeyTreeTopLayerProbe(void);
static void OverworldWildSpawns_UpdateMankeyTreeTopLayerProbe(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem);
static void OverworldWildSpawns_UpdateMankeyTreeTopBubbleProbe(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem);
static void OverworldWildSpawns_ClearMankeyTreeTopLateDrawEffect(int slot);
static void OverworldWildSpawns_EnsureMankeyTreeTopLateDrawEffect(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    LocalMapObject *object);
#if OW_WILD_SPAWNER_MANKEY_TREE_TOP_LATE_DRAW_EFFECT_ENABLED
static BOOL OverworldWildSpawns_MankeyTreeTopLateDrawEffectInit(void *effect, void *work);
static void OverworldWildSpawns_MankeyTreeTopLateDrawEffectDestroy(void *effect, void *work);
static void OverworldWildSpawns_MankeyTreeTopLateDrawEffectUpdate(void *effect, void *work);
static void OverworldWildSpawns_MankeyTreeTopLateDrawEffectRender(void *effect, void *work);
#endif
#endif
static BOOL OverworldWildSpawns_RunImmediateCanopyMovementCommand(LocalMapObject *object, u32 movementCommand);
#if OW_WILD_UPDATE_DIAGNOSTIC_READ_ONLY \
    || OW_WILD_UPDATE_DIAGNOSTIC_STATE_READ_ONLY \
    || OW_WILD_UPDATE_DIAGNOSTIC_SETTER_ONLY
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE void *sOverworldWildDiagnosticMapObjectMan;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE void *sOverworldWildDiagnosticMapObjects;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildDiagnosticStateMapId;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE void *sOverworldWildDiagnosticStateMapObjectMan;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE void *sOverworldWildDiagnosticStateMapObjects;
#endif
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE u8 sOverworldWildMovementDiagnosticSingleActive;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE u8 sOverworldWildMovementDiagnosticDirectionBlocked;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE u8 sOverworldWildMovementDiagnosticWalkIssued;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE u8 sOverworldWildMovementDiagnosticWalkSuppressedByBlocked;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE u32 sOverworldWildMovementDiagnosticLookCommand;
#if OW_WILD_SPAWNER_PERF_DIAGNOSTICS
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE u16 sOverworldWildPerfProfileResolvesThisFrame;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE u16 sOverworldWildPerfProfileCacheHitsThisFrame;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE u16 sOverworldWildPerfTargetScansThisFrame;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE u16 sOverworldWildPerfMapObjectCreatesThisFrame;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE u16 sOverworldWildPerfMapObjectDeletesThisFrame;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE u16 sOverworldWildPerfActiveFrameSlotsThisFrame;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE u16 sOverworldWildPerfMovementCommandsThisFrame;

static void OverworldWildSpawns_PerfResetFrameCounters(void)
{
    sOverworldWildPerfProfileResolvesThisFrame = 0;
    sOverworldWildPerfProfileCacheHitsThisFrame = 0;
    sOverworldWildPerfTargetScansThisFrame = 0;
    sOverworldWildPerfMapObjectCreatesThisFrame = 0;
    sOverworldWildPerfMapObjectDeletesThisFrame = 0;
    sOverworldWildPerfActiveFrameSlotsThisFrame = 0;
    sOverworldWildPerfMovementCommandsThisFrame = 0;
}

static void OverworldWildSpawns_PerfInc(u16 *counter)
{
    if (counter != NULL && *counter != 0xFFFF) {
        (*counter)++;
    }
}
#define OW_WILD_PERF_RESET_FRAME() OverworldWildSpawns_PerfResetFrameCounters()
#define OW_WILD_PERF_INC(counter) OverworldWildSpawns_PerfInc(&(counter))
#else
#define OW_WILD_PERF_RESET_FRAME() ((void)0)
#define OW_WILD_PERF_INC(counter) ((void)0)
#endif
#if OW_WILD_SPAWNER_TIRED_BUBBLE_ID_PROBE
static u8 sOverworldWildTiredBubbleIdProbe = OW_WILD_SPAWNER_TIRED_BUBBLE_ID_PROBE_MIN;
#endif

#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_UPDATE_COMMAND
static BOOL OverworldWildSpawns_UpdateSpawnerMovementCommand(LocalMapObject *object);
static BOOL OverworldWildSpawns_UpdateSpawnerMovementCommandForSlot(
    OverworldWildSpawnState *state,
    int slot,
    BOOL fieldContextAuthenticated);
#endif
static void OverworldWildSpawns_TickFrameMovementDecisionCounter(void);
static BOOL OverworldWildSpawns_ShouldRunFrameMovementDecisionForSpeed(u8 speed);
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
static void OverworldWildSpawns_FrameMovementTask(SysTask *task, void *data);
static void OverworldWildSpawns_EnsureFrameMovementTask(OverworldWildSpawnState *state, FieldSystem *fieldSystem);
static void OverworldWildSpawns_StopFrameMovementTask(void);
#endif
static void OverworldWildSpawns_CancelDeferredBattleScript(void);
static BOOL OverworldWildSpawns_QueueBattleScriptTask(FieldSystem *fieldSystem, LocalMapObject *object);
static BOOL OverworldWildSpawns_RequestBattleScript(
    FieldSystem *fieldSystem,
    OverworldWildSpawnState *state,
    int slot);
#if OW_WILD_SPAWNER_CUSTOM_JUMP_POST_LANDING_RENDER_SETTLE_ENABLED
static BOOL sOverworldWildCustomJumpRenderSettleActive[OW_WILD_MAX_SPAWNS];
static s32 sOverworldWildCustomJumpRenderSettleStartX[OW_WILD_MAX_SPAWNS];
static s32 sOverworldWildCustomJumpRenderSettleStartY[OW_WILD_MAX_SPAWNS];
static u32 sOverworldWildCustomJumpRenderSettleElapsedFrames[OW_WILD_MAX_SPAWNS];
#endif
#if OW_WILD_SPAWNER_CUSTOM_JUMP_VISIBLE_LEGS
static BOOL sOverworldWildCustomJumpVisibleLegFinished[OW_WILD_MAX_SPAWNS];
#endif
#if OW_WILD_SPAWNER_CUSTOM_JUMP_RAM_PROBE
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamStage;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamProbeTriggers;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamProbeStarts;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamProbeFailures;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamProbeActiveSlots;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamProbeCurrentSlots;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamProbeMankeySlots;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamProbeObjectSlots;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamProbeLastSpecies;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamSlot;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamStartX;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamStartY;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamTargetX;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamTargetY;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamCurrentX;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamCurrentY;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamRenderX;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamRenderY;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamRenderFx32X;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamRenderFx32Y;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamElapsedFrames;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamFrameCount;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamFlags;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamPending;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamActive;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamVisibleLeg;
static OW_WILD_SPAWNER_DIAGNOSTIC_STORAGE int sOverworldWildCustomJumpRamFinalLanding;
static BOOL sOverworldWildCustomJumpRamProbeKeyDown;
#endif

#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
static SysTask *sOverworldWildMovementFrameTask;
static SysTask *sOverworldWildStagedHopMovementTasks[OW_WILD_MAX_SPAWNS];
static u16 *sOverworldWildStagedHopMovementLists[OW_WILD_MAX_SPAWNS];
static u8 sOverworldWildMovementFrameDecisionCounter;
static u8 sOverworldWildMovementFrameTaskExecuting;
static SysTask *sOverworldWildDeferredBattleScriptTask;
static FieldSystem *sOverworldWildDeferredBattleFieldSystem;
static OverworldWildSpawnState *sOverworldWildDeferredBattleState;
static u16 sOverworldWildDeferredBattleGeneration;
static s8 sOverworldWildDeferredBattleSlot = -1;
#if OW_WILD_SPAWNER_MANKEY_TREE_TOP_RENDER_OVERRIDE_SAVE_ENABLED
static BOOL sOverworldWildMankeyTreeTopPriorityBitsSaved[OW_WILD_MAX_SPAWNS];
static u32 sOverworldWildMankeyTreeTopPrioritySavedBits[OW_WILD_MAX_SPAWNS];
#if OW_WILD_SPAWNER_MANKEY_TREE_TOP_DRAW_CALLBACK_OVERRIDE_ENABLED
static BOOL sOverworldWildMankeyTreeTopDrawCallbacksSaved[OW_WILD_MAX_SPAWNS];
static void (*sOverworldWildMankeyTreeTopSavedDrawCallbacks[OW_WILD_MAX_SPAWNS])(LocalMapObject *);
#endif
#endif
#if OW_WILD_SPAWNER_MANKEY_TREE_TOP_LAYER_PROBE_ENABLED
static BOOL sOverworldWildMankeyTreeTopLayerProbeActive;
static u8 sOverworldWildMankeyTreeTopLayerProbeTimer;
static u8 sOverworldWildMankeyTreeTopLayerProbePhase;
#endif
#if OW_WILD_SPAWNER_MANKEY_TREE_TOP_BUBBLE_PROBE_ENABLED
static u8 sOverworldWildMankeyTreeTopBubbleProbeTimer;
#endif
#if OW_WILD_SPAWNER_MANKEY_TREE_TOP_LATE_DRAW_EFFECT_ENABLED
static void *sOverworldWildMankeyTreeTopLateDrawEffects[OW_WILD_MAX_SPAWNS];
#endif
#endif
const OverworldWildSpawnsOverlayEntry gOverworldWildSpawnsOverlayEntry __attribute__((section(".overworld_wild_spawns_entry"), used)) = {
    OverworldWildSpawns_OverlayOnPlayerStep,
    OverworldWildSpawns_OverlayTryPrimeBattleFromTalk,
    OverworldWildSpawns_OverlayCleanupPendingBattle,
    OverworldWildSpawns_CleanupResidentData,
    OverworldWildSpawns_OverlayOnPlayerFrame,
    OverworldWildSpawns_OverlayOnFieldBusy,
    OverworldWildSpawns_PrepareMapHeaderChange,
};

static OverworldWildOverlayRuntimeState *OverworldWildSpawns_EnsureRuntimeState(
    OverworldWildSpawnState *state)
{
    OverworldWildOverlayRuntimeState *runtime;

    if (state == NULL) {
        return NULL;
    }

    sOverworldWildLastState = state;

    runtime = (OverworldWildOverlayRuntimeState *)state->movementRuntimeState;
    if (runtime == NULL) {
        runtime = sys_AllocMemory(HEAPID_WORLD, sizeof(*runtime));
        state->movementRuntimeState = runtime;
        if (runtime != NULL) {
            memset(runtime, 0, sizeof(*runtime));
        }
    }

    return runtime;
}

static LocalMapObject *sOverworldWildCollisionIgnoredObject;

static const OverworldWildCoordOffset sCustomJumpOffsets[] = {
    { 0, 1 },
    { 0, -1 },
    { -1, 0 },
    { 1, 0 },
#if OW_WILD_SPAWNER_CUSTOM_JUMP_ALLOW_DIAGONAL
    { 1, 1 },
    { 1, -1 },
    { -1, 1 },
    { -1, -1 },
#endif
};

#if OW_WILD_SPAWNER_MANKEY_TREE_TOP_LATE_DRAW_EFFECT_ENABLED
static const OverworldWildFieldEffectDescriptor sOverworldWildMankeyTreeTopLateDrawEffectDescriptor = {
    sizeof(OverworldWildMankeyTreeTopLateDrawEffectWork),
    OverworldWildSpawns_MankeyTreeTopLateDrawEffectInit,
    OverworldWildSpawns_MankeyTreeTopLateDrawEffectDestroy,
    OverworldWildSpawns_MankeyTreeTopLateDrawEffectUpdate,
    OverworldWildSpawns_MankeyTreeTopLateDrawEffectRender,
};
#endif
static int OverworldWildSpawns_Abs(int value)
{
    return value < 0 ? -value : value;
}

static int OverworldWildSpawns_Max(int a, int b)
{
    return a > b ? a : b;
}

static int OverworldWildSpawns_DistanceFromPlayer(FieldSystem *fieldSystem, int x, int y)
{
    int dx = x - GetPlayerXCoord(fieldSystem->playerAvatar);
    int dy = y - GetPlayerYCoord(fieldSystem->playerAvatar);

    return OverworldWildSpawns_Max(OverworldWildSpawns_Abs(dx), OverworldWildSpawns_Abs(dy));
}

static BOOL OverworldWildSpawns_IsNearActiveSpawn(OverworldWildSpawnState *state, int x, int y, int radius)
{
    int i;

    for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
        if (state->spawns[i].active && state->spawns[i].object != NULL) {
            int spawnX = MapObject_GetCurrentX(state->spawns[i].object);
            int spawnY = MapObject_GetCurrentY(state->spawns[i].object);
            int dx = OverworldWildSpawns_Abs(x - spawnX);
            int dy = OverworldWildSpawns_Abs(y - spawnY);

            if (OverworldWildSpawns_Max(dx, dy) <= radius) {
                return TRUE;
            }
        }
    }

    return FALSE;
}

static BOOL OverworldWildSpawns_IsCurrentMapObject(FieldSystem *fieldSystem, LocalMapObject *object)
{
    MapObjectMan *mapObjectMan = (MapObjectMan *)fieldSystem->mapObjectMan;
    LocalMapObject *objects;
    u32 objectAddr;
    u32 startAddr;
    u32 endAddr;

    if (object == NULL || mapObjectMan == NULL || mapObjectMan->objects == NULL) {
        return FALSE;
    }

    objects = mapObjectMan->objects;
    objectAddr = (u32)object;
    startAddr = (u32)objects;
    endAddr = startAddr + mapObjectMan->object_count * sizeof(LocalMapObject);

    return objectAddr >= startAddr && objectAddr < endAddr;
}

static BOOL OverworldWildSpawns_IsSpawnObjectId(u32 objectId)
{
    return objectId >= OW_WILD_OBJECT_ID_START
        && objectId < OW_WILD_OBJECT_ID_START + OW_WILD_MAX_SPAWNS;
}

#if OW_WILD_SPAWNER_MANKEY_TREE_TOP_PRIORITY_OVERRIDE_ENABLED
static void OverworldWildSpawns_SaveMankeyTreeTopPriorityBits(int slot, LocalMapObject *object)
{
    if (slot < 0 || slot >= OW_WILD_MAX_SPAWNS || object == NULL
        || sOverworldWildMankeyTreeTopPriorityBitsSaved[slot]) {
        return;
    }

    sOverworldWildMankeyTreeTopPrioritySavedBits[slot] =
        object->flags & OW_WILD_SPAWNER_MANKEY_HEADBUTT_TREE_TOP_PRIORITY_BITS;
    sOverworldWildMankeyTreeTopPriorityBitsSaved[slot] = TRUE;
}

static void OverworldWildSpawns_RestoreMankeyTreeTopPriorityBits(int slot, LocalMapObject *object)
{
    if (slot < 0 || slot >= OW_WILD_MAX_SPAWNS
        || !sOverworldWildMankeyTreeTopPriorityBitsSaved[slot]) {
        return;
    }

    if (object != NULL) {
        MapObject_ClearBits(object, OW_WILD_SPAWNER_MANKEY_HEADBUTT_TREE_TOP_PRIORITY_BITS);
        MapObject_SetBits(object, sOverworldWildMankeyTreeTopPrioritySavedBits[slot]);
    }
    sOverworldWildMankeyTreeTopPrioritySavedBits[slot] = 0;
    sOverworldWildMankeyTreeTopPriorityBitsSaved[slot] = FALSE;
}
#endif

#if OW_WILD_SPAWNER_MANKEY_TREE_TOP_DRAW_CALLBACK_OVERRIDE_ENABLED
static void OverworldWildSpawns_SaveMankeyTreeTopDrawCallback(int slot, LocalMapObject *object)
{
    if (slot < 0 || slot >= OW_WILD_MAX_SPAWNS || object == NULL
        || sOverworldWildMankeyTreeTopDrawCallbacksSaved[slot]) {
        return;
    }

    sOverworldWildMankeyTreeTopSavedDrawCallbacks[slot] = object->unkC8;
    sOverworldWildMankeyTreeTopDrawCallbacksSaved[slot] = TRUE;
}

static void OverworldWildSpawns_RestoreMankeyTreeTopDrawCallback(int slot, LocalMapObject *object)
{
    if (slot < 0 || slot >= OW_WILD_MAX_SPAWNS
        || !sOverworldWildMankeyTreeTopDrawCallbacksSaved[slot]) {
        return;
    }

    if (object != NULL) {
        object->unkC8 = sOverworldWildMankeyTreeTopSavedDrawCallbacks[slot];
    }
    sOverworldWildMankeyTreeTopSavedDrawCallbacks[slot] = NULL;
    sOverworldWildMankeyTreeTopDrawCallbacksSaved[slot] = FALSE;
}
#endif

static void OverworldWildSpawns_RestoreMankeyTreeTopRenderOverride(int slot, LocalMapObject *object)
{
#if OW_WILD_SPAWNER_MANKEY_TREE_TOP_RENDER_OVERRIDE_SAVE_ENABLED
#if OW_WILD_SPAWNER_MANKEY_TREE_TOP_DRAW_CALLBACK_OVERRIDE_ENABLED
    OverworldWildSpawns_RestoreMankeyTreeTopDrawCallback(slot, object);
#endif
#if OW_WILD_SPAWNER_MANKEY_TREE_TOP_PRIORITY_OVERRIDE_ENABLED
    OverworldWildSpawns_RestoreMankeyTreeTopPriorityBits(slot, object);
#else
    (void)slot;
    (void)object;
#endif
#else
    (void)slot;
    (void)object;
#endif
}

static void OverworldWildSpawns_ApplyMankeyTreeTopRenderOverride(int slot, LocalMapObject *object)
{
#if OW_WILD_SPAWNER_MANKEY_TREE_TOP_RENDER_OVERRIDE_SAVE_ENABLED
    if (slot < 0 || slot >= OW_WILD_MAX_SPAWNS || object == NULL) {
        return;
    }

#if OW_WILD_SPAWNER_MANKEY_TREE_TOP_PRIORITY_OVERRIDE_ENABLED
    OverworldWildSpawns_SaveMankeyTreeTopPriorityBits(slot, object);
    MapObject_ClearBits(object, OW_WILD_SPAWNER_MANKEY_HEADBUTT_TREE_TOP_PRIORITY_BITS);
#endif
#if OW_WILD_SPAWNER_MANKEY_TREE_TOP_DRAW_CALLBACK_OVERRIDE_ENABLED
    OverworldWildSpawns_SaveMankeyTreeTopDrawCallback(slot, object);
    object->unkC8 = OW_WILD_SPAWNER_MANKEY_HEADBUTT_TREE_TOP_DRAW_CALLBACK;
#endif
#else
    (void)slot;
    (void)object;
#endif
}

static void OverworldWildSpawns_ClearMankeyTreeTopCache(OverworldWildSpawnState *state, int slot)
{
    OverworldWildOverlayRuntimeState *runtime = OverworldWildSpawns_EnsureRuntimeState(state);

    if (runtime == NULL || slot < 0 || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    OverworldWildSpawns_ClearMankeyTreeTopLateDrawEffect(slot);
    runtime->movementMankeyTreeTopCacheValid[slot] = FALSE;
    runtime->movementMankeyTreeTopCacheResult[slot] = FALSE;
    runtime->movementMankeyTreeTopCacheX[slot] = OW_WILD_SPAWNER_PREVIOUS_TILE_NONE;
    runtime->movementMankeyTreeTopCacheY[slot] = OW_WILD_SPAWNER_PREVIOUS_TILE_NONE;
    runtime->movementMankeyTreeTopCacheMapId[slot] = MAP_NOTHING;
    runtime->movementMankeyTreeTopLandingExpected[slot] = FALSE;
    runtime->movementMankeyTreeTopSettled[slot] = FALSE;
    runtime->movementMankeyTreeTopSettledX[slot] = OW_WILD_SPAWNER_PREVIOUS_TILE_NONE;
    runtime->movementMankeyTreeTopSettledY[slot] = OW_WILD_SPAWNER_PREVIOUS_TILE_NONE;
}

static void OverworldWildSpawns_ClearMankeyTreeTopProxyObject(
    OverworldWildSpawnState *state,
    int slot,
    BOOL deleteObject)
{
    FieldSystem *fieldSystem;
    LocalMapObject *proxyObject;

    if (state == NULL || slot < 0 || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    proxyObject = state->movementMankeyTreeTopProxyObjects[slot];
    state->movementMankeyTreeTopProxyObjects[slot] = NULL;
    if (proxyObject == NULL || !deleteObject) {
        return;
    }

    fieldSystem = state->movementFieldSystem;
    if (OverworldWildSpawns_IsPresentationFieldContextCurrent(state, fieldSystem)
        && OverworldWildSpawns_IsCurrentMapObject(fieldSystem, proxyObject)
        && proxyObject->id == OW_WILD_MANKEY_TREE_TOP_PROXY_OBJECT_ID_START + slot) {
        DeleteMapObject(proxyObject);
        OW_WILD_PERF_INC(sOverworldWildPerfMapObjectDeletesThisFrame);
    }
}

static BOOL OverworldWildSpawns_IsCurrentSpawnObject(FieldSystem *fieldSystem, const OverworldWildSpawn *spawn)
{
    if (fieldSystem == NULL
        || fieldSystem->location == NULL
        || !spawn->active
        || spawn->mapId != fieldSystem->location->mapId
        || !OverworldWildSpawns_IsCurrentMapObject(fieldSystem, spawn->object)) {
        return FALSE;
    }

    return (spawn->object->flags & MAPOBJECTFLAG_ACTIVE) != 0
        && spawn->object->id == spawn->objectId
        && spawn->object->scriptId == OVERWORLD_WILD_SPAWNS_BATTLE_SCRIPT;
}

static void OverworldWildSpawns_PersistSavedShinies(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem)
{
#ifdef ALLOW_SAVE_CHANGES
    struct SAVE_MISC_DATA *saveMiscData;

    if (state == NULL || fieldSystem == NULL || fieldSystem->savedata == NULL) {
        return;
    }

    saveMiscData = Sav2_Misc_get(fieldSystem->savedata);
    if (saveMiscData == NULL
        || sizeof(saveMiscData->overworldWildSavedShinies) < sizeof(state->savedShinies)) {
        return;
    }

    memcpy(
        saveMiscData->overworldWildSavedShinies,
        state->savedShinies,
        sizeof(state->savedShinies));
#else
    (void)state;
    (void)fieldSystem;
#endif
}

static void OverworldWildSpawns_LoadSavedShinies(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem)
{
#ifdef ALLOW_SAVE_CHANGES
    struct SAVE_MISC_DATA *saveMiscData;
    int i;

    if (state == NULL
        || state->savedShiniesLoaded
        || fieldSystem == NULL
        || fieldSystem->savedata == NULL) {
        return;
    }

    saveMiscData = Sav2_Misc_get(fieldSystem->savedata);
    if (saveMiscData == NULL
        || sizeof(saveMiscData->overworldWildSavedShinies) < sizeof(state->savedShinies)) {
        return;
    }

    if (saveMiscData->overworldWildShinyCounterMagic != OVERWORLD_WILD_SHINY_COUNTER_SAVE_MAGIC) {
        if (saveMiscData->overworldWildShinyCounterMagic != OVERWORLD_WILD_SHINY_COUNTER_SAVE_MAGIC_V1) {
            saveMiscData->overworldWildShinySpawnCounter = 0;
        }
        saveMiscData->overworldWildShinyCounterMagic = OVERWORLD_WILD_SHINY_COUNTER_SAVE_MAGIC;
        memset(saveMiscData->overworldWildSavedShinies, 0, sizeof(saveMiscData->overworldWildSavedShinies));
    }

    memcpy(
        state->savedShinies,
        saveMiscData->overworldWildSavedShinies,
        sizeof(state->savedShinies));
    for (i = 0; i < OW_WILD_MAX_SAVED_SHINIES; i++) {
        if ((state->savedShinies[i].terrainAndActive & OW_WILD_SAVED_SHINY_ACTIVE) != 0
            && (state->savedShinies[i].mapId == MAP_NOTHING
                || (state->savedShinies[i].speciesAndForm & OW_WILD_SPECIES_MASK) == SPECIES_NONE
                || state->savedShinies[i].level == 0)) {
            state->savedShinies[i].terrainAndActive = 0;
        }
    }

    state->savedShiniesLoaded = TRUE;
#else
    (void)state;
    (void)fieldSystem;
#endif
}

static void OverworldWildSpawns_ClearSavedShiny(OverworldWildSpawnState *state, int slot)
{
    state->savedShinies[slot].terrainAndActive = 0;
    OverworldWildSpawns_PersistSavedShinies(state, state->movementFieldSystem);
}

static void OverworldWildSpawns_TrySaveShinyReservation(OverworldWildSpawnState *state, const OverworldWildSpawn *spawn)
{
    int i;

    if (!spawn->active
        || !spawn->shiny
        || spawn->species == SPECIES_NONE
        || spawn->mapId == MAP_NOTHING) {
        return;
    }

    for (i = 0; i < OW_WILD_MAX_SAVED_SHINIES; i++) {
        if ((state->savedShinies[i].terrainAndActive & OW_WILD_SAVED_SHINY_ACTIVE) == 0) {
            state->savedShinies[i].mapId = spawn->mapId;
            state->savedShinies[i].speciesAndForm =
                spawn->species | (spawn->form << OW_WILD_FORM_SHIFT);
            state->savedShinies[i].level = spawn->level;
            state->savedShinies[i].terrainAndActive =
                OW_WILD_SAVED_SHINY_ACTIVE | (spawn->terrain & OW_WILD_SAVED_SHINY_TERRAIN_MASK);
            OverworldWildSpawns_PersistSavedShinies(state, state->movementFieldSystem);
            return;
        }
    }
}

static int OverworldWildSpawns_FindSavedShiny(OverworldWildSpawnState *state, u16 mapId, OverworldWildSpawnTerrain terrain)
{
    int i;

    for (i = 0; i < OW_WILD_MAX_SAVED_SHINIES; i++) {
        if ((state->savedShinies[i].terrainAndActive & OW_WILD_SAVED_SHINY_ACTIVE) != 0
            && state->savedShinies[i].mapId == mapId
            && (state->savedShinies[i].terrainAndActive & OW_WILD_SAVED_SHINY_TERRAIN_MASK) == terrain) {
            return i;
        }
    }

    return -1;
}

static void OverworldWildSpawns_LoadSavedShinyEncounter(OverworldWildSpawnState *state, int slot, OverworldWildRolledEncounter *encounter)
{
    encounter->personality = gf_rand() | (gf_rand() << 16);
    encounter->species = state->savedShinies[slot].speciesAndForm & OW_WILD_SPECIES_MASK;
    encounter->form = state->savedShinies[slot].speciesAndForm >> OW_WILD_FORM_SHIFT;
    encounter->level = state->savedShinies[slot].level;
}

static void OverworldWildSpawns_ClearSlotAndSaveShiny(OverworldWildSpawnState *state, int slot, BOOL deleteObject)
{
    OverworldWildSpawns_TrySaveShinyReservation(state, &state->spawns[slot]);
    OverworldWildSpawns_ResetSlotState(state, slot, deleteObject);
}

static void OverworldWildSpawns_QuarantinePoisonedPresentation(
    FieldSystem *fieldSystem,
    int slot)
{
    MapObjectMan *manager = (MapObjectMan *)fieldSystem->mapObjectMan;
    int i;

    for (i = 0; i < (int)manager->object_count; i++) {
        LocalMapObject *object = &manager->objects[i];

        if ((object->flags & MAPOBJECTFLAG_ACTIVE) != 0
            && object->id == OW_WILD_OBJECT_ID_START + slot
            && object->scriptId == OVERWORLD_WILD_SPAWNS_BATTLE_SCRIPT) {
            DeleteMapObject(object);
        }
    }
}

static BOOL OverworldWildSpawns_ReconcileSpawnPresentations(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem)
{
    const OverworldWildHelperOverlayEntry *helperEntry =
        OverworldWildSpawns_GetHelperOverlayEntry();
    int result;
    int slot;

    if (helperEntry == NULL || helperEntry->reconcilePresentations == NULL) {
        state->presentationRestorePending = TRUE;
        return FALSE;
    }
    for (;;) {
        result = helperEntry->reconcilePresentations(
            fieldSystem,
            state,
            &OW_WILD_RUNTIME(state)->spawnPresentations,
            &OW_WILD_RUNTIME(state)->despawnTelemetry,
            OverworldWildSpawns_RecreateSpawnObjectAtTile);
        if (result == OW_WILD_RECONCILE_COMPLETE) {
            state->presentationRestorePending = FALSE;
            return TRUE;
        }
        state->presentationRestorePending = TRUE;
        slot = result - OW_WILD_RECONCILE_POISONED_SLOT_BASE;
        if (result < OW_WILD_RECONCILE_POISONED_SLOT_BASE
            || slot >= OW_WILD_MAX_SPAWNS
            || state->pendingSlot == slot
            || state->movementQueuedBattleSlot == slot
            || (OverworldWildSpawns_GetThrowParticipantMask(state)
                & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot)) != 0) {
            return FALSE;
        }
        OverworldWildSpawns_QuarantinePoisonedPresentation(fieldSystem, slot);
        OverworldWildSpawns_ClearSlotAndSaveShiny(state, slot, TRUE);
    }
}

#if !OW_WILD_STEP_DIAGNOSTIC_SKIP_AMBIENT_CRY || !OW_WILD_UPDATE_DIAGNOSTIC_SKIP_CLEAR
static void OverworldWildSpawns_ResetAmbientCryCooldown(OverworldWildSpawnState *state)
{
    state->ambientCryCooldown = OW_WILD_AMBIENT_CRY_MIN_COOLDOWN_STEPS
        + (gf_rand() % OW_WILD_AMBIENT_CRY_RANDOM_COOLDOWN_STEPS);
}
#endif

#if !OW_WILD_STEP_DIAGNOSTIC_SKIP_AMBIENT_CRY
static void OverworldWildSpawns_TryPlayAmbientCry(OverworldWildSpawnState *state)
{
    int i;
    int activeCount = 0;
    u8 cooldownTick;
    int chosen;
    u16 throwTargetMask = OW_WILD_RUNTIME(state)->throwState.targetMask;

    for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
        if (state->spawns[i].active
            && state->spawns[i].object != NULL
            && (throwTargetMask & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(i)) == 0) {
            activeCount++;
        }
    }

    if (activeCount == 0) {
        OverworldWildSpawns_ResetAmbientCryCooldown(state);
        return;
    }

    cooldownTick = 1 + (activeCount / 3);
    if (cooldownTick > OW_WILD_AMBIENT_CRY_MAX_COOLDOWN_TICK) {
        cooldownTick = OW_WILD_AMBIENT_CRY_MAX_COOLDOWN_TICK;
    }

    if (state->ambientCryCooldown > cooldownTick) {
        state->ambientCryCooldown -= cooldownTick;
        return;
    }

    chosen = gf_rand() % activeCount;
    for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
        if (state->spawns[i].active
            && state->spawns[i].object != NULL
            && (throwTargetMask & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(i)) == 0) {
            if (chosen == 0) {
                PlayCry(
                    state->spawns[i].species,
                    state->spawns[i].form);
                OverworldWildSpawns_ResetAmbientCryCooldown(state);
                return;
            }
            chosen--;
        }
    }

    OverworldWildSpawns_ResetAmbientCryCooldown(state);
}
#endif

#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_PARAM_TICK
static int OverworldWildSpawns_DiagnosticAbs(int value)
{
    return value < 0 ? -value : value;
}

static int OverworldWildSpawns_DiagnosticBuildDirections(int dx, int dy, u8 *directions)
{
    int count = 0;

    if (dx == 0 && dy == 0) {
        return 0;
    }

    if (OverworldWildSpawns_DiagnosticAbs(dx) >= OverworldWildSpawns_DiagnosticAbs(dy)) {
        if (dx > 0) {
            directions[count++] = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT;
        }
        if (dx < 0 && count < OW_WILD_SPAWNER_MOVEMENT_MAX_DIRECTIONS) {
            directions[count++] = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_LEFT;
        }
        if (dy > 0 && count < OW_WILD_SPAWNER_MOVEMENT_MAX_DIRECTIONS) {
            directions[count++] = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN;
        }
        if (dy < 0 && count < OW_WILD_SPAWNER_MOVEMENT_MAX_DIRECTIONS) {
            directions[count++] = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_UP;
        }
        return count;
    }

    if (dy > 0) {
        directions[count++] = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN;
    }
    if (dy < 0 && count < OW_WILD_SPAWNER_MOVEMENT_MAX_DIRECTIONS) {
        directions[count++] = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_UP;
    }
    if (dx > 0 && count < OW_WILD_SPAWNER_MOVEMENT_MAX_DIRECTIONS) {
        directions[count++] = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT;
    }
    if (dx < 0 && count < OW_WILD_SPAWNER_MOVEMENT_MAX_DIRECTIONS) {
        directions[count++] = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_LEFT;
    }

    return count;
}

static BOOL OverworldWildSpawns_IsMovementSlotInProgress(OverworldWildSpawnState *state, int slot)
{
    return (state->movementInProgressMask & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot)) != 0;
}

static BOOL OverworldWildSpawns_IsNativeHeldMovementOwned(OverworldWildSpawnState *state, int slot)
{
    return (OW_WILD_RUNTIME(state)->movementNativeHeldMask
        & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot)) != 0;
}

static BOOL OverworldWildSpawns_IsValidBehaviorKind(u8 behavior)
{
    return behavior <= OW_WILD_BEHAVIOR_KIND_NO_VISUAL;
}

static BOOL OverworldWildSpawns_IsValidMovementStyle(u8 movementStyle)
{
    return movementStyle <= OW_WILD_BEHAVIOR_LOCOMOTION_APPEAR_HOP;
}

static u8 __attribute__((noinline)) OverworldWildSpawns_GetAllowedTileForSpotState(
    const OverworldWildBehaviorProfile *profile,
    u8 spotState)
{
    u8 primary;
    u8 secondary;

    switch (spotState) {
    case OW_WILD_SPAWNER_SPOT_STATE_ACTIVE:
        primary = profile->attentiveAllowedTile;
        secondary = profile->attentiveAllowedTile2;
        break;
    case OW_WILD_SPAWNER_SPOT_STATE_TIRED:
        primary = profile->tiredAllowedTile;
        secondary = profile->tiredAllowedTile2;
        break;
    case OW_WILD_SPAWNER_SPOT_STATE_CHILL:
    default:
        primary = profile->chillAllowedTile;
        secondary = profile->chillAllowedTile2;
        break;
    }
    return OW_WILD_BEHAVIOR_ALLOWED_TILE_PACK(primary, secondary);
}

static u8 OverworldWildSpawns_SelectStateMovementByte(
    u8 spotState,
    u8 chillValue,
    u8 attentiveValue,
    u8 tiredValue)
{
    if (spotState == OW_WILD_SPAWNER_SPOT_STATE_ACTIVE) {
        return attentiveValue;
    }
    if (spotState == OW_WILD_SPAWNER_SPOT_STATE_TIRED) {
        return tiredValue;
    }
    return chillValue;
}

static u8 OverworldWildSpawns_GetPhantomTeleportMoveFrames(
    const OverworldWildBehaviorProfile *profile,
    u8 spotState)
{
    u8 teleportTime;

    if (profile == NULL) {
        return OW_WILD_SPAWNER_PHANTOM_STALK_TELEPORT_MOVE_FRAMES;
    }

    teleportTime = OverworldWildSpawns_SelectStateMovementByte(
        spotState,
        profile->teleportTime,
        profile->attentiveTeleportTime,
        profile->tiredTeleportTime);

    return teleportTime != 0
        ? teleportTime
        : OW_WILD_SPAWNER_PHANTOM_STALK_TELEPORT_MOVE_FRAMES;
}

static u8 OverworldWildSpawns_GetPhantomTeleportPauseFrames(
    const OverworldWildBehaviorProfile *profile,
    u8 spotState)
{
    u8 teleportPause;

    if (profile == NULL) {
        return OW_WILD_SPAWNER_PHANTOM_STALK_POST_TELEPORT_COOLDOWN_FRAMES;
    }

    teleportPause = OverworldWildSpawns_SelectStateMovementByte(
        spotState,
        profile->teleportPause,
        profile->attentiveTeleportPause,
        profile->tiredTeleportPause);

    return teleportPause != 0
        ? teleportPause
        : OW_WILD_SPAWNER_PHANTOM_STALK_POST_TELEPORT_COOLDOWN_FRAMES;
}

static BOOL OverworldWildSpawns_BehaviorUsesAsleep(u8 behavior)
{
    return behavior == OW_WILD_BEHAVIOR_KIND_ASLEEP;
}

static BOOL OverworldWildSpawns_BehaviorKindUsesMovement(u8 behavior)
{
    switch (behavior) {
    case OW_WILD_BEHAVIOR_KIND_WANDER:
    case OW_WILD_BEHAVIOR_KIND_CHASE:
    case OW_WILD_BEHAVIOR_KIND_FLEE:
    case OW_WILD_BEHAVIOR_KIND_PLAYFUL:
    case OW_WILD_BEHAVIOR_KIND_RAM:
    case OW_WILD_BEHAVIOR_KIND_HEADBUTT_TREE_HOP:
        return TRUE;
    default:
        return FALSE;
    }
}

static u32 OverworldWildSpawns_GetFrameMovementWorkForSlot(
    OverworldWildSpawnState *state,
    int slot)
{
    u16 slotMask;

    if (!state->spawns[slot].active
        || state->spawns[slot].object == NULL) {
        return FALSE;
    }
    slotMask = OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot);

    if (OverworldWildSpawns_IsMovementSlotInProgress(state, slot)
        || state->movementSpawnRunActive[slot]
        || state->movementStagedHopPending[slot]
        || state->movementQueuedBattleSlot == slot
        || state->movementRamCrashShakeTimers[slot] != 0
        || OW_WILD_RUNTIME(state)->movementCustomJumpElapsedFrames[slot] != 0
        || (state->movementPhantomVisiblePause[slot]
            && state->movementCooldowns[slot] != 0)
        || state->movementPhantomFlickerTimers[slot] != 0) {
        return slotMask | ((u32)slotMask << OW_WILD_SPAWNER_FRAME_WORK_HEAVY_SHIFT);
    }

    if (state->movementSpotStates[slot] == OW_WILD_SPAWNER_SPOT_STATE_EMOTING
        || (state->movementSpotStates[slot] == OW_WILD_SPAWNER_SPOT_STATE_TIRED
            && state->movementEmoteTimers[slot] != 0)) {
        return slotMask | ((u32)slotMask << OW_WILD_SPAWNER_FRAME_WORK_HEAVY_SHIFT);
    }
    if (state->movementCooldowns[slot] != 0) {
        return slotMask;
    }
    if (state->movementSpotStates[slot] == OW_WILD_SPAWNER_SPOT_STATE_CHILL) {
        return OW_WILD_RUNTIME(state)->movementFrameDrivenChillMask & slotMask;
    }
    if (state->movementSpotStates[slot] == OW_WILD_SPAWNER_SPOT_STATE_ACTIVE) {
        return OW_WILD_RUNTIME(state)->movementFrameDrivenActiveMask & slotMask;
    }

    return FALSE;
}

static u32 OverworldWildSpawns_GetFrameMovementWork(
    OverworldWildSpawnState *state)
{
    u32 frameWork;
    int i;

    if (state == NULL) {
        return FALSE;
    }
    frameWork = state->movementInProgressMask
        | ((u32)state->movementInProgressMask << OW_WILD_SPAWNER_FRAME_WORK_HEAVY_SHIFT);
    if (OverworldWildSpawns_HasQueuedBattle(state)
        || (state->movementRuntimeState != NULL
            && ((OW_WILD_RUNTIME(state)->movementHelpSpawnParentSlotPlusOne != 0
                    && OW_WILD_RUNTIME(state)->movementHelpSpawnRemaining != 0)
                || OW_WILD_RUNTIME(state)->throwState.targetMask != 0
                || OW_WILD_RUNTIME(state)->throwState.carrierMask != 0))) {
        frameWork |= OW_WILD_SPAWNER_FRAME_WORK_GLOBAL;
    }

    for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
        frameWork |= OverworldWildSpawns_GetFrameMovementWorkForSlot(state, i);
    }

    return frameWork;
}

static u16 OverworldWildSpawns_SelectIdleAiWork(
    OverworldWildSpawnState *state,
    u16 eligibleMask)
{
    OverworldWildOverlayRuntimeState *runtime = OW_WILD_RUNTIME(state);
    u16 slotMask = OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(runtime->movementIdleAiCursor);

    if (++runtime->movementIdleAiCursor >= OW_WILD_MAX_SPAWNS) {
        runtime->movementIdleAiCursor = 0;
    }
    return eligibleMask & slotMask;
}

static BOOL OverworldWildSpawns_IsPresentationFieldContextCurrent(OverworldWildSpawnState *state, FieldSystem *fieldSystem)
{
    const OverworldWildHelperOverlayEntry *helperEntry;

    if (fieldSystem == NULL
        || (!sOverworldWildHelperOverlayReady
            && !IsOverlayLoaded(OVERLAY_OVERWORLD_WILD_HELPER))) {
        return FALSE;
    }
    helperEntry = OverworldWildSpawns_GetHelperOverlayEntry();

    return helperEntry != NULL
        && helperEntry->isPresentationContextCurrent(fieldSystem, state);
}

static BOOL OverworldWildSpawns_IsMovementFieldContextCurrent(OverworldWildSpawnState *state, FieldSystem *fieldSystem)
{
    return fieldSystem != NULL
        && fieldSystem->playerAvatar != NULL
        && OverworldWildSpawns_IsPresentationFieldContextCurrent(state, fieldSystem);
}

static u16 OverworldWildSpawns_GetCurrentMovementSpawnMask(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem)
{
    u16 currentMask = 0;
    int i;

    if (state == NULL || fieldSystem == NULL) {
        return FALSE;
    }

    for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
        if (!state->spawns[i].active) {
            continue;
        }
        if (OverworldWildSpawns_IsCurrentSpawnObject(fieldSystem, &state->spawns[i])) {
            currentMask |= OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(i);
        } else {
            state->presentationRestorePending = TRUE;
            OW_WILD_RUNTIME(state)->spawnPresentations.managerRestoreMask |=
                OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(i);
        }
    }

    return currentMask;
}

static void OverworldWildSpawns_SetMovementSlotInProgress(OverworldWildSpawnState *state, int slot)
{
    state->movementInProgressMask |= OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot);
}

static void OverworldWildSpawns_CancelNativeHeldMovementForSlot(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object)
{
    u16 slotMask;

    if (!OverworldWildSpawns_IsNativeHeldMovementOwned(state, slot)) {
        return;
    }
    slotMask = OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot);
    MapObject_ClearHeldMovement(object);
    MapObject_ClearSingleMovementActive(object);
    OW_WILD_RUNTIME(state)->movementNativeHeldMask &= ~slotMask;
    state->movementInProgressMask &= ~slotMask;
    state->movementPreviousTileLocked[slot] = FALSE;
}

static void OverworldWildSpawns_CancelNativeHeldMovement(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem)
{
    int i;

    if (state->movementRuntimeState == NULL
        || OW_WILD_RUNTIME(state)->movementNativeHeldMask == 0
        || !OverworldWildSpawns_IsMovementFieldContextCurrent(state, fieldSystem)) {
        return;
    }
    for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
        if (OverworldWildSpawns_IsNativeHeldMovementOwned(state, i)) {
            OverworldWildSpawns_CancelNativeHeldMovementForSlot(
                state,
                i,
                state->spawns[i].object);
        }
    }
}

static u32 OverworldWildSpawns_GetBehaviorTypeGroupFlag(u8 type)
{
    switch (type) {
    case TYPE_NORMAL:
        return OW_WILD_BEHAVIOR_GROUP_TYPE_NORMAL;
    case TYPE_FIGHTING:
        return OW_WILD_BEHAVIOR_GROUP_TYPE_FIGHTING;
    case TYPE_FLYING:
        return OW_WILD_BEHAVIOR_GROUP_TYPE_FLYING;
    case TYPE_POISON:
        return OW_WILD_BEHAVIOR_GROUP_TYPE_POISON;
    case TYPE_GROUND:
        return OW_WILD_BEHAVIOR_GROUP_TYPE_GROUND;
    case TYPE_ROCK:
        return OW_WILD_BEHAVIOR_GROUP_TYPE_ROCK;
    case TYPE_BUG:
        return OW_WILD_BEHAVIOR_GROUP_TYPE_BUG;
    case TYPE_GHOST:
        return OW_WILD_BEHAVIOR_GROUP_TYPE_GHOST;
    case TYPE_STEEL:
        return OW_WILD_BEHAVIOR_GROUP_TYPE_STEEL;
    case TYPE_FAIRY:
        return OW_WILD_BEHAVIOR_GROUP_TYPE_FAIRY;
    case TYPE_FIRE:
        return OW_WILD_BEHAVIOR_GROUP_TYPE_FIRE;
    case TYPE_WATER:
        return OW_WILD_BEHAVIOR_GROUP_TYPE_WATER;
    case TYPE_GRASS:
        return OW_WILD_BEHAVIOR_GROUP_TYPE_GRASS;
    case TYPE_ELECTRIC:
        return OW_WILD_BEHAVIOR_GROUP_TYPE_ELECTRIC;
    case TYPE_PSYCHIC:
        return OW_WILD_BEHAVIOR_GROUP_TYPE_PSYCHIC;
    case TYPE_ICE:
        return OW_WILD_BEHAVIOR_GROUP_TYPE_ICE;
    case TYPE_DRAGON:
        return OW_WILD_BEHAVIOR_GROUP_TYPE_DRAGON;
    case TYPE_DARK:
        return OW_WILD_BEHAVIOR_GROUP_TYPE_DARK;
    case TYPE_TYPELESS:
        return OW_WILD_BEHAVIOR_GROUP_TYPE_TYPELESS;
    case TYPE_STELLAR:
        return OW_WILD_BEHAVIOR_GROUP_TYPE_STELLAR;
    default:
        return OW_WILD_BEHAVIOR_GROUP_NONE;
    }
}

static u32 OverworldWildSpawns_GetBehaviorGroupFlagsForTypes(
    u16 species,
    u8 type1,
    u8 type2)
{
    u32 flags = OW_WILD_BEHAVIOR_GROUP_NONE;

    switch (species) {
    case SPECIES_GASTLY:
    case SPECIES_HAUNTER:
    case SPECIES_GENGAR:
    case SPECIES_MISDREAVUS:
    case SPECIES_DUSKULL:
        flags |= OW_WILD_BEHAVIOR_GROUP_GHOST;
        break;
    case SPECIES_PICHU:
    case SPECIES_CLEFFA:
    case SPECIES_IGGLYBUFF:
    case SPECIES_TOGEPI:
    case SPECIES_TYROGUE:
    case SPECIES_SMOOCHUM:
    case SPECIES_ELEKID:
    case SPECIES_MAGBY:
    case SPECIES_AZURILL:
    case SPECIES_WYNAUT:
    case SPECIES_BUDEW:
    case SPECIES_CHINGLING:
    case SPECIES_BONSLY:
    case SPECIES_MIME_JR:
    case SPECIES_HAPPINY:
    case SPECIES_MUNCHLAX:
    case SPECIES_RIOLU:
    case SPECIES_MANTYKE:
        flags |= OW_WILD_BEHAVIOR_GROUP_BABY;
        break;
    default:
        break;
    }

    if (species != SPECIES_NONE) {
        flags |= OverworldWildSpawns_GetBehaviorTypeGroupFlag(type1);
        flags |= OverworldWildSpawns_GetBehaviorTypeGroupFlag(type2);
    }

    return flags;
}

static u32 OverworldWildSpawns_GetBehaviorGroupFlags(u16 species)
{
    u8 type1 = 0;
    u8 type2 = 0;

    if (species != SPECIES_NONE) {
        type1 = (u8)PokePersonalParaGet(species, PERSONAL_TYPE_1);
        type2 = (u8)PokePersonalParaGet(species, PERSONAL_TYPE_2);
    }
    return OverworldWildSpawns_GetBehaviorGroupFlagsForTypes(
        species,
        type1,
        type2);
}

static OverworldWildBehaviorContext OverworldWildSpawns_BuildBehaviorContext(
    const OverworldWildSpawn *spawn,
    u16 species,
    u8 level,
    u8 terrain,
    u8 shiny)
{
    OverworldWildBehaviorContext context;

    if (spawn != NULL && spawn->active) {
        context.species = spawn->species;
        context.groupFlags = OverworldWildSpawns_GetBehaviorGroupFlags(spawn->species);
        context.level = spawn->level;
        context.terrain = spawn->terrain;
        context.shiny = spawn->shiny;
        context.behaviorClass = OW_WILD_BEHAVIOR_CLASS_DEFAULT;
        return context;
    }

    context.species = species;
    context.groupFlags = OverworldWildSpawns_GetBehaviorGroupFlags(species);
    context.level = level;
    context.terrain = terrain;
    context.shiny = shiny;
    context.behaviorClass = OW_WILD_BEHAVIOR_CLASS_DEFAULT;
    return context;
}

static BOOL OverworldWildSpawns_BehaviorMatchApplies(
    const OverworldWildBehaviorContext *context,
    const OverworldWildBehaviorMatch *match)
{
    if (context == NULL || match == NULL) {
        return FALSE;
    }

    if (match->species != OW_WILD_BEHAVIOR_MATCH_ANY_SPECIES
        && match->species != context->species) {
        return FALSE;
    }
    if (match->groupMask != OW_WILD_BEHAVIOR_GROUP_NONE
        && (context->groupFlags & match->groupMask) == 0) {
        return FALSE;
    }
    if (match->terrain != OW_WILD_BEHAVIOR_MATCH_ANY_TERRAIN
        && match->terrain != context->terrain) {
        return FALSE;
    }
    if (match->minLevel != OW_WILD_BEHAVIOR_MATCH_LEVEL_ANY
        && context->level < match->minLevel) {
        return FALSE;
    }
    if (match->maxLevel != OW_WILD_BEHAVIOR_MATCH_LEVEL_ANY
        && context->level > match->maxLevel) {
        return FALSE;
    }
    if (match->shiny != OW_WILD_BEHAVIOR_MATCH_ANY_SHINY
        && match->shiny != context->shiny) {
        return FALSE;
    }
    if (match->behaviorClass != OW_WILD_BEHAVIOR_MATCH_ANY_CLASS
        && match->behaviorClass != context->behaviorClass) {
        return FALSE;
    }

    return TRUE;
}

static BOOL OverworldWildSpawns_OverrideProfileTargetsContext(
    const OverworldWildBehaviorDataBlob *behaviorData,
    const OverworldWildBehaviorContext *context,
    const OverworldWildBehaviorOverrideProfile *overrideProfile)
{
    u32 memberEnd;
    int i;

    if (behaviorData == NULL || context == NULL || overrideProfile == NULL
        || overrideProfile->targetMode == OW_WILD_BEHAVIOR_OVERRIDE_TARGET_DISABLED
        || !OverworldWildSpawns_BehaviorMatchApplies(context, &overrideProfile->match)) {
        return FALSE;
    }
    if (overrideProfile->targetMode == OW_WILD_BEHAVIOR_OVERRIDE_TARGET_ALL) {
        return TRUE;
    }
    if (overrideProfile->targetMode != OW_WILD_BEHAVIOR_OVERRIDE_TARGET_MEMBERS
        || overrideProfile->memberCount == 0) {
        return FALSE;
    }

    memberEnd = (u32)overrideProfile->memberStart + overrideProfile->memberCount;
    if (memberEnd > OWBD_OVERRIDE_MEMBER_COUNT) {
        return FALSE;
    }
    for (i = overrideProfile->memberStart; i < memberEnd; i++) {
        if (behaviorData->overrideMembers[i] == context->species) {
            return TRUE;
        }
    }
    return FALSE;
}

#define OW_WILD_PROFILE_OFFSET(field) ((u8)(u32)&(((OverworldWildBehaviorProfile *)0)->field))

static const u8 sOverworldWildBehaviorOverrideFieldOffsets[] = {
    OW_WILD_PROFILE_OFFSET(chillState),
    OW_WILD_PROFILE_OFFSET(alertState),
    OW_WILD_PROFILE_OFFSET(alertEmote),
    OW_WILD_PROFILE_OFFSET(alertness),
    OW_WILD_PROFILE_OFFSET(attentiveState),
    OW_WILD_PROFILE_OFFSET(stamina),
    OW_WILD_PROFILE_OFFSET(tiredState),
    OW_WILD_PROFILE_OFFSET(restTime),
    OW_WILD_PROFILE_OFFSET(chillSpeed),
    OW_WILD_PROFILE_OFFSET(attentiveSpeed),
    OW_WILD_PROFILE_OFFSET(range),
    OW_WILD_PROFILE_OFFSET(jumpLevel),
    OW_WILD_PROFILE_OFFSET(profileId),
    OW_WILD_PROFILE_OFFSET(spawnState),
    OW_WILD_PROFILE_OFFSET(chillAction),
    OW_WILD_PROFILE_OFFSET(alertRange),
    OW_WILD_PROFILE_OFFSET(tiredSpeed),
    OW_WILD_PROFILE_OFFSET(targetSelector),
    OW_WILD_PROFILE_OFFSET(movementStyle),
    OW_WILD_PROFILE_OFFSET(alertChance),
    OW_WILD_PROFILE_OFFSET(alertTime),
    OW_WILD_PROFILE_OFFSET(spawnDestination),
    OW_WILD_PROFILE_OFFSET(attentiveBattle),
    OW_WILD_PROFILE_OFFSET(specialAction),
    OW_WILD_PROFILE_OFFSET(hopAllowNonCardinal),
    OW_WILD_PROFILE_OFFSET(hopMinDistance),
    OW_WILD_PROFILE_OFFSET(hopMaxDistance),
    OW_WILD_PROFILE_OFFSET(hopPause),
    OW_WILD_PROFILE_OFFSET(teleportTime),
    OW_WILD_PROFILE_OFFSET(teleportPause),
    OW_WILD_PROFILE_OFFSET(alertSpecialAction),
    OW_WILD_PROFILE_OFFSET(spawnDestinationMinDistance),
    OW_WILD_PROFILE_OFFSET(spawnDestinationMaxDistance),
    OW_WILD_PROFILE_OFFSET(ramAccelerationSteps),
    OW_WILD_PROFILE_OFFSET(ramMaxSpeed),
    OW_WILD_PROFILE_OFFSET(chillAllowedTile),
    OW_WILD_PROFILE_OFFSET(attentiveAllowedTile),
    OW_WILD_PROFILE_OFFSET(tiredAllowedTile),
    OW_WILD_PROFILE_OFFSET(chillAllowedTile2),
    OW_WILD_PROFILE_OFFSET(attentiveAllowedTile2),
    OW_WILD_PROFILE_OFFSET(tiredAllowedTile2),
    OW_WILD_PROFILE_OFFSET(chillTarget),
    OW_WILD_PROFILE_OFFSET(attentiveHopAllowNonCardinal),
    OW_WILD_PROFILE_OFFSET(attentiveHopMinDistance),
    OW_WILD_PROFILE_OFFSET(attentiveHopMaxDistance),
    OW_WILD_PROFILE_OFFSET(attentiveHopPause),
    OW_WILD_PROFILE_OFFSET(attentiveTeleportTime),
    OW_WILD_PROFILE_OFFSET(attentiveTeleportPause),
    OW_WILD_PROFILE_OFFSET(attentiveRamAccelerationSteps),
    OW_WILD_PROFILE_OFFSET(attentiveRamMaxSpeed),
    OW_WILD_PROFILE_OFFSET(tiredHopAllowNonCardinal),
    OW_WILD_PROFILE_OFFSET(tiredHopMinDistance),
    OW_WILD_PROFILE_OFFSET(tiredHopMaxDistance),
    OW_WILD_PROFILE_OFFSET(tiredHopPause),
    OW_WILD_PROFILE_OFFSET(tiredTeleportTime),
    OW_WILD_PROFILE_OFFSET(tiredTeleportPause),
    OW_WILD_PROFILE_OFFSET(tiredRamAccelerationSteps),
    OW_WILD_PROFILE_OFFSET(tiredRamMaxSpeed),
    OW_WILD_PROFILE_OFFSET(overworldLimit),
    OW_WILD_PROFILE_OFFSET(hopTime),
    OW_WILD_PROFILE_OFFSET(attentiveChaseBoostDistance),
    OW_WILD_PROFILE_OFFSET(attentiveChaseBoostSpeed),
    OW_WILD_PROFILE_OFFSET(hopSpinSpeed),
    OW_WILD_PROFILE_OFFSET(spawnHopTime),
    OW_WILD_PROFILE_OFFSET(attentiveHopSpinSpeed),
    OW_WILD_PROFILE_OFFSET(attentiveCircleRadius),
    OW_WILD_PROFILE_OFFSET(attentiveContinueWhenArrived),
    OW_WILD_PROFILE_OFFSET(attentiveAvoidPreviousTile),
    OW_WILD_PROFILE_OFFSET(chainPauseAction),
};

#undef OW_WILD_PROFILE_OFFSET

static void OverworldWildSpawns_ApplyBehaviorOverrideMask(
    OverworldWildBehaviorProfile *profile,
    const OverworldWildBehaviorProfile *overrideProfile,
    u32 mask,
    u8 fieldIndex)
{
    u8 *profileBytes = (u8 *)profile;
    const u8 *overrideBytes = (const u8 *)overrideProfile;

    while (mask != 0 && fieldIndex < NELEMS(sOverworldWildBehaviorOverrideFieldOffsets)) {
        if (mask & 1u) {
            u8 offset = sOverworldWildBehaviorOverrideFieldOffsets[fieldIndex];
            profileBytes[offset] = overrideBytes[offset];
        }
        mask >>= 1;
        fieldIndex++;
    }
}

static void OverworldWildSpawns_ApplyBehaviorOverride(
    OverworldWildBehaviorProfile *profile,
    const OverworldWildBehaviorOverrideProfile *overrideProfile)
{
    if (profile == NULL || overrideProfile == NULL) {
        return;
    }

    OverworldWildSpawns_ApplyBehaviorOverrideMask(profile, &overrideProfile->profile, overrideProfile->mask, 0);
    OverworldWildSpawns_ApplyBehaviorOverrideMask(profile, &overrideProfile->profile, overrideProfile->mask2, 27);
    OverworldWildSpawns_ApplyBehaviorOverrideMask(profile, &overrideProfile->profile, overrideProfile->mask3, 42);
}

static BOOL OverworldWildSpawns_OverrideSetsOverworldLimit(
    const OverworldWildBehaviorOverrideProfile *overrideProfile)
{
    return overrideProfile != NULL
        && (overrideProfile->mask3 & OW_WILD_BEHAVIOR_OVERRIDE3_OVERWORLD_LIMIT) != 0;
}

static u8 OverworldWildSpawns_GetBehaviorClassForContext(const OverworldWildBehaviorContext *context)
{
    const OverworldWildBehaviorDataBlob *behaviorData;
    u8 behaviorClass = OW_WILD_BEHAVIOR_CLASS_DEFAULT;
    int i;

    if (context == NULL) {
        return behaviorClass;
    }

    behaviorData = OverworldWildSpawns_GetBehaviorDataBlob();
    if (behaviorData == NULL) {
        return behaviorClass;
    }

    for (i = 0; i < OWBD_CLASS_RULE_COUNT; i++) {
        if (OverworldWildSpawns_BehaviorMatchApplies(context, &behaviorData->classRules[i].match)) {
            behaviorClass = behaviorData->classRules[i].behaviorClass;
        }
    }
    for (i = 0; i < OWBD_SPECIES_CLASS_RULE_COUNT; i++) {
        if (behaviorData->speciesClassRules[i].species == context->species) {
            behaviorClass = behaviorData->speciesClassRules[i].behaviorClass;
        }
    }

    if (behaviorClass >= OWBD_CLASS_PROFILE_COUNT) {
        behaviorClass = OW_WILD_BEHAVIOR_CLASS_DEFAULT;
    }

    return behaviorClass;
}

static OverworldWildBehaviorProfile OverworldWildSpawns_ResolveBehaviorProfileForContext(
    const OverworldWildBehaviorContext *context,
    u8 *behaviorLimitKeyOut)
{
    const OverworldWildBehaviorDataBlob *behaviorData;
    OverworldWildBehaviorProfile profile = OverworldWildSpawns_GetFallbackBehaviorProfile();
    u8 behaviorClass = OW_WILD_BEHAVIOR_CLASS_DEFAULT;
    u8 behaviorLimitKey = OW_WILD_BEHAVIOR_CLASS_DEFAULT;
    int overrideProfileIndex;

    if (context != NULL) {
        behaviorClass = context->behaviorClass;
        behaviorLimitKey = behaviorClass;
    }

    behaviorData = OverworldWildSpawns_GetBehaviorDataBlob();
    if (behaviorData == NULL) {
        if (behaviorLimitKeyOut != NULL) {
            *behaviorLimitKeyOut = behaviorLimitKey;
        }
        return profile;
    }

    if (behaviorClass >= OWBD_CLASS_PROFILE_COUNT) {
        behaviorClass = OW_WILD_BEHAVIOR_CLASS_DEFAULT;
    }
    behaviorLimitKey = behaviorClass;

    profile = behaviorData->classProfiles[behaviorClass];

    if (context != NULL && behaviorClass != OW_WILD_BEHAVIOR_CLASS_PICKED_UP) {
        /* Each ordered override profile owns one target and applies at most once. */
        for (overrideProfileIndex = 0;
             overrideProfileIndex < OWBD_OVERRIDE_PROFILE_COUNT;
             overrideProfileIndex++) {
            if (!OverworldWildSpawns_OverrideProfileTargetsContext(
                    behaviorData,
                    context,
                    &behaviorData->overrideProfiles[overrideProfileIndex])) {
                continue;
            }

            OverworldWildSpawns_ApplyBehaviorOverride(
                &profile,
                &behaviorData->overrideProfiles[overrideProfileIndex]);
            if (OverworldWildSpawns_OverrideSetsOverworldLimit(
                    &behaviorData->overrideProfiles[overrideProfileIndex])) {
                behaviorLimitKey = (u8)(OW_WILD_BEHAVIOR_LIMIT_KEY_OVERRIDE_BASE
                    + overrideProfileIndex);
            }
        }
    }

    if (profile.attentiveSpeed == 0) {
        profile.attentiveSpeed = OW_WILD_SPAWNER_MOVEMENT_SPEED_DEFAULT;
    }
    if (profile.chillSpeed == 0) {
        profile.chillSpeed = OW_WILD_SPAWNER_MOVEMENT_SPEED_DEFAULT;
    }
    if (profile.tiredSpeed == 0) {
        profile.tiredSpeed = profile.chillSpeed;
    }
    if (!OverworldWildSpawns_IsValidBehaviorKind(profile.chillState)) {
        profile.chillState = OW_WILD_BEHAVIOR_KIND_IDLE;
    }
    if (!OverworldWildSpawns_IsValidMovementStyle(profile.chillAction)) {
        profile.chillAction = OW_WILD_BEHAVIOR_LOCOMOTION_NONE;
    }
    if (!OverworldWildSpawns_IsValidMovementStyle(profile.movementStyle)) {
        profile.movementStyle = OW_WILD_BEHAVIOR_LOCOMOTION_NONE;
    }
    if (!OverworldWildSpawns_IsValidMovementStyle(profile.specialAction)) {
        profile.specialAction = OW_WILD_BEHAVIOR_LOCOMOTION_NONE;
    }
    if (profile.alertSpecialAction > OW_WILD_BEHAVIOR_ALERT_SPECIAL_PICKUP_THROW) {
        profile.alertSpecialAction = OW_WILD_BEHAVIOR_ALERT_SPECIAL_NONE;
    }
    if (profile.overworldLimit > OW_WILD_MAX_SPAWNS) {
        profile.overworldLimit = OW_WILD_MAX_SPAWNS;
    }
    if (profile.attentiveChaseBoostDistance > OW_WILD_SPAWNER_MOVEMENT_RANGE) {
        profile.attentiveChaseBoostDistance = OW_WILD_SPAWNER_MOVEMENT_RANGE;
    }
    if (profile.attentiveChaseBoostSpeed > OW_WILD_SPAWNER_MOVEMENT_SPEED_4) {
        profile.attentiveChaseBoostSpeed = OW_WILD_SPAWNER_MOVEMENT_SPEED_4;
    }
    if (behaviorLimitKeyOut != NULL) {
        *behaviorLimitKeyOut = behaviorLimitKey;
    }
    if (profile.hopAllowNonCardinal > OW_WILD_BEHAVIOR_BOOL_YES) {
        profile.hopAllowNonCardinal = OW_WILD_BEHAVIOR_BOOL_YES;
    }
    if (profile.hopMinDistance == 0) {
        profile.hopMinDistance = 1;
    }
    if (profile.hopMaxDistance == 0) {
        profile.hopMaxDistance = profile.hopMinDistance;
    }
    if (profile.hopMaxDistance < profile.hopMinDistance) {
        profile.hopMaxDistance = profile.hopMinDistance;
    }
    if (profile.ramAccelerationSteps > OW_WILD_SPAWNER_MOVEMENT_RANGE) {
        profile.ramAccelerationSteps = OW_WILD_SPAWNER_MOVEMENT_RANGE;
    }
    if (profile.chainPauseAction > OW_WILD_BEHAVIOR_CHAIN_PAUSE_ACTION_LOOK_AROUND) {
        profile.chainPauseAction = OW_WILD_BEHAVIOR_CHAIN_PAUSE_ACTION_NONE;
    }
    if (profile.teleportTime == 0) {
        profile.teleportTime = OW_WILD_SPAWNER_PHANTOM_STALK_TELEPORT_MOVE_FRAMES;
    }
    if (profile.teleportPause == 0) {
        profile.teleportPause = OW_WILD_SPAWNER_PHANTOM_STALK_POST_TELEPORT_COOLDOWN_FRAMES;
    }
    if (profile.attentiveCircleRadius > OW_WILD_SPAWNER_CIRCLE_PLAYER_MAX_RADIUS) {
        profile.attentiveCircleRadius = OW_WILD_SPAWNER_CIRCLE_PLAYER_MAX_RADIUS;
    }
    if (profile.attentiveContinueWhenArrived > OW_WILD_BEHAVIOR_BOOL_YES) {
        profile.attentiveContinueWhenArrived = OW_WILD_BEHAVIOR_BOOL_YES;
    }
    if (profile.attentiveAvoidPreviousTile > OW_WILD_BEHAVIOR_BOOL_YES) {
        profile.attentiveAvoidPreviousTile = OW_WILD_BEHAVIOR_BOOL_YES;
    }
    if (profile.chillTarget > OW_WILD_BEHAVIOR_TARGET_PLAYER_CARDINAL_LINE) {
        profile.chillTarget = OW_WILD_BEHAVIOR_TARGET_NONE;
    }
    if (profile.targetSelector > OW_WILD_BEHAVIOR_TARGET_CIRCLE_PLAYER) {
        profile.targetSelector = OW_WILD_BEHAVIOR_TARGET_NONE;
    }
    if (!OverworldWildSpawns_IsValidBehaviorKind(profile.attentiveState)) {
        profile.attentiveState = OW_WILD_BEHAVIOR_KIND_NONE;
    }
    if (!OverworldWildSpawns_IsValidBehaviorKind(profile.tiredState)) {
        profile.tiredState = OW_WILD_BEHAVIOR_KIND_NONE;
    }
    if (OverworldWildSpawns_BehaviorUsesAsleep(profile.chillState)) {
        profile.tiredState = OW_WILD_BEHAVIOR_KIND_ASLEEP;
        profile.stamina = 1;
        profile.alertness = 0;
        profile.alertChance = 0;
    } else if (OverworldWildSpawns_BehaviorUsesAsleep(profile.tiredState)) {
        profile.stamina = 1;
    }
    if ((profile.attentiveState != OW_WILD_BEHAVIOR_KIND_NONE
            || profile.targetSelector != OW_WILD_BEHAVIOR_TARGET_NONE
            || profile.movementStyle != OW_WILD_BEHAVIOR_LOCOMOTION_NONE
            || profile.attentiveBattle != OW_WILD_BEHAVIOR_BATTLE_TRIGGER_NONE)
        && profile.tiredState != OW_WILD_BEHAVIOR_KIND_NONE
        && profile.stamina == 0) {
        profile.stamina = 1;
    }
    if (profile.tiredState != OW_WILD_BEHAVIOR_KIND_NONE
        && !OverworldWildSpawns_BehaviorUsesAsleep(profile.tiredState)
        && profile.restTime == 0) {
        profile.restTime = 1;
    }
    if (profile.range == 0) {
        profile.range = OW_WILD_SPAWNER_MOVEMENT_RANGE;
    }
    if (profile.jumpLevel > OW_WILD_BEHAVIOR_JUMP_LEVEL_BOTH) {
        profile.jumpLevel = OW_WILD_BEHAVIOR_JUMP_LEVEL_BOTH;
    }
    if (profile.spawnState > OW_WILD_BEHAVIOR_SPAWN_STATE_APPEAR_HOP) {
        profile.spawnState = OW_WILD_BEHAVIOR_SPAWN_STATE_APPEAR;
    }
    if (profile.alertState > OW_WILD_BEHAVIOR_ALERT_STATE_SPEECH) {
        profile.alertState = OW_WILD_BEHAVIOR_ALERT_STATE_NONE;
    }
    if (profile.alertEmote > OW_WILD_SPAWNER_BUBBLE_ID_SLEEP
        && profile.alertEmote != OW_WILD_SPAWNER_BUBBLE_ID_NONE) {
        profile.alertEmote = OW_WILD_SPAWNER_BUBBLE_ID_NONE;
    }
    if (profile.alertRange > OW_WILD_BEHAVIOR_ALERT_RANGE_TERRAIN_ONLY) {
        profile.alertRange = OW_WILD_BEHAVIOR_ALERT_RANGE_NONE;
    }
    if (profile.spawnDestination > OW_WILD_SPAWN_DESTINATION_NEXT_TO_PLAYER) {
        profile.spawnDestination = OW_WILD_SPAWN_DESTINATION_POOL;
    }
    if (profile.spawnDestinationMinDistance == 0) {
        profile.spawnDestinationMinDistance = OW_WILD_PLAYER_RELATIVE_SPAWN_MIN_DISTANCE;
    }
    if (profile.spawnDestinationMinDistance > OW_WILD_PLAYER_RELATIVE_SPAWN_MAX_DISTANCE) {
        profile.spawnDestinationMinDistance = OW_WILD_PLAYER_RELATIVE_SPAWN_MAX_DISTANCE;
    }
    if (profile.spawnDestinationMaxDistance == 0) {
        profile.spawnDestinationMaxDistance = OW_WILD_PLAYER_RELATIVE_SPAWN_MAX_DISTANCE;
    }
    if (profile.spawnDestinationMaxDistance > OW_WILD_PLAYER_RELATIVE_SPAWN_MAX_DISTANCE) {
        profile.spawnDestinationMaxDistance = OW_WILD_PLAYER_RELATIVE_SPAWN_MAX_DISTANCE;
    }
    if (profile.spawnDestinationMaxDistance < profile.spawnDestinationMinDistance) {
        profile.spawnDestinationMaxDistance = profile.spawnDestinationMinDistance;
    }
    if (profile.attentiveBattle > OW_WILD_BEHAVIOR_BATTLE_TRIGGER_RAM_CRASH) {
        profile.attentiveBattle = OW_WILD_BEHAVIOR_BATTLE_TRIGGER_NONE;
    }
    return profile;
}

static void OverworldWildSpawns_AdjustCanopyHopperTreeTopProfileForSlot(
    OverworldWildSpawnState *state,
    int slot,
    OverworldWildBehaviorProfile *profile)
{
    LocalMapObject *object;

    if (state == NULL
        || profile == NULL
        || state->movementRuntimeState == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !OverworldWildSpawns_IsCanopyHopperTreeTopProfile(profile)
        || !state->spawns[slot].active) {
        return;
    }

    object = state->spawns[slot].object;
    if (object == NULL
        || !OW_WILD_RUNTIME(state)->movementMankeyTreeTopSettled[slot]
        || OW_WILD_RUNTIME(state)->movementMankeyTreeTopSettledX[slot] != (s16)MapObject_GetCurrentX(object)
        || OW_WILD_RUNTIME(state)->movementMankeyTreeTopSettledY[slot] != (s16)MapObject_GetCurrentY(object)
        || state->movementStagedHopPending[slot]
        || OverworldWildSpawns_IsMovementSlotInProgress(state, slot)
        || MapObject_IsSingleMovementActive(object)) {
        profile->alertness = 0;
        return;
    }
}

static OverworldWildBehaviorSlotCache *OverworldWildSpawns_EnsureBehaviorSlotCaches(
    OverworldWildSpawnState *state)
{
    OverworldWildOverlayRuntimeState *runtime;
    OverworldWildBehaviorSlotCache *cache;

    runtime = OverworldWildSpawns_EnsureRuntimeState(state);
    if (runtime == NULL) {
        return NULL;
    }

    cache = runtime->movementBehaviorSlotCaches;
    if (cache == NULL) {
        cache = sys_AllocMemory(
            HEAPID_WORLD,
            OW_WILD_MAX_SPAWNS * sizeof(*cache));
        runtime->movementBehaviorSlotCaches = cache;
        if (cache != NULL) {
            memset(
                cache,
                0,
                OW_WILD_MAX_SPAWNS * sizeof(*cache));
        }
    }

    return cache;
}

static void OverworldWildSpawns_ClearCachedBehaviorProfile(OverworldWildSpawnState *state, int slot)
{
    OverworldWildOverlayRuntimeState *runtime;
    OverworldWildBehaviorSlotCache *cache;
    u16 slotMask;

    runtime = OverworldWildSpawns_EnsureRuntimeState(state);
    if (runtime == NULL || slot < 0 || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    slotMask = OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot);
    runtime->movementFrameDrivenChillMask &= ~slotMask;
    runtime->movementFrameDrivenActiveMask &= ~slotMask;
    runtime->movementChillPhantomMask &= ~slotMask;
    runtime->movementActivePhantomMask &= ~slotMask;
    cache = runtime->movementBehaviorSlotCaches;
    if (cache != NULL) {
        cache[slot].valid = FALSE;
    }
}

static BOOL OverworldWildSpawns_BehaviorSlotCacheMatches(
    const OverworldWildBehaviorSlotCache *cache,
    const OverworldWildSpawn *spawn,
    u8 behaviorClass)
{
    return cache != NULL
        && spawn != NULL
        && cache->valid
        && cache->species == spawn->species
        && cache->mapId == spawn->mapId
        && cache->form == spawn->form
        && cache->level == spawn->level
        && cache->terrain == spawn->terrain
        && cache->shiny == spawn->shiny
        && cache->behaviorClass == behaviorClass;
}

static void OverworldWildSpawns_StoreBehaviorSlotCache(
    OverworldWildBehaviorSlotCache *cache,
    const OverworldWildSpawn *spawn,
    u8 behaviorClass,
    const OverworldWildBehaviorProfile *profile)
{
    if (cache == NULL
        || spawn == NULL
        || profile == NULL) {
        return;
    }

    cache->profile = *profile;
    cache->species = spawn->species;
    cache->mapId = spawn->mapId;
    cache->form = spawn->form;
    cache->level = spawn->level;
    cache->terrain = spawn->terrain;
    cache->shiny = spawn->shiny;
    cache->behaviorClass = behaviorClass;
    cache->valid = TRUE;
}

static void OverworldWildSpawns_GetBehaviorProfileAndPrimitivesForSlot(
    OverworldWildSpawnState *state,
    int slot,
    BOOL applyMovementState,
    OverworldWildBehaviorProfile *profileOut,
    OverworldWildBehaviorPrimitives *primitivesOut)
{
    OverworldWildBehaviorContext context;
    OverworldWildBehaviorProfile profile;
    OverworldWildBehaviorPrimitives primitives;
    OverworldWildBehaviorSlotCache *cache;
    OverworldWildSpawn *spawn;
    u8 behaviorClass = OW_WILD_BEHAVIOR_CLASS_DEFAULT;
    BOOL treeTopProfile;

    (void)applyMovementState;

    if (state == NULL || slot < 0 || slot >= OW_WILD_MAX_SPAWNS) {
        profile = OverworldWildSpawns_GetFallbackBehaviorProfile();
        primitives = OverworldWildSpawns_ResolveBehaviorPrimitives(&profile);
        if (profileOut != NULL) {
            *profileOut = profile;
        }
        if (primitivesOut != NULL) {
            *primitivesOut = primitives;
        }
        return;
    }

    spawn = &state->spawns[slot];
#if OW_WILD_SPAWNER_PROFILE_CACHE_FAST_HIT
    if (spawn->active) {
        behaviorClass = state->movementBehaviorClasses[slot];
    }
    cache = OverworldWildSpawns_EnsureBehaviorSlotCaches(state);
    if (cache != NULL) {
        cache = &cache[slot];
    }
    if (spawn->active
        && cache != NULL
        && OverworldWildSpawns_BehaviorSlotCacheMatches(cache, spawn, behaviorClass)) {
        OW_WILD_PERF_INC(sOverworldWildPerfProfileCacheHitsThisFrame);
        profile = cache->profile;
        primitives = OverworldWildSpawns_ResolveBehaviorPrimitives(&profile);
        goto copy_outputs;
    }
#endif
    context = OverworldWildSpawns_BuildBehaviorContext(
        spawn,
        SPECIES_NONE,
        0,
        OW_WILD_SPAWN_TERRAIN_LAND,
        FALSE);
    if (spawn->active) {
        behaviorClass = state->movementBehaviorClasses[slot];
    } else {
        behaviorClass = OverworldWildSpawns_GetBehaviorClassForContext(&context);
    }
    context.behaviorClass = behaviorClass;
#if !OW_WILD_SPAWNER_PROFILE_CACHE_FAST_HIT
    cache = OverworldWildSpawns_EnsureBehaviorSlotCaches(state);
    if (cache != NULL) {
        cache = &cache[slot];
    }

    if (spawn->active
        && cache != NULL
        && OverworldWildSpawns_BehaviorSlotCacheMatches(cache, spawn, behaviorClass)) {
        OW_WILD_PERF_INC(sOverworldWildPerfProfileCacheHitsThisFrame);
        profile = cache->profile;
        primitives = OverworldWildSpawns_ResolveBehaviorPrimitives(&profile);
    } else {
#else
    {
#endif
        OW_WILD_PERF_INC(sOverworldWildPerfProfileResolvesThisFrame);
        profile = OverworldWildSpawns_ResolveBehaviorProfileForContext(&context, NULL);
        treeTopProfile = OverworldWildSpawns_IsCanopyHopperTreeTopProfile(&profile);
        if (treeTopProfile) {
            OverworldWildSpawns_AdjustCanopyHopperTreeTopProfileForSlot(state, slot, &profile);
        }
        primitives = OverworldWildSpawns_ResolveBehaviorPrimitives(&profile);
        if (spawn->active) {
            state->movementBehaviorClasses[slot] = behaviorClass;
            if (!treeTopProfile && cache != NULL) {
                OverworldWildSpawns_StoreBehaviorSlotCache(
                    cache,
                    spawn,
                    behaviorClass,
                    &profile);
            }
        }
    }

#if OW_WILD_SPAWNER_PROFILE_CACHE_FAST_HIT
copy_outputs:
#endif
    if (profileOut != NULL) {
        *profileOut = profile;
    }
    if (primitivesOut != NULL) {
        *primitivesOut = primitives;
    }
}

static OverworldWildBehaviorProfile OverworldWildSpawns_GetBehaviorProfile(OverworldWildSpawnState *state, int slot)
{
    OverworldWildBehaviorProfile profile;

    OverworldWildSpawns_GetBehaviorProfileAndPrimitivesForSlot(state, slot, FALSE, &profile, NULL);
    return profile;
}

static OverworldWildBehaviorProfile OverworldWildSpawns_GetMovementTickProfile(
    OverworldWildSpawnState *state,
    int slot)
{
    OverworldWildBehaviorProfile profile;

    OverworldWildSpawns_GetMovementTickProfileAndPrimitives(state, slot, &profile, NULL);
    return profile;
}

static void OverworldWildSpawns_GetMovementTickProfileAndPrimitives(
    OverworldWildSpawnState *state,
    int slot,
    OverworldWildBehaviorProfile *profileOut,
    OverworldWildBehaviorPrimitives *primitivesOut)
{
    OverworldWildSpawns_GetBehaviorProfileAndPrimitivesForSlot(
        state,
        slot,
        TRUE,
        profileOut,
        primitivesOut);
}

static OverworldWildBehaviorPrimitives OverworldWildSpawns_ResolveBehaviorPrimitives(
    const OverworldWildBehaviorProfile *profile)
{
    OverworldWildBehaviorPrimitives primitives = {
        OW_WILD_BEHAVIOR_LOCOMOTION_NONE,
        OW_WILD_BEHAVIOR_LOCOMOTION_NONE,
        OW_WILD_BEHAVIOR_TARGET_NONE,
        OW_WILD_BEHAVIOR_ALERT_LOGIC_NONE,
        OW_WILD_BEHAVIOR_REACTION_NONE,
        OW_WILD_BEHAVIOR_LOCOMOTION_NONE,
        OW_WILD_BEHAVIOR_TARGET_NONE,
        OW_WILD_BEHAVIOR_REACTION_NONE,
        OW_WILD_BEHAVIOR_LOCOMOTION_NONE,
        OW_WILD_BEHAVIOR_TARGET_NONE,
        OW_WILD_BEHAVIOR_REACTION_NONE,
    };

    if (profile == NULL) {
        return primitives;
    }

    if (profile->spawnState < NELEMS(sOverworldWildSpawnLocomotionBySpawnState)) {
        primitives.spawnLocomotion =
            sOverworldWildSpawnLocomotionBySpawnState[profile->spawnState];
    }

    primitives.chillLocomotion = profile->chillAction;
    primitives.chillTarget = profile->chillTarget;
    switch (profile->chillState) {
    case OW_WILD_BEHAVIOR_KIND_WANDER:
        if (primitives.chillTarget == OW_WILD_BEHAVIOR_TARGET_NONE) {
            primitives.chillTarget = OW_WILD_BEHAVIOR_TARGET_RANDOM_NEARBY;
        }
        break;
    case OW_WILD_BEHAVIOR_KIND_CHASE:
        if (primitives.chillTarget == OW_WILD_BEHAVIOR_TARGET_NONE) {
            primitives.chillTarget = OW_WILD_BEHAVIOR_TARGET_TOWARD_PLAYER;
        }
        break;
    case OW_WILD_BEHAVIOR_KIND_FLEE:
        if (primitives.chillTarget == OW_WILD_BEHAVIOR_TARGET_NONE) {
            primitives.chillTarget = OW_WILD_BEHAVIOR_TARGET_AWAY_FROM_PLAYER;
        }
        break;
    case OW_WILD_BEHAVIOR_KIND_PLAYFUL:
        if (primitives.chillTarget == OW_WILD_BEHAVIOR_TARGET_NONE) {
            primitives.chillTarget = OW_WILD_BEHAVIOR_TARGET_TOWARD_PLAYER;
        }
        break;
    case OW_WILD_BEHAVIOR_KIND_RAM:
        if (primitives.chillTarget == OW_WILD_BEHAVIOR_TARGET_NONE) {
            primitives.chillTarget = OW_WILD_BEHAVIOR_TARGET_TOWARD_PLAYER;
        }
        break;
    case OW_WILD_BEHAVIOR_KIND_HEADBUTT_TREE_HOP:
        if (primitives.chillTarget == OW_WILD_BEHAVIOR_TARGET_NONE) {
            primitives.chillTarget = OW_WILD_BEHAVIOR_TARGET_TREE_TOP;
        }
        break;
    case OW_WILD_BEHAVIOR_KIND_IDLE:
    case OW_WILD_BEHAVIOR_KIND_ASLEEP:
    default:
        primitives.chillLocomotion = OW_WILD_BEHAVIOR_LOCOMOTION_NONE;
        primitives.chillTarget = OW_WILD_BEHAVIOR_TARGET_NONE;
        break;
    }

    primitives.attentiveLocomotion = profile->movementStyle;
    primitives.attentiveTarget = profile->targetSelector;
    switch (profile->attentiveState) {
    case OW_WILD_BEHAVIOR_KIND_WANDER:
        primitives.attentiveTarget = OW_WILD_BEHAVIOR_TARGET_RANDOM_NEARBY;
        break;
    case OW_WILD_BEHAVIOR_KIND_CHASE:
        primitives.activeReaction = OW_WILD_BEHAVIOR_REACTION_CONTACT;
        if (primitives.attentiveTarget == OW_WILD_BEHAVIOR_TARGET_NONE) {
            primitives.attentiveTarget = OW_WILD_BEHAVIOR_TARGET_TOWARD_PLAYER;
        }
        break;
    case OW_WILD_BEHAVIOR_KIND_FLEE:
        primitives.activeReaction = OW_WILD_BEHAVIOR_REACTION_FLEE;
        if (primitives.attentiveTarget == OW_WILD_BEHAVIOR_TARGET_NONE) {
            primitives.attentiveTarget = OW_WILD_BEHAVIOR_TARGET_AWAY_FROM_PLAYER;
        }
        break;
    case OW_WILD_BEHAVIOR_KIND_PLAYFUL:
        primitives.activeReaction = OW_WILD_BEHAVIOR_REACTION_EMOTE;
        if (primitives.attentiveTarget == OW_WILD_BEHAVIOR_TARGET_NONE) {
            primitives.attentiveTarget = OW_WILD_BEHAVIOR_TARGET_TOWARD_PLAYER;
        }
        break;
    case OW_WILD_BEHAVIOR_KIND_RAM:
        primitives.activeReaction = OW_WILD_BEHAVIOR_REACTION_CONTACT;
        if (primitives.attentiveTarget == OW_WILD_BEHAVIOR_TARGET_NONE) {
            primitives.attentiveTarget = OW_WILD_BEHAVIOR_TARGET_TOWARD_PLAYER;
        }
        break;
    case OW_WILD_BEHAVIOR_KIND_HEADBUTT_TREE_HOP:
        primitives.activeReaction = OW_WILD_BEHAVIOR_REACTION_CONTACT;
        if (primitives.attentiveTarget == OW_WILD_BEHAVIOR_TARGET_NONE) {
            primitives.attentiveTarget = OW_WILD_BEHAVIOR_TARGET_TREE_TOP;
        }
        break;
    case OW_WILD_BEHAVIOR_KIND_NONE:
    case OW_WILD_BEHAVIOR_KIND_IDLE:
    case OW_WILD_BEHAVIOR_KIND_ASLEEP:
    default:
        primitives.activeReaction = OW_WILD_BEHAVIOR_REACTION_NONE;
        break;
    }

    if (profile->alertness != 0 && profile->alertChance != 0) {
        if (profile->alertRange < NELEMS(sOverworldWildAlertPrimitivesByRange)) {
            primitives.alertLogic =
                sOverworldWildAlertPrimitivesByRange[profile->alertRange].logic;
            primitives.alertReaction =
                sOverworldWildAlertPrimitivesByRange[profile->alertRange].reaction;
        }
    }
    if (OverworldWildSpawns_BehaviorKindUsesMovement(profile->tiredState)) {
        primitives.tiredLocomotion = profile->specialAction;
    }
    switch (profile->tiredState) {
    case OW_WILD_BEHAVIOR_KIND_WANDER:
        primitives.tiredTarget = OW_WILD_BEHAVIOR_TARGET_RANDOM_NEARBY;
        break;
    case OW_WILD_BEHAVIOR_KIND_CHASE:
        primitives.tiredTarget = OW_WILD_BEHAVIOR_TARGET_TOWARD_PLAYER;
        break;
    case OW_WILD_BEHAVIOR_KIND_FLEE:
        primitives.tiredTarget = OW_WILD_BEHAVIOR_TARGET_AWAY_FROM_PLAYER;
        break;
    case OW_WILD_BEHAVIOR_KIND_PLAYFUL:
        primitives.tiredTarget = OW_WILD_BEHAVIOR_TARGET_TOWARD_PLAYER;
        break;
    case OW_WILD_BEHAVIOR_KIND_RAM:
        primitives.tiredTarget = OW_WILD_BEHAVIOR_TARGET_TOWARD_PLAYER;
        break;
    case OW_WILD_BEHAVIOR_KIND_HEADBUTT_TREE_HOP:
        primitives.tiredTarget = OW_WILD_BEHAVIOR_TARGET_TREE_TOP;
        break;
    case OW_WILD_BEHAVIOR_KIND_NONE:
    case OW_WILD_BEHAVIOR_KIND_IDLE:
    case OW_WILD_BEHAVIOR_KIND_ASLEEP:
    case OW_WILD_BEHAVIOR_KIND_TIRED_EMOTE:
    case OW_WILD_BEHAVIOR_KIND_NO_VISUAL:
    default:
        primitives.tiredLocomotion = OW_WILD_BEHAVIOR_LOCOMOTION_NONE;
        primitives.tiredTarget = OW_WILD_BEHAVIOR_TARGET_NONE;
        break;
    }
    if (profile->tiredState != OW_WILD_BEHAVIOR_KIND_NONE) {
        primitives.tiredReaction = OW_WILD_BEHAVIOR_REACTION_TIRED;
    }

    return primitives;
}

static u8 OverworldWildSpawns_GetBehaviorHopPauseFrames(
    const OverworldWildBehaviorProfile *profile,
    u8 spotState)
{
    u8 hopPause;

    if (profile != NULL) {
        hopPause = OverworldWildSpawns_SelectStateMovementByte(
            spotState,
            profile->hopPause,
            profile->attentiveHopPause,
            profile->tiredHopPause);
        if (hopPause != 0) {
            return hopPause;
        }
    }
    if (profile != NULL && OverworldWildSpawns_IsCanopyHopperTreeTopProfile(profile)) {
        if (spotState == OW_WILD_SPAWNER_SPOT_STATE_ACTIVE) {
            return OW_WILD_SPAWNER_MOVEMENT_DECISION_COOLDOWN;
        }
        if (spotState == OW_WILD_SPAWNER_SPOT_STATE_CHILL) {
            return OW_WILD_SPAWNER_DEFAULT_MOVEMENT_PAUSE_FRAMES;
        }
    }
    return OW_WILD_SPAWNER_CANOPY_HOPPER_COOLDOWN_FRAMES;
}

static u8 OverworldWildSpawns_GetBehaviorHopSpinSpeed(
    const OverworldWildBehaviorProfile *profile,
    u8 spotState)
{
    if (profile == NULL) {
        return 0;
    }

    return OverworldWildSpawns_SelectStateMovementByte(
        spotState,
        profile->hopSpinSpeed,
        profile->attentiveHopSpinSpeed,
        profile->hopSpinSpeed);
}

static BOOL OverworldWildSpawns_RollBehaviorChance(u8 chance)
{
    return chance >= 100 || (chance != 0 && (gf_rand() % 100) < chance);
}

static BOOL OverworldWildSpawns_SlotUsesChillPhantomMovement(
    OverworldWildSpawnState *state,
    int slot)
{
    return state != NULL
        && slot >= 0
        && slot < OW_WILD_MAX_SPAWNS
        && state->spawns[slot].active
        && (OW_WILD_RUNTIME(state)->movementChillPhantomMask & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot)) != 0;
}

static BOOL OverworldWildSpawns_SlotUsesActivePhantomMovement(
    OverworldWildSpawnState *state,
    int slot)
{
    return state != NULL
        && slot >= 0
        && slot < OW_WILD_MAX_SPAWNS
        && state->spawns[slot].active
        && (OW_WILD_RUNTIME(state)->movementActivePhantomMask & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot)) != 0;
}

static BOOL OverworldWildSpawns_SlotUsesPhantomMovement(
    OverworldWildSpawnState *state,
    int slot)
{
    return state != NULL
        && slot >= 0
        && slot < OW_WILD_MAX_SPAWNS
        && state->spawns[slot].active
        && ((OW_WILD_RUNTIME(state)->movementChillPhantomMask | OW_WILD_RUNTIME(state)->movementActivePhantomMask)
            & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot)) != 0;
}

static void OverworldWildSpawns_SetObjectPassThrough(LocalMapObject *object, BOOL passThrough)
{
    if (object == NULL) {
        return;
    }

    if (passThrough) {
        object->flags |= MAPOBJECTFLAG_UNK18;
    } else {
        object->flags &= ~MAPOBJECTFLAG_UNK18;
    }
}

static BOOL OverworldWildSpawns_IsObjectOnPlayerTile(FieldSystem *fieldSystem, LocalMapObject *object)
{
    if (fieldSystem == NULL
        || fieldSystem->playerAvatar == NULL
        || object == NULL) {
        return FALSE;
    }

    return (int)MapObject_GetCurrentX(object) == GetPlayerXCoord(fieldSystem->playerAvatar)
        && (int)MapObject_GetCurrentY(object) == GetPlayerYCoord(fieldSystem->playerAvatar);
}

static void OverworldWildSpawns_ApplySpawnPassThroughFlag(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object)
{
    BOOL passThrough = state != NULL
        && slot >= 0
        && slot < OW_WILD_MAX_SPAWNS
        && OverworldWildSpawns_SlotUsesPhantomMovement(state, slot)
        && (state->movementPhantomFlickerTimers[slot] != 0
            || OverworldWildSpawns_IsObjectOnPlayerTile(state->movementFieldSystem, object));

    OverworldWildSpawns_SetObjectPassThrough(object, passThrough);
}

static void OverworldWildSpawns_RestorePickedUpBehaviorClass(OverworldWildSpawnState *state, int slot)
{
    if (state->movementBehaviorClasses[slot] == OW_WILD_BEHAVIOR_CLASS_PICKED_UP) {
        const OverworldWildHelperOverlayEntry *helperEntry;
        const OverworldWildSpawn *spawn = &state->spawns[slot];

        state->movementBehaviorClasses[slot] = OverworldWildSpawns_GetBehaviorClassForSpawn(
            spawn->species,
            spawn->level,
            spawn->terrain,
            spawn->shiny);
        helperEntry = OverworldWildSpawns_GetHelperOverlayEntry();
        if (helperEntry != NULL) {
            helperEntry->normalizeThrowPresentation(
                state->movementFieldSystem,
                state,
                slot);
            OverworldWildSpawns_ReconcileNativeShadow(
                state->movementFieldSystem,
                state->spawns[slot].object);
        }
    }
}

static void OverworldWildSpawns_ClearThrowStateForSlot(OverworldWildSpawnState *state, int slot)
{
    const OverworldWildHelperOverlayEntry *helperEntry;
    OverworldWildOverlayRuntimeState *runtime;
    u16 restoreMask;
    int i;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    runtime = OW_WILD_RUNTIME(state);
    helperEntry = OverworldWildSpawns_GetHelperOverlayEntry();
    if (helperEntry == NULL || helperEntry->clearPickupThrowState == NULL) {
        return;
    }
    restoreMask = helperEntry->clearPickupThrowState(
        state,
        &runtime->throwState,
        &runtime->spawnPresentations,
        slot);
    for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
        if ((restoreMask & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(i)) != 0) {
            OverworldWildSpawns_RestorePickedUpBehaviorClass(state, i);
        }
    }
}

static u16 __attribute__((noinline)) OverworldWildSpawns_GetThrowParticipantMask(OverworldWildSpawnState *state)
{
    OverworldWildOverlayRuntimeState *runtime = OW_WILD_RUNTIME(state);
    return runtime->throwState.targetMask
        | runtime->throwState.carrierMask
        | state->captureTargetMask;
}

static inline BOOL __attribute__((always_inline))
OverworldWildSpawns_IsValidPickupThrowTarget(
    OverworldWildSpawnState *state,
    int carrierSlot,
    int targetSlot)
{
    const OverworldWildHelperOverlayEntry *helperEntry;

    if (state == NULL || state->movementRuntimeState == NULL) {
        return FALSE;
    }
    helperEntry = OverworldWildSpawns_GetHelperOverlayEntry();
    return helperEntry != NULL
        && helperEntry->queryPickupThrowTarget != NULL
        && helperEntry->queryPickupThrowTarget(
            state,
            &OW_WILD_RUNTIME(state)->throwState,
            carrierSlot,
            targetSlot,
            OW_WILD_HELPER_PICKUP_THROW_QUERY_VALID,
            0);
}

static u16 OverworldWildSpawns_GetPickupThrowUnstableMask(
    OverworldWildSpawnState *state)
{
    OverworldWildOverlayRuntimeState *runtime = OW_WILD_RUNTIME(state);
    u16 unstableMask = 0;
    int i;

    for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
        if (runtime->movementEmotePartnerPrepObjects[i] != NULL
            || runtime->movementCustomJumpActive[i]
            || runtime->movementCustomJumpPrepActive[i]
            || state->movementRamCrashShakeTimers[i] != 0) {
            unstableMask |= OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(i);
        }
    }
    return unstableMask;
}

static inline BOOL __attribute__((always_inline))
OverworldWildSpawns_IsStablePickupThrowTarget(
    OverworldWildSpawnState *state,
    int carrierSlot,
    int targetSlot)
{
    const OverworldWildHelperOverlayEntry *helperEntry;

    if (state == NULL || state->movementRuntimeState == NULL) {
        return FALSE;
    }
    helperEntry = OverworldWildSpawns_GetHelperOverlayEntry();
    return helperEntry != NULL
        && helperEntry->queryPickupThrowTarget != NULL
        && helperEntry->queryPickupThrowTarget(
            state,
            &OW_WILD_RUNTIME(state)->throwState,
            carrierSlot,
            targetSlot,
            OW_WILD_HELPER_PICKUP_THROW_QUERY_STABLE,
            OverworldWildSpawns_GetPickupThrowUnstableMask(state));
}

static BOOL OverworldWildSpawns_IsReservedPickupTargetNearCarrier(
    OverworldWildSpawnState *state,
    int targetSlot)
{
    const OverworldWildHelperOverlayEntry *helperEntry;
    OverworldWildOverlayRuntimeState *runtime;

    if (state == NULL
        || state->movementRuntimeState == NULL
        || targetSlot < 0
        || targetSlot >= OW_WILD_MAX_SPAWNS) {
        return FALSE;
    }
    runtime = OW_WILD_RUNTIME(state);
    if ((runtime->throwState.targetMask
            & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(targetSlot)) == 0
        || state->movementBehaviorClasses[targetSlot]
            == OW_WILD_BEHAVIOR_CLASS_PICKED_UP) {
        return FALSE;
    }
    helperEntry = OverworldWildSpawns_GetHelperOverlayEntry();
    return helperEntry != NULL
        && helperEntry->queryPickupThrowTarget != NULL
        && helperEntry->queryPickupThrowTarget(
            state,
            &runtime->throwState,
            0,
            targetSlot,
            OW_WILD_HELPER_PICKUP_THROW_QUERY_RESERVED_NEAR,
            0);
}

static void OverworldWildSpawns_SyncCarriedThrowTarget(
    OverworldWildSpawnState *state,
    int carrierSlot,
    int targetSlot)
{
    const OverworldWildHelperOverlayEntry *helperEntry;

    if (state == NULL
        || state->movementFieldSystem == NULL) {
        return;
    }

    helperEntry = OverworldWildSpawns_GetHelperOverlayEntry();
    if (helperEntry != NULL) {
        helperEntry->syncCarriedThrowTarget(
            state->movementFieldSystem,
            state,
            &OW_WILD_RUNTIME(state)->spawnPresentations,
            carrierSlot,
            targetSlot);
    }
}

static void OverworldWildSpawns_TryStartPickupThrowAction(
    OverworldWildSpawnState *state,
    int slot)
{
    const OverworldWildHelperOverlayEntry *helperEntry;
    OverworldWildOverlayRuntimeState *runtime;

    if (state == NULL || state->movementRuntimeState == NULL) {
        return;
    }
    runtime = OW_WILD_RUNTIME(state);
    helperEntry = OverworldWildSpawns_GetHelperOverlayEntry();
    if (helperEntry != NULL && helperEntry->tryStartPickupThrowAction != NULL) {
        (void)helperEntry->tryStartPickupThrowAction(
            state,
            &runtime->throwState,
            &runtime->spawnPresentations,
            slot,
            OverworldWildSpawns_GetPickupThrowUnstableMask(state));
    }
}

static BOOL OverworldWildSpawns_IsFrameDrivenActiveLocomotion(u8 locomotion)
{
    return locomotion == OW_WILD_BEHAVIOR_LOCOMOTION_WANDER
        || locomotion == OW_WILD_BEHAVIOR_LOCOMOTION_HOP
        || locomotion == OW_WILD_BEHAVIOR_LOCOMOTION_RAM
        || locomotion == OW_WILD_BEHAVIOR_LOCOMOTION_PHANTOM_TELEPORT;
}

static u8 OverworldWildSpawns_GetBehaviorClassForSpawn(
    u16 species,
    u8 level,
    u8 terrain,
    u8 shiny)
{
    OverworldWildBehaviorContext context = OverworldWildSpawns_BuildBehaviorContext(
        NULL,
        species,
        level,
        terrain,
        shiny);

    return OverworldWildSpawns_GetBehaviorClassForContext(&context);
}

static BOOL OverworldWildSpawns_AlertStateUsesHop(u8 alertState)
{
    return alertState == OW_WILD_BEHAVIOR_ALERT_STATE_HOP;
}

static u8 OverworldWildSpawns_GetSpotEmoteJumpCount(const OverworldWildBehaviorProfile *profile)
{
    if (profile == NULL || !OverworldWildSpawns_AlertStateUsesHop(profile->alertState)) {
        return 0;
    }
    return OW_WILD_SPAWNER_SPOT_EMOTE_JUMPS_DEFAULT;
}

static u8 OverworldWildSpawns_GetAlertStateFrameCount(
    const OverworldWildBehaviorProfile *profile,
    u8 jumpCount)
{
    if (profile != NULL && profile->alertTime != OW_WILD_SPAWNER_ALERT_TIME_AUTO) {
        return profile->alertTime;
    }
    if (jumpCount != 0) {
        return OW_WILD_SPAWNER_SPOT_EMOTE_FRAMES_PER_JUMP * jumpCount;
    }

    return OW_WILD_SPAWNER_SPOT_EMOTE_SPEECH_FRAMES;
}

static u32 OverworldWildSpawns_GetMovementWalkCommandForSpeed(u8 speed)
{
    switch (speed) {
    case OW_WILD_SPAWNER_MOVEMENT_SPEED_2:
        return OW_WILD_SPAWNER_MOVEMENT_SPEED_2_COMMAND;
    case OW_WILD_SPAWNER_MOVEMENT_SPEED_3:
        return OW_WILD_SPAWNER_MOVEMENT_SPEED_3_COMMAND;
    case OW_WILD_SPAWNER_MOVEMENT_SPEED_4:
        return OW_WILD_SPAWNER_MOVEMENT_SPEED_4_COMMAND;
    case OW_WILD_SPAWNER_MOVEMENT_SPEED_1:
    default:
        return OW_WILD_SPAWNER_MOVEMENT_SPEED_1_COMMAND;
    }
}

static u8 OverworldWildSpawns_GetMovementSpeedForState(
    const OverworldWildBehaviorProfile *profile,
    u8 spotState)
{
    if (profile == NULL) {
        return OW_WILD_SPAWNER_MOVEMENT_SPEED_DEFAULT;
    }

    if (spotState == OW_WILD_SPAWNER_SPOT_STATE_CHILL) {
        return profile->chillSpeed != 0
            ? profile->chillSpeed
            : OW_WILD_SPAWNER_MOVEMENT_SPEED_DEFAULT;
    }
    if (spotState == OW_WILD_SPAWNER_SPOT_STATE_TIRED) {
        return profile->tiredSpeed != 0
            ? profile->tiredSpeed
            : OverworldWildSpawns_GetMovementSpeedForState(
                profile,
                OW_WILD_SPAWNER_SPOT_STATE_CHILL);
    }

    return profile->attentiveSpeed != 0
        ? profile->attentiveSpeed
        : OW_WILD_SPAWNER_MOVEMENT_SPEED_DEFAULT;
}

static BOOL OverworldWildSpawns_CurrentSpotUsesRamLocomotion(
    const OverworldWildBehaviorPrimitives *primitives,
    u8 spotState)
{
    if (spotState == OW_WILD_SPAWNER_SPOT_STATE_CHILL) {
        return primitives->chillLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_RAM;
    }
    if (spotState == OW_WILD_SPAWNER_SPOT_STATE_ACTIVE) {
        return primitives->attentiveLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_RAM;
    }
    if (spotState == OW_WILD_SPAWNER_SPOT_STATE_TIRED) {
        return primitives->tiredLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_RAM;
    }
    return FALSE;
}

static u8 OverworldWildSpawns_GetRamStartSpeedForState(
    const OverworldWildBehaviorProfile *profile,
    u8 spotState)
{
    u8 speed = OverworldWildSpawns_GetMovementSpeedForState(profile, spotState);

    if (speed < OW_WILD_SPAWNER_MOVEMENT_SPEED_1) {
        return OW_WILD_SPAWNER_MOVEMENT_SPEED_1;
    }
    if (speed > OW_WILD_SPAWNER_MOVEMENT_SPEED_4) {
        return OW_WILD_SPAWNER_MOVEMENT_SPEED_4;
    }
    return speed;
}

static u8 OverworldWildSpawns_GetRamMaxSpeed(
    const OverworldWildBehaviorProfile *profile,
    u8 spotState,
    u8 startSpeed)
{
    u8 maxSpeed;
    u8 profileMaxSpeed;

    if (profile == NULL) {
        maxSpeed = OW_WILD_SPAWNER_ONIX_RAM_MAX_SPEED;
    } else {
        profileMaxSpeed = OverworldWildSpawns_SelectStateMovementByte(
            spotState,
            profile->ramMaxSpeed,
            profile->attentiveRamMaxSpeed,
            profile->tiredRamMaxSpeed);
        if (profileMaxSpeed == 0) {
            maxSpeed = startSpeed;
        } else {
            maxSpeed = profileMaxSpeed;
        }
    }
    if (maxSpeed > OW_WILD_SPAWNER_MOVEMENT_SPEED_4) {
        maxSpeed = OW_WILD_SPAWNER_MOVEMENT_SPEED_4;
    }
    return maxSpeed < startSpeed ? startSpeed : maxSpeed;
}

static u8 OverworldWildSpawns_GetRamAccelerationSteps(
    const OverworldWildBehaviorProfile *profile,
    u8 spotState)
{
    if (profile == NULL) {
        return OW_WILD_SPAWNER_ONIX_RAM_SPEED_UP_TILES;
    }

    return OverworldWildSpawns_SelectStateMovementByte(
        spotState,
        profile->ramAccelerationSteps,
        profile->attentiveRamAccelerationSteps,
        profile->tiredRamAccelerationSteps);
}

static void OverworldWildSpawns_InitRamMovementState(
    OverworldWildSpawnState *state,
    int slot,
    u8 direction,
    const OverworldWildBehaviorProfile *profile,
    u8 spotState)
{
    if (state == NULL || slot < 0 || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    state->movementRamDirections[slot] = direction;
    state->movementRamStepCounters[slot] = 0;
    state->movementRamSpeeds[slot] =
        OverworldWildSpawns_GetRamStartSpeedForState(
            profile,
            spotState);
}

static void OverworldWildSpawns_AdvanceRamMovementSpeed(
    OverworldWildSpawnState *state,
    int slot,
    const OverworldWildBehaviorProfile *profile)
{
    u8 accelerationSteps;
    u8 maxSpeed;

    if (state == NULL || slot < 0 || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    if (state->movementRamSpeeds[slot] == 0) {
        state->movementRamSpeeds[slot] =
            OverworldWildSpawns_GetRamStartSpeedForState(
                profile,
                state->movementSpotStates[slot]);
    }

    accelerationSteps = OverworldWildSpawns_GetRamAccelerationSteps(
        profile,
        state->movementSpotStates[slot]);
    if (accelerationSteps == 0) {
        return;
    }

    maxSpeed = OverworldWildSpawns_GetRamMaxSpeed(
        profile,
        state->movementSpotStates[slot],
        state->movementRamSpeeds[slot]);
    if (state->movementRamSpeeds[slot] < maxSpeed) {
        if (state->movementRamStepCounters[slot] >= accelerationSteps) {
            state->movementRamStepCounters[slot] = 0;
            state->movementRamSpeeds[slot]++;
        }
    } else {
        state->movementRamStepCounters[slot] = 0;
    }
}

static u32 OverworldWildSpawns_GetMovementWalkCommandForProfile(
    OverworldWildSpawnState *state,
    int slot,
    const OverworldWildBehaviorProfile *profile,
    const OverworldWildBehaviorPrimitives *primitives)
{
    OverworldWildBehaviorProfile resolvedProfile;
    OverworldWildBehaviorPrimitives resolvedPrimitives;
    u8 speed;

    if (profile == NULL) {
        resolvedProfile = OverworldWildSpawns_GetBehaviorProfile(state, slot);
        profile = &resolvedProfile;
    }
    if (primitives == NULL) {
        resolvedPrimitives = OverworldWildSpawns_ResolveBehaviorPrimitives(profile);
        primitives = &resolvedPrimitives;
    }

    if (state != NULL
        && slot >= 0
        && slot < OW_WILD_MAX_SPAWNS
        && ((state->movementSpotStates[slot] == OW_WILD_SPAWNER_SPOT_STATE_CHILL
                && primitives->chillLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_HOP)
            || (state->movementSpotStates[slot] == OW_WILD_SPAWNER_SPOT_STATE_ACTIVE
                && primitives->attentiveLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_HOP)
            || (state->movementSpotStates[slot] == OW_WILD_SPAWNER_SPOT_STATE_TIRED
                && primitives->tiredLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_HOP))) {
        return OW_WILD_SPAWNER_CANOPY_HOPPER_JUMP_1_COMMAND;
    }

    if (state != NULL
        && slot >= 0
        && slot < OW_WILD_MAX_SPAWNS
        && state->movementSpawnRunActive[slot]) {
        speed = OverworldWildSpawns_GetMovementSpeedForState(
            profile,
            OW_WILD_SPAWNER_SPOT_STATE_ACTIVE);
    } else if (state != NULL
        && slot >= 0
        && slot < OW_WILD_MAX_SPAWNS
        && OverworldWildSpawns_CurrentSpotUsesRamLocomotion(
            primitives,
            state->movementSpotStates[slot])
        && state->movementRamSpeeds[slot] != 0) {
        speed = state->movementRamSpeeds[slot];
    } else if (state != NULL
        && slot >= 0
        && slot < OW_WILD_MAX_SPAWNS
        && (state->movementSpotStates[slot] == OW_WILD_SPAWNER_SPOT_STATE_CHILL
            || state->movementSpotStates[slot] == OW_WILD_SPAWNER_SPOT_STATE_TIRED)) {
        speed = OverworldWildSpawns_GetMovementSpeedForState(
            profile,
            state->movementSpotStates[slot]);
    } else {
        speed = OverworldWildSpawns_GetMovementSpeedForState(
            profile,
            OW_WILD_SPAWNER_SPOT_STATE_ACTIVE);
    }

    return OverworldWildSpawns_GetMovementWalkCommandForSpeed(speed);
}

static void OverworldWildSpawns_ClearMovementSlotInProgress(OverworldWildSpawnState *state, int slot)
{
    state->movementInProgressMask &= ~OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot);
    state->movementCooldowns[slot] = OW_WILD_SPAWNER_MOVEMENT_DECISION_COOLDOWN;
    state->movementPhantomVisiblePause[slot] = FALSE;
}

static BOOL OverworldWildSpawns_StartCarriedThrowTarget(
    OverworldWildSpawnState *state,
    int carrierSlot,
    int targetSlot,
    LocalMapObject *object)
{
    const OverworldWildHelperOverlayEntry *helperEntry;
    OverworldWildOverlayRuntimeState *runtime;

    (void)object;
    if (state == NULL || state->movementRuntimeState == NULL) {
        return FALSE;
    }
    runtime = OW_WILD_RUNTIME(state);
    helperEntry = OverworldWildSpawns_GetHelperOverlayEntry();
    if (helperEntry != NULL && helperEntry->startCarriedThrowTarget != NULL) {
        return helperEntry->startCarriedThrowTarget(
            state,
            &runtime->throwState,
            &runtime->spawnPresentations,
            carrierSlot,
            targetSlot);
    }
    return FALSE;
}

static s32 OverworldWildSpawns_GetRamCrashShakeOffset(u8 timer)
{
    switch (timer & 3) {
    case 0:
        return OW_WILD_SPAWNER_RAM_CRASH_SHAKE_FX32_AMPLITUDE;
    case 1:
        return -OW_WILD_SPAWNER_RAM_CRASH_SHAKE_FX32_AMPLITUDE;
    case 2:
        return OW_WILD_SPAWNER_RAM_CRASH_SHAKE_FX32_AMPLITUDE / 2;
    default:
        return -(OW_WILD_SPAWNER_RAM_CRASH_SHAKE_FX32_AMPLITUDE / 2);
    }
}

static void OverworldWildSpawns_RestoreRamCrashShake(OverworldWildSpawnState *state, int slot)
{
    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || state->movementRamCrashShakeTimers[slot] == 0) {
        return;
    }

    if (state->spawns[slot].active && state->spawns[slot].object != NULL) {
        state->spawns[slot].object->posVec[0] = state->movementRamCrashShakeBaseX[slot];
        state->spawns[slot].object->posVec[2] = state->movementRamCrashShakeBaseZ[slot];
    }

    state->movementRamCrashShakeTimers[slot] = 0;
}

static void OverworldWildSpawns_ClearPhantomTeleportTarget(OverworldWildSpawnState *state, int slot)
{
    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    state->movementPhantomTeleportOriginX[slot] = 0;
    state->movementPhantomTeleportOriginY[slot] = 0;
    state->movementPhantomTeleportTargetX[slot] = 0;
    state->movementPhantomTeleportTargetY[slot] = 0;
    state->movementPhantomTeleportHasTarget[slot] = FALSE;
}

static void OverworldWildSpawns_ClearCustomJumpPresentationState(OverworldWildSpawnState *state, int slot)
{
    FieldSystem *fieldSystem;
    LocalMapObject *object;

    if (state == NULL || slot < 0 || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    fieldSystem = state->movementFieldSystem;
    object = state->spawns[slot].object;
    if (object != NULL
        && OverworldWildSpawns_IsPresentationFieldContextCurrent(state, fieldSystem)
        && OverworldWildSpawns_IsCurrentSpawnObject(fieldSystem, &state->spawns[slot])) {
        MapObject_ClearBits(
            object,
            OW_WILD_SPAWNER_CUSTOM_JUMP_OWNED_BITS
                | (OW_WILD_RUNTIME(state)->movementCustomJumpPrepActive[slot]
                    ? MAPOBJECTFLAG_UNK8
                    : 0));
    }
}

static void OverworldWildSpawns_ClearCustomJump(OverworldWildSpawnState *state, int slot)
{
    OverworldWildOverlayRuntimeState *runtime;

    if (state == NULL || slot < 0 || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    runtime = OW_WILD_RUNTIME(state);
    OverworldWildSpawns_ClearCustomJumpPresentationState(state, slot);
    runtime->movementCustomJumpActive[slot] = FALSE;
    runtime->movementCustomJumpFrameCounts[slot] = 0;
    runtime->movementCustomJumpElapsedFrames[slot] = 0;
    runtime->movementCustomJumpSpinElapsedFrames[slot] = 0;
    runtime->movementCustomJumpSpinSpeeds[slot] = 0;
    runtime->movementCustomJumpSpinTimers[slot] = 0;
    runtime->movementCustomJumpSpinSteps[slot] = 0;
    runtime->movementCustomJumpTargetBaseY[slot] = 0;
#if OW_WILD_SPAWNER_CUSTOM_JUMP_POST_LANDING_RENDER_SETTLE_ENABLED
    sOverworldWildCustomJumpRenderSettleActive[slot] = FALSE;
    sOverworldWildCustomJumpRenderSettleStartX[slot] = 0;
    sOverworldWildCustomJumpRenderSettleStartY[slot] = 0;
    sOverworldWildCustomJumpRenderSettleElapsedFrames[slot] = 0;
#endif
#if OW_WILD_SPAWNER_CUSTOM_JUMP_VISIBLE_LEGS
    sOverworldWildCustomJumpVisibleLegFinished[slot] = FALSE;
#endif
}

static void OverworldWildSpawns_ClearStagedHopTarget(OverworldWildSpawnState *state, int slot)
{
    if (state == NULL || slot < 0 || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    OverworldWildSpawns_ClearStagedHopMovementListTask(state, slot);
    OverworldWildSpawns_ClearCustomJump(state, slot);
    OW_WILD_RUNTIME(state)->movementCustomJumpPrepActive[slot] = FALSE;
    state->movementStagedHopOriginX[slot] = 0;
    state->movementStagedHopOriginY[slot] = 0;
    state->movementStagedHopTargetX[slot] = 0;
    state->movementStagedHopTargetY[slot] = 0;
    state->movementStagedHopAvoidX[slot] = 0;
    state->movementStagedHopAvoidY[slot] = 0;
    state->movementStagedHopDistances[slot] = 0;
    state->movementStagedHopFinishWithTired[slot] = FALSE;
    state->movementStagedHopPending[slot] = FALSE;
    state->movementStagedHopAvoidValid[slot] = FALSE;
    OW_WILD_RUNTIME(state)->movementMankeyTreeTopLandingExpected[slot] = FALSE;
}

#if OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_WRAPPED_JUMP2_PROBE \
    || OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_WRAPPED_CHAINED_JUMP2_PROBE \
    || OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_SINGLE_WRAPPER_CHAINED_JUMP2_PROBE
static u16 *OverworldWildSpawns_AllocStagedHopMovementList(int slot)
{
    u16 *movementList;

    if (slot < 0 || slot >= OW_WILD_MAX_SPAWNS) {
        return NULL;
    }

    movementList = sOverworldWildStagedHopMovementLists[slot];
    if (movementList == NULL) {
        movementList = sys_AllocMemory(
            HEAPID_WORLD,
            OW_WILD_STAGED_HOP_MOVEMENT_LIST_WORDS * sizeof(*movementList));
        if (movementList == NULL) {
            return NULL;
        }
        sOverworldWildStagedHopMovementLists[slot] = movementList;
    }

    memset(
        movementList,
        0,
        OW_WILD_STAGED_HOP_MOVEMENT_LIST_WORDS * sizeof(*movementList));
    movementList[0] = OW_WILD_SPAWNER_CANOPY_HOPPER_MOVEMENT_END_COMMAND;
    return movementList;
}
#endif

static void OverworldWildSpawns_FreeStagedHopMovementList(int slot)
{
    if (slot < 0 || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    if (sOverworldWildStagedHopMovementLists[slot] != NULL) {
        sys_FreeMemoryEz(sOverworldWildStagedHopMovementLists[slot]);
        sOverworldWildStagedHopMovementLists[slot] = NULL;
    }
}

static void OverworldWildSpawns_ClearStagedHopMovementListTask(OverworldWildSpawnState *state, int slot)
{
    SysTask *task;

    (void)state;
    if (slot < 0 || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    task = sOverworldWildStagedHopMovementTasks[slot];
    sOverworldWildStagedHopMovementTasks[slot] = NULL;
    if (task != NULL) {
        MapObject_CleanupMovementListTask(task);
    }

    OverworldWildSpawns_FreeStagedHopMovementList(slot);
}

static void OverworldWildSpawns_SetObjectFacing(LocalMapObject *object, u8 direction)
{
    if (object == NULL || direction > OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT) {
        return;
    }

    object->facingInit = direction;
    object->curFacing = direction;
    object->nextFacing = direction;
    object->curFacingBak = direction;
    object->nextFacingBak = direction;
}

static void OverworldWildSpawns_SetObjectTile(LocalMapObject *object, int x, int y)
{
    if (object == NULL) {
        return;
    }

    MapObject_SetCurrentX(object, (u32)x);
    MapObject_SetCurrentY(object, (u32)y);
    object->xInit = x;
    object->yInit = y;
    object->xPrev = x;
    object->yPrev = y;
    object->posVec[0] = (u32)OW_WILD_SPAWNER_TILE_CENTER_COORD_FX32(x);
    object->posVec[2] = (u32)OW_WILD_SPAWNER_TILE_CENTER_COORD_FX32(y);
}

static void OverworldWildSpawns_SetObjectLogicalTileOnly(LocalMapObject *object, int x, int y)
{
    if (object == NULL) {
        return;
    }

    MapObject_SetCurrentX(object, (u32)x);
    MapObject_SetCurrentY(object, (u32)y);
    object->xInit = x;
    object->yInit = y;
    object->xPrev = x;
    object->yPrev = y;
}

#if OW_WILD_SPAWNER_CUSTOM_JUMP_POST_LANDING_RENDER_SETTLE_ENABLED
static void OverworldWildSpawns_SetObjectRenderTileOnly(LocalMapObject *object, int x, int y)
{
    if (object == NULL) {
        return;
    }

    object->posVec[0] = (u32)OW_WILD_SPAWNER_TILE_CENTER_COORD_FX32(x);
    object->posVec[2] = (u32)OW_WILD_SPAWNER_TILE_CENTER_COORD_FX32(y);
}
#endif

static void OverworldWildSpawns_SetObjectLandingTile(
    FieldSystem *fieldSystem,
    LocalMapObject *object,
    int x,
    int y)
{
    if (object == NULL) {
        return;
    }

    OverworldWildSpawns_SetObjectTile(object, x, y);
    (void)MapObject_RefreshHeightFromTerrain(object);
    object->faceVec[0] = 0;
    object->faceVec[1] = 0;
    object->faceVec[2] = 0;
    object->unk88[1] = 0;
    object->unk94[1] = 0;
    MapObject_ClearBits(
        object,
        BIT_VANISH | OW_WILD_SPAWNER_CUSTOM_JUMP_OWNED_BITS);
    OverworldWildSpawns_ReconcileNativeShadow(fieldSystem, object);
}

static inline void __attribute__((always_inline))
OverworldWildSpawns_ReleaseThrowTargetAtCurrentTile(
    OverworldWildSpawnState *state,
    int carrierSlot,
    int targetSlot,
    LocalMapObject *targetObject)
{
    if (state == NULL
        || targetSlot < 0
        || targetSlot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    OverworldWildSpawns_ClearThrowStateForSlot(state, carrierSlot);
    OW_WILD_RUNTIME(state)->movementCustomJumpPrepActive[targetSlot] = FALSE;
    OverworldWildSpawns_ClearCustomJump(state, targetSlot);
    if (targetObject != NULL) {
        OverworldWildSpawns_SetObjectLandingTile(
            state->movementFieldSystem,
            targetObject,
            MapObject_GetCurrentX(targetObject),
            MapObject_GetCurrentY(targetObject));
        MapObject_ClearBits(targetObject, BIT_VANISH | MAPOBJECTFLAG_UNK18);
    }
}

static s32 OverworldWildSpawns_GetObjectGroundBaseYAt(LocalMapObject *object, int x, int y)
{
    u32 savedFlags;
    u32 savedFlags2;
    u32 savedPosX;
    u32 savedPosY;
    u32 savedPosZ;
    int savedHInit;
    int savedHPrev;
    int savedHCurr;
    s32 baseY;

    if (object == NULL) {
        return 0;
    }

    savedFlags = object->flags;
    savedFlags2 = object->flags2;
    savedPosX = object->posVec[0];
    savedPosY = object->posVec[1];
    savedPosZ = object->posVec[2];
    savedHInit = object->hInit;
    savedHPrev = object->hPrev;
    savedHCurr = object->hCurr;
    baseY = (s32)savedPosY;

    object->posVec[0] = (u32)OW_WILD_SPAWNER_TILE_CENTER_COORD_FX32(x);
    object->posVec[2] = (u32)OW_WILD_SPAWNER_TILE_CENTER_COORD_FX32(y);
    if (MapObject_RefreshHeightFromTerrain(object)) {
        baseY = (s32)object->posVec[1];
    }

    object->flags = savedFlags;
    object->flags2 = savedFlags2;
    object->posVec[0] = savedPosX;
    object->posVec[1] = savedPosY;
    object->posVec[2] = savedPosZ;
    object->hInit = savedHInit;
    object->hPrev = savedHPrev;
    object->hCurr = savedHCurr;
    return baseY;
}

static BOOL OverworldWildSpawns_IsCanopyHopperProfile(const OverworldWildBehaviorProfile *profile)
{
    OverworldWildBehaviorPrimitives primitives =
        OverworldWildSpawns_ResolveBehaviorPrimitives(profile);

    return primitives.chillTarget == OW_WILD_BEHAVIOR_TARGET_TREE_TOP
        || primitives.attentiveTarget == OW_WILD_BEHAVIOR_TARGET_TREE_TOP;
}

static BOOL OverworldWildSpawns_IsCanopyHopperTreeTopProfile(const OverworldWildBehaviorProfile *profile)
{
    return OverworldWildSpawns_IsCanopyHopperProfile(profile);
}

static BOOL OverworldWildSpawns_IsCanopyHopperTreeTopSlot(OverworldWildSpawnState *state, int slot)
{
    OverworldWildBehaviorProfile profile;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !state->spawns[slot].active) {
        return FALSE;
    }

    profile = OverworldWildSpawns_GetBehaviorProfile(state, slot);
    return OverworldWildSpawns_IsCanopyHopperTreeTopProfile(&profile);
}

static LocalMapObject *OverworldWildSpawns_NormalizeSpawnHopLandingObject(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    LocalMapObject *object,
    int x,
    int y)
{
    (void)slot;

    if (object == NULL) {
        return NULL;
    }
    OverworldWildSpawns_SetObjectLandingTile(fieldSystem, object, x, y);
    return object;
}

#if 0
static BOOL OverworldWildSpawns_ShouldRefreshCanopyObjectAtTile(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    int x,
    int y,
    BOOL finalLanding)
{
    OverworldWildBehaviorProfile profile;

    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS) {
        return FALSE;
    }
    (void)x;
    (void)y;
    (void)finalLanding;

    profile = OverworldWildSpawns_GetBehaviorProfile(state, slot);
    if (!OverworldWildSpawns_IsCanopyHopperProfile(&profile)) {
        return TRUE;
    }
    return FALSE;
}
#endif

static void OverworldWildSpawns_ClearCanopyHopperVisualStateAtBoundary(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot)
{
    LocalMapObject *object;
    OverworldWildBehaviorProfile profile;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !state->spawns[slot].active) {
        return;
    }

    profile = OverworldWildSpawns_GetBehaviorProfile(state, slot);
    if (!OverworldWildSpawns_IsCanopyHopperProfile(&profile)) {
        return;
    }

    object = state->spawns[slot].object;
    if (object == NULL) {
        return;
    }

    if (fieldSystem != NULL && !OverworldWildSpawns_IsCurrentMapObject(fieldSystem, object)) {
        return;
    }

    OverworldWildSpawns_RestoreMankeyTreeTopRenderOverride(slot, object);
    OverworldWildSpawns_ClearMankeyTreeTopProxyObject(state, slot, TRUE);
    MapObject_ClearBits(object, BIT_VANISH);
}

static void OverworldWildSpawns_RefreshCanopyHopperVisualStateAtLanding(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    LocalMapObject *object)
{
    BOOL treeTopLanding;
    int objectX;
    int objectY;

    if (state == NULL
        || fieldSystem == NULL
        || object == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !state->spawns[slot].active) {
        return;
    }

    OverworldWildSpawns_ClearCanopyHopperVisualStateAtBoundary(state, fieldSystem, slot);
    objectX = MapObject_GetCurrentX(object);
    objectY = MapObject_GetCurrentY(object);
    treeTopLanding = OverworldWildSpawns_IsCanopyHopperTreeTopSlot(state, slot)
        && OverworldWildSpawns_IsHeadbuttTreeTopLocation(fieldSystem, objectX, objectY);
    OW_WILD_RUNTIME(state)->movementMankeyTreeTopSettled[slot] = treeTopLanding;
    if (treeTopLanding) {
        OW_WILD_RUNTIME(state)->movementMankeyTreeTopSettledX[slot] = (s16)objectX;
        OW_WILD_RUNTIME(state)->movementMankeyTreeTopSettledY[slot] = (s16)objectY;
    } else {
        OW_WILD_RUNTIME(state)->movementMankeyTreeTopSettledX[slot] = OW_WILD_SPAWNER_PREVIOUS_TILE_NONE;
        OW_WILD_RUNTIME(state)->movementMankeyTreeTopSettledY[slot] = OW_WILD_SPAWNER_PREVIOUS_TILE_NONE;
    }
    OverworldWildSpawns_UpdateMankeyTreeTopPriorityBits(state, fieldSystem, slot, object);
    MapObject_ClearBits(object, BIT_VANISH);
}

static void OverworldWildSpawns_RevealPhantomStalker(OverworldWildSpawnState *state, int slot, LocalMapObject *object)
{
    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    if (object == NULL && state->spawns[slot].active) {
        object = state->spawns[slot].object;
    }
    OverworldWildSpawns_ClearPhantomFlickerObject(state, slot, TRUE);
    if (object != NULL) {
        MapObject_ClearBits(object, BIT_VANISH);
        OverworldWildSpawns_SetObjectPassThrough(object, FALSE);
    }

    state->movementPhantomHidden[slot] = FALSE;
    state->movementPhantomHiddenSteps[slot] = 0;
    state->movementPhantomFlickerTimers[slot] = 0;
    state->movementPhantomVisiblePause[slot] = FALSE;
    OverworldWildSpawns_ClearPhantomTeleportTarget(state, slot);
}

static BOOL OverworldWildSpawns_ShouldShowPhantomFlickerWithDurations(
    u8 timer,
    u8 visibleFrames,
    u8 hiddenFrames)
{
    u8 cycleFrames;
    u8 phaseFrame;

    if (timer == 0 || visibleFrames == 0) {
        return FALSE;
    }
    if (hiddenFrames == 0) {
        return TRUE;
    }

    cycleFrames = visibleFrames + hiddenFrames;
    phaseFrame = (timer - 1) % cycleFrames;
    return phaseFrame < visibleFrames;
}

#if OW_WILD_SPAWNER_PHANTOM_STALK_ACTIVE_REAL_FLICKER
static BOOL OverworldWildSpawns_ShouldShowActivePhantomRealFlicker(u8 timer)
{
    u8 elapsedFrames;

    if (timer == 0 || timer > OW_WILD_SPAWNER_PHANTOM_STALK_ACTIVE_FLICKER_FRAMES) {
        return TRUE;
    }

    elapsedFrames = OW_WILD_SPAWNER_PHANTOM_STALK_ACTIVE_FLICKER_FRAMES - timer;
    return elapsedFrames < OW_WILD_SPAWNER_PHANTOM_STALK_ACTIVE_VISIBLE_FRAMES;
}
#endif

static BOOL OverworldWildSpawns_IsActivePhantomStalker(OverworldWildSpawnState *state, int slot)
{
    return state != NULL
        && slot >= 0
        && slot < OW_WILD_MAX_SPAWNS
        && state->movementSpotStates[slot] == OW_WILD_SPAWNER_SPOT_STATE_ACTIVE
        && OverworldWildSpawns_SlotUsesActivePhantomMovement(state, slot);
}

static BOOL OverworldWildSpawns_IsChillPhantomStalker(OverworldWildSpawnState *state, int slot)
{
    return state != NULL
        && slot >= 0
        && slot < OW_WILD_MAX_SPAWNS
        && state->movementSpotStates[slot] == OW_WILD_SPAWNER_SPOT_STATE_CHILL
        && OverworldWildSpawns_SlotUsesChillPhantomMovement(state, slot);
}

static BOOL OverworldWildSpawns_IsPhantomStalkerTeleportState(OverworldWildSpawnState *state, int slot)
{
    return OverworldWildSpawns_IsActivePhantomStalker(state, slot)
        || OverworldWildSpawns_IsChillPhantomStalker(state, slot);
}

static BOOL OverworldWildSpawns_IsActivePhantomTeleportMove(OverworldWildSpawnState *state, int slot)
{
    return state != NULL
        && slot >= 0
        && slot < OW_WILD_MAX_SPAWNS
        && state->movementPhantomTeleportHasTarget[slot]
        && state->movementPhantomFlickerTimers[slot] != 0
        && state->movementPhantomHidden[slot]
        && OverworldWildSpawns_IsMovementSlotInProgress(state, slot)
        && OverworldWildSpawns_IsPhantomStalkerTeleportState(state, slot);
}

static BOOL OverworldWildSpawns_ShouldShowPhantomFlickerForSlot(
    OverworldWildSpawnState *state,
    int slot)
{
    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS) {
        return FALSE;
    }

    if (OverworldWildSpawns_IsActivePhantomStalker(state, slot)) {
        return OverworldWildSpawns_ShouldShowPhantomFlickerWithDurations(
            state->movementPhantomFlickerTimers[slot],
            OW_WILD_SPAWNER_PHANTOM_STALK_ACTIVE_VISIBLE_FRAMES,
            OW_WILD_SPAWNER_PHANTOM_STALK_ACTIVE_HIDDEN_FRAMES);
    }

    return OverworldWildSpawns_ShouldShowPhantomFlickerWithDurations(
        state->movementPhantomFlickerTimers[slot],
        OW_WILD_SPAWNER_PHANTOM_STALK_FLICKER_VISIBLE_FRAMES,
        OW_WILD_SPAWNER_PHANTOM_STALK_FLICKER_HIDDEN_FRAMES);
}

static BOOL OverworldWildSpawns_IsPhantomFlickerObjectId(int id)
{
    return (id >= OW_WILD_PHANTOM_TELEPORT_FLICKER_OBJECT_ID_START
            && id < OW_WILD_PHANTOM_TELEPORT_FLICKER_OBJECT_ID_START + OW_WILD_MAX_SPAWNS)
        || (id >= OW_WILD_PHANTOM_FLICKER_OBJECT_ID_START
            && id < OW_WILD_PHANTOM_FLICKER_OBJECT_ID_START + OW_WILD_MAX_SPAWNS);
}

static BOOL OverworldWildSpawns_IsMankeyTreeTopProxyObjectId(int id)
{
    return id >= OW_WILD_MANKEY_TREE_TOP_PROXY_OBJECT_ID_START
        && id < OW_WILD_MANKEY_TREE_TOP_PROXY_OBJECT_ID_START + OW_WILD_MAX_SPAWNS;
}

static BOOL OverworldWildSpawns_SpawnSlotAllowsVanish(OverworldWildSpawnState *state, int slot)
{
    return OverworldWildSpawns_SlotUsesPhantomMovement(state, slot)
        || (state->captureTargetMask
            & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot)) != 0;
}

static void OverworldWildSpawns_RevealNonPhantomObjects(OverworldWildSpawnState *state, FieldSystem *fieldSystem)
{
    int i;

    if (state == NULL || fieldSystem == NULL) {
        return;
    }

    for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
        LocalMapObject *object = state->spawns[i].object;

        if (state->spawns[i].active
            && OverworldWildSpawns_IsCurrentSpawnObject(
                fieldSystem,
                &state->spawns[i])
            && (object->flags & BIT_VANISH) != 0
            && !OverworldWildSpawns_SpawnSlotAllowsVanish(state, i)) {
            MapObject_ClearBits(object, BIT_VANISH);
        }
    }
}

static BOOL OverworldWildSpawns_IsIgnoredVisualObjectId(int id)
{
    return id == OW_WILD_PLAYER_BALL_PROJECTILE_OBJECT_ID
        || OverworldWildSpawns_IsPhantomFlickerObjectId(id)
        || OverworldWildSpawns_IsMankeyTreeTopProxyObjectId(id);
}

static void OverworldWildSpawns_ReconcileNativeShadow(
    FieldSystem *fieldSystem,
    LocalMapObject *object)
{
    const u16 *descriptor;
    u8 behavior;
    int x;
    int y;

    if (fieldSystem == NULL
        || fieldSystem != gFieldSysPtr
        || fieldSystem->mapObjectMan == NULL
        || fieldSystem->location == NULL
        || object == NULL
        || (object->flags & MAPOBJECTFLAG_ACTIVE) == 0
        || !OverworldWildSpawns_IsCurrentMapObject(fieldSystem, object)) {
        return;
    }

    descriptor = ov01_021F9318(object);
    if (descriptor == NULL
        || ((*descriptor >> 4) & 7) == 0) {
        return;
    }

    x = (int)((s32)object->posVec[0] >> 16);
    y = (int)((s32)object->posVec[2] >> 16);
    behavior = GetMetatileBehaviorAt(fieldSystem, x, y);
    if (behavior == OW_WILD_TILE_HEADBUTT || behavior == 0xFF) {
        MapObject_SetBits(object, MAPOBJECTFLAG_UNK20);
        return;
    }

    /* Custom movement bypasses the stock terrain transition that clears this. */
    MapObject_ClearBits(object, MAPOBJECTFLAG_UNK20);
    sub_020603F8(object, behavior, behavior, descriptor);
}

static void OverworldWildSpawns_ResetPlayerBallShadowTracking(
    OverworldWildSpawnState *state)
{
    if (state == NULL || state->movementRuntimeState == NULL) {
        return;
    }
    OW_WILD_RUNTIME(state)->playerBallShadowTrackingValid = FALSE;
}

static void OverworldWildSpawns_SyncPlayerBallShadowObject(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    LocalMapObject *ballObject)
{
    OverworldWildOverlayRuntimeState *runtime;
    int currentX;
    int currentY;

    if (state == NULL
        || state->movementRuntimeState == NULL
        || !OverworldWildSpawns_IsMovementFieldContextCurrent(state, fieldSystem)
        || fieldSystem->taskman != NULL
        || !OverworldWildSpawns_IsCurrentMapObject(fieldSystem, ballObject)
        || (ballObject->flags & MAPOBJECTFLAG_ACTIVE) == 0
        || ballObject->id != OW_WILD_PLAYER_BALL_PROJECTILE_OBJECT_ID) {
        OverworldWildSpawns_ResetPlayerBallShadowTracking(state);
        return;
    }

    runtime = OW_WILD_RUNTIME(state);
    currentX = (int)((s32)ballObject->posVec[0] >> 16);
    currentY = (int)((s32)ballObject->posVec[2] >> 16);
    if (runtime->playerBallShadowTrackingValid
        && runtime->playerBallShadowTileX == currentX
        && runtime->playerBallShadowTileY == currentY
        && runtime->playerBallShadowGfxId == ballObject->gfxId
        && (ballObject->flags & (MAPOBJECTFLAG_UNK15 | MAPOBJECTFLAG_UNK20)) != 0) {
        return;
    }

    runtime->playerBallShadowTileX = (s16)currentX;
    runtime->playerBallShadowTileY = (s16)currentY;
    runtime->playerBallShadowGfxId = (u16)ballObject->gfxId;
    runtime->playerBallShadowTrackingValid = TRUE;
    OverworldWildSpawns_ReconcileNativeShadow(fieldSystem, ballObject);
}

static void OverworldWildSpawns_ClearPhantomFlickerObject(
    OverworldWildSpawnState *state,
    int slot,
    BOOL deleteObject)
{
    FieldSystem *fieldSystem;
    LocalMapObject *object;
    LocalMapObject *teleportObject;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    object = state->movementPhantomFlickerObjects[slot];
    state->movementPhantomFlickerObjects[slot] = NULL;
    teleportObject = state->movementPhantomTeleportFlickerObjects[slot];
    state->movementPhantomTeleportFlickerObjects[slot] = NULL;
    fieldSystem = state->movementFieldSystem;
    if (!deleteObject) {
        if (object != NULL
            && OverworldWildSpawns_IsPresentationFieldContextCurrent(state, fieldSystem)
            && OverworldWildSpawns_IsCurrentMapObject(fieldSystem, object)) {
            OverworldWildSpawns_SetObjectPassThrough(object, FALSE);
        }
        if (teleportObject != NULL
            && OverworldWildSpawns_IsPresentationFieldContextCurrent(state, fieldSystem)
            && OverworldWildSpawns_IsCurrentMapObject(fieldSystem, teleportObject)) {
            OverworldWildSpawns_SetObjectPassThrough(teleportObject, FALSE);
        }
        return;
    }

    if (object != NULL
        && OverworldWildSpawns_IsPresentationFieldContextCurrent(state, fieldSystem)
        && OverworldWildSpawns_IsCurrentMapObject(fieldSystem, object)
        && object->id == OW_WILD_PHANTOM_FLICKER_OBJECT_ID_START + slot) {
        DeleteMapObject(object);
        OW_WILD_PERF_INC(sOverworldWildPerfMapObjectDeletesThisFrame);
    }
    if (teleportObject != NULL
        && OverworldWildSpawns_IsPresentationFieldContextCurrent(state, fieldSystem)
        && OverworldWildSpawns_IsCurrentMapObject(fieldSystem, teleportObject)
        && teleportObject->id == OW_WILD_PHANTOM_TELEPORT_FLICKER_OBJECT_ID_START + slot) {
        DeleteMapObject(teleportObject);
        OW_WILD_PERF_INC(sOverworldWildPerfMapObjectDeletesThisFrame);
    }
}

static void OverworldWildSpawns_SyncPhantomFlickerObject(LocalMapObject *flickerObject, LocalMapObject *sourceObject)
{
    if (flickerObject == NULL || sourceObject == NULL) {
        return;
    }

    MapObject_SetCurrentX(flickerObject, MapObject_GetCurrentX(sourceObject));
    MapObject_SetCurrentY(flickerObject, MapObject_GetCurrentY(sourceObject));
    flickerObject->xInit = sourceObject->xInit;
    flickerObject->yInit = sourceObject->yInit;
    flickerObject->xPrev = sourceObject->xPrev;
    flickerObject->yPrev = sourceObject->yPrev;
    flickerObject->posVec[0] = sourceObject->posVec[0];
    flickerObject->posVec[1] = sourceObject->posVec[1];
    flickerObject->posVec[2] = sourceObject->posVec[2];
    flickerObject->curFacing = sourceObject->curFacing;
    flickerObject->nextFacing = sourceObject->nextFacing;
    MapObject_ClearBits(flickerObject, BIT_VANISH);
}

static void OverworldWildSpawns_UpdatePhantomFlickerObjectFacing(
    LocalMapObject *flickerObject,
    LocalMapObject *sourceObject)
{
    if (flickerObject == NULL || sourceObject == NULL) {
        return;
    }

    flickerObject->curFacing = sourceObject->curFacing;
    flickerObject->nextFacing = sourceObject->nextFacing;
    MapObject_ClearBits(flickerObject, BIT_VANISH);
}

static void OverworldWildSpawns_HidePhantomFlickerObjectPointer(
    OverworldWildSpawnState *state,
    LocalMapObject **flickerObjectSlot)
{
    FieldSystem *fieldSystem;
    LocalMapObject *flickerObject;

    if (state == NULL || flickerObjectSlot == NULL) {
        return;
    }

    flickerObject = *flickerObjectSlot;
    if (flickerObject == NULL) {
        return;
    }

    fieldSystem = state->movementFieldSystem;
    if (!OverworldWildSpawns_IsMovementFieldContextCurrent(state, fieldSystem)
        || !OverworldWildSpawns_IsCurrentMapObject(fieldSystem, flickerObject)) {
        *flickerObjectSlot = NULL;
        return;
    }

    OverworldWildSpawns_SetObjectPassThrough(flickerObject, FALSE);
    MapObject_SetBits(flickerObject, BIT_VANISH);
}

static void OverworldWildSpawns_EnsurePhantomFlickerObjectAtPosition(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *sourceObject,
    LocalMapObject **flickerObjectSlot,
    int objectId,
    int x,
    int y)
{
    FieldSystem *fieldSystem;
    LocalMapObject *flickerObject;
    u16 species;
    u8 form;
    BOOL shiny;
    u32 spriteId;
    u8 range;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || sourceObject == NULL
        || flickerObjectSlot == NULL
        || !state->spawns[slot].active
        || state->spawns[slot].species == SPECIES_NONE) {
        return;
    }

    fieldSystem = state->movementFieldSystem;
    if (!OverworldWildSpawns_IsMovementFieldContextCurrent(state, fieldSystem)) {
        return;
    }

    flickerObject = *flickerObjectSlot;
    if (flickerObject != NULL) {
        if (OverworldWildSpawns_IsCurrentMapObject(fieldSystem, flickerObject)) {
            if ((int)MapObject_GetCurrentX(flickerObject) != x
                || (int)MapObject_GetCurrentY(flickerObject) != y) {
                OverworldWildSpawns_SetObjectTile(flickerObject, x, y);
            }
            OverworldWildSpawns_SetObjectPassThrough(flickerObject, TRUE);
            OverworldWildSpawns_UpdatePhantomFlickerObjectFacing(flickerObject, sourceObject);
            return;
        }
        *flickerObjectSlot = NULL;
    }

    species = state->spawns[slot].species;
    form = state->spawns[slot].form;
    shiny = state->spawns[slot].shiny;
    spriteId = OverworldWildSpawns_GetSpriteID(species, form);
    flickerObject = CreateSpecialFieldObjectWithParams(
        fieldSystem->mapObjectMan,
        x,
        y,
        sourceObject->curFacing,
        spriteId,
        OW_WILD_SPAWNER_MOVEMENT_OBJECT_MOVEMENT,
        fieldSystem->location->mapId,
        0,
        OW_WILD_MOVEMENT_BEHAVIOR_CHASE_PLAYER,
        OW_WILD_PAL_PARAM_ENABLE | (shiny ? OW_WILD_PAL_PARAM_SHINY : 0));
    if (flickerObject == NULL) {
        return;
    }

    OW_WILD_PERF_INC(sOverworldWildPerfMapObjectCreatesThisFrame);
    MapObject_SetID(flickerObject, objectId);
    OverworldWildSpawns_SetObjectPassThrough(flickerObject, TRUE);
    range = OverworldWildSpawns_GetBehaviorProfile(state, slot).range;
    if (range == 0) {
        range = OW_WILD_SPAWNER_MOVEMENT_RANGE;
    }
    MapObject_SetXRange(flickerObject, range);
    MapObject_SetYRange(flickerObject, range);
    OverworldWildSpawns_ApplyPokemonRenderParams(
        flickerObject,
        species,
        form,
        spriteId,
        shiny);
    *flickerObjectSlot = flickerObject;
    OverworldWildSpawns_UpdatePhantomFlickerObjectFacing(flickerObject, sourceObject);
}

static void OverworldWildSpawns_HidePhantomFlickerObject(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *sourceObject)
{
    FieldSystem *fieldSystem;
    LocalMapObject *flickerObject;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    flickerObject = state->movementPhantomFlickerObjects[slot];
    if (flickerObject == NULL) {
        return;
    }

    fieldSystem = state->movementFieldSystem;
    if (!OverworldWildSpawns_IsMovementFieldContextCurrent(state, fieldSystem)
        || !OverworldWildSpawns_IsCurrentMapObject(fieldSystem, flickerObject)) {
        state->movementPhantomFlickerObjects[slot] = NULL;
        return;
    }

    OverworldWildSpawns_SyncPhantomFlickerObject(flickerObject, sourceObject);
    OverworldWildSpawns_SetObjectPassThrough(flickerObject, FALSE);
    MapObject_SetBits(flickerObject, BIT_VANISH);
}

static void OverworldWildSpawns_EnsurePhantomFlickerObject(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *sourceObject)
{
    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || sourceObject == NULL) {
        return;
    }

    OverworldWildSpawns_EnsurePhantomFlickerObjectAtPosition(
        state,
        slot,
        sourceObject,
        &state->movementPhantomFlickerObjects[slot],
        OW_WILD_PHANTOM_FLICKER_OBJECT_ID_START + slot,
        MapObject_GetCurrentX(sourceObject),
        MapObject_GetCurrentY(sourceObject));
    if (state->movementPhantomFlickerObjects[slot] != NULL) {
        OverworldWildSpawns_SyncPhantomFlickerObject(
            state->movementPhantomFlickerObjects[slot],
            sourceObject);
    }
}

static BOOL OverworldWildSpawns_IsPhantomTeleportPositionFlickerStep(
    OverworldWildSpawnState *state,
    int slot)
{
    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !OverworldWildSpawns_IsActivePhantomTeleportMove(state, slot)) {
        return FALSE;
    }

    return TRUE;
}

static BOOL OverworldWildSpawns_ShouldShowPhantomTeleportTargetPosition(
    OverworldWildSpawnState *state,
    int slot)
{
    u8 remainingFrames;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || state->movementPhantomFlickerTimers[slot] == 0) {
        return FALSE;
    }

    remainingFrames = state->movementPhantomFlickerTimers[slot];
    return ((remainingFrames / OW_WILD_SPAWNER_PHANTOM_STALK_TELEPORT_MOVE_PHASE_FRAMES) & 1) != 0;
}

static BOOL OverworldWildSpawns_ApplyPhantomTeleportPositionVisual(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object)
{
    BOOL showTarget;

    if (!OverworldWildSpawns_IsPhantomTeleportPositionFlickerStep(state, slot)
        || object == NULL) {
        return FALSE;
    }

    showTarget = OverworldWildSpawns_ShouldShowPhantomTeleportTargetPosition(state, slot);
    if (showTarget) {
        OverworldWildSpawns_HidePhantomFlickerObjectPointer(
            state,
            &state->movementPhantomFlickerObjects[slot]);
        OverworldWildSpawns_HidePhantomFlickerObjectPointer(
            state,
            &state->movementPhantomTeleportFlickerObjects[slot]);
        OverworldWildSpawns_ApplySpawnPassThroughFlag(state, slot, object);
        MapObject_ClearBits(object, BIT_VANISH);
    } else {
        OverworldWildSpawns_EnsurePhantomFlickerObjectAtPosition(
            state,
            slot,
            object,
            &state->movementPhantomFlickerObjects[slot],
            OW_WILD_PHANTOM_FLICKER_OBJECT_ID_START + slot,
            state->movementPhantomTeleportOriginX[slot],
            state->movementPhantomTeleportOriginY[slot]);
        OverworldWildSpawns_HidePhantomFlickerObjectPointer(
            state,
            &state->movementPhantomTeleportFlickerObjects[slot]);
        OverworldWildSpawns_ApplySpawnPassThroughFlag(state, slot, object);
        MapObject_SetBits(object, BIT_VANISH);
    }
    return TRUE;
}

static void OverworldWildSpawns_ApplyPhantomHiddenVisual(OverworldWildSpawnState *state, int slot, LocalMapObject *object)
{
    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || object == NULL
        || !state->movementPhantomHidden[slot]) {
        return;
    }

    if (OverworldWildSpawns_ApplyPhantomTeleportPositionVisual(state, slot, object)) {
        return;
    }

    OverworldWildSpawns_ApplySpawnPassThroughFlag(state, slot, object);
    if (OverworldWildSpawns_ShouldShowPhantomFlickerForSlot(state, slot)) {
        OverworldWildSpawns_EnsurePhantomFlickerObject(state, slot, object);
    } else if (state->movementPhantomFlickerTimers[slot] != 0) {
        OverworldWildSpawns_HidePhantomFlickerObject(state, slot, object);
    } else {
        OverworldWildSpawns_ClearPhantomFlickerObject(state, slot, TRUE);
    }
    MapObject_SetBits(object, BIT_VANISH);
}

static void OverworldWildSpawns_UpdatePhantomFlicker(OverworldWildSpawnState *state, int slot)
{
    LocalMapObject *object;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !state->spawns[slot].active
        || !state->movementPhantomHidden[slot]) {
        return;
    }

    object = state->spawns[slot].object;
    if (object == NULL) {
        return;
    }

    OverworldWildSpawns_ApplyPhantomHiddenVisual(state, slot, object);
    if (state->movementPhantomFlickerTimers[slot] != 0) {
        state->movementPhantomFlickerTimers[slot]--;
        if (state->movementPhantomFlickerTimers[slot] == 0) {
            OverworldWildSpawns_ClearPhantomFlickerObject(state, slot, TRUE);
            OverworldWildSpawns_ApplySpawnPassThroughFlag(state, slot, object);
        }
    }
}

static void OverworldWildSpawns_StartActivePhantomRealFlicker(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object)
{
    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || object == NULL) {
        return;
    }

    OverworldWildSpawns_ClearPhantomFlickerObject(state, slot, TRUE);
    state->movementPhantomHidden[slot] = FALSE;
    state->movementPhantomHiddenSteps[slot] = 0;
#if OW_WILD_SPAWNER_PHANTOM_STALK_ACTIVE_REAL_FLICKER
    state->movementPhantomFlickerTimers[slot] = OW_WILD_SPAWNER_PHANTOM_STALK_ACTIVE_FLICKER_FRAMES;
#else
    state->movementPhantomFlickerTimers[slot] = 0;
#endif
    MapObject_ClearBits(object, BIT_VANISH);
    OverworldWildSpawns_ApplySpawnPassThroughFlag(state, slot, object);
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
    OverworldWildSpawns_EnsureFrameMovementTask(state, state->movementFieldSystem);
#endif
}

static void OverworldWildSpawns_StartPhantomVisibleCooldown(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object,
    const OverworldWildBehaviorProfile *profile)
{
    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    OverworldWildSpawns_RevealPhantomStalker(state, slot, object);
    state->movementPhantomVisiblePause[slot] = TRUE;
    state->movementCooldowns[slot] = OverworldWildSpawns_GetPhantomTeleportPauseFrames(
        profile,
        state->movementSpotStates[slot]);
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
    OverworldWildSpawns_EnsureFrameMovementTask(state, state->movementFieldSystem);
#endif
}

#if OW_WILD_SPAWNER_PHANTOM_STALK_ACTIVE_REAL_FLICKER
static BOOL OverworldWildSpawns_UpdateActivePhantomRealFlicker(OverworldWildSpawnState *state, int slot)
{
    LocalMapObject *object;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !state->spawns[slot].active
        || state->movementPhantomHidden[slot]
        || !OverworldWildSpawns_IsActivePhantomStalker(state, slot)) {
        return FALSE;
    }

    object = state->spawns[slot].object;
    if (object == NULL) {
        return FALSE;
    }

    OverworldWildSpawns_ClearPhantomFlickerObject(state, slot, TRUE);
    if (state->movementPhantomFlickerTimers[slot] == 0
        || state->movementPhantomFlickerTimers[slot] > OW_WILD_SPAWNER_PHANTOM_STALK_ACTIVE_FLICKER_FRAMES) {
        state->movementPhantomFlickerTimers[slot] = OW_WILD_SPAWNER_PHANTOM_STALK_ACTIVE_FLICKER_FRAMES;
    }

    if (OverworldWildSpawns_ShouldShowActivePhantomRealFlicker(state->movementPhantomFlickerTimers[slot])) {
        MapObject_ClearBits(object, BIT_VANISH);
    } else {
        MapObject_SetBits(object, BIT_VANISH);
    }
    OverworldWildSpawns_ApplySpawnPassThroughFlag(state, slot, object);

    if (state->movementPhantomFlickerTimers[slot] != 0) {
        state->movementPhantomFlickerTimers[slot]--;
        if (state->movementPhantomFlickerTimers[slot] == 0) {
            OverworldWildSpawns_ApplySpawnPassThroughFlag(state, slot, object);
        }
    }
    return TRUE;
}
#endif

static LocalMapObject *OverworldWildSpawns_RecreatePhantomObjectForActiveState(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object)
{
    FieldSystem *fieldSystem;
    int x;
    int y;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !state->spawns[slot].active
        || state->spawns[slot].species == SPECIES_NONE) {
        return object;
    }

    if (object == NULL) {
        object = state->spawns[slot].object;
    }
    fieldSystem = state->movementFieldSystem;
    if (object == NULL
        || object != state->spawns[slot].object
        || !OverworldWildSpawns_IsMovementFieldContextCurrent(state, fieldSystem)
        || !OverworldWildSpawns_IsCurrentSpawnObject(fieldSystem, &state->spawns[slot])) {
        return object;
    }

    OverworldWildSpawns_CancelSpotEmotePresentation(state, slot, object);
    OverworldWildSpawns_FinishActivePresentationCommand(object);
    OverworldWildSpawns_RevealPhantomStalker(state, slot, object);
    OverworldWildSpawns_ClearMovementSlotInProgress(state, slot);
    OverworldWildSpawns_ClearPhantomFlickerObject(state, slot, TRUE);

    x = (int)MapObject_GetCurrentX(object);
    y = (int)MapObject_GetCurrentY(object);
    OverworldWildSpawns_SetObjectLandingTile(fieldSystem, object, x, y);
    OverworldWildSpawns_ApplySpawnPassThroughFlag(state, slot, object);
    return object;
}

static void OverworldWildSpawns_TryStartPhantomFlicker(OverworldWildSpawnState *state, int slot, LocalMapObject *object)
{
    OverworldWildBehaviorProfile profile;
    OverworldWildBehaviorPrimitives primitives;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || object == NULL
        || !state->movementPhantomHidden[slot]) {
        return;
    }

    profile = OverworldWildSpawns_GetBehaviorProfile(state, slot);
    primitives = OverworldWildSpawns_ResolveBehaviorPrimitives(&profile);
    if (primitives.attentiveLocomotion != OW_WILD_BEHAVIOR_LOCOMOTION_PHANTOM_TELEPORT
        || primitives.attentiveTarget != OW_WILD_BEHAVIOR_TARGET_PLAYER_FRONT) {
        return;
    }

    if (OverworldWildSpawns_IsActivePhantomStalker(state, slot)) {
        state->movementPhantomFlickerTimers[slot] = OW_WILD_SPAWNER_PHANTOM_STALK_ACTIVE_FLICKER_FRAMES;
    } else if ((gf_rand() % 100) < OW_WILD_SPAWNER_PHANTOM_STALK_FLICKER_CHANCE_PERCENT) {
        state->movementPhantomFlickerTimers[slot] = OW_WILD_SPAWNER_PHANTOM_STALK_FLICKER_FRAMES;
    } else {
        state->movementPhantomFlickerTimers[slot] = 0;
    }
    OverworldWildSpawns_ApplyPhantomHiddenVisual(state, slot, object);
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
    OverworldWildSpawns_EnsureFrameMovementTask(state, state->movementFieldSystem);
#endif
}

static BOOL OverworldWildSpawns_IsPhantomTeleportTile(
    FieldSystem *fieldSystem,
    LocalMapObject *object,
    u8 allowedTile,
    int x,
    int y)
{
    if (fieldSystem == NULL
        || object == NULL
        || x < 0
        || y < 0
        || ((int)MapObject_GetCurrentX(object) == x && (int)MapObject_GetCurrentY(object) == y)) {
        return FALSE;
    }

    return OverworldWildSpawns_IsBehaviorAllowedMovementTile(fieldSystem, allowedTile, x, y);
}

static int OverworldWildSpawns_ClampDeltaToStep(int delta, int step)
{
    if (delta > step) {
        return step;
    }
    if (delta < -step) {
        return -step;
    }
    return delta;
}

static u8 OverworldWildSpawns_GetOppositeDirection(u8 direction)
{
    switch (direction) {
    case OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_UP:
        return OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN;
    case OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN:
        return OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_UP;
    case OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_LEFT:
        return OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT;
    case OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT:
        return OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_LEFT;
    default:
        return OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN;
    }
}

static u8 OverworldWildSpawns_GetFacingTowardTile(
    int fromX,
    int fromY,
    int toX,
    int toY,
    u8 fallback)
{
    u8 directions[OW_WILD_SPAWNER_MOVEMENT_MAX_DIRECTIONS];

    if (OverworldWildSpawns_DiagnosticBuildDirections(toX - fromX, toY - fromY, directions) > 0) {
        return directions[0];
    }
    return fallback;
}

static BOOL OverworldWildSpawns_TryUsePhantomFrontTeleportCandidate(
    FieldSystem *fieldSystem,
    LocalMapObject *object,
    int objectX,
    int objectY,
    int playerX,
    int playerY,
    u8 playerFacing,
    int candidateX,
    int candidateY,
    u8 allowedTile,
    int *targetX,
    int *targetY,
    u8 *targetFacing)
{
    int moveDistance = OverworldWildSpawns_Max(
        OverworldWildSpawns_Abs(candidateX - objectX),
        OverworldWildSpawns_Abs(candidateY - objectY));

    if (moveDistance == 0 || moveDistance > OW_WILD_SPAWNER_PHANTOM_STALK_TELEPORT_MOVE_DISTANCE) {
        return FALSE;
    }
    if (!OverworldWildSpawns_IsPhantomTeleportTile(
            fieldSystem,
            object,
            allowedTile,
            candidateX,
            candidateY)) {
        return FALSE;
    }

    *targetX = candidateX;
    *targetY = candidateY;
    *targetFacing = OverworldWildSpawns_GetFacingTowardTile(
        candidateX,
        candidateY,
        playerX,
        playerY,
        OverworldWildSpawns_GetOppositeDirection(playerFacing));
    return TRUE;
}

static BOOL OverworldWildSpawns_TryGetPhantomFrontTeleportTarget(
    FieldSystem *fieldSystem,
    LocalMapObject *object,
    u8 allowedTile,
    int *targetX,
    int *targetY,
    u8 *targetFacing)
{
    LocalMapObject *playerObject;
    int playerX;
    int playerY;
    int objectX;
    int objectY;
    int facingDx;
    int facingDy;
    int desiredX;
    int desiredY;
    int deltaX;
    int deltaY;
    int maxStep;
    int distance;
    int step;
    u8 playerFacing;

    if (fieldSystem == NULL
        || fieldSystem->playerAvatar == NULL
        || object == NULL
        || targetX == NULL
        || targetY == NULL
        || targetFacing == NULL) {
        return FALSE;
    }

    playerObject = fieldSystem->playerAvatar->mapObject;
    if (playerObject == NULL
        || !OverworldWildSpawns_IsCurrentMapObject(fieldSystem, playerObject)
        || (playerObject->flags & MAPOBJECTFLAG_ACTIVE) == 0) {
        return FALSE;
    }

    playerFacing = playerObject->curFacing;
    if (playerFacing > OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT) {
        return FALSE;
    }

    playerX = MapObject_GetCurrentX(playerObject);
    playerY = MapObject_GetCurrentY(playerObject);
    objectX = MapObject_GetCurrentX(object);
    objectY = MapObject_GetCurrentY(object);
    facingDx = OverworldWildSpawns_MovementDirectionDeltaX(playerFacing);
    facingDy = OverworldWildSpawns_MovementDirectionDeltaY(playerFacing);

    for (distance = 1; distance <= OW_WILD_SPAWNER_PHANTOM_STALK_TELEPORT_DISTANCE; distance++) {
        int candidateX = playerX + facingDx * distance;
        int candidateY = playerY + facingDy * distance;

        if (OverworldWildSpawns_TryUsePhantomFrontTeleportCandidate(
                fieldSystem,
                object,
                objectX,
                objectY,
                playerX,
                playerY,
                playerFacing,
                candidateX,
                candidateY,
                allowedTile,
                targetX,
                targetY,
                targetFacing)) {
            return TRUE;
        }
    }

    desiredX = playerX + facingDx;
    desiredY = playerY + facingDy;
    deltaX = desiredX - objectX;
    deltaY = desiredY - objectY;
    maxStep = OverworldWildSpawns_Max(OverworldWildSpawns_Abs(deltaX), OverworldWildSpawns_Abs(deltaY));
    if (maxStep > OW_WILD_SPAWNER_PHANTOM_STALK_TELEPORT_MOVE_DISTANCE) {
        maxStep = OW_WILD_SPAWNER_PHANTOM_STALK_TELEPORT_MOVE_DISTANCE;
    }

    for (step = maxStep; step > 0; step--) {
        int candidateX = objectX + OverworldWildSpawns_ClampDeltaToStep(deltaX, step);
        int candidateY = objectY + OverworldWildSpawns_ClampDeltaToStep(deltaY, step);

        if (OverworldWildSpawns_TryUsePhantomFrontTeleportCandidate(
                fieldSystem,
                object,
                objectX,
                objectY,
                playerX,
                playerY,
                playerFacing,
                candidateX,
                candidateY,
                allowedTile,
                targetX,
                targetY,
                targetFacing)) {
            return TRUE;
        }
    }

    return FALSE;
}

static BOOL OverworldWildSpawns_TryGetPhantomDirectionalTeleportTarget(
    FieldSystem *fieldSystem,
    LocalMapObject *object,
    u8 allowedTile,
    const u8 *directions,
    int directionCount,
    int *targetX,
    int *targetY,
    u8 *targetFacing)
{
    int objectX;
    int objectY;
    int directionIndex;
    int distance;

    if (fieldSystem == NULL
        || object == NULL
        || directions == NULL
        || directionCount <= 0
        || targetX == NULL
        || targetY == NULL
        || targetFacing == NULL) {
        return FALSE;
    }

    objectX = MapObject_GetCurrentX(object);
    objectY = MapObject_GetCurrentY(object);
    for (directionIndex = 0; directionIndex < directionCount; directionIndex++) {
        u8 direction = directions[directionIndex];

        for (distance = OW_WILD_SPAWNER_PHANTOM_STALK_TELEPORT_MOVE_DISTANCE; distance > 0; distance--) {
            int candidateX = objectX
                + OverworldWildSpawns_MovementDirectionDeltaX(direction) * distance;
            int candidateY = objectY
                + OverworldWildSpawns_MovementDirectionDeltaY(direction) * distance;

            if (OverworldWildSpawns_IsPhantomTeleportTile(
                    fieldSystem,
                    object,
                    allowedTile,
                    candidateX,
                    candidateY)) {
                *targetX = candidateX;
                *targetY = candidateY;
                *targetFacing = direction;
                return TRUE;
            }
        }
    }

    return FALSE;
}

static inline BOOL __attribute__((always_inline))
OverworldWildSpawns_IsPhantomOnPlayerFrontTile(
    FieldSystem *fieldSystem,
    LocalMapObject *object)
{
    LocalMapObject *playerObject;
    int frontX;
    int frontY;
    u8 playerFacing;

    if (fieldSystem == NULL
        || fieldSystem->playerAvatar == NULL
        || object == NULL) {
        return FALSE;
    }

    playerObject = fieldSystem->playerAvatar->mapObject;
    if (playerObject == NULL
        || !OverworldWildSpawns_IsCurrentMapObject(fieldSystem, playerObject)
        || (playerObject->flags & MAPOBJECTFLAG_ACTIVE) == 0) {
        return FALSE;
    }

    playerFacing = playerObject->curFacing;
    if (playerFacing > OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT) {
        return FALSE;
    }

    frontX = MapObject_GetCurrentX(playerObject) + OverworldWildSpawns_MovementDirectionDeltaX(playerFacing);
    frontY = MapObject_GetCurrentY(playerObject) + OverworldWildSpawns_MovementDirectionDeltaY(playerFacing);
    return (int)MapObject_GetCurrentX(object) == frontX
        && (int)MapObject_GetCurrentY(object) == frontY;
}

static void OverworldWildSpawns_StartRamCrashShake(OverworldWildSpawnState *state, int slot, LocalMapObject *object)
{
    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || object == NULL) {
        return;
    }

    OverworldWildSpawns_RestoreRamCrashShake(state, slot);
    state->movementRamCrashShakeBaseX[slot] = object->posVec[0];
    state->movementRamCrashShakeBaseZ[slot] = object->posVec[2];
    state->movementRamCrashShakeTimers[slot] = OW_WILD_SPAWNER_RAM_CRASH_SHAKE_FRAMES;

#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
    OverworldWildSpawns_EnsureFrameMovementTask(state, state->movementFieldSystem);
#endif
}

static void OverworldWildSpawns_TickRamCrashShake(OverworldWildSpawnState *state, FieldSystem *fieldSystem)
{
    int i;

    if (state == NULL) {
        return;
    }

    for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
        LocalMapObject *object;
        s32 offset;

        if (state->movementRamCrashShakeTimers[i] == 0) {
            continue;
        }

        if (!OverworldWildSpawns_IsCurrentSpawnObject(fieldSystem, &state->spawns[i])) {
            state->movementRamCrashShakeTimers[i] = 0;
            continue;
        }

        if (state->movementRamCrashShakeTimers[i] <= 1) {
            OverworldWildSpawns_RestoreRamCrashShake(state, i);
            continue;
        }

        state->movementRamCrashShakeTimers[i]--;
        object = state->spawns[i].object;
        offset = OverworldWildSpawns_GetRamCrashShakeOffset(state->movementRamCrashShakeTimers[i]);
        object->posVec[0] = (u32)((s32)state->movementRamCrashShakeBaseX[i] + offset);
        object->posVec[2] = (u32)((s32)state->movementRamCrashShakeBaseZ[i] - offset);
    }
}

static void OverworldWildSpawns_ResetSlotSpotState(OverworldWildSpawnState *state, int slot)
{
    if (state == NULL || slot < 0 || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    OverworldWildSpawns_ClearThrowStateForSlot(state, slot);
    state->movementSpotStates[slot] = OW_WILD_SPAWNER_SPOT_STATE_CHILL;
    state->movementCooldowns[slot] = 0;
    state->movementEmoteTimers[slot] = 0;
    state->movementEmoteSteps[slot] = OW_WILD_SPAWNER_SPOT_EMOTE_STEP_DONE;
    state->movementEmoteDirections[slot] = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_NONE;
    state->movementEmoteJumpsRemaining[slot] = 0;
    state->movementEmoteEndStates[slot] = OW_WILD_SPAWNER_SPOT_STATE_CHILL;
    state->movementEmoteBubbleIds[slot] = OW_WILD_SPAWNER_BUBBLE_ID_NONE;
    state->movementEmoteShowBubbleEachJump[slot] = FALSE;
    state->movementEmotePlayCryOnHop[slot] = FALSE;
    OW_WILD_RUNTIME(state)->movementEmotePartnerPrepObjects[slot] = NULL;
    OW_WILD_RUNTIME(state)->movementEmotePlayHopSound[slot] = TRUE;
    OW_WILD_RUNTIME(state)->movementHopStartSoundSuppressFrames[slot] = 0;
    state->movementActiveSteps[slot] = 0;
    state->movementSpotCooldowns[slot] = 0;
    if (state->movementQueuedBattleSlot == slot) {
        state->movementQueuedBattleSlot = -1;
    }
    state->movementPreviousTileX[slot] = OW_WILD_SPAWNER_PREVIOUS_TILE_NONE;
    state->movementPreviousTileY[slot] = OW_WILD_SPAWNER_PREVIOUS_TILE_NONE;
    state->movementPreviousTileLocked[slot] = FALSE;
    state->movementPendingDirections[slot] = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_NONE;
    state->movementPendingDistances[slot] = 0;
    state->movementLastDirections[slot] = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_NONE;
    state->movementLastDistances[slot] = 0;
    state->movementSpawnRunTargetX[slot] = 0;
    state->movementSpawnRunTargetY[slot] = 0;
    state->movementSpawnRunActive[slot] = FALSE;
    OverworldWildSpawns_ClearStagedHopTarget(state, slot);
    state->movementRamDirections[slot] = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_NONE;
    state->movementRamStepCounters[slot] = 0;
    state->movementRamSpeeds[slot] = 0;
    state->movementRamCrashShakeTimers[slot] = 0;
    state->movementRamCrashShakeBaseX[slot] = 0;
    state->movementRamCrashShakeBaseZ[slot] = 0;
    state->movementPhantomHidden[slot] = FALSE;
    state->movementPhantomHiddenSteps[slot] = 0;
    state->movementPhantomFlickerTimers[slot] = 0;
    state->movementPhantomVisiblePause[slot] = FALSE;
    state->movementMankeyTreeTopProxyObjects[slot] = NULL;
    state->movementPhantomFlickerObjects[slot] = NULL;
    state->movementPhantomTeleportFlickerObjects[slot] = NULL;
    OverworldWildSpawns_ClearPhantomTeleportTarget(state, slot);
}

static void OverworldWildSpawns_SetPreviousTile(
    OverworldWildSpawnState *state,
    int slot,
    int x,
    int y)
{
    if (state == NULL || slot < 0 || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    state->movementPreviousTileX[slot] = (s16)x;
    state->movementPreviousTileY[slot] = (s16)y;
    state->movementPreviousTileLocked[slot] = TRUE;
}

static void OverworldWildSpawns_RecordFinishedMovementHistory(
    OverworldWildSpawnState *state,
    int slot)
{
    LocalMapObject *object;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    if (state->movementPendingDirections[slot] != OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_NONE
        && state->movementPendingDistances[slot] != 0) {
        state->movementLastDirections[slot] = state->movementPendingDirections[slot];
        state->movementLastDistances[slot] = state->movementPendingDistances[slot];
    }
    state->movementPendingDirections[slot] = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_NONE;
    state->movementPendingDistances[slot] = 0;
    object = state->spawns[slot].object;
    if (object != NULL) {
        OW_WILD_RUNTIME(state)->spawnPresentations.lastKnownX[slot] =
            (s16)MapObject_GetCurrentX(object);
        OW_WILD_RUNTIME(state)->spawnPresentations.lastKnownY[slot] =
            (s16)MapObject_GetCurrentY(object);
    }
}

static void OverworldWildSpawns_FinishActivePresentationCommand(LocalMapObject *object)
{
    int i;

    if (object == NULL) {
        return;
    }

    for (i = 0;
         i < OW_WILD_SPAWNER_MOVEMENT_BURST_UPDATE_STEPS
             && MapObject_IsSingleMovementActive(object);
         i++) {
        if (MapObject_UpdateMovementCommand(object)) {
            MapObject_ClearSingleMovementActive(object);
            return;
        }
    }

    if (MapObject_IsSingleMovementActive(object)) {
        MapObject_ClearSingleMovementActive(object);
    }
}

static void OverworldWildSpawns_CancelSpotEmotePresentation(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object)
{
    OverworldWildOverlayRuntimeState *runtime;
    LocalMapObject *owner;
    BOOL restoreAlreadyStarted;

    if (state == NULL
        || state->movementRuntimeState == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    runtime = OW_WILD_RUNTIME(state);
    owner = runtime->movementEmotePartnerPrepObjects[slot];
    restoreAlreadyStarted = state->movementEmoteSteps[slot]
        == OW_WILD_SPAWNER_SPOT_EMOTE_STEP_DONE;
    runtime->movementEmotePartnerPrepObjects[slot] = NULL;
    if (owner == NULL
        || owner != object
        || object != state->spawns[slot].object) {
        return;
    }

    OverworldWildSpawns_FinishActivePresentationCommand(object);
    if (!restoreAlreadyStarted) {
        (void)OverworldWildSpawns_RunImmediateCanopyMovementCommand(
            object,
            OW_WILD_SPAWNER_SPOT_EMOTE_FREEZE_COMMAND);
        (void)OverworldWildSpawns_RunImmediateCanopyMovementCommand(
            object,
            OW_WILD_SPAWNER_SPOT_EMOTE_PARTNER_RESTORE_COMMAND);
    }
}

static void OverworldWildSpawns_ResetSlotMovementCommand(OverworldWildSpawnState *state, int slot, BOOL clearObjectCommand)
{
    LocalMapObject *object = NULL;

    OverworldWildSpawns_ClearStagedHopMovementListTask(state, slot);
    if (state != NULL
        && slot >= 0
        && slot < OW_WILD_MAX_SPAWNS
        && state->spawns[slot].active
        && state->movementFieldSystem != NULL
        && OverworldWildSpawns_IsPresentationFieldContextCurrent(state, state->movementFieldSystem)
        && OverworldWildSpawns_IsCurrentSpawnObject(state->movementFieldSystem, &state->spawns[slot])) {
        object = state->spawns[slot].object;
    }
    if (clearObjectCommand) {
        OverworldWildSpawns_CancelSpotEmotePresentation(state, slot, object);
    }
    if (state != NULL
        && slot >= 0
        && slot < OW_WILD_MAX_SPAWNS
        && clearObjectCommand
        && OW_WILD_RUNTIME(state)->movementCustomJumpPrepActive[slot]
        && object != NULL) {
        OverworldWildSpawns_FinishActivePresentationCommand(object);
        (void)OverworldWildSpawns_RunImmediateCanopyMovementCommand(
            object,
            OW_WILD_SPAWNER_CANOPY_HOPPER_FREEZE_COMMAND);
        (void)OverworldWildSpawns_RunImmediateCanopyMovementCommand(
            object,
            OW_WILD_SPAWNER_CANOPY_HOPPER_PARTNER_RESTORE_COMMAND);
    }
    if (slot >= 0 && slot < OW_WILD_MAX_SPAWNS) {
        OverworldWildSpawns_ClearCustomJump(state, slot);
        OW_WILD_RUNTIME(state)->movementCustomJumpPrepActive[slot] = FALSE;
    }
    if (clearObjectCommand) {
        OverworldWildSpawns_RestoreRamCrashShake(state, slot);
        OverworldWildSpawns_RevealPhantomStalker(state, slot, NULL);
        OverworldWildSpawns_ClearMankeyTreeTopProxyObject(state, slot, TRUE);
    } else {
        state->movementRamCrashShakeTimers[slot] = 0;
        OverworldWildSpawns_ClearMankeyTreeTopProxyObject(state, slot, FALSE);
        OverworldWildSpawns_ClearPhantomFlickerObject(state, slot, FALSE);
    }

    if (clearObjectCommand && object != NULL) {
        OverworldWildSpawns_CancelNativeHeldMovementForSlot(state, slot, object);
        MapObject_ClearBits(
            object,
            OW_WILD_SPAWNER_CUSTOM_JUMP_OWNED_BITS);
        OverworldWildSpawns_FinishActivePresentationCommand(object);
    }

    OW_WILD_RUNTIME(state)->movementNativeHeldMask &=
        ~OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot);
    OverworldWildSpawns_ClearMovementSlotInProgress(state, slot);
    OverworldWildSpawns_ResetSlotSpotState(state, slot);
}

static void OverworldWildSpawns_ResetSlotMovementCommandForMapHeaderChange(
    OverworldWildSpawnState *state,
    int slot,
    BOOL clearObjectCommand)
{
    u8 spotState;
    u8 tiredTimer;
    u8 activeSteps;
    u8 spotCooldown;
    u8 movementCooldown;

    if (state == NULL || slot < 0 || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    spotState = state->movementSpotStates[slot];
    tiredTimer = spotState == OW_WILD_SPAWNER_SPOT_STATE_TIRED
        ? state->movementEmoteTimers[slot]
        : 0;
    if (spotState == OW_WILD_SPAWNER_SPOT_STATE_EMOTING) {
        spotState = state->movementEmoteEndStates[slot];
    }
    activeSteps = state->movementActiveSteps[slot];
    spotCooldown = state->movementSpotCooldowns[slot];
    movementCooldown = state->movementCooldowns[slot];

    OverworldWildSpawns_ResetSlotMovementCommand(state, slot, clearObjectCommand);

    state->movementSpotStates[slot] = spotState;
    state->movementActiveSteps[slot] = activeSteps;
    state->movementSpotCooldowns[slot] = spotCooldown;
    state->movementCooldowns[slot] = movementCooldown;
    if (spotState == OW_WILD_SPAWNER_SPOT_STATE_TIRED) {
        state->movementEmoteTimers[slot] = tiredTimer;
    }
    if (state->movementBehaviorClasses[slot] == OW_WILD_BEHAVIOR_CLASS_PICKED_UP) {
        const OverworldWildSpawn *spawn = &state->spawns[slot];

        state->movementBehaviorClasses[slot] = OverworldWildSpawns_GetBehaviorClassForSpawn(
            spawn->species,
            spawn->level,
            spawn->terrain,
            spawn->shiny);
    }
}

static void OverworldWildSpawns_PrepareSlotForCapture(
    OverworldWildSpawnState *state,
    int slot)
{
    if (state == NULL || slot < 0 || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }
    OverworldWildSpawns_ClearThrowStateForSlot(state, slot);
    OverworldWildSpawns_ResetSlotMovementCommand(state, slot, TRUE);
    OverworldWildSpawns_ClearStagedHopTarget(state, slot);
    OverworldWildSpawns_ClearSpawnRunState(state, slot);
    OverworldWildSpawns_ClearRamCrashMovementState(state, slot);
    if (state->movementQueuedBattleSlot == slot) {
        state->movementQueuedBattleSlot = -1;
        state->movementBattleSettleFrames = 0;
    }
}

static void OverworldWildSpawns_ResetAllMovementCommands(
    OverworldWildSpawnState *state,
    BOOL clearObjectCommand,
    BOOL preserveMapHeaderState)
{
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
    if (!sOverworldWildMovementFrameTaskExecuting) {
        OverworldWildSpawns_StopFrameMovementTask();
    }
#endif

    if (state == NULL) {
        return;
    }

    if (state->movementRuntimeState != NULL) {
        OW_WILD_RUNTIME(state)->playerStepMaintenanceState =
            OW_WILD_PLAYER_STEP_MAINTENANCE_NONE;
    }
    OverworldWildSpawns_ClearQueuedHelpChildren(state);
    OverworldWildSpawns_ResetAllMovementStateOnly(
        state,
        clearObjectCommand,
        preserveMapHeaderState);
    state->movementFieldSystem = NULL;
}

static void OverworldWildSpawns_ResetAllMovementStateOnly(
    OverworldWildSpawnState *state,
    BOOL clearObjectCommand,
    BOOL preserveMapHeaderState)
{
    int i;

    if (state == NULL) {
        return;
    }

    state->movementBattleSettleFrames = 0;
    state->movementQueuedBattleSlot = -1;
    for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
        if (preserveMapHeaderState) {
            OverworldWildSpawns_ResetSlotMovementCommandForMapHeaderChange(
                state,
                i,
                clearObjectCommand);
        } else {
            OverworldWildSpawns_ResetSlotMovementCommand(state, i, clearObjectCommand);
        }
    }
}

static void OverworldWildSpawns_DetachAllMovementStateOnContextLoss(
    OverworldWildSpawnState *state,
    BOOL preserveMapHeaderState)
{
    OverworldWildOverlayRuntimeState *runtime;
    FieldSystem *fieldSystem;
    int i;

    if (state == NULL) {
        return;
    }
    OverworldWildSpawns_CancelDeferredBattleScript();
    OverworldWildSpawns_ResetPendingBattle(state);
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
    OverworldWildSpawns_StopFrameMovementTask();
#endif
    fieldSystem = state->movementFieldSystem;
    if (!preserveMapHeaderState
        || OverworldWildSpawns_IsPlayerBallProjectileActive()) {
        OverworldWildSpawns_TransitionPlayerBallProjectile(
            fieldSystem,
            state,
            preserveMapHeaderState
                ? OW_WILD_HELPER_THROW_PRESENTATION_TRANSITION_SUSPEND
                : OW_WILD_HELPER_THROW_PRESENTATION_TRANSITION_DISCARD);
    }
    OverworldWildSpawns_RestoreMankeyTreeTopLayerProbe();
    if (!preserveMapHeaderState) {
        state->captureTargetMask = 0;
    }
    state->movementBattleSettleFrames = 0;
    state->movementQueuedBattleSlot = -1;

    if (state->movementRuntimeState == NULL) {
        state->movementFieldSystem = NULL;
        return;
    }
    runtime = OW_WILD_RUNTIME(state);
    if (!preserveMapHeaderState) {
        runtime->movementNativeShadowRestorePending = FALSE;
    }
    runtime->playerStepMaintenanceState =
        OW_WILD_PLAYER_STEP_MAINTENANCE_NONE;
    OverworldWildSpawns_ResetPlayerBallShadowTracking(state);
    if (OverworldWildSpawns_IsPresentationFieldContextCurrent(state, fieldSystem)) {
        /* Finish every object-owned command, including the paired 0x4A restore. */
        OverworldWildSpawns_ResetAllMovementCommands(
            state,
            TRUE,
            preserveMapHeaderState);
    } else {
        /* The old manager is no longer safe to touch. Cancel code-owned tasks only. */
        OverworldWildSpawns_ResetAllMovementStateOnly(
            state,
            FALSE,
            preserveMapHeaderState);
        OverworldWildSpawns_ClearQueuedHelpChildren(state);
        for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
            OverworldWildSpawns_ClearStagedHopTarget(state, i);
            OverworldWildSpawns_ClearSpawnRunState(state, i);
            OverworldWildSpawns_ClearRamCrashMovementState(state, i);
            OverworldWildSpawns_ClearMankeyTreeTopCache(state, i);
            runtime->movementEmotePartnerPrepObjects[i] = NULL;
            state->movementPhantomHidden[i] = FALSE;
            state->movementPhantomHiddenSteps[i] = 0;
            state->movementPhantomFlickerTimers[i] = 0;
            state->movementPhantomVisiblePause[i] = FALSE;
            state->movementPhantomTeleportHasTarget[i] = FALSE;
        }
    }

    memset(&runtime->throwState, 0, sizeof(runtime->throwState));
    memset(runtime->spawnPresentations.farSamples, 0,
        sizeof(runtime->spawnPresentations.farSamples));
    runtime->spawnPresentations.managerRestoreMask = 0;
    state->movementFieldSystem = NULL;
}

static void OverworldWildSpawns_PrepareMapHeaderChange(
    OverworldWildSpawnState *state,
    OverworldWildMapHeaderChangeMode mode)
{
    int i;

    if (state == NULL) {
        return;
    }
    if (mode == OW_WILD_MAP_HEADER_CHANGE_CANONICALIZE) {
        for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
            LocalMapObject *object = state->spawns[i].object;
            BOOL restoreSpotPartner;

            if (!state->spawns[i].active
                || !OverworldWildSpawns_IsCurrentSpawnObject(gFieldSysPtr, &state->spawns[i])) {
                continue;
            }
            restoreSpotPartner = state->movementRuntimeState != NULL
                && OW_WILD_RUNTIME(state)->movementEmotePartnerPrepObjects[i]
                    == object;
            if (restoreSpotPartner) {
                /* Complete any in-flight 0x4A before clearing held ownership. */
                OverworldWildSpawns_FinishActivePresentationCommand(object);
            }
            MapObject_ClearHeldMovement(object);
            MapObject_ClearSingleMovementActive(object);
            if (restoreSpotPartner) {
                OW_WILD_RUNTIME(state)->movementEmotePartnerPrepObjects[i] = NULL;
                (void)OverworldWildSpawns_RunImmediateCanopyMovementCommand(
                    object,
                    OW_WILD_SPAWNER_SPOT_EMOTE_FREEZE_COMMAND);
                (void)OverworldWildSpawns_RunImmediateCanopyMovementCommand(
                    object,
                    OW_WILD_SPAWNER_SPOT_EMOTE_PARTNER_RESTORE_COMMAND);
            }
            if (state->movementRuntimeState != NULL) {
                OverworldWildSpawns_ResetSlotMovementCommandForMapHeaderChange(
                    state,
                    i,
                    TRUE);
            }
            OverworldWildSpawns_RestoreMankeyTreeTopRenderOverride(i, object);
            MapObject_ClearBits(object, OW_WILD_SPAWNER_CUSTOM_JUMP_OWNED_BITS);
            OverworldWildSpawns_FinishActivePresentationCommand(object);
            if (state->movementRuntimeState != NULL) {
                OW_WILD_RUNTIME(state)->spawnPresentations.lastKnownX[i] =
                    (s16)MapObject_GetCurrentX(object);
                OW_WILD_RUNTIME(state)->spawnPresentations.lastKnownY[i] =
                    (s16)MapObject_GetCurrentY(object);
                OW_WILD_RUNTIME(state)->spawnPresentations.farSamples[i] = 0;
            }
        }
        if (state->movementRuntimeState != NULL) {
            OW_WILD_RUNTIME(state)->movementNativeShadowRestorePending = TRUE;
        }
        return;
    }
    if (mode == OW_WILD_MAP_HEADER_CHANGE_PRESERVE
        && state->movementRuntimeState != NULL) {
        OverworldWildOverlayRuntimeState *runtime = OW_WILD_RUNTIME(state);
        u16 partnerPrepMask = 0;
        u16 customJumpPrepMask = 0;

        for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
            if (state->spawns[i].active
                && runtime->movementEmotePartnerPrepObjects[i]
                    == state->spawns[i].object) {
                partnerPrepMask |= OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(i);
            }
            if (state->spawns[i].active
                && runtime->movementCustomJumpPrepActive[i]) {
                customJumpPrepMask |= OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(i);
            }
        }
        OverworldWildSpawns_DetachAllMovementStateOnContextLoss(state, TRUE);
        for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
            if ((partnerPrepMask & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(i)) != 0) {
                runtime->movementEmotePartnerPrepObjects[i] =
                    state->spawns[i].object;
            }
            runtime->movementCustomJumpPrepActive[i] =
                (customJumpPrepMask & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(i)) != 0;
        }
        return;
    }
    if (mode == OW_WILD_MAP_HEADER_CHANGE_DISCARD) {
        OverworldWildSpawns_ClearContextLite(state);
    }
    OverworldWildSpawns_DetachAllMovementStateOnContextLoss(state, FALSE);
}

static void __attribute__((noinline)) OverworldWildSpawns_CleanupPresentationBeforeUnload(OverworldWildSpawnState *state)
{
    if (state == NULL || state->movementRuntimeState == NULL) {
        return;
    }

    OverworldWildSpawns_DetachAllMovementStateOnContextLoss(state, FALSE);
}

static void OverworldWildSpawns_StartMovementCommandForSlot(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object,
    u32 movementCommand,
    u8 direction,
    u8 distance)
{
    OverworldWildSpawns_SetPreviousTile(
        state,
        slot,
        MapObject_GetCurrentX(object),
        MapObject_GetCurrentY(object));
    state->movementPendingDirections[slot] = direction;
    state->movementPendingDistances[slot] = distance;
    /* The native per-object task advances held movement exactly once a frame. */
    MapObject_StartMovementCommandInternal(object, movementCommand);
    OW_WILD_RUNTIME(state)->movementNativeHeldMask |=
        OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot);
    MapObject_SetSingleMovementActive(object);
    state->movementBattleSettleFrames = 0;
    OverworldWildSpawns_SetMovementSlotInProgress(state, slot);
    OverworldWildSpawns_TryStartPhantomFlicker(state, slot, object);
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
    OverworldWildSpawns_EnsureFrameMovementTask(state, state->movementFieldSystem);
#endif
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_UPDATE_COMMAND && OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_BURST_UPDATE
    if (OverworldWildSpawns_UpdateSpawnerMovementCommandForSlot(state, slot, FALSE)) {
        OverworldWildSpawns_ClearMovementSlotInProgress(state, slot);
    }
#endif
    sOverworldWildMovementDiagnosticLookCommand = movementCommand;
    sOverworldWildMovementDiagnosticWalkIssued = TRUE;
    OW_WILD_PERF_INC(sOverworldWildPerfMovementCommandsThisFrame);
}

static BOOL OverworldWildSpawns_IsLedgeBehavior(u8 behavior)
{
    return behavior == OW_WILD_TILE_LEDGE_EAST
        || behavior == OW_WILD_TILE_LEDGE_WEST
        || behavior == OW_WILD_TILE_LEDGE_NORTH
        || behavior == OW_WILD_TILE_LEDGE_SOUTH;
}

static BOOL OverworldWildSpawns_IsDownhillLedgeForDirection(u8 behavior, u8 direction)
{
    switch (direction) {
    case OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_UP:
        return behavior == OW_WILD_TILE_LEDGE_NORTH;
    case OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN:
        return behavior == OW_WILD_TILE_LEDGE_SOUTH;
    case OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_LEFT:
        return behavior == OW_WILD_TILE_LEDGE_WEST;
    case OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT:
        return behavior == OW_WILD_TILE_LEDGE_EAST;
    default:
        return FALSE;
    }
}

static BOOL OverworldWildSpawns_IsUphillLedgeForDirection(u8 behavior, u8 direction)
{
    switch (direction) {
    case OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_UP:
        return behavior == OW_WILD_TILE_LEDGE_SOUTH;
    case OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN:
        return behavior == OW_WILD_TILE_LEDGE_NORTH;
    case OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_LEFT:
        return behavior == OW_WILD_TILE_LEDGE_EAST;
    case OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT:
        return behavior == OW_WILD_TILE_LEDGE_WEST;
    default:
        return FALSE;
    }
}

static BOOL OverworldWildSpawns_CanJumpLedgeBehavior(u8 behavior, u8 direction, u8 jumpLevel)
{
    if (jumpLevel == OW_WILD_BEHAVIOR_JUMP_LEVEL_NONE) {
        return FALSE;
    }
    if (OverworldWildSpawns_IsDownhillLedgeForDirection(behavior, direction)) {
        return TRUE;
    }
    if (jumpLevel >= OW_WILD_BEHAVIOR_JUMP_LEVEL_BOTH
        && OverworldWildSpawns_IsUphillLedgeForDirection(behavior, direction)) {
        return TRUE;
    }

    return FALSE;
}

static BOOL OverworldWildSpawns_IsValidLedgeLandingTile(FieldSystem *fieldSystem, int ledgeX, int ledgeY, int landingX, int landingY)
{
    if (fieldSystem == NULL || fieldSystem->playerAvatar == NULL) {
        return FALSE;
    }
    if (ledgeX < 0 || ledgeY < 0 || landingX < 0 || landingY < 0) {
        return FALSE;
    }
    if (OverworldWildSpawns_IsTileOccupiedByObject(fieldSystem, ledgeX, ledgeY)
        || OverworldWildSpawns_IsTileOccupiedByObject(fieldSystem, landingX, landingY)) {
        return FALSE;
    }
    if (IsMetatileBlockedAt(fieldSystem, landingX, landingY)) {
        return FALSE;
    }

    return TRUE;
}

static BOOL OverworldWildSpawns_IsPreviousTileLockedAt(
    OverworldWildSpawnState *state,
    int slot,
    int x,
    int y)
{
    return state != NULL
        && slot >= 0
        && slot < OW_WILD_MAX_SPAWNS
        && state->movementPreviousTileLocked[slot]
        && state->movementPreviousTileX[slot] == x
        && state->movementPreviousTileY[slot] == y;
}

static BOOL OverworldWildSpawns_ShouldAvoidPreviousTileForActiveProfile(
    OverworldWildSpawnState *state,
    int slot,
    const OverworldWildBehaviorProfile *profile)
{
    return state->movementSpotStates[slot] == OW_WILD_SPAWNER_SPOT_STATE_ACTIVE
        && profile->attentiveState == OW_WILD_BEHAVIOR_KIND_CHASE
        && profile->targetSelector != OW_WILD_BEHAVIOR_TARGET_RANDOM_NEARBY;
}

static BOOL OverworldWildSpawns_IsAvoidedBacktrackStep(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object,
    u8 direction,
    int distance,
    BOOL avoidPreviousTile)
{
    int targetX;
    int targetY;

    if (!avoidPreviousTile || object == NULL) {
        return FALSE;
    }

    targetX = (int)MapObject_GetCurrentX(object)
        + OverworldWildSpawns_MovementDirectionDeltaX(direction) * distance;
    targetY = (int)MapObject_GetCurrentY(object)
        + OverworldWildSpawns_MovementDirectionDeltaY(direction) * distance;

    return OverworldWildSpawns_IsPreviousTileLockedAt(state, slot, targetX, targetY);
}

static BOOL OverworldWildSpawns_TryStartLedgeJumpCommand(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    LocalMapObject *object,
    u8 allowedTile,
    u8 jumpLevel,
    u8 direction,
    BOOL *blockedByLedge,
    BOOL avoidPreviousTile)
{
    int dx;
    int dy;
    int objectX;
    int objectY;
    int ledgeX;
    int ledgeY;
    int landingX;
    int landingY;
    u8 behavior;
    u32 movementCommand;

    if (blockedByLedge != NULL) {
        *blockedByLedge = FALSE;
    }
    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !state->spawns[slot].active
        || object == NULL) {
        return FALSE;
    }

    dx = OverworldWildSpawns_MovementDirectionDeltaX(direction);
    dy = OverworldWildSpawns_MovementDirectionDeltaY(direction);
    if (dx == 0 && dy == 0) {
        return FALSE;
    }

    objectX = MapObject_GetCurrentX(object);
    objectY = MapObject_GetCurrentY(object);
    ledgeX = objectX + dx;
    ledgeY = objectY + dy;
    if (ledgeX < 0 || ledgeY < 0) {
        return FALSE;
    }

    behavior = GetMetatileBehaviorAt(fieldSystem, ledgeX, ledgeY);
    if (!OverworldWildSpawns_IsLedgeBehavior(behavior)) {
        return FALSE;
    }

    if (blockedByLedge != NULL) {
        *blockedByLedge = TRUE;
    }

    landingX = objectX + dx * 2;
    landingY = objectY + dy * 2;
    if (OverworldWildSpawns_IsAvoidedBacktrackStep(
            state,
            slot,
            object,
            direction,
            2,
            avoidPreviousTile)) {
        return FALSE;
    }
    if (!OverworldWildSpawns_CanJumpLedgeBehavior(behavior, direction, jumpLevel)
        || !OverworldWildSpawns_IsBehaviorAllowedMovementTile(
            fieldSystem,
            allowedTile,
            landingX,
            landingY)
        || OverworldWildSpawns_IsTileReservedByOtherWild(
            state,
            slot,
            landingX,
            landingY)
        || !OverworldWildSpawns_IsValidLedgeLandingTile(fieldSystem, ledgeX, ledgeY, landingX, landingY)) {
        return FALSE;
    }

    movementCommand = MapObject_MovementCommandFromDirection(
        direction,
        OW_WILD_SPAWNER_MOVEMENT_LEDGE_JUMP_COMMAND);
    OverworldWildSpawns_StartMovementCommandForSlot(
        state,
        slot,
        object,
        movementCommand,
        direction,
        OW_WILD_SPAWNER_MOVEMENT_DISTANCE_LEDGE_JUMP);
    return TRUE;
}

static OverworldWildDirectionStepResult OverworldWildSpawns_TryStartSingleDirectionMovementStep(
    const OverworldWildDirectionStepContext *stepContext,
    u8 direction)
{
    BOOL blockedByLedge = FALSE;
    u32 walkCommand;
    u32 movementCommand;
    int targetX;
    int targetY;

    if (stepContext == NULL
        || stepContext->state == NULL
        || stepContext->slot >= OW_WILD_MAX_SPAWNS
        || !stepContext->state->spawns[stepContext->slot].active
        || stepContext->object == NULL
        || stepContext->profile == NULL
        || MapObject_IsSingleMovementActive(stepContext->object)) {
        return OW_WILD_DIRECTION_STEP_BUSY;
    }

    if (OverworldWildSpawns_TryStartLedgeJumpCommand(
            stepContext->state,
            stepContext->fieldSystem,
            stepContext->slot,
            stepContext->object,
            stepContext->allowedTile,
            stepContext->jumpLevel,
            direction,
            &blockedByLedge,
            stepContext->avoidPreviousTile)) {
        return OW_WILD_DIRECTION_STEP_STARTED;
    }
    if (blockedByLedge) {
        sOverworldWildMovementDiagnosticDirectionBlocked = TRUE;
        return OW_WILD_DIRECTION_STEP_BLOCKED;
    }

    targetX = MapObject_GetCurrentX(stepContext->object)
        + OverworldWildSpawns_MovementDirectionDeltaX(direction);
    targetY = MapObject_GetCurrentY(stepContext->object)
        + OverworldWildSpawns_MovementDirectionDeltaY(direction);
    if (!OverworldWildSpawns_IsBehaviorAllowedMovementTile(
            stepContext->fieldSystem,
            stepContext->allowedTile,
            targetX,
            targetY)
        || OverworldWildSpawns_IsTileReservedByOtherWild(
            stepContext->state,
            stepContext->slot,
            targetX,
            targetY)) {
        sOverworldWildMovementDiagnosticDirectionBlocked = TRUE;
        return OW_WILD_DIRECTION_STEP_BLOCKED;
    }
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_BLOCKED_CHECK
    sOverworldWildMovementDiagnosticDirectionBlocked =
        MapObject_IsMovementDirectionBlocked(
            stepContext->object,
            direction);
    if (sOverworldWildMovementDiagnosticDirectionBlocked) {
        return OW_WILD_DIRECTION_STEP_BLOCKED;
    }
#endif

    walkCommand = OverworldWildSpawns_GetMovementWalkCommandForProfile(
        stepContext->state,
        stepContext->slot,
        stepContext->profile,
        stepContext->primitives);
    movementCommand = MapObject_MovementCommandFromDirection(
        direction,
        walkCommand);
    OverworldWildSpawns_StartMovementCommandForSlot(
        stepContext->state,
        stepContext->slot,
        stepContext->object,
        movementCommand,
        direction,
        OW_WILD_SPAWNER_MOVEMENT_DISTANCE_STEP);
    return OW_WILD_DIRECTION_STEP_STARTED;
}

#if !OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_WALK_COMMAND
static OverworldWildDirectionStepResult OverworldWildSpawns_TryStartSingleDirectionLookCommand(
    const OverworldWildDirectionStepContext *stepContext,
    u8 direction)
{
    u32 movementCommand;
    int targetX;
    int targetY;

    if (stepContext == NULL
        || stepContext->state == NULL
        || stepContext->slot >= OW_WILD_MAX_SPAWNS
        || !stepContext->state->spawns[stepContext->slot].active
        || stepContext->object == NULL
        || MapObject_IsSingleMovementActive(stepContext->object)) {
        return OW_WILD_DIRECTION_STEP_BUSY;
    }

    targetX = MapObject_GetCurrentX(stepContext->object)
        + OverworldWildSpawns_MovementDirectionDeltaX(direction);
    targetY = MapObject_GetCurrentY(stepContext->object)
        + OverworldWildSpawns_MovementDirectionDeltaY(direction);
    if (!OverworldWildSpawns_IsBehaviorAllowedMovementTile(
            stepContext->fieldSystem,
            stepContext->allowedTile,
            targetX,
            targetY)) {
        sOverworldWildMovementDiagnosticDirectionBlocked = TRUE;
        return OW_WILD_DIRECTION_STEP_BLOCKED;
    }
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_BLOCKED_CHECK
    sOverworldWildMovementDiagnosticDirectionBlocked =
        MapObject_IsMovementDirectionBlocked(
            stepContext->object,
            direction);
    if (sOverworldWildMovementDiagnosticDirectionBlocked) {
        return OW_WILD_DIRECTION_STEP_BLOCKED;
    }
#endif

    movementCommand = MapObject_MovementCommandFromDirection(
        direction,
        OW_WILD_SPAWNER_MOVEMENT_LOOK_UP_COMMAND);
    MapObject_StartMovementCommand(stepContext->object, movementCommand);
    MapObject_SetSingleMovementActive(stepContext->object);
    sOverworldWildMovementDiagnosticLookCommand = movementCommand;
    sOverworldWildMovementDiagnosticLookIssued = TRUE;
    return OW_WILD_DIRECTION_STEP_STARTED;
}
#endif

static BOOL OverworldWildSpawns_TryStartSpawnerMovementCommand(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    const u8 *directions,
    int directionCount,
    const OverworldWildBehaviorProfile *profile,
    const OverworldWildBehaviorPrimitives *primitives)
{
    int pass;
    BOOL deferredBacktrack = FALSE;
    BOOL blockedAnyDirection = FALSE;
    LocalMapObject *object;
    OverworldWildBehaviorProfile fallbackProfile;
    OverworldWildDirectionStepContext stepContext;
    u8 allowedTile;
    u8 jumpLevel;
    BOOL avoidPreviousTile;

    if (profile == NULL) {
        fallbackProfile = OverworldWildSpawns_GetMovementTickProfile(state, slot);
        profile = &fallbackProfile;
    }

    object = state->spawns[slot].object;
    jumpLevel = profile->jumpLevel;
    if (object == NULL || MapObject_IsSingleMovementActive(object)) {
        sOverworldWildMovementDiagnosticSingleActive = object != NULL ? MapObject_IsSingleMovementActive(object) : FALSE;
        return FALSE;
    }

    sOverworldWildMovementDiagnosticSingleActive = FALSE;
    allowedTile = OverworldWildSpawns_GetAllowedTileForSpotState(
        profile,
        state->movementSpotStates[slot]);
    avoidPreviousTile = OverworldWildSpawns_ShouldAvoidPreviousTileForActiveProfile(
        state,
        slot,
        profile);
    stepContext.state = state;
    stepContext.fieldSystem = fieldSystem;
    stepContext.object = object;
    stepContext.profile = profile;
    stepContext.primitives = primitives;
    stepContext.slot = (u8)slot;
    stepContext.allowedTile = allowedTile;
    stepContext.jumpLevel = jumpLevel;
    stepContext.avoidPreviousTile = (u8)avoidPreviousTile;

    for (pass = 0; pass < 2; pass++) {
        int j;

        if (pass != 0 && !deferredBacktrack) {
            break;
        }

        for (j = 0; j < directionCount; j++) {
            OverworldWildDirectionStepResult stepResult;
            u8 direction = directions[j];
            BOOL avoidedBacktrack = OverworldWildSpawns_IsAvoidedBacktrackStep(
                state,
                slot,
                object,
                direction,
                1,
                avoidPreviousTile);

            if (avoidedBacktrack) {
                if (pass == 0) {
                    deferredBacktrack = TRUE;
                    blockedAnyDirection = TRUE;
                    continue;
                }
            } else if (pass != 0) {
                continue;
            }

#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_WALK_COMMAND
            stepResult = OverworldWildSpawns_TryStartSingleDirectionMovementStep(
                &stepContext,
                direction);
#else
            stepResult = OverworldWildSpawns_TryStartSingleDirectionLookCommand(
                &stepContext,
                direction);
#endif
            if (stepResult == OW_WILD_DIRECTION_STEP_STARTED) {
                return TRUE;
            }
            if (stepResult == OW_WILD_DIRECTION_STEP_BUSY) {
                return FALSE;
            }
            blockedAnyDirection = TRUE;
        }
    }

    sOverworldWildMovementDiagnosticWalkSuppressedByBlocked = blockedAnyDirection;
    return FALSE;
}

static void OverworldWildSpawns_AddChaseFallbackDirection(
    u8 *directions,
    int *directionCount,
    u8 direction)
{
    int i;

    if (directions == NULL || directionCount == NULL) {
        return;
    }
    if (*directionCount < 0) {
        *directionCount = 0;
    }
    if (*directionCount > OW_WILD_SPAWNER_MOVEMENT_MAX_DIRECTIONS) {
        *directionCount = OW_WILD_SPAWNER_MOVEMENT_MAX_DIRECTIONS;
    }

    for (i = 0; i < *directionCount; i++) {
        if (directions[i] == direction) {
            return;
        }
    }

    if (*directionCount >= OW_WILD_SPAWNER_MOVEMENT_MAX_DIRECTIONS) {
        return;
    }

    directions[*directionCount] = direction;
    (*directionCount)++;
}

static void OverworldWildSpawns_AppendFrameDrivenChaseFallbackDirections(
    u8 *directions,
    int *directionCount)
{
    u8 primaryDirection;

    if (*directionCount <= 0) {
        return;
    }

    primaryDirection = directions[0];
    if (primaryDirection < OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_LEFT) {
        OverworldWildSpawns_AddChaseFallbackDirection(
            directions,
            directionCount,
            OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_LEFT);
        OverworldWildSpawns_AddChaseFallbackDirection(
            directions,
            directionCount,
            OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT);
    } else {
        OverworldWildSpawns_AddChaseFallbackDirection(
            directions,
            directionCount,
            OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_UP);
        OverworldWildSpawns_AddChaseFallbackDirection(
            directions,
            directionCount,
            OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN);
    }
    OverworldWildSpawns_AddChaseFallbackDirection(
        directions,
        directionCount,
        primaryDirection ^ 1);
}

static inline void OverworldWildSpawns_ApplyWalkPursuitDirectionInertia(
    OverworldWildSpawnState *state,
    int slot,
    u8 *directions,
    int directionCount)
{
    u8 direction = state->movementLastDirections[slot];

    if (directionCount > 1
        && state->movementLastDistances[slot] == OW_WILD_SPAWNER_MOVEMENT_DISTANCE_STEP
        && directions[1] == direction) {
        directions[1] = directions[0];
        directions[0] = direction;
    }
}

static void OverworldWildSpawns_AppendFleeFallbackDirections(
    u8 *directions,
    int *directionCount,
    int fleeDx,
    int fleeDy)
{
    OVERWORLD_WILD_HELPER_FLEE_FALLBACK_ENTRY->appendDirections(
        directions,
        directionCount,
        fleeDx,
        fleeDy);
}

static BOOL OverworldWildSpawns_IsCirclePlayerTargetTile(
    int x,
    int y,
    int playerX,
    int playerY,
    u8 radius)
{
    return OverworldWildSpawns_Max(
            OverworldWildSpawns_Abs(x - playerX),
            OverworldWildSpawns_Abs(y - playerY))
        == radius;
}

static BOOL OverworldWildSpawns_IsCirclePlayerCandidateUsable(
    FieldSystem *fieldSystem,
    const OverworldWildBehaviorProfile *profile,
    int objectX,
    int objectY,
    int candidateX,
    int candidateY)
{
    if (candidateX == objectX && candidateY == objectY) {
        return TRUE;
    }

    return OverworldWildSpawns_IsBehaviorAllowedMovementTile(
        fieldSystem,
        OverworldWildSpawns_GetAllowedTileForSpotState(
            profile,
            OW_WILD_SPAWNER_SPOT_STATE_ACTIVE),
        candidateX,
        candidateY);
}

static BOOL OverworldWildSpawns_TryGetCirclePlayerMovementTarget(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    const OverworldWildBehaviorProfile *profile,
    int objectX,
    int objectY,
    int playerX,
    int playerY,
    int *targetX,
    int *targetY)
{
    int bestX = 0;
    int bestY = 0;
    int bestScore = 0x7FFF;
    int bestCount = 0;
    int fallbackX = 0;
    int fallbackY = 0;
    int fallbackScore = 0x7FFF;
    int fallbackCount = 0;
    int dx;
    int dy;
    u8 radius;
    BOOL currentIsTarget;
    BOOL continueWhenArrived;

    if (state == NULL
        || fieldSystem == NULL
        || profile == NULL
        || targetX == NULL
        || targetY == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS) {
        return FALSE;
    }

    radius = profile->attentiveCircleRadius;
    if (radius == 0) {
        radius = 1;
    }
    currentIsTarget = OverworldWildSpawns_IsCirclePlayerTargetTile(
        objectX,
        objectY,
        playerX,
        playerY,
        radius);
    continueWhenArrived =
        profile->attentiveContinueWhenArrived == OW_WILD_BEHAVIOR_BOOL_YES;
    if (currentIsTarget && !continueWhenArrived) {
        *targetX = objectX;
        *targetY = objectY;
        return TRUE;
    }

    for (dy = -(int)radius; dy <= (int)radius; dy++) {
        for (dx = -(int)radius; dx <= (int)radius; dx++) {
            int candidateX;
            int candidateY;
            int score;
            BOOL isPreviousTile;

            if (OverworldWildSpawns_Max(
                    OverworldWildSpawns_Abs(dx),
                    OverworldWildSpawns_Abs(dy)) != radius) {
                continue;
            }

            candidateX = playerX + dx;
            candidateY = playerY + dy;
            if (candidateX == objectX && candidateY == objectY) {
                continue;
            }
            if (!OverworldWildSpawns_IsCirclePlayerCandidateUsable(
                    fieldSystem,
                    profile,
                    objectX,
                    objectY,
                    candidateX,
                    candidateY)) {
                continue;
            }

            score = OverworldWildSpawns_Max(
                OverworldWildSpawns_Abs(candidateX - objectX),
                OverworldWildSpawns_Abs(candidateY - objectY));
            isPreviousTile = OverworldWildSpawns_IsPreviousTileLockedAt(
                state,
                slot,
                candidateX,
                candidateY);
            if (isPreviousTile) {
                fallbackCount++;
                if (score < fallbackScore
                    || (score == fallbackScore && (gf_rand() % fallbackCount) == 0)) {
                    fallbackScore = score;
                    fallbackX = candidateX;
                    fallbackY = candidateY;
                }
                continue;
            }

            bestCount++;
            if (score < bestScore
                || (score == bestScore && (gf_rand() % bestCount) == 0)) {
                bestScore = score;
                bestX = candidateX;
                bestY = candidateY;
            }
        }
    }

    if (bestCount != 0) {
        *targetX = bestX;
        *targetY = bestY;
        return TRUE;
    }
    if (fallbackCount != 0) {
        *targetX = fallbackX;
        *targetY = fallbackY;
        return TRUE;
    }
    if (currentIsTarget) {
        *targetX = objectX;
        *targetY = objectY;
        return TRUE;
    }

    return FALSE;
}

static BOOL OverworldWildSpawns_TryStartFrameDrivenActiveMovementCommand(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object,
    const OverworldWildBehaviorProfile *profile,
    const OverworldWildBehaviorPrimitives *primitives)
{
    OverworldWildOverlayRuntimeState *runtime;
    FieldSystem *fieldSystem;
    LocalMapObject *targetObject;
    int objectX;
    int objectY;
    int playerX;
    int playerY;
    int impactX;
    int impactY;
    int dx;
    int dy;
    u8 movementTarget;
    u8 throwTarget;
    u8 targetSlot;
    u8 throwDistance;
    u8 directions[OW_WILD_SPAWNER_MOVEMENT_MAX_DIRECTIONS];
    int directionCount;
    const OverworldWildBehaviorProfile *movementProfile = profile;
    OverworldWildBehaviorProfile boostedProfile;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || object == NULL
        || profile == NULL
        || primitives == NULL
        || state->movementSpotStates[slot] != OW_WILD_SPAWNER_SPOT_STATE_ACTIVE
        || (OW_WILD_RUNTIME(state)->movementFrameDrivenActiveMask & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot)) == 0) {
        return FALSE;
    }

    fieldSystem = state->movementFieldSystem;
    if (fieldSystem == NULL || fieldSystem->playerAvatar == NULL) {
        return FALSE;
    }
    runtime = OW_WILD_RUNTIME(state);
    throwTarget = runtime->throwState.targets[slot];
    movementTarget = primitives->attentiveTarget;
    objectX = MapObject_GetCurrentX(object);
    objectY = MapObject_GetCurrentY(object);
    playerX = GetPlayerXCoord(fieldSystem->playerAvatar);
    playerY = GetPlayerYCoord(fieldSystem->playerAvatar);
    impactX = playerX;
    impactY = playerY;
    targetObject = object;
    targetSlot = (u8)slot;
    if (throwTarget != OW_WILD_SPAWNER_THROW_TARGET_NONE) {
        targetSlot = OW_WILD_SPAWNER_THROW_TARGET_DECODE(throwTarget);
        if (targetSlot >= OW_WILD_MAX_SPAWNS
            || !state->spawns[targetSlot].active
            || state->spawns[targetSlot].object == NULL
            || !OverworldWildSpawns_IsCurrentSpawnObject(
                fieldSystem,
                &state->spawns[targetSlot])) {
            OverworldWildSpawns_ClearThrowStateForSlot(state, slot);
            return FALSE;
        }
        targetObject = state->spawns[targetSlot].object;
        if ((throwTarget & OW_WILD_SPAWNER_THROW_TARGET_CARRIED_FLAG) == 0) {
            if (!OverworldWildSpawns_IsValidPickupThrowTarget(state, slot, targetSlot)
                || state->movementEmoteTimers[slot] == 0) {
                OverworldWildSpawns_ClearThrowStateForSlot(state, slot);
                return FALSE;
            }
            state->movementEmoteTimers[slot]--;
            movementTarget = OW_WILD_BEHAVIOR_TARGET_TOWARD_PLAYER;
        }
    }
    if (throwTarget == OW_WILD_SPAWNER_THROW_TARGET_NONE
        && movementTarget == OW_WILD_BEHAVIOR_TARGET_CIRCLE_PLAYER) {
        OverworldWildSpawns_TryGetCirclePlayerMovementTarget(
            state,
            fieldSystem,
            slot,
            profile,
            objectX,
            objectY,
            playerX,
            playerY,
            &playerX,
            &playerY);
    } else if (throwTarget == OW_WILD_SPAWNER_THROW_TARGET_NONE) {
        OverworldWildSpawns_TryGetClosestPlayerMovementTarget(
            fieldSystem,
            objectX,
            objectY,
            &playerX,
            &playerY);
        if (movementTarget == OW_WILD_BEHAVIOR_TARGET_PLAYER_FRONT) {
            OverworldWildSpawns_TryGetCanopyHopperPlayerFrontTarget(
                fieldSystem,
                &playerX,
                &playerY);
        }
    } else if ((throwTarget & OW_WILD_SPAWNER_THROW_TARGET_CARRIED_FLAG) == 0) {
        playerX = MapObject_GetCurrentX(targetObject);
        playerY = MapObject_GetCurrentY(targetObject);
    }

    dx = playerX - objectX;
    dy = playerY - objectY;
    if (throwTarget & OW_WILD_SPAWNER_THROW_TARGET_CARRIED_FLAG) {
        if (throwTarget & OW_WILD_SPAWNER_THROW_TARGET_WINDUP_FLAG) {
            impactX = runtime->movementCustomJumpTargetX[slot];
            impactY = runtime->movementCustomJumpTargetY[slot];
        }
        dx = impactX - objectX;
        dy = impactY - objectY;
        throwDistance = (u8)(OverworldWildSpawns_Abs(dx) + OverworldWildSpawns_Abs(dy));
        directions[0] = dx != 0
            ? OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_LEFT + (dx > 0)
            : (dy > 0);
        if (throwTarget & OW_WILD_SPAWNER_THROW_TARGET_WINDUP_FLAG) {
            MapObject_ClearSingleMovementActive(targetObject);
            if (throwDistance != 0
                && OverworldWildSpawns_StartPreparedCustomJumpCommand(
                    state,
                    fieldSystem,
                    targetSlot,
                    targetObject,
                    directions[0],
                    throwDistance,
                    impactX,
                    impactY,
                    profile,
                    TRUE)) {
                runtime->spawnPresentations.lastKnownX[targetSlot] = (s16)impactX;
                runtime->spawnPresentations.lastKnownY[targetSlot] = (s16)impactY;
                runtime->throwState.targets[slot] = OW_WILD_SPAWNER_THROW_TARGET_NONE;
                runtime->throwState.carrierMask &= ~OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot);
                state->movementCooldowns[slot] = OW_WILD_SPAWNER_THROW_RECOVERY_FRAMES;
                OverworldWildSpawns_StartTiredEmote(state, slot);
            } else {
                OverworldWildSpawns_ReleaseThrowTargetAtCurrentTile(
                    state,
                    slot,
                    targetSlot,
                    targetObject);
                state->movementCooldowns[slot] = OW_WILD_SPAWNER_THROW_RECOVERY_FRAMES;
            }
            return TRUE;
        }
        if ((dx == 0 || dy == 0)
            && throwDistance <= profile->hopMaxDistance) {
            runtime->movementCustomJumpTargetX[slot] = (s16)impactX;
            runtime->movementCustomJumpTargetY[slot] = (s16)impactY;
            runtime->throwState.targets[slot] =
                throwTarget | OW_WILD_SPAWNER_THROW_TARGET_WINDUP_FLAG;
            OverworldWildSpawns_SetObjectFacing(object, directions[0]);
            OverworldWildSpawns_ShowBubble(object, OW_WILD_SPAWNER_BUBBLE_ID_ANGRY);
            PlayCry(
                state->spawns[slot].species,
                state->spawns[slot].form);
            state->movementCooldowns[slot] = OW_WILD_SPAWNER_THROW_WINDUP_FRAMES;
            return TRUE;
        }
        if (movementTarget == OW_WILD_BEHAVIOR_TARGET_PLAYER_CARDINAL_LINE) {
            if (OverworldWildSpawns_Abs(dx) > OverworldWildSpawns_Abs(dy)) {
                playerX = impactX + (dx < 0
                    ? profile->hopMaxDistance
                    : -profile->hopMaxDistance);
                playerY = impactY;
            } else {
                playerX = impactX;
                playerY = impactY + (dy < 0
                    ? profile->hopMaxDistance
                    : -profile->hopMaxDistance);
            }
        }
        dx = playerX - objectX;
        dy = playerY - objectY;
    }
    if (movementTarget == OW_WILD_BEHAVIOR_TARGET_AWAY_FROM_PLAYER) {
        dx = -dx;
        dy = -dy;
    } else if (movementTarget == OW_WILD_BEHAVIOR_TARGET_NONE) {
        dx = 0;
        dy = 0;
    }

    if (movementTarget == OW_WILD_BEHAVIOR_TARGET_RANDOM_NEARBY) {
        directionCount = OverworldWildSpawns_BuildChillWanderDirections(
            fieldSystem,
            object,
            OverworldWildSpawns_GetAllowedTileForSpotState(
                profile,
            state->movementSpotStates[slot]),
            directions);
    } else {
        directionCount = OverworldWildSpawns_DiagnosticBuildDirections(dx, dy, directions);
    }
    if (throwTarget != OW_WILD_SPAWNER_THROW_TARGET_NONE
        && (throwTarget & OW_WILD_SPAWNER_THROW_TARGET_CARRIED_FLAG) == 0) {
        if (directionCount == 0
            || (primitives->attentiveLocomotion != OW_WILD_BEHAVIOR_LOCOMOTION_HOP
                && OverworldWildSpawns_Max(
                    OverworldWildSpawns_Abs(dx),
                    OverworldWildSpawns_Abs(dy)) <= OW_WILD_SPAWNER_MOVEMENT_DISTANCE_STEP)) {
            if (!OverworldWildSpawns_IsStablePickupThrowTarget(state, slot, targetSlot)) {
                state->movementCooldowns[slot] = OW_WILD_SPAWNER_MOVEMENT_DECISION_COOLDOWN;
                return TRUE;
            }
            if (!OverworldWildSpawns_StartCarriedThrowTarget(
                    state,
                    slot,
                    targetSlot,
                    targetObject)) {
                OverworldWildSpawns_ClearThrowStateForSlot(state, slot);
                state->movementCooldowns[slot] = OW_WILD_SPAWNER_MOVEMENT_DECISION_COOLDOWN;
            }
            return TRUE;
        }
    }
    if (directionCount == 0) {
        return FALSE;
    }
    if (movementTarget == OW_WILD_BEHAVIOR_TARGET_RANDOM_NEARBY) {
        dx = 0;
        dy = 0;
    } else if (movementTarget == OW_WILD_BEHAVIOR_TARGET_AWAY_FROM_PLAYER) {
        OverworldWildSpawns_AppendFleeFallbackDirections(
            directions,
            &directionCount,
            dx,
            dy);
    } else {
        if (primitives->attentiveLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_WANDER) {
            OverworldWildSpawns_ApplyWalkPursuitDirectionInertia(
                state,
                slot,
                directions,
                directionCount);
        }
        OverworldWildSpawns_AppendFrameDrivenChaseFallbackDirections(
            directions,
            &directionCount);
    }
    if (throwTarget == OW_WILD_SPAWNER_THROW_TARGET_NONE
        && profile->attentiveChaseBoostDistance != 0
        && profile->attentiveChaseBoostSpeed != 0
        && profile->attentiveState == OW_WILD_BEHAVIOR_KIND_CHASE
        && movementTarget != OW_WILD_BEHAVIOR_TARGET_NONE
        && movementTarget != OW_WILD_BEHAVIOR_TARGET_RANDOM_NEARBY
        && movementTarget != OW_WILD_BEHAVIOR_TARGET_AWAY_FROM_PLAYER
        && OverworldWildSpawns_Max(
            OverworldWildSpawns_Abs(dx),
            OverworldWildSpawns_Abs(dy)) >= profile->attentiveChaseBoostDistance) {
        boostedProfile = *profile;
        if (boostedProfile.attentiveSpeed == 0) {
            boostedProfile.attentiveSpeed = OW_WILD_SPAWNER_MOVEMENT_SPEED_DEFAULT;
        }
        if (boostedProfile.attentiveSpeed < profile->attentiveChaseBoostSpeed) {
            boostedProfile.attentiveSpeed = profile->attentiveChaseBoostSpeed;
        }
        if (boostedProfile.attentiveSpeed > OW_WILD_SPAWNER_MOVEMENT_SPEED_4) {
            boostedProfile.attentiveSpeed = OW_WILD_SPAWNER_MOVEMENT_SPEED_4;
        }
        movementProfile = &boostedProfile;
    }

    if (primitives->attentiveLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_HOP) {
        if (throwTarget != OW_WILD_SPAWNER_THROW_TARGET_NONE
            && (throwTarget & OW_WILD_SPAWNER_THROW_TARGET_CARRIED_FLAG) == 0) {
            sOverworldWildCollisionIgnoredObject = targetObject;
            movementTarget = OW_WILD_BEHAVIOR_TARGET_NONE;
        }
        directionCount = OverworldWildSpawns_TryStartDirectedBehaviorHopCommand(
            state,
            fieldSystem,
            slot,
            movementProfile,
            dx,
            dy,
            directions,
            directionCount,
            movementTarget != OW_WILD_BEHAVIOR_TARGET_AWAY_FROM_PLAYER
                && movementTarget != OW_WILD_BEHAVIOR_TARGET_RANDOM_NEARBY
                && movementTarget != OW_WILD_BEHAVIOR_TARGET_NONE);
        sOverworldWildCollisionIgnoredObject = NULL;
        return directionCount;
    }

    if (primitives->attentiveLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_PHANTOM_TELEPORT) {
        return OverworldWildSpawns_TryStartPhantomTeleportMovementCommand(
            state,
            fieldSystem,
            slot,
            directions,
            directionCount,
            movementProfile,
            primitives);
    }

    if (primitives->attentiveLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_RAM) {
        return OverworldWildSpawns_TryStartRamMovementCommandFromDirections(
            state,
            fieldSystem,
            slot,
            directions,
            directionCount,
            movementProfile,
            primitives);
    }

    return OverworldWildSpawns_TryStartSpawnerMovementCommand(
        state,
        fieldSystem,
        slot,
        directions,
        directionCount,
        movementProfile,
        primitives);
}

static BOOL OverworldWildSpawns_TryStartPhantomTeleportToTile(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    u8 allowedTile,
    int targetX,
    int targetY,
    u8 targetFacing,
    u8 teleportTime)
{
    LocalMapObject *object;
    u8 movementDirections[OW_WILD_SPAWNER_MOVEMENT_MAX_DIRECTIONS];
    u8 movementDirection;
    u8 moveFrames;
    int objectX;
    int objectY;
    int movementDistance;

    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !OverworldWildSpawns_IsPhantomStalkerTeleportState(state, slot)) {
        return FALSE;
    }

    object = state->spawns[slot].object;
    if (object == NULL
        || MapObject_IsSingleMovementActive(object)
        || OverworldWildSpawns_IsMovementSlotInProgress(state, slot)) {
        sOverworldWildMovementDiagnosticSingleActive = object != NULL ? MapObject_IsSingleMovementActive(object) : FALSE;
        return FALSE;
    }

    sOverworldWildMovementDiagnosticSingleActive = FALSE;
    if (!OverworldWildSpawns_IsPhantomTeleportTile(
            fieldSystem,
            object,
            allowedTile,
            targetX,
            targetY)
        || OverworldWildSpawns_IsTileReservedByOtherWild(
            state,
            slot,
            targetX,
            targetY)) {
        sOverworldWildMovementDiagnosticDirectionBlocked = TRUE;
        sOverworldWildMovementDiagnosticWalkSuppressedByBlocked = TRUE;
        return FALSE;
    }

    objectX = MapObject_GetCurrentX(object);
    objectY = MapObject_GetCurrentY(object);
    movementDirection = targetFacing;
    if (OverworldWildSpawns_DiagnosticBuildDirections(targetX - objectX, targetY - objectY, movementDirections) > 0) {
        movementDirection = movementDirections[0];
    }
    movementDistance = OverworldWildSpawns_Max(
        OverworldWildSpawns_Abs(targetX - objectX),
        OverworldWildSpawns_Abs(targetY - objectY));
    moveFrames = teleportTime != 0
        ? teleportTime
        : OW_WILD_SPAWNER_PHANTOM_STALK_TELEPORT_MOVE_FRAMES;

    OverworldWildSpawns_ClearPhantomFlickerObject(state, slot, TRUE);
    OverworldWildSpawns_SetPreviousTile(state, slot, objectX, objectY);
    state->movementPendingDirections[slot] = movementDirection;
    state->movementPendingDistances[slot] = (u8)movementDistance;
    state->movementPhantomTeleportOriginX[slot] = (s16)objectX;
    state->movementPhantomTeleportOriginY[slot] = (s16)objectY;
    state->movementPhantomTeleportTargetX[slot] = (s16)targetX;
    state->movementPhantomTeleportTargetY[slot] = (s16)targetY;
    state->movementPhantomTeleportHasTarget[slot] = TRUE;
    state->movementPhantomHidden[slot] = TRUE;
    state->movementPhantomHiddenSteps[slot] = 0;
    state->movementPhantomFlickerTimers[slot] = moveFrames;
    state->movementPhantomVisiblePause[slot] = FALSE;
    OverworldWildSpawns_SetObjectFacing(object, targetFacing);
    OverworldWildSpawns_SetObjectTile(object, targetX, targetY);
    OverworldWildSpawns_ReconcileNativeShadow(fieldSystem, object);
    state->movementBattleSettleFrames = 0;
    OverworldWildSpawns_SetMovementSlotInProgress(state, slot);
    OverworldWildSpawns_ApplyPhantomHiddenVisual(state, slot, object);
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
    OverworldWildSpawns_EnsureFrameMovementTask(state, fieldSystem);
#endif
    sOverworldWildMovementDiagnosticDirectionBlocked = FALSE;
    sOverworldWildMovementDiagnosticLookCommand = moveFrames;
    sOverworldWildMovementDiagnosticWalkIssued = TRUE;
    return TRUE;
}

static BOOL OverworldWildSpawns_TryGetPhantomChillTeleportTarget(
    FieldSystem *fieldSystem,
    LocalMapObject *object,
    u8 allowedTile,
    int *targetX,
    int *targetY,
    u8 *targetFacing)
{
    static const u8 baseDirections[OW_WILD_SPAWNER_UNTANGLE_MAX_DIRECTIONS] = {
        OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_UP,
        OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT,
        OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN,
        OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_LEFT,
    };
    const int distanceSpan = OW_WILD_SPAWNER_PHANTOM_STALK_CHILL_TELEPORT_MAX_DISTANCE
        - OW_WILD_SPAWNER_PHANTOM_STALK_CHILL_TELEPORT_MIN_DISTANCE
        + 1;
    int objectX;
    int objectY;
    int directionStart;
    int distanceStart;
    int directionTry;
    int distanceTry;

    if (fieldSystem == NULL
        || object == NULL
        || targetX == NULL
        || targetY == NULL
        || targetFacing == NULL
        || distanceSpan <= 0) {
        return FALSE;
    }

    objectX = MapObject_GetCurrentX(object);
    objectY = MapObject_GetCurrentY(object);
    directionStart = gf_rand() % OW_WILD_SPAWNER_UNTANGLE_MAX_DIRECTIONS;
    distanceStart = gf_rand() % distanceSpan;

    for (distanceTry = 0; distanceTry < distanceSpan; distanceTry++) {
        int distance = OW_WILD_SPAWNER_PHANTOM_STALK_CHILL_TELEPORT_MIN_DISTANCE
            + ((distanceStart + distanceTry) % distanceSpan);

        for (directionTry = 0; directionTry < OW_WILD_SPAWNER_UNTANGLE_MAX_DIRECTIONS; directionTry++) {
            u8 direction = baseDirections[
                (directionStart + directionTry) % OW_WILD_SPAWNER_UNTANGLE_MAX_DIRECTIONS];
            int candidateX = objectX
                + OverworldWildSpawns_MovementDirectionDeltaX(direction) * distance;
            int candidateY = objectY
                + OverworldWildSpawns_MovementDirectionDeltaY(direction) * distance;

            if (OverworldWildSpawns_IsPhantomTeleportTile(
                    fieldSystem,
                    object,
                    allowedTile,
                    candidateX,
                    candidateY)) {
                *targetX = candidateX;
                *targetY = candidateY;
                *targetFacing = direction;
                return TRUE;
            }
        }
    }

    return FALSE;
}

static BOOL OverworldWildSpawns_TryStartPhantomChillTeleportMovementCommand(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    const OverworldWildBehaviorProfile *profile)
{
    LocalMapObject *object;
    int targetX;
    int targetY;
    u8 targetFacing;
    u8 allowedTile;

    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !OverworldWildSpawns_IsChillPhantomStalker(state, slot)) {
        return FALSE;
    }

    object = state->spawns[slot].object;
    allowedTile = OverworldWildSpawns_GetAllowedTileForSpotState(
        profile,
        state->movementSpotStates[slot]);
    if (!OverworldWildSpawns_TryGetPhantomChillTeleportTarget(
            fieldSystem,
            object,
            allowedTile,
            &targetX,
            &targetY,
            &targetFacing)) {
        state->movementCooldowns[slot] = OW_WILD_SPAWNER_DEFAULT_MOVEMENT_PAUSE_FRAMES;
        sOverworldWildMovementDiagnosticDirectionBlocked = TRUE;
        sOverworldWildMovementDiagnosticWalkSuppressedByBlocked = TRUE;
        return FALSE;
    }

    if (OverworldWildSpawns_TryStartPhantomTeleportToTile(
            state,
            fieldSystem,
            slot,
            allowedTile,
            targetX,
            targetY,
            targetFacing,
            OverworldWildSpawns_GetPhantomTeleportMoveFrames(
                profile,
                state->movementSpotStates[slot]))) {
        return TRUE;
    }

    state->movementCooldowns[slot] = OW_WILD_SPAWNER_DEFAULT_MOVEMENT_PAUSE_FRAMES;
    return FALSE;
}

static BOOL OverworldWildSpawns_TryStartPhantomTeleportMovementCommand(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    const u8 *directions,
    int directionCount,
    const OverworldWildBehaviorProfile *profile,
    const OverworldWildBehaviorPrimitives *primitives)
{
    LocalMapObject *object;
    u8 targetFacing;
    int targetX;
    int targetY;
    u8 allowedTile;

    if (state == NULL
        || fieldSystem == NULL
        || primitives == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !OverworldWildSpawns_IsActivePhantomStalker(state, slot)) {
        return FALSE;
    }

    object = state->spawns[slot].object;
    allowedTile = OverworldWildSpawns_GetAllowedTileForSpotState(
        profile,
        state->movementSpotStates[slot]);
    if (primitives->attentiveTarget == OW_WILD_BEHAVIOR_TARGET_PLAYER_FRONT
        && OverworldWildSpawns_IsPhantomOnPlayerFrontTile(fieldSystem, object)) {
        sOverworldWildMovementDiagnosticDirectionBlocked = FALSE;
        sOverworldWildMovementDiagnosticWalkSuppressedByBlocked = FALSE;
        OverworldWildSpawns_StartPhantomVisibleCooldown(state, slot, object, profile);
        return TRUE;
    }

    if (primitives->attentiveTarget == OW_WILD_BEHAVIOR_TARGET_PLAYER_FRONT) {
        if (!OverworldWildSpawns_TryGetPhantomFrontTeleportTarget(
                fieldSystem,
                object,
                allowedTile,
                &targetX,
                &targetY,
                &targetFacing)) {
            state->movementCooldowns[slot] = OW_WILD_SPAWNER_PHANTOM_STALK_FAIL_COOLDOWN_FRAMES;
            sOverworldWildMovementDiagnosticDirectionBlocked = TRUE;
            sOverworldWildMovementDiagnosticWalkSuppressedByBlocked = TRUE;
            return FALSE;
        }
    } else if (!OverworldWildSpawns_TryGetPhantomDirectionalTeleportTarget(
            fieldSystem,
            object,
            allowedTile,
            directions,
            directionCount,
            &targetX,
            &targetY,
            &targetFacing)) {
        state->movementCooldowns[slot] = OW_WILD_SPAWNER_PHANTOM_STALK_FAIL_COOLDOWN_FRAMES;
        sOverworldWildMovementDiagnosticDirectionBlocked = TRUE;
        sOverworldWildMovementDiagnosticWalkSuppressedByBlocked = TRUE;
        return FALSE;
    }

    if (OverworldWildSpawns_TryStartPhantomTeleportToTile(
            state,
            fieldSystem,
            slot,
            allowedTile,
            targetX,
            targetY,
            targetFacing,
            OverworldWildSpawns_GetPhantomTeleportMoveFrames(
                profile,
                state->movementSpotStates[slot]))) {
        return TRUE;
    }

    state->movementCooldowns[slot] = OW_WILD_SPAWNER_PHANTOM_STALK_FAIL_COOLDOWN_FRAMES;
    return FALSE;
}

static void OverworldWildSpawns_PlayRamCrashFeedback(OverworldWildSpawnState *state)
{
    (void)state;
    PlaySE(OW_WILD_SPAWNER_ONIX_RAM_CRASH_SE);
}

#ifndef DISABLE_FOLLOWER_POKEMON
static BOOL OverworldWildSpawns_IsActiveMapObjectAt(FieldSystem *fieldSystem, LocalMapObject *object, int x, int y)
{
    return OverworldWildSpawns_IsCurrentMapObject(fieldSystem, object)
        && (object->flags & MAPOBJECTFLAG_ACTIVE) != 0
        && (int)MapObject_GetCurrentX(object) == x
        && (int)MapObject_GetCurrentY(object) == y;
}
#endif

static BOOL OverworldWildSpawns_IsRamCrashBattleTarget(FieldSystem *fieldSystem, int x, int y)
{
#ifndef DISABLE_FOLLOWER_POKEMON
    MapObjectMan *mapObjectMan;
    LocalMapObject *objects;
    LocalMapObject *followerObject;
    u32 i;
#endif

    if (fieldSystem == NULL || fieldSystem->playerAvatar == NULL) {
        return FALSE;
    }

    if (x == GetPlayerXCoord(fieldSystem->playerAvatar)
        && y == GetPlayerYCoord(fieldSystem->playerAvatar)) {
        return TRUE;
    }

#ifndef DISABLE_FOLLOWER_POKEMON
    followerObject = fieldSystem->followMon.mapObject;
    if (OverworldWildSpawns_IsActiveMapObjectAt(fieldSystem, followerObject, x, y)) {
        return TRUE;
    }

    mapObjectMan = (MapObjectMan *)fieldSystem->mapObjectMan;
    if (mapObjectMan == NULL || mapObjectMan->objects == NULL) {
        return FALSE;
    }

    objects = mapObjectMan->objects;
    for (i = 0; i < mapObjectMan->object_count; i++) {
        LocalMapObject *object = &objects[i];

        if (object->id == OW_WILD_FOLLOWER_OBJECT_ID
            && OverworldWildSpawns_IsActiveMapObjectAt(fieldSystem, object, x, y)) {
            return TRUE;
        }
    }
#endif

    return FALSE;
}

static BOOL OverworldWildSpawns_TryPrimeBattleForSlotWithoutMovementReset(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot)
{
    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || OverworldWildSpawns_HasPendingBattle(state)
        || OverworldWildSpawns_IsPlayerBallProjectileActive()
        || !OverworldWildSpawns_IsCurrentSpawnObject(fieldSystem, &state->spawns[slot])) {
        return FALSE;
    }

    state->pendingPersonality = state->spawns[slot].personality;
    state->pendingSpecies = state->spawns[slot].species | (state->spawns[slot].form << OW_WILD_FORM_SHIFT);
    state->pendingLevel = state->spawns[slot].level;
    state->pendingShiny = state->spawns[slot].shiny;
    state->pendingSlot = slot;
    state->pendingMapGeneration = state->mapGeneration;
    state->pendingEncounterGeneration = state->spawns[slot].encounterGeneration;
    state->spawnCooldown = OW_WILD_REFILL_COOLDOWN_STEPS;
    state->battleGraceSteps = 0;
    state->justSpawned = FALSE;
    return TRUE;
}

static BOOL OverworldWildSpawns_TryStartBattleForRamCrashAtTile(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    LocalMapObject *object,
    int targetX,
    int targetY,
    const OverworldWildBehaviorProfile *profile)
{
    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || object == NULL
        || profile == NULL) {
        return FALSE;
    }

    if (state->movementSpotStates[slot] != OW_WILD_SPAWNER_SPOT_STATE_ACTIVE
        || profile->attentiveBattle != OW_WILD_BEHAVIOR_BATTLE_TRIGGER_RAM_CRASH) {
        return FALSE;
    }

    if (!OverworldWildSpawns_IsRamCrashBattleTarget(fieldSystem, targetX, targetY)) {
        return FALSE;
    }

#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
    if (sOverworldWildMovementFrameTaskExecuting) {
        if (!OverworldWildSpawns_TryPrimeBattleForSlotWithoutMovementReset(state, fieldSystem, slot)) {
            return FALSE;
        }
        if (!OverworldWildSpawns_RequestBattleScript(fieldSystem, state, slot)) {
            OverworldWildSpawns_ResetPendingBattle(state);
            return FALSE;
        }
        return TRUE;
    }
#endif

    return OverworldWildSpawns_TryStartBattleForSlotOrQueue(state, fieldSystem, slot);
}

static BOOL OverworldWildSpawns_TryStartBattleForRamCrash(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    LocalMapObject *object,
    u8 direction,
    const OverworldWildBehaviorProfile *profile)
{
    int targetX;
    int targetY;

    if (object == NULL) {
        return FALSE;
    }

    targetX = (int)MapObject_GetCurrentX(object) + OverworldWildSpawns_MovementDirectionDeltaX(direction);
    targetY = (int)MapObject_GetCurrentY(object) + OverworldWildSpawns_MovementDirectionDeltaY(direction);
    return OverworldWildSpawns_TryStartBattleForRamCrashAtTile(
        state,
        fieldSystem,
        slot,
        object,
        targetX,
        targetY,
        profile);
}

static void OverworldWildSpawns_ClearRamCrashMovementState(OverworldWildSpawnState *state, int slot)
{
    if (state == NULL || slot < 0 || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    state->movementRamDirections[slot] = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_NONE;
    state->movementRamStepCounters[slot] = 0;
    state->movementRamSpeeds[slot] = 0;
}

static BOOL OverworldWildSpawns_TryStartRamCrashBattleImpact(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    LocalMapObject *object,
    u8 direction,
    const OverworldWildBehaviorProfile *profile)
{
    if (!OverworldWildSpawns_TryStartBattleForRamCrash(
            state,
            fieldSystem,
            slot,
            object,
            direction,
            profile)) {
        return FALSE;
    }

    OverworldWildSpawns_PlayRamCrashFeedback(state);
    OverworldWildSpawns_ClearRamCrashMovementState(state, slot);
    return TRUE;
}

static void OverworldWildSpawns_EndRamCrash(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object,
    const OverworldWildBehaviorProfile *profile,
    const OverworldWildBehaviorPrimitives *primitives)
{
    u8 spotState = state->movementSpotStates[slot];

    OverworldWildSpawns_PlayRamCrashFeedback(state);
    OverworldWildSpawns_StartRamCrashShake(state, slot, object);
    OverworldWildSpawns_ClearRamCrashMovementState(state, slot);
    /* Only attentive RAM owns the profile's active crash-to-tired policy. */
    if (spotState == OW_WILD_SPAWNER_SPOT_STATE_CHILL
        || spotState == OW_WILD_SPAWNER_SPOT_STATE_TIRED) {
        state->movementCooldowns[slot] = OW_WILD_SPAWNER_RAM_CRASH_SHAKE_FRAMES;
        return;
    }
    OverworldWildSpawns_StartTiredEmoteWithProfile(
        state,
        slot,
        profile,
        primitives);
    if (state->movementCooldowns[slot] < state->movementRamCrashShakeTimers[slot]) {
        state->movementCooldowns[slot] = state->movementRamCrashShakeTimers[slot];
    }
}

static void OverworldWildSpawns_PlayRamStepFeedback(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object,
    const OverworldWildBehaviorPrimitives *primitives)
{
    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !state->spawns[slot].active
        || object == NULL
        || primitives == NULL
        || state->movementRamSpeeds[slot] < 2) {
        return;
    }

    if (!OverworldWildSpawns_CurrentSpotUsesRamLocomotion(
            primitives,
            state->movementSpotStates[slot])) {
        return;
    }

    if (OverworldWildSpawns_IsMovementFieldContextCurrent(state, state->movementFieldSystem)) {
        MapObject_SetBits(object, OW_WILD_SPAWNER_WAIT_JUMP_SITE_SET_BITS);
        ov01_022000DC(object);
        MapObject_ClearBits(object, OW_WILD_SPAWNER_WAIT_JUMP_SITE_CLEAR_BITS);
    }
    PlaySE(OW_WILD_SPAWNER_ONIX_RAM_STEP_SE);
}

static BOOL OverworldWildSpawns_TryStartRamMovementCommand(
    const OverworldWildDirectionStepContext *stepContext)
{
    OverworldWildSpawnState *state;
    LocalMapObject *object;
    OverworldWildDirectionStepResult stepResult;
    int slot;
    u8 direction;

    if (stepContext == NULL
        || stepContext->state == NULL
        || stepContext->slot >= OW_WILD_MAX_SPAWNS
        || stepContext->profile == NULL
        || stepContext->primitives == NULL) {
        return FALSE;
    }

    state = stepContext->state;
    slot = stepContext->slot;
    object = stepContext->object;
    if (!state->spawns[slot].active) {
        return FALSE;
    }
    direction = state->movementRamDirections[slot];
    if (object == NULL || MapObject_IsSingleMovementActive(object)) {
        return FALSE;
    }
    if (direction == OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_NONE) {
        OverworldWildSpawns_EndRamCrash(
            state,
            slot,
            object,
            stepContext->profile,
            stepContext->primitives);
        return TRUE;
    }

    stepResult = OverworldWildSpawns_TryStartSingleDirectionMovementStep(
        stepContext,
        direction);
    if (stepResult == OW_WILD_DIRECTION_STEP_STARTED) {
        OverworldWildSpawns_PlayRamStepFeedback(
            state,
            slot,
            object,
            stepContext->primitives);
        return TRUE;
    }
    if (stepResult == OW_WILD_DIRECTION_STEP_BLOCKED) {
        if (OverworldWildSpawns_TryStartRamCrashBattleImpact(
                state,
                stepContext->fieldSystem,
                slot,
                object,
                direction,
                stepContext->profile)) {
            return TRUE;
        }
        OverworldWildSpawns_EndRamCrash(
            state,
            slot,
            object,
            stepContext->profile,
            stepContext->primitives);
        return TRUE;
    }

    return FALSE;
}

static BOOL OverworldWildSpawns_TryStartRamMovementCommandFromDirections(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    const u8 *directions,
    int directionCount,
    const OverworldWildBehaviorProfile *profile,
    const OverworldWildBehaviorPrimitives *primitives)
{
    int i;
    OverworldWildDirectionStepContext stepContext;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || profile == NULL
        || primitives == NULL) {
        return FALSE;
    }
    if (state->movementRamCrashShakeTimers[slot] != 0) {
        if (state->movementCooldowns[slot] < state->movementRamCrashShakeTimers[slot]) {
            state->movementCooldowns[slot] = state->movementRamCrashShakeTimers[slot];
        }
        return TRUE;
    }
    if (directions == NULL || directionCount <= 0) {
        return FALSE;
    }

    stepContext.state = state;
    stepContext.fieldSystem = fieldSystem;
    stepContext.object = state->spawns[slot].object;
    stepContext.profile = profile;
    stepContext.primitives = primitives;
    stepContext.slot = (u8)slot;
    stepContext.allowedTile = OverworldWildSpawns_GetAllowedTileForSpotState(
        profile,
        state->movementSpotStates[slot]);
    stepContext.jumpLevel = profile->jumpLevel;
    stepContext.avoidPreviousTile = (u8)
        OverworldWildSpawns_ShouldAvoidPreviousTileForActiveProfile(
            state,
            slot,
            profile);

    if (state->movementRamDirections[slot] <= OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT) {
        return OverworldWildSpawns_TryStartRamMovementCommand(&stepContext);
    }

    for (i = 0; i < directionCount; i++) {
        if (directions[i] > OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT) {
            continue;
        }

        OverworldWildSpawns_InitRamMovementState(
            state,
            slot,
            directions[i],
            profile,
            state->movementSpotStates[slot]);

        if (OverworldWildSpawns_TryStartRamMovementCommand(&stepContext)) {
            return TRUE;
        }
    }

    if (state->spawns[slot].object != NULL
        && !MapObject_IsSingleMovementActive(state->spawns[slot].object)) {
        OverworldWildSpawns_EndRamCrash(
            state,
            slot,
            state->spawns[slot].object,
            profile,
            primitives);
        return TRUE;
    }

    return FALSE;
}

static int OverworldWildSpawns_MovementDirectionDeltaX(u8 direction)
{
    if (direction == OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_LEFT) {
        return -1;
    }
    if (direction == OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT) {
        return 1;
    }
    return 0;
}

static int OverworldWildSpawns_MovementDirectionDeltaY(u8 direction)
{
    if (direction == OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_UP) {
        return -1;
    }
    if (direction == OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN) {
        return 1;
    }
    return 0;
}

static void OverworldWildSpawns_StartTiredCooldown(OverworldWildSpawnState *state, int slot)
{
    state->movementSpotStates[slot] = OW_WILD_SPAWNER_SPOT_STATE_CHILL;
    OverworldWildSpawns_ClearRamCrashMovementState(state, slot);
    state->movementEmoteTimers[slot] = 0;
    state->movementEmoteSteps[slot] = OW_WILD_SPAWNER_SPOT_EMOTE_STEP_DONE;
    state->movementEmoteDirections[slot] = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_NONE;
    state->movementEmoteJumpsRemaining[slot] = 0;
    state->movementActiveSteps[slot] = 0;
    state->movementSpotCooldowns[slot] = 0;
    state->movementCooldowns[slot] = OW_WILD_SPAWNER_TIRED_WANDER_PAUSE_FRAMES;
#if OW_WILD_SPAWNER_TIRED_PLAY_COOLDOWN_SE
    PlaySE(OW_WILD_SPAWNER_TIRED_EMOTE_SE);
#endif
}

#if OW_WILD_SPAWNER_TIRED_USE_FOLLOWER_BUBBLE
static u8 OverworldWildSpawns_GetTiredBubbleId(void)
{
#if OW_WILD_SPAWNER_TIRED_BUBBLE_ID_PROBE
    u8 bubbleId = sOverworldWildTiredBubbleIdProbe;

    if (bubbleId < OW_WILD_SPAWNER_TIRED_BUBBLE_ID_PROBE_MIN
        || bubbleId > OW_WILD_SPAWNER_TIRED_BUBBLE_ID_PROBE_MAX) {
        bubbleId = OW_WILD_SPAWNER_TIRED_BUBBLE_ID_PROBE_MIN;
    }

    sOverworldWildTiredBubbleIdProbe = bubbleId + 1;
    if (sOverworldWildTiredBubbleIdProbe > OW_WILD_SPAWNER_TIRED_BUBBLE_ID_PROBE_MAX) {
        sOverworldWildTiredBubbleIdProbe = OW_WILD_SPAWNER_TIRED_BUBBLE_ID_PROBE_MIN;
    }

    return bubbleId;
#else
    return OW_WILD_SPAWNER_TIRED_BUBBLE_ID;
#endif
}

static u8 OverworldWildSpawns_GetTiredBubbleIdForState(u8 tiredState)
{
    switch (tiredState) {
    case OW_WILD_BEHAVIOR_KIND_TIRED_EMOTE:
        return OW_WILD_SPAWNER_BUBBLE_ID_WATER_DROPLET;
    case OW_WILD_BEHAVIOR_KIND_ASLEEP:
        return OW_WILD_SPAWNER_BUBBLE_ID_SLEEP;
    case OW_WILD_BEHAVIOR_KIND_IDLE:
    case OW_WILD_BEHAVIOR_KIND_NO_VISUAL:
        return OW_WILD_SPAWNER_BUBBLE_ID_NONE;
    case OW_WILD_BEHAVIOR_KIND_NONE:
    default:
        return OverworldWildSpawns_GetTiredBubbleId();
    }
}
#endif

static u8 OverworldWildSpawns_GetRestFrameCount(const OverworldWildBehaviorProfile *profile)
{
    if (profile == NULL
        || profile->tiredState == OW_WILD_BEHAVIOR_KIND_NONE
        || profile->stamina == 0
        || profile->restTime == 0) {
        return 0;
    }

    return profile->restTime < OW_WILD_SPAWNER_ASLEEP_REST_TIMER
        ? profile->restTime
        : OW_WILD_SPAWNER_ASLEEP_REST_TIMER - 1;
}

static void OverworldWildSpawns_StartTiredEmoteWithProfile(
    OverworldWildSpawnState *state,
    int slot,
    const OverworldWildBehaviorProfile *profileOverride,
    const OverworldWildBehaviorPrimitives *primitivesOverride)
{
#if OW_WILD_SPAWNER_TIRED_USE_FOLLOWER_BUBBLE
    FieldSystem *fieldSystem;
#endif
    LocalMapObject *object;
    OverworldWildBehaviorProfile profile;
    OverworldWildBehaviorPrimitives primitives;
    u8 restFrames;
#if OW_WILD_SPAWNER_TIRED_USE_FOLLOWER_BUBBLE
    u8 bubbleId;
#endif

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !state->spawns[slot].active) {
        return;
    }

    object = state->spawns[slot].object;
    if (profileOverride != NULL) {
        profile = *profileOverride;
    } else {
        profile = OverworldWildSpawns_GetMovementTickProfile(state, slot);
    }
    if (profileOverride != NULL && primitivesOverride != NULL) {
        primitives = *primitivesOverride;
    } else {
        primitives = OverworldWildSpawns_ResolveBehaviorPrimitives(&profile);
    }
    if (primitives.attentiveLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_PHANTOM_TELEPORT) {
        OverworldWildSpawns_RevealPhantomStalker(state, slot, object);
    }
    if (profile.tiredState == OW_WILD_BEHAVIOR_KIND_NONE) {
        profile.tiredState = OW_WILD_BEHAVIOR_KIND_TIRED_EMOTE;
        profile.stamina = 1;
        profile.restTime = OW_WILD_SPAWNER_FLEE_TIRED_REST_TIME;
    }
    if (OverworldWildSpawns_BehaviorUsesAsleep(profile.tiredState)
        && object != NULL
        && MapObject_IsSingleMovementActive(object)) {
        MapObject_ClearSingleMovementActive(object);
    }
    restFrames =
        OverworldWildSpawns_BehaviorUsesAsleep(profile.tiredState)
            && profile.restTime == 0
        ? OW_WILD_SPAWNER_ASLEEP_REST_TIMER
        : OverworldWildSpawns_GetRestFrameCount(&profile);
    state->movementSpotStates[slot] = OW_WILD_SPAWNER_SPOT_STATE_TIRED;
    /* Let the tired target selector initialize a fresh RAM direction and speed. */
    OverworldWildSpawns_ClearRamCrashMovementState(state, slot);
    state->movementEmoteTimers[slot] = restFrames;
    state->movementEmoteSteps[slot] = OW_WILD_SPAWNER_SPOT_EMOTE_STEP_DONE;
    state->movementEmoteDirections[slot] = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_NONE;
    state->movementEmoteJumpsRemaining[slot] = 0;
    state->movementEmoteEndStates[slot] = OW_WILD_SPAWNER_SPOT_STATE_CHILL;
    state->movementEmoteBubbleIds[slot] = OW_WILD_SPAWNER_BUBBLE_ID_NONE;
    state->movementEmoteShowBubbleEachJump[slot] = FALSE;
    state->movementActiveSteps[slot] = 0;
    state->movementCooldowns[slot] = OW_WILD_SPAWNER_TIRED_WANDER_PAUSE_FRAMES;

    if (OverworldWildSpawns_BehaviorKindUsesMovement(profile.tiredState)) {
        if (restFrames == 0 || object == NULL) {
            OverworldWildSpawns_StartTiredCooldown(state, slot);
            return;
        }
        if (MapObject_IsSingleMovementActive(object)) {
            MapObject_ClearSingleMovementActive(object);
        }
        state->movementCooldowns[slot] = OW_WILD_SPAWNER_MOVEMENT_DECISION_COOLDOWN;
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
        OverworldWildSpawns_EnsureFrameMovementTask(state, state->movementFieldSystem);
#endif
        return;
    }

    if (restFrames == 0
        || profile.tiredState == OW_WILD_BEHAVIOR_KIND_NONE
        || object == NULL
        || MapObject_IsSingleMovementActive(object)) {
        OverworldWildSpawns_StartTiredCooldown(state, slot);
        return;
    }

    if (profile.tiredState == OW_WILD_BEHAVIOR_KIND_NO_VISUAL
        || profile.tiredState == OW_WILD_BEHAVIOR_KIND_IDLE) {
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
        OverworldWildSpawns_EnsureFrameMovementTask(state, state->movementFieldSystem);
#endif
        return;
    }

#if OW_WILD_SPAWNER_TIRED_USE_FOLLOWER_BUBBLE
    fieldSystem = state->movementFieldSystem;
    if (OverworldWildSpawns_IsMovementFieldContextCurrent(state, fieldSystem)) {
        bubbleId = OverworldWildSpawns_GetTiredBubbleIdForState(profile.tiredState);
#if OW_WILD_SPAWNER_TIRED_USE_DIRECT_BUBBLE_CREATOR
        ov01_02203A48(object, bubbleId);
#if OW_WILD_SPAWNER_TIRED_STOP_BUBBLE_SE
        StopSE(SEQ_SE_DP_DECIDE);
#endif
#else
        ov01_02203AB4(fieldSystem, object, bubbleId);
#endif
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
        OverworldWildSpawns_EnsureFrameMovementTask(state, fieldSystem);
#endif
        return;
    }
#endif

    MapObject_StartMovementCommand(object, OW_WILD_SPAWNER_TIRED_EMOTE_COMMAND);
    MapObject_SetSingleMovementActive(object);
    sOverworldWildMovementDiagnosticLookCommand = OW_WILD_SPAWNER_TIRED_EMOTE_COMMAND;
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
    OverworldWildSpawns_EnsureFrameMovementTask(state, state->movementFieldSystem);
#endif
}

static void OverworldWildSpawns_StartTiredEmote(OverworldWildSpawnState *state, int slot)
{
    OverworldWildSpawns_StartTiredEmoteWithProfile(state, slot, NULL, NULL);
}

static u8 OverworldWildSpawns_GetCurrentMovementLocomotion(
    const OverworldWildBehaviorPrimitives *primitives,
    u8 spotState)
{
    if (primitives == NULL) {
        return OW_WILD_BEHAVIOR_LOCOMOTION_NONE;
    }
    if (spotState == OW_WILD_SPAWNER_SPOT_STATE_CHILL) {
        return primitives->chillLocomotion;
    }
    if (spotState == OW_WILD_SPAWNER_SPOT_STATE_ACTIVE) {
        return primitives->attentiveLocomotion;
    }
    if (spotState == OW_WILD_SPAWNER_SPOT_STATE_TIRED) {
        return primitives->tiredLocomotion;
    }
    return OW_WILD_BEHAVIOR_LOCOMOTION_NONE;
}

static u8 OverworldWildSpawns_GetCurrentMovementTarget(
    const OverworldWildBehaviorPrimitives *primitives,
    u8 spotState)
{
    if (primitives == NULL) {
        return OW_WILD_BEHAVIOR_TARGET_NONE;
    }
    if (spotState == OW_WILD_SPAWNER_SPOT_STATE_CHILL) {
        return primitives->chillTarget;
    }
    if (spotState == OW_WILD_SPAWNER_SPOT_STATE_ACTIVE) {
        return primitives->attentiveTarget;
    }
    if (spotState == OW_WILD_SPAWNER_SPOT_STATE_TIRED) {
        return primitives->tiredTarget;
    }
    return OW_WILD_BEHAVIOR_TARGET_NONE;
}

static BOOL OverworldWildSpawns_IsChainableMovementLocomotion(u8 locomotion)
{
    return locomotion == OW_WILD_BEHAVIOR_LOCOMOTION_WANDER
        || locomotion == OW_WILD_BEHAVIOR_LOCOMOTION_HOP
        || locomotion == OW_WILD_BEHAVIOR_LOCOMOTION_PHANTOM_TELEPORT;
}

static void OverworldWildSpawns_ApplyUniversalChainMovementPause(
    OverworldWildSpawnState *state,
    int slot,
    const OverworldWildBehaviorProfile *profile,
    const OverworldWildBehaviorPrimitives *primitives)
{
    if (profile->ramAccelerationSteps == 0
        || !OverworldWildSpawns_IsChainableMovementLocomotion(
            OverworldWildSpawns_GetCurrentMovementLocomotion(
                primitives,
                state->movementSpotStates[slot]))) {
        return;
    }

    if (++state->movementRamStepCounters[slot] >= profile->ramAccelerationSteps) {
        state->movementRamStepCounters[slot] = 0;
        state->movementCooldowns[slot] =
            OW_WILD_SPAWNER_REAL_FRAMES_TO_LOGICAL_TICKS(profile->ramMaxSpeed);
        if (OverworldWildSpawns_TryStartChainPauseAction(state, slot, profile)) {
            state->movementCooldowns[slot] = 0;
        }
    }
}

static void OverworldWildSpawns_HandleFinishedMovementCommand(OverworldWildSpawnState *state, int slot)
{
    OverworldWildOverlayRuntimeState *runtime;
    OverworldWildBehaviorProfile profile;
    OverworldWildBehaviorPrimitives primitives;
    LocalMapObject *object;
    u16 slotMask;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !state->spawns[slot].active) {
        return;
    }

    runtime = OW_WILD_RUNTIME(state);
    slotMask = OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot);
    object = state->spawns[slot].object;
    if (object != NULL) {
        OverworldWildSpawns_RecordFinishedMovementHistory(state, slot);
        if ((runtime->throwState.targetMask & slotMask) != 0
            && state->movementBehaviorClasses[slot] == OW_WILD_BEHAVIOR_CLASS_PICKED_UP) {
            runtime->throwState.targetMask &= ~slotMask;
            if (runtime->movementCustomJumpPrepActive[slot]) {
                (void)OverworldWildSpawns_RunImmediateCanopyMovementCommand(
                    object,
                    OW_WILD_SPAWNER_CANOPY_HOPPER_PARTNER_RESTORE_COMMAND);
            }
            if (runtime->movementCustomJumpActive[slot]
                || runtime->movementCustomJumpPrepActive[slot]) {
                OverworldWildSpawns_ClearCustomJump(state, slot);
            }
            runtime->movementCustomJumpPrepActive[slot] = FALSE;
            OverworldWildSpawns_RestorePickedUpBehaviorClass(state, slot);
            OverworldWildSpawns_SetObjectLandingTile(
                state->movementFieldSystem,
                object,
                MapObject_GetCurrentX(object),
                MapObject_GetCurrentY(object));
            MapObject_ClearBits(object, BIT_VANISH | MAPOBJECTFLAG_UNK18);
            if (OverworldWildSpawns_IsObjectOnPlayerTile(state->movementFieldSystem, object)) {
                if (OverworldWildSpawns_TryStartBattleForSlotOrQueue(
                        state,
                        state->movementFieldSystem,
                        slot)) {
                    return;
                }
            }
            return;
        }
    }

    /* A reservation owns the carrier's active loop until pickup or expiry. */
    if (runtime->throwState.targets[slot] != OW_WILD_SPAWNER_THROW_TARGET_NONE) {
        return;
    }

    OverworldWildSpawns_GetMovementTickProfileAndPrimitives(
        state,
        slot,
        &profile,
        &primitives);
    if (OverworldWildSpawns_CurrentSpotUsesRamLocomotion(
            &primitives,
            state->movementSpotStates[slot])) {
        state->movementRamStepCounters[slot]++;
        OverworldWildSpawns_AdvanceRamMovementSpeed(state, slot, &profile);
        return;
    }

    if (state->movementSpotStates[slot] == OW_WILD_SPAWNER_SPOT_STATE_ACTIVE) {
        if (primitives.attentiveLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_PHANTOM_TELEPORT
            && state->movementPhantomHidden[slot]
            && state->movementPhantomHiddenSteps[slot] < 255) {
            state->movementPhantomHiddenSteps[slot]++;
        }
        if (primitives.attentiveLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_HOP
            && primitives.attentiveTarget == OW_WILD_BEHAVIOR_TARGET_TREE_TOP) {
            return;
        }
        if (primitives.attentiveLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_HOP) {
            state->movementCooldowns[slot] =
                OverworldWildSpawns_GetBehaviorHopPauseFrames(
                    &profile,
                    state->movementSpotStates[slot]);
        }
        OverworldWildSpawns_ApplyUniversalChainMovementPause(
            state,
            slot,
            &profile,
            &primitives);

        if (profile.stamina == 0 || primitives.tiredReaction == OW_WILD_BEHAVIOR_REACTION_NONE) {
            return;
        }

        if (state->movementActiveSteps[slot] + 1 < profile.stamina) {
            state->movementActiveSteps[slot]++;
        } else {
            state->movementActiveSteps[slot] = profile.stamina;
            if (runtime->throwState.targets[slot] == OW_WILD_SPAWNER_THROW_TARGET_NONE) {
                OverworldWildSpawns_StartTiredEmote(state, slot);
                return;
            }
        }
    } else if (state->movementSpotStates[slot] == OW_WILD_SPAWNER_SPOT_STATE_CHILL) {
        state->movementCooldowns[slot] =
            primitives.chillLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_HOP
            ? OverworldWildSpawns_GetBehaviorHopPauseFrames(
                &profile,
                state->movementSpotStates[slot])
            : OW_WILD_SPAWNER_DEFAULT_MOVEMENT_PAUSE_FRAMES;
        OverworldWildSpawns_ApplyUniversalChainMovementPause(
            state,
            slot,
            &profile,
            &primitives);
    } else if (state->movementSpotStates[slot] == OW_WILD_SPAWNER_SPOT_STATE_TIRED) {
        if (primitives.tiredLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_HOP
            && primitives.tiredTarget == OW_WILD_BEHAVIOR_TARGET_TREE_TOP) {
            return;
        }
        state->movementCooldowns[slot] =
            primitives.tiredLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_HOP
            ? OverworldWildSpawns_GetBehaviorHopPauseFrames(
                &profile,
                state->movementSpotStates[slot])
            : OW_WILD_SPAWNER_MOVEMENT_DECISION_COOLDOWN;
        OverworldWildSpawns_ApplyUniversalChainMovementPause(
            state,
            slot,
            &profile,
            &primitives);
    }
}

static BOOL OverworldWildSpawns_TickPhantomTeleportMovementCommand(
    OverworldWildSpawnState *state,
    int slot,
    BOOL *finished)
{
    LocalMapObject *object;

    if (!OverworldWildSpawns_IsActivePhantomTeleportMove(state, slot)) {
        return FALSE;
    }

    object = state->spawns[slot].object;
    if (object == NULL) {
        OverworldWildSpawns_ClearMovementSlotInProgress(state, slot);
        OverworldWildSpawns_ClearPhantomFlickerObject(state, slot, TRUE);
        OverworldWildSpawns_ClearPhantomTeleportTarget(state, slot);
        state->movementPhantomHidden[slot] = FALSE;
        state->movementPhantomHiddenSteps[slot] = 0;
        state->movementPhantomFlickerTimers[slot] = 0;
        if (finished != NULL) {
            *finished = TRUE;
        }
        return TRUE;
    }

    OverworldWildSpawns_ApplyPhantomHiddenVisual(state, slot, object);
    if (state->movementPhantomFlickerTimers[slot] != 0) {
        state->movementPhantomFlickerTimers[slot]--;
    }
    if (state->movementPhantomFlickerTimers[slot] != 0) {
        return TRUE;
    }

    OverworldWildSpawns_RevealPhantomStalker(state, slot, object);
    OverworldWildSpawns_ClearMovementSlotInProgress(state, slot);
    OverworldWildSpawns_HandleFinishedMovementCommand(state, slot);
    if (state->spawns[slot].active
        && OverworldWildSpawns_IsPhantomStalkerTeleportState(state, slot)
        && state->movementCooldowns[slot] == 0) {
        OverworldWildBehaviorProfile profile = OverworldWildSpawns_GetMovementTickProfile(state, slot);

        OverworldWildSpawns_StartPhantomVisibleCooldown(
            state,
            slot,
            state->spawns[slot].object,
            &profile);
    }
    if (finished != NULL) {
        *finished = TRUE;
    }
    return TRUE;
}

static int OverworldWildSpawns_BuildChillWanderDirections(
    FieldSystem *fieldSystem,
    LocalMapObject *object,
    u8 allowedTile,
    u8 *directions)
{
    static const u8 baseDirections[OW_WILD_SPAWNER_UNTANGLE_MAX_DIRECTIONS] = {
        OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_UP,
        OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT,
        OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN,
        OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_LEFT,
    };
    int objectX;
    int objectY;
    int start;
    int i;

#if OW_WILD_SPAWNER_SINGLE_PASS_WANDER_VALIDATION
    (void)fieldSystem;
    (void)object;
    (void)allowedTile;
    if (directions == NULL) {
        return 0;
    }

    start = gf_rand() % OW_WILD_SPAWNER_UNTANGLE_MAX_DIRECTIONS;
    for (i = 0; i < OW_WILD_SPAWNER_UNTANGLE_MAX_DIRECTIONS; i++) {
        directions[i] = baseDirections[
            (start + i) % OW_WILD_SPAWNER_UNTANGLE_MAX_DIRECTIONS];
    }
    return OW_WILD_SPAWNER_UNTANGLE_MAX_DIRECTIONS;
#else
    int count = 0;

    if (fieldSystem == NULL || object == NULL || directions == NULL) {
        return 0;
    }

    objectX = MapObject_GetCurrentX(object);
    objectY = MapObject_GetCurrentY(object);
    start = gf_rand() % OW_WILD_SPAWNER_UNTANGLE_MAX_DIRECTIONS;

    for (i = 0; i < OW_WILD_SPAWNER_UNTANGLE_MAX_DIRECTIONS; i++) {
        u8 direction = baseDirections[(start + i) % OW_WILD_SPAWNER_UNTANGLE_MAX_DIRECTIONS];
        int targetX = objectX + OverworldWildSpawns_MovementDirectionDeltaX(direction);
        int targetY = objectY + OverworldWildSpawns_MovementDirectionDeltaY(direction);

        if (!OverworldWildSpawns_IsBehaviorAllowedMovementTile(
                fieldSystem,
                allowedTile,
                targetX,
                targetY)) {
            continue;
        }

        directions[count++] = direction;
    }

    return count;
#endif
}

static BOOL OverworldWildSpawns_TryStartChillWanderCommand(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    const OverworldWildBehaviorProfile *profile,
    const OverworldWildBehaviorPrimitives *primitives)
{
    u8 directions[OW_WILD_SPAWNER_UNTANGLE_MAX_DIRECTIONS];
    int directionCount;
    u8 locomotion;

    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || profile == NULL
        || primitives == NULL
        || (state->movementSpotStates[slot] != OW_WILD_SPAWNER_SPOT_STATE_CHILL
            && state->movementSpotStates[slot] != OW_WILD_SPAWNER_SPOT_STATE_TIRED)) {
        return FALSE;
    }

    locomotion = OverworldWildSpawns_GetCurrentMovementLocomotion(
        primitives,
        state->movementSpotStates[slot]);
    if (locomotion == OW_WILD_BEHAVIOR_LOCOMOTION_HOP) {
        if (OverworldWildSpawns_TryStartRandomBehaviorHopCommand(
                state,
                fieldSystem,
                slot,
                profile)) {
            return TRUE;
        }
    } else {
        directionCount = OverworldWildSpawns_BuildChillWanderDirections(
            fieldSystem,
            state->spawns[slot].object,
            OverworldWildSpawns_GetAllowedTileForSpotState(
                profile,
                state->movementSpotStates[slot]),
            directions);
        if (directionCount > 0
            && locomotion == OW_WILD_BEHAVIOR_LOCOMOTION_RAM
            && OverworldWildSpawns_TryStartRamMovementCommandFromDirections(
                state,
                fieldSystem,
                slot,
                directions,
                directionCount,
                profile,
                primitives)) {
            return TRUE;
        }
        if (directionCount > 0
            && OverworldWildSpawns_TryStartSpawnerMovementCommand(
                state,
                fieldSystem,
                slot,
                directions,
                directionCount,
                profile,
                primitives)) {
            return TRUE;
        }
    }

    state->movementCooldowns[slot] =
        state->movementSpotStates[slot] == OW_WILD_SPAWNER_SPOT_STATE_TIRED
        ? OW_WILD_SPAWNER_TIRED_WANDER_PAUSE_FRAMES
        : (OW_WILD_RUNTIME(state)->movementFrameDrivenChillMask
                & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot)) != 0
        ? OW_WILD_SPAWNER_DEFAULT_MOVEMENT_PAUSE_FRAMES
        : OW_WILD_SPAWNER_MOVEMENT_DECISION_COOLDOWN;
    return FALSE;
}

static BOOL OverworldWildSpawns_TryBattleSettleRetry(OverworldWildSpawnState *state, FieldSystem *fieldSystem)
{
    if (state == NULL
        || fieldSystem == NULL
        || fieldSystem->playerAvatar == NULL
        || state->movementInProgressMask != 0) {
        return FALSE;
    }
    if (!OverworldWildSpawns_HasQueuedBattle(state)) {
        return FALSE;
    }

    if (OverworldWildSpawns_TryStartQueuedBattle(state, fieldSystem)) {
        return TRUE;
    }
    return FALSE;
}

static BOOL OverworldWildSpawns_TryFrameBattleSettleRetry(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem)
{
    if (state == NULL
        || fieldSystem == NULL
        || fieldSystem->playerAvatar == NULL
        || state->movementInProgressMask != 0) {
        return FALSE;
    }

    if (OverworldWildSpawns_TryStartQueuedBattle(state, fieldSystem)) {
        return TRUE;
    }
    return FALSE;
}

static BOOL OverworldWildSpawns_IsPlayerInFacingLine(int dx, int dy, u8 alertness, u8 facing)
{
    switch (facing) {
    case OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_UP:
        return dx == 0 && dy < 0 && -dy <= alertness;
    case OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN:
        return dx == 0 && dy > 0 && dy <= alertness;
    case OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_LEFT:
        return dy == 0 && dx < 0 && -dx <= alertness;
    case OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT:
        return dy == 0 && dx > 0 && dx <= alertness;
    default:
        return FALSE;
    }
}

static BOOL OverworldWildSpawns_IsPlayerInCardinalLine(int dx, int dy, u8 alertness)
{
    if (dx == 0 && dy != 0) {
        return OverworldWildSpawns_DiagnosticAbs(dy) <= alertness;
    }
    if (dy == 0 && dx != 0) {
        return OverworldWildSpawns_DiagnosticAbs(dx) <= alertness;
    }

    return FALSE;
}

static BOOL OverworldWildSpawns_IsPlayerInAlertRadius(int dx, int dy, u8 radius)
{
    return (dx != 0 || dy != 0)
        && OverworldWildSpawns_DiagnosticAbs(dx) <= radius
        && OverworldWildSpawns_DiagnosticAbs(dy) <= radius;
}

static BOOL OverworldWildSpawns_IsPlayerInAlertLine(
    const OverworldWildBehaviorProfile *profile,
    const OverworldWildBehaviorPrimitives *primitives,
    int dx,
    int dy,
    u8 facing)
{
    if (profile == NULL || primitives == NULL) {
        return FALSE;
    }

    switch (primitives->alertLogic) {
    case OW_WILD_BEHAVIOR_ALERT_LOGIC_CARDINAL_LINE:
        return OverworldWildSpawns_IsPlayerInCardinalLine(dx, dy, profile->alertness);
    case OW_WILD_BEHAVIOR_ALERT_LOGIC_RADIUS:
        return OverworldWildSpawns_IsPlayerInAlertRadius(dx, dy, profile->alertness);
    case OW_WILD_BEHAVIOR_ALERT_LOGIC_FACING_LINE_CLOSE_RADIUS:
        return OverworldWildSpawns_IsPlayerInFacingLine(
                dx,
                dy,
                profile->alertness,
                facing)
            || OverworldWildSpawns_IsPlayerInAlertRadius(
                dx,
                dy,
                OW_WILD_SPAWNER_CLOSE_ALERT_RADIUS);
    case OW_WILD_BEHAVIOR_ALERT_LOGIC_FACING_LINE:
        return OverworldWildSpawns_IsPlayerInFacingLine(
            dx,
            dy,
            profile->alertness,
            facing);
    case OW_WILD_BEHAVIOR_ALERT_LOGIC_TERRAIN_ONLY:
    case OW_WILD_BEHAVIOR_ALERT_LOGIC_NONE:
    default:
        return FALSE;
    }
}

static void OverworldWildSpawns_StartSpotEmoteCommand(LocalMapObject *object, u32 movementCommand)
{
    MapObject_StartMovementCommand(object, movementCommand);
    MapObject_SetSingleMovementActive(object);
    sOverworldWildMovementDiagnosticLookCommand = movementCommand;
}

static void OverworldWildSpawns_StartHopStartSoundSuppression(OverworldWildSpawnState *state, int slot)
{
    if (state == NULL || slot < 0 || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    OW_WILD_RUNTIME(state)->movementHopStartSoundSuppressFrames[slot] =
        OW_WILD_SPAWNER_HOP_START_SE_SUPPRESS_FRAMES;
    StopSE(OW_WILD_SPAWNER_HOP_START_SE);
}

static void OverworldWildSpawns_LoadAndPlayCanopyHopSE(
    OverworldWildSpawnState *state,
    u16 seqNo)
{
    (void)state;
    GF_Snd_LoadSeqEx(seqNo, NNS_SND_ARC_LOAD_ALL);
    PlaySE(seqNo);
}

static void OverworldWildSpawns_PlayCanopyHopSE(OverworldWildSpawnState *state)
{
    switch (gf_rand() % OW_WILD_SPAWNER_CANOPY_HOP_SOUND_VARIANTS) {
    case 0:
        OverworldWildSpawns_LoadAndPlayCanopyHopSE(
            state,
            OW_WILD_SPAWNER_CANOPY_HOP_RUSTLE_SE);
        break;
    case 1:
    default:
        OverworldWildSpawns_LoadAndPlayCanopyHopSE(
            state,
            OW_WILD_SPAWNER_CANOPY_HOP_BALLOON_SE);
        break;
    }
}

static void OverworldWildSpawns_TickHopStartSoundSuppression(OverworldWildSpawnState *state, int slot)
{
    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }
    if (OW_WILD_RUNTIME(state)->movementHopStartSoundSuppressFrames[slot] == 0) {
        return;
    }

    StopSE(OW_WILD_SPAWNER_HOP_START_SE);
    if (OW_WILD_RUNTIME(state)->movementHopStartSoundSuppressFrames[slot] != 0) {
        OW_WILD_RUNTIME(state)->movementHopStartSoundSuppressFrames[slot]--;
    }
}

#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_UPDATE_COMMAND
static BOOL OverworldWildSpawns_UpdateSpawnerMovementCommandSuppressingHopStartSound(
    OverworldWildSpawnState *state,
    LocalMapObject *object,
    int slot)
{
    BOOL suppress;
    BOOL objectWasVanished;
    BOOL commandFinished;

    if (state == NULL || object == NULL) {
        return FALSE;
    }

    suppress = slot >= 0
        && slot < OW_WILD_MAX_SPAWNS
        && OW_WILD_RUNTIME(state)->movementHopStartSoundSuppressFrames[slot] != 0;
    if (!suppress) {
        return OverworldWildSpawns_UpdateSpawnerMovementCommand(object);
    }

    objectWasVanished = (object->flags & BIT_VANISH) != 0;
    MapObject_SetBits(object, BIT_VANISH);
    commandFinished = OverworldWildSpawns_UpdateSpawnerMovementCommand(object);
    if (!objectWasVanished) {
        MapObject_ClearBits(object, BIT_VANISH);
    }
    StopSE(OW_WILD_SPAWNER_HOP_START_SE);
    return commandFinished;
}
#endif

static u8 OverworldWildSpawns_GetLookAroundEmoteDirection(u8 baseDirection, u8 emoteStep)
{
    if (baseDirection > OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT) {
        baseDirection = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN;
    }

    switch (emoteStep) {
    case OW_WILD_SPAWNER_SPOT_EMOTE_STEP_LOOK_FIRST:
        return baseDirection <= OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN
            ? OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_LEFT
            : OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_UP;
    case OW_WILD_SPAWNER_SPOT_EMOTE_STEP_LOOK_SECOND:
        return baseDirection <= OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN
            ? OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT
            : OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN;
    case OW_WILD_SPAWNER_SPOT_EMOTE_STEP_LOOK_RETURN:
    default:
        return baseDirection;
    }
}

static BOOL OverworldWildSpawns_IsLookAroundEmoteStep(u8 emoteStep)
{
    return emoteStep >= OW_WILD_SPAWNER_SPOT_EMOTE_STEP_LOOK_FIRST
        && emoteStep <= OW_WILD_SPAWNER_SPOT_EMOTE_STEP_LOOK_RETURN;
}

static BOOL OverworldWildSpawns_ShouldStartLookAroundEmoteStep(OverworldWildSpawnState *state, int slot)
{
    u8 totalFrames;
    u8 elapsedFrames;
    u8 firstThreshold;
    u8 secondThreshold;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !OverworldWildSpawns_IsLookAroundEmoteStep(state->movementEmoteSteps[slot])) {
        return FALSE;
    }

    totalFrames = state->movementEmoteJumpsRemaining[slot];
    if (totalFrames == 0) {
        return TRUE;
    }

    elapsedFrames = state->movementEmoteTimers[slot] >= totalFrames
        ? 0
        : totalFrames - state->movementEmoteTimers[slot];
    firstThreshold = totalFrames / 3;
    if (firstThreshold == 0) {
        firstThreshold = 1;
    }
    secondThreshold = (totalFrames * 2) / 3;
    if (secondThreshold <= firstThreshold) {
        secondThreshold = firstThreshold + 1;
    }

    switch (state->movementEmoteSteps[slot]) {
    case OW_WILD_SPAWNER_SPOT_EMOTE_STEP_LOOK_FIRST:
        return TRUE;
    case OW_WILD_SPAWNER_SPOT_EMOTE_STEP_LOOK_SECOND:
        return elapsedFrames >= firstThreshold;
    case OW_WILD_SPAWNER_SPOT_EMOTE_STEP_LOOK_RETURN:
        return elapsedFrames >= secondThreshold;
    default:
        return FALSE;
    }
}

static BOOL OverworldWildSpawns_StartNextSpotEmoteStep(OverworldWildSpawnState *state, int slot, LocalMapObject *object)
{
    u32 movementCommand;
    BOOL playJumpSound = FALSE;

    if (object == NULL || MapObject_IsSingleMovementActive(object)) {
        return FALSE;
    }

    switch (state->movementEmoteSteps[slot]) {
    case OW_WILD_SPAWNER_SPOT_EMOTE_STEP_PARTNER_PREP:
        movementCommand = OW_WILD_SPAWNER_SPOT_EMOTE_PARTNER_PREP_COMMAND;
        break;
    case OW_WILD_SPAWNER_SPOT_EMOTE_STEP_JUMP:
        movementCommand = MapObject_MovementCommandFromDirection(
            state->movementEmoteDirections[slot],
            OW_WILD_SPAWNER_SPOT_EMOTE_JUMP_SITE_COMMAND);
        playJumpSound = TRUE;
        break;
    case OW_WILD_SPAWNER_SPOT_EMOTE_STEP_FREEZE:
        movementCommand = OW_WILD_SPAWNER_SPOT_EMOTE_FREEZE_COMMAND;
        break;
    case OW_WILD_SPAWNER_SPOT_EMOTE_STEP_PARTNER_RESTORE:
        movementCommand = OW_WILD_SPAWNER_SPOT_EMOTE_PARTNER_RESTORE_COMMAND;
        break;
    case OW_WILD_SPAWNER_SPOT_EMOTE_STEP_LOOK_FIRST:
    case OW_WILD_SPAWNER_SPOT_EMOTE_STEP_LOOK_SECOND:
    case OW_WILD_SPAWNER_SPOT_EMOTE_STEP_LOOK_RETURN:
        movementCommand = MapObject_MovementCommandFromDirection(
            OverworldWildSpawns_GetLookAroundEmoteDirection(
                state->movementEmoteDirections[slot],
                state->movementEmoteSteps[slot]),
            OW_WILD_SPAWNER_MOVEMENT_LOOK_UP_COMMAND);
        break;
    default:
        state->movementEmoteSteps[slot] = OW_WILD_SPAWNER_SPOT_EMOTE_STEP_DONE;
        return FALSE;
    }

    OverworldWildSpawns_StartSpotEmoteCommand(object, movementCommand);
    if (state->movementEmoteSteps[slot] == OW_WILD_SPAWNER_SPOT_EMOTE_STEP_PARTNER_PREP) {
        OW_WILD_RUNTIME(state)->movementEmotePartnerPrepObjects[slot] = object;
    }
    if (playJumpSound) {
        if (OW_WILD_RUNTIME(state)->movementEmotePlayHopSound[slot]) {
            PlaySE(OW_WILD_SPAWNER_SPOT_EMOTE_SE);
        } else {
            OverworldWildSpawns_StartHopStartSoundSuppression(state, slot);
        }
    }
    state->movementEmoteSteps[slot]++;
    return TRUE;
}

static BOOL OverworldWildSpawns_TryStartPendingLookAroundEmoteStep(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object)
{
    if (object == NULL
        || MapObject_IsSingleMovementActive(object)
        || !OverworldWildSpawns_ShouldStartLookAroundEmoteStep(state, slot)) {
        return FALSE;
    }

    if (state->movementEmoteSteps[slot] == OW_WILD_SPAWNER_SPOT_EMOTE_STEP_LOOK_RETURN) {
        state->movementEmoteJumpsRemaining[slot] = 0;
    }

    return OverworldWildSpawns_StartNextSpotEmoteStep(state, slot, object);
}

static u8 OverworldWildSpawns_GetAlertBubbleIdForProfile(const OverworldWildBehaviorProfile *profile)
{
    if (profile == NULL || profile->alertState == OW_WILD_BEHAVIOR_ALERT_STATE_NONE) {
        return OW_WILD_SPAWNER_BUBBLE_ID_NONE;
    }
    if (profile->alertEmote <= OW_WILD_SPAWNER_BUBBLE_ID_SLEEP) {
        return profile->alertEmote;
    }
    return OW_WILD_SPAWNER_BUBBLE_ID_NONE;
}

static void OverworldWildSpawns_ShowBubble(LocalMapObject *object, u8 bubbleId)
{
    if (bubbleId == OW_WILD_SPAWNER_BUBBLE_ID_NONE) {
        return;
    }

    ov01_02203A48(object, bubbleId);
}

static void OverworldWildSpawns_EnterPhantomTeleportActiveState(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object)
{
    OverworldWildBehaviorProfile profile;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    profile = OverworldWildSpawns_GetMovementTickProfile(state, slot);

    state->movementSpotStates[slot] = OW_WILD_SPAWNER_SPOT_STATE_ACTIVE;
    state->movementRamStepCounters[slot] = 0;
    state->movementEmoteTimers[slot] = 0;
    state->movementEmoteSteps[slot] = OW_WILD_SPAWNER_SPOT_EMOTE_STEP_DONE;
    state->movementEmoteEndStates[slot] = OW_WILD_SPAWNER_SPOT_STATE_CHILL;
    state->movementEmoteBubbleIds[slot] = OW_WILD_SPAWNER_BUBBLE_ID_NONE;
    state->movementEmoteShowBubbleEachJump[slot] = FALSE;
    state->movementEmotePlayCryOnHop[slot] = FALSE;
    OW_WILD_RUNTIME(state)->movementEmotePlayHopSound[slot] = TRUE;
    state->movementActiveSteps[slot] = 0;
    object = OverworldWildSpawns_RecreatePhantomObjectForActiveState(state, slot, object);
    OverworldWildSpawns_StartActivePhantomRealFlicker(state, slot, object);
    OverworldWildSpawns_StartPhantomVisibleCooldown(state, slot, object, &profile);
}

static void OverworldWildSpawns_EnterActiveStateFromGenericAlert(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object)
{
    OverworldWildBehaviorProfile profile;
    OverworldWildBehaviorPrimitives primitives;

    if (state == NULL || slot < 0 || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    profile = OverworldWildSpawns_GetBehaviorProfile(state, slot);
    primitives = OverworldWildSpawns_ResolveBehaviorPrimitives(&profile);
    if (primitives.attentiveLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_PHANTOM_TELEPORT) {
        OverworldWildSpawns_EnterPhantomTeleportActiveState(state, slot, object);
        return;
    }

    state->movementRamStepCounters[slot] = 0;
    state->movementCooldowns[slot] = OW_WILD_SPAWNER_MOVEMENT_DECISION_COOLDOWN;
    if (state->movementQueuedBattleSlot == slot) {
        return;
    }
    if (profile.alertSpecialAction == OW_WILD_BEHAVIOR_ALERT_SPECIAL_PICKUP_THROW) {
        OverworldWildSpawns_TryStartPickupThrowAction(state, slot);
    }
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
    if ((OW_WILD_RUNTIME(state)->movementFrameDrivenActiveMask & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot)) != 0) {
        OverworldWildSpawns_EnsureFrameMovementTask(state, state->movementFieldSystem);
    }
#endif
    (void)OverworldWildSpawns_TryStartFrameDrivenActiveMovementCommand(
        state,
        slot,
        object,
        &profile,
        &primitives);
}

static BOOL OverworldWildSpawns_TickSpotEmote(OverworldWildSpawnState *state, int slot, LocalMapObject *object)
{
    BOOL commandFinished = FALSE;

    if (state->movementSpotStates[slot] != OW_WILD_SPAWNER_SPOT_STATE_EMOTING) {
        return FALSE;
    }

#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_UPDATE_COMMAND
    if (object != NULL && MapObject_IsSingleMovementActive(object)) {
        commandFinished =
            OverworldWildSpawns_UpdateSpawnerMovementCommandSuppressingHopStartSound(state, object, slot);
    }
#endif
    if (commandFinished) {
        OW_WILD_RUNTIME(state)->movementHopStartSoundSuppressFrames[slot] = 0;
        if (state->movementEmoteSteps[slot] == OW_WILD_SPAWNER_SPOT_EMOTE_STEP_DONE
            && OW_WILD_RUNTIME(state)->movementEmotePartnerPrepObjects[slot] == object) {
            OW_WILD_RUNTIME(state)->movementEmotePartnerPrepObjects[slot] = NULL;
        }
    } else {
        OverworldWildSpawns_TickHopStartSoundSuppression(state, slot);
    }

    if (state->movementEmoteTimers[slot] != 0) {
        state->movementEmoteTimers[slot]--;
    }

    if (commandFinished
        && state->movementEmoteSteps[slot] == OW_WILD_SPAWNER_SPOT_EMOTE_STEP_FREEZE
        && state->spawns[slot].active) {
        if (state->movementEmotePlayCryOnHop[slot]) {
            PlayCry(
                state->spawns[slot].species,
                state->spawns[slot].form);
        }
        if (state->movementEmoteShowBubbleEachJump[slot]
            || state->movementEmoteJumpsRemaining[slot] <= 1) {
            OverworldWildSpawns_ShowBubble(object, state->movementEmoteBubbleIds[slot]);
        }
    }

    if (commandFinished
        && OverworldWildSpawns_IsLookAroundEmoteStep(state->movementEmoteSteps[slot])
        && object != NULL
        && MapObject_IsSingleMovementActive(object)) {
        MapObject_ClearSingleMovementActive(object);
    }

    if (OverworldWildSpawns_TryStartPendingLookAroundEmoteStep(state, slot, object)) {
        return TRUE;
    }
    if (OverworldWildSpawns_IsLookAroundEmoteStep(state->movementEmoteSteps[slot])) {
        return TRUE;
    }

    if (commandFinished
        && state->movementEmoteSteps[slot] != OW_WILD_SPAWNER_SPOT_EMOTE_STEP_DONE
        && OverworldWildSpawns_StartNextSpotEmoteStep(state, slot, object)) {
        return TRUE;
    }

    if (commandFinished
        && state->movementEmoteSteps[slot] == OW_WILD_SPAWNER_SPOT_EMOTE_STEP_DONE
        && state->movementEmoteJumpsRemaining[slot] > 1
        && state->movementEmoteTimers[slot] != 0) {
        state->movementEmoteJumpsRemaining[slot]--;
        state->movementEmoteSteps[slot] = OW_WILD_SPAWNER_SPOT_EMOTE_STEP_PARTNER_PREP;
        if (OverworldWildSpawns_StartNextSpotEmoteStep(state, slot, object)) {
            return TRUE;
        }
        state->movementEmoteSteps[slot] = OW_WILD_SPAWNER_SPOT_EMOTE_STEP_DONE;
    }

    if (commandFinished
        && state->movementEmoteSteps[slot] == OW_WILD_SPAWNER_SPOT_EMOTE_STEP_DONE
        && object != NULL
        && MapObject_IsSingleMovementActive(object)) {
        MapObject_ClearSingleMovementActive(object);
    }

    if (object == NULL
        || state->movementEmoteTimers[slot] == 0) {
        OverworldWildSpawns_CancelSpotEmotePresentation(state, slot, object);
        state->movementEmoteTimers[slot] = 0;
        state->movementEmoteSteps[slot] = OW_WILD_SPAWNER_SPOT_EMOTE_STEP_DONE;
        state->movementEmoteJumpsRemaining[slot] = 0;
        state->movementSpotStates[slot] = state->movementEmoteEndStates[slot];
        if (state->movementSpotStates[slot] == OW_WILD_SPAWNER_SPOT_STATE_ACTIVE) {
            OverworldWildSpawns_EnterActiveStateFromGenericAlert(state, slot, object);
        }
        state->movementEmoteEndStates[slot] = OW_WILD_SPAWNER_SPOT_STATE_CHILL;
        state->movementEmoteBubbleIds[slot] = OW_WILD_SPAWNER_BUBBLE_ID_NONE;
        state->movementEmoteShowBubbleEachJump[slot] = FALSE;
        state->movementEmotePlayCryOnHop[slot] = FALSE;
        OW_WILD_RUNTIME(state)->movementEmotePlayHopSound[slot] = TRUE;
    }

    return TRUE;
}

static BOOL OverworldWildSpawns_TickTiredEmote(OverworldWildSpawnState *state, int slot, LocalMapObject *object)
{
    OverworldWildBehaviorProfile profile;
    BOOL commandFinished = FALSE;

    if (state->movementSpotStates[slot] != OW_WILD_SPAWNER_SPOT_STATE_TIRED) {
        return FALSE;
    }

    profile = OverworldWildSpawns_GetMovementTickProfile(state, slot);
    if (OverworldWildSpawns_BehaviorKindUsesMovement(profile.tiredState)) {
        if (state->movementEmoteTimers[slot] == 0) {
            OverworldWildSpawns_StartTiredCooldown(state, slot);
            return TRUE;
        }
        state->movementEmoteTimers[slot]--;
        if (state->movementEmoteTimers[slot] == 0) {
            OverworldWildSpawns_StartTiredCooldown(state, slot);
            return TRUE;
        }
        return FALSE;
    }

#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_UPDATE_COMMAND
    if (object != NULL && MapObject_IsSingleMovementActive(object)) {
        commandFinished = OverworldWildSpawns_UpdateSpawnerMovementCommand(object);
    }
#endif
    if (commandFinished && object != NULL && MapObject_IsSingleMovementActive(object)) {
        MapObject_ClearSingleMovementActive(object);
    }

    if (object != NULL
        && state->movementEmoteTimers[slot] == OW_WILD_SPAWNER_ASLEEP_REST_TIMER) {
        return TRUE;
    }

    if (state->movementEmoteTimers[slot] != 0) {
        state->movementEmoteTimers[slot]--;
    }

    if (object == NULL || state->movementEmoteTimers[slot] == 0) {
        if (object != NULL && MapObject_IsSingleMovementActive(object)) {
            MapObject_ClearSingleMovementActive(object);
        }
        OverworldWildSpawns_StartTiredCooldown(state, slot);
    }

    return TRUE;
}

static BOOL OverworldWildSpawns_TryStartSpotEmote(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object,
    u8 direction,
    const OverworldWildBehaviorProfile *profile,
    const OverworldWildBehaviorPrimitives *primitives)
{
    u8 jumpCount;
    u8 endState;
    u8 bubbleId;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || profile == NULL
        || primitives == NULL
        || state->movementSpotStates[slot] != OW_WILD_SPAWNER_SPOT_STATE_CHILL
        || object == NULL
        || MapObject_IsSingleMovementActive(object)) {
        return FALSE;
    }

    endState = OW_WILD_SPAWNER_SPOT_STATE_ACTIVE;
    bubbleId = OverworldWildSpawns_GetAlertBubbleIdForProfile(profile);

    if (primitives->attentiveLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_RAM) {
        OverworldWildSpawns_InitRamMovementState(
            state,
            slot,
            direction,
            profile,
            OW_WILD_SPAWNER_SPOT_STATE_ACTIVE);
    }
    if (profile->alertSpecialAction == OW_WILD_BEHAVIOR_ALERT_SPECIAL_CALL_FOR_HELP) {
        OverworldWildSpawns_TrySpawnHelpChildren(
            state,
            state->movementFieldSystem,
            slot,
            profile);
    }

    if (primitives->alertReaction == OW_WILD_BEHAVIOR_REACTION_NONE) {
        state->movementSpotStates[slot] = endState;
        state->movementEmotePlayCryOnHop[slot] = FALSE;
        OW_WILD_RUNTIME(state)->movementEmotePlayHopSound[slot] = TRUE;
        state->movementActiveSteps[slot] = 0;
        state->movementBattleSettleFrames = 0;
        OverworldWildSpawns_EnterActiveStateFromGenericAlert(state, slot, object);
        return TRUE;
    }

    jumpCount = OverworldWildSpawns_GetSpotEmoteJumpCount(profile);
    if (jumpCount == 0) {
        state->movementSpotStates[slot] = OW_WILD_SPAWNER_SPOT_STATE_EMOTING;
        state->movementEmoteJumpsRemaining[slot] = 0;
        state->movementEmoteTimers[slot] =
            OverworldWildSpawns_GetAlertStateFrameCount(profile, jumpCount);
        state->movementEmoteSteps[slot] = OW_WILD_SPAWNER_SPOT_EMOTE_STEP_DONE;
        state->movementEmoteDirections[slot] = direction;
        state->movementEmoteEndStates[slot] = endState;
        state->movementEmoteBubbleIds[slot] = bubbleId;
        state->movementEmoteShowBubbleEachJump[slot] = FALSE;
        state->movementEmotePlayCryOnHop[slot] = FALSE;
        OW_WILD_RUNTIME(state)->movementEmotePlayHopSound[slot] = TRUE;
        state->movementActiveSteps[slot] = 0;
        state->movementBattleSettleFrames = 0;
        PlayCry(
            state->spawns[slot].species,
            state->spawns[slot].form);
        OverworldWildSpawns_ShowBubble(object, bubbleId);
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
        OverworldWildSpawns_EnsureFrameMovementTask(state, state->movementFieldSystem);
#endif
        return TRUE;
    }

    state->movementSpotStates[slot] = OW_WILD_SPAWNER_SPOT_STATE_EMOTING;
    state->movementEmoteJumpsRemaining[slot] = jumpCount;
    state->movementEmoteTimers[slot] =
        OverworldWildSpawns_GetAlertStateFrameCount(profile, jumpCount);
    state->movementEmoteSteps[slot] = OW_WILD_SPAWNER_SPOT_EMOTE_STEP_PARTNER_PREP;
    state->movementEmoteDirections[slot] = direction;
    state->movementEmoteEndStates[slot] = endState;
    state->movementEmoteBubbleIds[slot] = bubbleId;
    state->movementEmoteShowBubbleEachJump[slot] = FALSE;
    state->movementEmotePlayCryOnHop[slot] = TRUE;
    OW_WILD_RUNTIME(state)->movementEmotePlayHopSound[slot] = TRUE;
    state->movementActiveSteps[slot] = 0;
    state->movementBattleSettleFrames = 0;
    OverworldWildSpawns_StartNextSpotEmoteStep(state, slot, object);
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
    OverworldWildSpawns_EnsureFrameMovementTask(state, state->movementFieldSystem);
#endif
    return TRUE;
}

static BOOL OverworldWildSpawns_TryStartManualHopEmote(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object,
    u8 requiredState,
    u8 endState,
    u8 direction,
    u8 jumpCount,
    u8 emoteFrames,
    u8 bubbleId,
    BOOL showBubbleEachJump,
    BOOL playHopSound)
{
    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || state->movementSpotStates[slot] != requiredState
        || object == NULL
        || MapObject_IsSingleMovementActive(object)
        || jumpCount == 0) {
        return FALSE;
    }

    if (direction == OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_NONE
        || direction > OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT) {
        direction = object->curFacing <= OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT
            ? object->curFacing
            : OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN;
    }

    state->movementSpotStates[slot] = OW_WILD_SPAWNER_SPOT_STATE_EMOTING;
    state->movementEmoteJumpsRemaining[slot] = jumpCount;
    state->movementEmoteTimers[slot] = emoteFrames;
    state->movementEmoteSteps[slot] = OW_WILD_SPAWNER_SPOT_EMOTE_STEP_PARTNER_PREP;
    state->movementEmoteDirections[slot] = direction;
    state->movementEmoteEndStates[slot] = endState;
    state->movementEmoteBubbleIds[slot] = bubbleId;
    state->movementEmoteShowBubbleEachJump[slot] = showBubbleEachJump;
    state->movementEmotePlayCryOnHop[slot] = FALSE;
    OW_WILD_RUNTIME(state)->movementEmotePlayHopSound[slot] = playHopSound;
    state->movementBattleSettleFrames = 0;
    OverworldWildSpawns_StartNextSpotEmoteStep(state, slot, object);
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
    OverworldWildSpawns_EnsureFrameMovementTask(state, state->movementFieldSystem);
#endif
    return TRUE;
}

static BOOL OverworldWildSpawns_TryStartChainPauseAction(
    OverworldWildSpawnState *state,
    int slot,
    const OverworldWildBehaviorProfile *profile)
{
    LocalMapObject *object;
    u8 spotState;
    u8 direction;
    u8 lookFrames;

    if (profile->chainPauseAction == OW_WILD_BEHAVIOR_CHAIN_PAUSE_ACTION_NONE) {
        return FALSE;
    }

    object = state->spawns[slot].object;
    spotState = state->movementSpotStates[slot];
    if ((spotState != OW_WILD_SPAWNER_SPOT_STATE_CHILL
            && spotState != OW_WILD_SPAWNER_SPOT_STATE_ACTIVE
            && spotState != OW_WILD_SPAWNER_SPOT_STATE_TIRED)
        || object == NULL
        || MapObject_IsSingleMovementActive(object)) {
        return FALSE;
    }

    direction = object->curFacing <= OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT
        ? object->curFacing
        : OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN;

    if (profile->chainPauseAction == OW_WILD_BEHAVIOR_CHAIN_PAUSE_ACTION_HOP_IN_PLACE) {
        return OverworldWildSpawns_TryStartManualHopEmote(
            state,
            slot,
            object,
            spotState,
            spotState,
            direction,
            OW_WILD_SPAWNER_SPOT_EMOTE_JUMPS_DEFAULT,
            OW_WILD_SPAWNER_SPOT_EMOTE_FRAMES_PER_JUMP
                * OW_WILD_SPAWNER_SPOT_EMOTE_JUMPS_DEFAULT,
            OW_WILD_SPAWNER_BUBBLE_ID_NONE,
            FALSE,
            TRUE);
    }

    if (profile->chainPauseAction != OW_WILD_BEHAVIOR_CHAIN_PAUSE_ACTION_LOOK_AROUND) {
        return FALSE;
    }

    lookFrames = profile->ramMaxSpeed != 0
        ? OW_WILD_SPAWNER_REAL_FRAMES_TO_LOGICAL_TICKS(profile->ramMaxSpeed)
        : OW_WILD_SPAWNER_SPOT_EMOTE_SPEECH_FRAMES * 3;
    if (lookFrames < 3) {
        lookFrames = 3;
    }

    state->movementSpotStates[slot] = OW_WILD_SPAWNER_SPOT_STATE_EMOTING;
    state->movementEmoteJumpsRemaining[slot] = lookFrames;
    state->movementEmoteTimers[slot] = lookFrames;
    state->movementEmoteSteps[slot] = OW_WILD_SPAWNER_SPOT_EMOTE_STEP_LOOK_FIRST;
    state->movementEmoteDirections[slot] = direction;
    state->movementEmoteEndStates[slot] = spotState;
    state->movementEmoteBubbleIds[slot] = OW_WILD_SPAWNER_BUBBLE_ID_NONE;
    state->movementEmoteShowBubbleEachJump[slot] = FALSE;
    state->movementEmotePlayCryOnHop[slot] = FALSE;
    OW_WILD_RUNTIME(state)->movementEmotePlayHopSound[slot] = TRUE;
    state->movementBattleSettleFrames = 0;
    OverworldWildSpawns_StartNextSpotEmoteStep(state, slot, object);
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
    OverworldWildSpawns_EnsureFrameMovementTask(state, state->movementFieldSystem);
#endif
    return TRUE;
}

static void OverworldWildSpawns_TryAddMovementTarget(
    OverworldWildMovementTarget *targets,
    int *targetCount,
    int x,
    int y)
{
    int i;

    if (targets == NULL
        || targetCount == NULL
        || *targetCount >= OW_WILD_SPAWNER_MOVEMENT_TARGET_MAX) {
        return;
    }

    for (i = 0; i < *targetCount; i++) {
        if (targets[i].x == x && targets[i].y == y) {
            return;
        }
    }

    targets[*targetCount].x = x;
    targets[*targetCount].y = y;
    (*targetCount)++;
}

static void OverworldWildSpawns_TryAddMovementMapObjectTargets(
    OverworldWildMovementTarget *targets,
    int *targetCount,
    LocalMapObject *object,
    BOOL includeMovementTrail)
{
    int currentX;
    int currentY;
    int previousX;
    int previousY;

    if (object == NULL) {
        return;
    }

    currentX = (int)MapObject_GetCurrentX(object);
    currentY = (int)MapObject_GetCurrentY(object);
    if (currentX >= 0 && currentY >= 0) {
        OverworldWildSpawns_TryAddMovementTarget(targets, targetCount, currentX, currentY);
    }

    if (!includeMovementTrail || !MapObject_IsSingleMovementActive(object)) {
        return;
    }

    previousX = object->xPrev;
    previousY = object->yPrev;
    if (previousX < 0
        || previousY < 0
        || (previousX == currentX && previousY == currentY)) {
        return;
    }

    OverworldWildSpawns_TryAddMovementTarget(targets, targetCount, previousX, previousY);
}

static int OverworldWildSpawns_GetMovementTargetDistance(
    int x,
    int y,
    const OverworldWildMovementTarget *target)
{
    int dx;
    int dy;

    if (target == NULL) {
        return 0x7FFF;
    }

    dx = OverworldWildSpawns_DiagnosticAbs(x - target->x);
    dy = OverworldWildSpawns_DiagnosticAbs(y - target->y);
    return dx > dy ? dx : dy;
}

static BOOL OverworldWildSpawns_TrySelectClosestMovementTarget(
    const OverworldWildMovementTarget *targets,
    int targetCount,
    int fromX,
    int fromY,
    int *targetX,
    int *targetY)
{
    int bestIndex = 0;
    int bestDistance = 0x7FFF;
    int i;

    if (targets == NULL
        || targetCount <= 0
        || targetX == NULL
        || targetY == NULL) {
        return FALSE;
    }

    for (i = 0; i < targetCount; i++) {
        int distance = OverworldWildSpawns_GetMovementTargetDistance(fromX, fromY, &targets[i]);

        if (distance != 0 && distance < bestDistance) {
            bestDistance = distance;
            bestIndex = i;
        }
    }

    *targetX = targets[bestIndex].x;
    *targetY = targets[bestIndex].y;
    return TRUE;
}

static int OverworldWildSpawns_BuildPlayerMovementTargets(
    FieldSystem *fieldSystem,
    OverworldWildMovementTarget *targets)
{
    LocalMapObject *playerObject;
    int targetCount = 0;

    if (fieldSystem == NULL
        || fieldSystem->playerAvatar == NULL
        || targets == NULL) {
        return 0;
    }

    playerObject = fieldSystem->playerAvatar->mapObject;
    if (playerObject != NULL
        && OverworldWildSpawns_IsCurrentMapObject(fieldSystem, playerObject)
        && (playerObject->flags & MAPOBJECTFLAG_ACTIVE) != 0) {
        OverworldWildSpawns_TryAddMovementMapObjectTargets(
            targets,
            &targetCount,
            playerObject,
            TRUE);
    } else {
        OverworldWildSpawns_TryAddMovementTarget(
            targets,
            &targetCount,
            GetPlayerXCoord(fieldSystem->playerAvatar),
            GetPlayerYCoord(fieldSystem->playerAvatar));
    }

    return targetCount;
}

static BOOL OverworldWildSpawns_TryGetClosestPlayerMovementTarget(
    FieldSystem *fieldSystem,
    int fromX,
    int fromY,
    int *targetX,
    int *targetY)
{
    OverworldWildMovementTarget targets[OW_WILD_SPAWNER_MOVEMENT_TARGET_MAX];
    int targetCount;

    targetCount = OverworldWildSpawns_BuildPlayerMovementTargets(fieldSystem, targets);
    return OverworldWildSpawns_TrySelectClosestMovementTarget(
        targets,
        targetCount,
        fromX,
        fromY,
        targetX,
        targetY);
}

static void OverworldWildSpawns_TickMovementParams(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    BOOL frameTick,
    u16 frameWorkMask
#if OW_WILD_SPAWNER_IMMEDIATE_AI_AFTER_COMMAND_COMPLETION
    , u16 immediateDecisionMask
#endif
)
{
    OverworldWildOverlayRuntimeState *runtime = OW_WILD_RUNTIME(state);
    int i;

    state->movementFieldSystem = fieldSystem;
    if (frameTick) {
        OverworldWildSpawns_TickFrameMovementDecisionCounter();
    }

    if (!frameTick && runtime->throwState.targetMask == 0) {
        if (OverworldWildSpawns_TryBattleSettleRetry(state, fieldSystem)) {
            return;
        }
    } else if (OverworldWildSpawns_TryFrameBattleSettleRetry(state, fieldSystem)) {
        return;
    }
    if (OverworldWildSpawns_TryStartBattleFromAButton(state, fieldSystem)) {
        return;
    }
    if (!frameTick || runtime->movementIdleAiCursor == 0) {
        const OverworldWildHelperOverlayEntry *helperEntry =
            OverworldWildSpawns_GetHelperOverlayEntry();

        if (helperEntry != NULL
            && helperEntry->queryPickupThrowTarget != NULL
            && OVERWORLD_WILD_OVERLAP_RESOLVER_ENTRY->tryResolve != NULL
            && OVERWORLD_WILD_OVERLAP_RESOLVER_ENTRY->tryResolve(
                state,
                &runtime->throwState,
                OverworldWildSpawns_GetPickupThrowUnstableMask(state)
                    | runtime->movementNativeHeldMask,
                helperEntry->queryPickupThrowTarget,
                OverworldWildSpawns_TryStartSpawnerMovementCommand)) {
            return;
        }
    }

    for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
        if (state->spawns[i].active
            && state->spawns[i].object != NULL
            && ((frameTick
                    && (frameWorkMask & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(i)) != 0)
                || (!frameTick
                    && OverworldWildSpawns_IsCurrentSpawnObject(fieldSystem, &state->spawns[i])))) {
            LocalMapObject *object = state->spawns[i].object;
            int cooldown = state->movementCooldowns[i];
            int shouldIssueLookCommand = FALSE;
            u8 throwTarget = runtime->throwState.targets[i];
            OverworldWildBehaviorProfile profile;
            OverworldWildBehaviorPrimitives primitives;
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_COORD_READ \
    && OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_LOOK_COMMAND
            BOOL canSpotPlayer;
#endif

            if ((state->captureTargetMask
                & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(i)) != 0) {
                continue;
            }

            if ((throwTarget & OW_WILD_SPAWNER_THROW_TARGET_CARRIED_FLAG) != 0) {
                u8 targetSlot = OW_WILD_SPAWNER_THROW_TARGET_DECODE(throwTarget);
                OverworldWildSpawns_SyncCarriedThrowTarget(state, i, targetSlot);
            }

#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_UPDATE_COMMAND
            if (OverworldWildSpawns_IsMovementSlotInProgress(state, i)) {
                if (frameTick) {
                    continue;
                }
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
                OverworldWildSpawns_EnsureFrameMovementTask(state, fieldSystem);
#else
                BOOL stagedHopRenderFinished = FALSE;
                if (OverworldWildSpawns_TickStagedHopMovementListTask(state, fieldSystem, i, &stagedHopRenderFinished)) {
                    if (stagedHopRenderFinished) {
                        state->movementBattleSettleFrames = OW_WILD_SPAWNER_BATTLE_SETTLE_FRAMES;
                    }
                    continue;
                }
                if (OverworldWildSpawns_UpdateSpawnerMovementCommandForSlot(state, i, FALSE)) {
                    OverworldWildSpawns_ClearMovementSlotInProgress(state, i);
                    if (OverworldWildSpawns_HandleFinishedSpawnRunMovementCommand(state, fieldSystem, i)) {
                        continue;
                    }
                    if (!OverworldWildSpawns_HandleFinishedStagedHopMovementCommand(
                            state,
                            fieldSystem,
                            i,
                            TRUE)) {
                        OverworldWildSpawns_HandleFinishedMovementCommand(state, i);
                    }
                }
#endif
                continue;
            }
#endif

            if (!frameTick
                && OverworldWildSpawns_TickCustomJumpRenderSettle(state, fieldSystem, i)) {
                continue;
            }

            if (state->movementSpawnRunActive[i]) {
                if (!frameTick) {
                    OverworldWildSpawns_TryStartSpawnRunStep(state, fieldSystem, i);
                }
                continue;
            }

            if (state->movementStagedHopPending[i]) {
                if (frameTick) {
                    continue;
                }
                if (cooldown > 0) {
                    state->movementCooldowns[i] = cooldown - 1;
                    continue;
                }
                OverworldWildSpawns_ContinuePendingStagedHop(state, fieldSystem, i);
                continue;
            }

            if ((runtime->throwState.targetMask & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(i)) != 0
                && state->movementBehaviorClasses[i] == OW_WILD_BEHAVIOR_CLASS_PICKED_UP) {
                continue;
            }

            if (OverworldWildSpawns_TickSpotEmote(state, i, object)) {
                continue;
            }

            if (OverworldWildSpawns_TickTiredEmote(state, i, object)) {
                continue;
            }

            if (state->movementRamCrashShakeTimers[i] != 0) {
                if (cooldown > 0) {
                    state->movementCooldowns[i] = cooldown - 1;
                }
                continue;
            }

            if (OverworldWildSpawns_IsReservedPickupTargetNearCarrier(state, i)) {
                continue;
            }

            if (state->movementQueuedBattleSlot == i) {
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
                OverworldWildSpawns_EnsureFrameMovementTask(state, fieldSystem);
#endif
                continue;
            }

            if (throwTarget != OW_WILD_SPAWNER_THROW_TARGET_NONE) {
                if (throwTarget & OW_WILD_SPAWNER_THROW_TARGET_WINDUP_FLAG) {
                    if (cooldown > 0) {
                        state->movementCooldowns[i] = cooldown - 1;
                        continue;
                    }
                    OverworldWildSpawns_GetMovementTickProfileAndPrimitives(
                        state,
                        i,
                        &profile,
                        &primitives);
                    OverworldWildSpawns_TryStartFrameDrivenActiveMovementCommand(
                        state,
                        i,
                        object,
                        &profile,
                        &primitives);
                    continue;
                }
            }

            if (cooldown > 0) {
                state->movementCooldowns[i] = cooldown - 1;
                if (cooldown == 1) {
                    state->movementPhantomVisiblePause[i] = FALSE;
                }
                if (frameTick) {
                    if (cooldown == 1) {
                        shouldIssueLookCommand = TRUE;
                    }
                }
            } else {
                state->movementPhantomVisiblePause[i] = FALSE;
                state->movementCooldowns[i] = OW_WILD_SPAWNER_MOVEMENT_DECISION_COOLDOWN;
                shouldIssueLookCommand = TRUE;
            }
            if (frameTick
                && !shouldIssueLookCommand
                && (sOverworldWildMovementFrameDecisionCounter & 1) != 0) {
                continue;
            }

#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_COORD_READ \
    && OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_LOOK_COMMAND
            canSpotPlayer = state->movementSpotStates[i] == OW_WILD_SPAWNER_SPOT_STATE_CHILL
                && state->movementSpotCooldowns[i] == 0;
            if (state->movementSpotStates[i] == OW_WILD_SPAWNER_SPOT_STATE_CHILL
                && state->movementSpotCooldowns[i] != 0) {
                state->movementSpotCooldowns[i]--;
            }
#endif
            OverworldWildSpawns_GetMovementTickProfileAndPrimitives(
                state,
                i,
                &profile,
                &primitives);
            if (frameTick
                && throwTarget == OW_WILD_SPAWNER_THROW_TARGET_NONE
                && cooldown == 0
#if OW_WILD_SPAWNER_IMMEDIATE_AI_AFTER_COMMAND_COMPLETION
                && (immediateDecisionMask
                    & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(i)) == 0
#endif
            ) {
                u8 decisionSpeed = state->movementRamSpeeds[i];

                if (decisionSpeed == 0
                    || !OverworldWildSpawns_CurrentSpotUsesRamLocomotion(
                        &primitives,
                        state->movementSpotStates[i])) {
                    decisionSpeed = OverworldWildSpawns_GetMovementSpeedForState(
                        &profile,
                        state->movementSpotStates[i]);
                }
                if (!OverworldWildSpawns_ShouldRunFrameMovementDecisionForSpeed(decisionSpeed)) {
                    continue;
                }
            }
            if (state->movementSpotStates[i] == OW_WILD_SPAWNER_SPOT_STATE_ACTIVE) {
                if (shouldIssueLookCommand) {
                    BOOL movementStarted;

                    if (profile.alertSpecialAction == OW_WILD_BEHAVIOR_ALERT_SPECIAL_PICKUP_THROW) {
                        OverworldWildSpawns_TryStartPickupThrowAction(state, i);
                    }
                    movementStarted = OverworldWildSpawns_TryStartFrameDrivenActiveMovementCommand(
                        state,
                        i,
                        object,
                        &profile,
                        &primitives);
                    if (movementStarted
                        && (OverworldWildSpawns_HasPendingBattle(state)
                            || state->movementQueuedBattleSlot >= 0)) {
                        goto movement_tick_done;
                    }
                }
                continue;
            }

#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_COORD_READ
            if (fieldSystem != NULL && fieldSystem->playerAvatar != NULL) {
                int objectX = MapObject_GetCurrentX(object);
                int objectY = MapObject_GetCurrentY(object);
                int playerX = GetPlayerXCoord(fieldSystem->playerAvatar);
                int playerY = GetPlayerYCoord(fieldSystem->playerAvatar);
                int spotDx = playerX - objectX;
                int spotDy = playerY - objectY;
                int dx = spotDx;
                int dy = spotDy;
                u8 spotDirections[OW_WILD_SPAWNER_MOVEMENT_MAX_DIRECTIONS];
                u8 directions[OW_WILD_SPAWNER_MOVEMENT_MAX_DIRECTIONS];
                u8 currentLocomotion = OverworldWildSpawns_GetCurrentMovementLocomotion(
                    &primitives,
                    state->movementSpotStates[i]);
                u8 movementTarget = OverworldWildSpawns_GetCurrentMovementTarget(
                    &primitives,
                    state->movementSpotStates[i]);
                int spotDirectionCount = 0;
                int directionCount;

                OverworldWildSpawns_TryGetClosestPlayerMovementTarget(
                    fieldSystem,
                    objectX,
                    objectY,
                    &playerX,
                    &playerY);
                if (movementTarget == OW_WILD_BEHAVIOR_TARGET_PLAYER_FRONT) {
                    OverworldWildSpawns_TryGetCanopyHopperPlayerFrontTarget(
                        fieldSystem,
                        &playerX,
                        &playerY);
                }
                spotDx = playerX - objectX;
                spotDy = playerY - objectY;
                dx = spotDx;
                dy = spotDy;

                spotDirectionCount = OverworldWildSpawns_DiagnosticBuildDirections(
                    spotDx,
                    spotDy,
                    spotDirections);
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_LOOK_COMMAND
                if (canSpotPlayer
                    && spotDirectionCount > 0
                    && OverworldWildSpawns_RollBehaviorChance(profile.alertChance)
                    && OverworldWildSpawns_IsPlayerInAlertLine(
                        &profile,
                        &primitives,
                        spotDx,
                        spotDy,
                        object->curFacing)) {
                    OverworldWildSpawns_TryStartSpotEmote(
                        state,
                        i,
                        object,
                        spotDirections[0],
                        &profile,
                        &primitives);
                    continue;
                }
#endif
                if (!shouldIssueLookCommand) {
                    continue;
                }

                if (movementTarget == OW_WILD_BEHAVIOR_TARGET_AWAY_FROM_PLAYER) {
                    dx = -dx;
                    dy = -dy;
                } else if (movementTarget == OW_WILD_BEHAVIOR_TARGET_NONE) {
                    dx = 0;
                    dy = 0;
                }

                if (movementTarget == OW_WILD_BEHAVIOR_TARGET_RANDOM_NEARBY) {
                    directionCount = OverworldWildSpawns_BuildChillWanderDirections(
                        fieldSystem,
                        object,
                        OverworldWildSpawns_GetAllowedTileForSpotState(
                            &profile,
                            state->movementSpotStates[i]),
                        directions);
                } else {
                    directionCount = OverworldWildSpawns_DiagnosticBuildDirections(dx, dy, directions);
                }
                if (movementTarget == OW_WILD_BEHAVIOR_TARGET_AWAY_FROM_PLAYER) {
                    OverworldWildSpawns_AppendFleeFallbackDirections(
                        directions,
                        &directionCount,
                        dx,
                        dy);
                } else if (movementTarget != OW_WILD_BEHAVIOR_TARGET_RANDOM_NEARBY) {
                    OverworldWildSpawns_AppendFrameDrivenChaseFallbackDirections(
                        directions,
                        &directionCount);
                }
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_LOOK_COMMAND
                {
                    if (shouldIssueLookCommand
                        && currentLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_HOP) {
                        if (movementTarget == OW_WILD_BEHAVIOR_TARGET_RANDOM_NEARBY) {
                            OverworldWildSpawns_TryStartChillWanderCommand(
                                state,
                                fieldSystem,
                                i,
                                &profile,
                                &primitives);
                        } else {
                            OverworldWildSpawns_TryStartDirectedBehaviorHopCommand(
                                state,
                                fieldSystem,
                                i,
                                &profile,
                                dx,
                                dy,
                                directions,
                                directionCount,
                                movementTarget != OW_WILD_BEHAVIOR_TARGET_AWAY_FROM_PLAYER
                                    && movementTarget != OW_WILD_BEHAVIOR_TARGET_RANDOM_NEARBY
                                    && movementTarget != OW_WILD_BEHAVIOR_TARGET_NONE);
                        }
                    }
                    else if (shouldIssueLookCommand
                        && currentLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_PHANTOM_TELEPORT) {
                        OverworldWildSpawns_TryStartPhantomChillTeleportMovementCommand(
                            state,
                            fieldSystem,
                            i,
                            &profile);
                    }
                    else if (shouldIssueLookCommand
                        && (currentLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_WANDER
                            || currentLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_RAM)) {
                        if (movementTarget == OW_WILD_BEHAVIOR_TARGET_RANDOM_NEARBY) {
                            OverworldWildSpawns_TryStartChillWanderCommand(
                                state,
                                fieldSystem,
                                i,
                                &profile,
                                &primitives);
                        } else if (currentLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_RAM) {
                            OverworldWildSpawns_TryStartRamMovementCommandFromDirections(
                                state,
                                fieldSystem,
                                i,
                                directions,
                                directionCount,
                                &profile,
                                &primitives);
                        } else {
                            OverworldWildSpawns_TryStartSpawnerMovementCommand(
                                state,
                                fieldSystem,
                                i,
                                directions,
                                directionCount,
                                &profile,
                                &primitives);
                        }
                    }
                    continue;
                }
#endif
            }
#else
            (void)fieldSystem;
#endif
        }
    }
movement_tick_done:
    ;
}
#endif

#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_UPDATE_COMMAND
static BOOL OverworldWildSpawns_UpdateSpawnerMovementCommand(LocalMapObject *object)
{
    int i;

    for (i = 0; i < OW_WILD_SPAWNER_MOVEMENT_BURST_UPDATE_STEPS; i++) {
        BOOL singleMovementActive = MapObject_IsSingleMovementActive(object);

        if (!singleMovementActive) {
            return TRUE;
        }

        if (MapObject_UpdateMovementCommand(object)) {
            MapObject_ClearSingleMovementActive(object);
            return TRUE;
        }

#if !OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_BURST_UPDATE
        break;
#endif
    }

    return FALSE;
}

static s32 OverworldWildSpawns_LerpCustomJumpFx32(
    s32 start,
    s32 target,
    u32 elapsed,
    u32 total)
{
    if (total == 0 || elapsed >= total) {
        return target;
    }

    return start + (s32)(((target - start) * (s32)elapsed) / (s32)total);
}

static int OverworldWildSpawns_GetCustomJumpTileFx32(s32 value)
{
    if (value >= 0) {
        return value / OW_WILD_SPAWNER_TILE_FX32;
    }
    return (value - (OW_WILD_SPAWNER_TILE_FX32 - 1)) / OW_WILD_SPAWNER_TILE_FX32;
}

static BOOL OverworldWildSpawns_SyncCustomJumpLogicalTile(LocalMapObject *object)
{
#if !OW_WILD_SPAWNER_CUSTOM_JUMP_SYNC_LOGICAL_SECONDARY
    (void)object;
    return FALSE;
#else
    int x;
    int y;

    if (object == NULL) {
        return FALSE;
    }

    x = OverworldWildSpawns_GetCustomJumpTileFx32((s32)object->posVec[0]);
    y = OverworldWildSpawns_GetCustomJumpTileFx32((s32)object->posVec[2]);
    if (object->xCurr == x && object->yCurr == y) {
        return FALSE;
    }

    object->xPrev = object->xCurr;
    object->yPrev = object->yCurr;
    object->xCurr = x;
    object->yCurr = y;
    return TRUE;
#endif
}

static BOOL OverworldWildSpawns_StartCustomJumpRenderSettle(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object)
{
#if !OW_WILD_SPAWNER_CUSTOM_JUMP_POST_LANDING_RENDER_SETTLE_ENABLED
    (void)state;
    (void)slot;
    (void)object;
    return FALSE;
#else
    s32 targetX;
    s32 targetY;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || object == NULL
        || !OW_WILD_RUNTIME(state)->movementCustomJumpActive[slot]
        || OW_WILD_SPAWNER_CUSTOM_JUMP_POST_LANDING_RENDER_SETTLE_FRAMES == 0) {
        return FALSE;
    }

    targetX = OW_WILD_SPAWNER_TILE_CENTER_COORD_FX32(
        OW_WILD_RUNTIME(state)->movementCustomJumpTargetX[slot]);
    targetY = OW_WILD_SPAWNER_TILE_CENTER_COORD_FX32(
        OW_WILD_RUNTIME(state)->movementCustomJumpTargetY[slot]);
    if ((s32)object->posVec[0] == targetX && (s32)object->posVec[2] == targetY) {
        return FALSE;
    }

    sOverworldWildCustomJumpRenderSettleActive[slot] = TRUE;
    sOverworldWildCustomJumpRenderSettleStartX[slot] = (s32)object->posVec[0];
    sOverworldWildCustomJumpRenderSettleStartY[slot] = (s32)object->posVec[2];
    sOverworldWildCustomJumpRenderSettleElapsedFrames[slot] = 0;
    return TRUE;
#endif
}

#if OW_WILD_SPAWNER_CUSTOM_JUMP_POST_LANDING_RENDER_SETTLE_ENABLED
static BOOL OverworldWildSpawns_CompleteCustomJumpRenderSettle(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    LocalMapObject *object)
{
    OverworldWildBehaviorProfile profile;

    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || object == NULL) {
        return FALSE;
    }

    OverworldWildSpawns_SetObjectRenderTileOnly(
        object,
        OW_WILD_RUNTIME(state)->movementCustomJumpTargetX[slot],
        OW_WILD_RUNTIME(state)->movementCustomJumpTargetY[slot]);
    MapObject_ClearBits(object, BIT_VANISH | OW_WILD_SPAWNER_CUSTOM_JUMP_OWNED_BITS);
    sOverworldWildCustomJumpRenderSettleActive[slot] = FALSE;
    OverworldWildSpawns_RecordCustomJumpRamObject(state, slot, object, 20);
    OverworldWildSpawns_ClearCustomJump(state, slot);

    if (state->movementStagedHopPending[slot]) {
        profile = OverworldWildSpawns_GetBehaviorProfile(state, slot);
        OverworldWildSpawns_FinishPendingStagedHop(state, slot, object, &profile);
        OverworldWildSpawns_UpdateMankeyTreeTopPriorityBits(state, fieldSystem, slot, object);
        OverworldWildSpawns_RecordCustomJumpRamObject(state, slot, object, 15);
    }
    return TRUE;
}

static BOOL OverworldWildSpawns_TickCustomJumpRenderSettle(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot)
{
    LocalMapObject *object;
    u32 elapsed;
    u32 total;
    s32 targetX;
    s32 targetY;

    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !sOverworldWildCustomJumpRenderSettleActive[slot]) {
        return FALSE;
    }
    if (!OverworldWildSpawns_IsCurrentSpawnObject(fieldSystem, &state->spawns[slot])) {
        OverworldWildSpawns_ClearCustomJump(state, slot);
        return FALSE;
    }

    object = state->spawns[slot].object;
    if (object == NULL) {
        OverworldWildSpawns_ClearCustomJump(state, slot);
        return FALSE;
    }

    total = OW_WILD_SPAWNER_CUSTOM_JUMP_POST_LANDING_RENDER_SETTLE_FRAMES;
    elapsed = sOverworldWildCustomJumpRenderSettleElapsedFrames[slot] + 1;
    if (elapsed >= total) {
        return OverworldWildSpawns_CompleteCustomJumpRenderSettle(
            state,
            fieldSystem,
            slot,
            object);
    }

    sOverworldWildCustomJumpRenderSettleElapsedFrames[slot] = elapsed;
    targetX = OW_WILD_SPAWNER_TILE_CENTER_COORD_FX32(
        OW_WILD_RUNTIME(state)->movementCustomJumpTargetX[slot]);
    targetY = OW_WILD_SPAWNER_TILE_CENTER_COORD_FX32(
        OW_WILD_RUNTIME(state)->movementCustomJumpTargetY[slot]);
    object->posVec[0] = (u32)OverworldWildSpawns_LerpCustomJumpFx32(
        sOverworldWildCustomJumpRenderSettleStartX[slot],
        targetX,
        elapsed,
        total);
    object->posVec[2] = (u32)OverworldWildSpawns_LerpCustomJumpFx32(
        sOverworldWildCustomJumpRenderSettleStartY[slot],
        targetY,
        elapsed,
        total);
    MapObject_ClearBits(object, BIT_VANISH);
    OverworldWildSpawns_RecordCustomJumpRamObject(state, slot, object, 19);
    return TRUE;
}
#else
static BOOL OverworldWildSpawns_TickCustomJumpRenderSettle(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot)
{
    (void)state;
    (void)fieldSystem;
    (void)slot;
    return FALSE;
}
#endif

static u8 OverworldWildSpawns_GetCustomJumpSpinStep(u8 direction)
{
    switch (direction) {
    case OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT:
        return 1;
    case OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN:
        return 2;
    case OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_LEFT:
        return 3;
    default:
        return 0;
    }
}

static u8 OverworldWildSpawns_GetCustomJumpSpinDirection(u8 step)
{
    static const u8 sSpinDirections[] = {
        OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_UP,
        OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT,
        OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN,
        OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_LEFT,
    };

    return sSpinDirections[step & 3];
}

static void OverworldWildSpawns_TickCustomJumpSpin(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object,
    u32 elapsed)
{
    OverworldWildOverlayRuntimeState *runtime;
    u8 spinSpeed;
    u8 spinStep;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || object == NULL) {
        return;
    }

    runtime = OW_WILD_RUNTIME(state);
    spinSpeed = runtime->movementCustomJumpSpinSpeeds[slot];
    if (spinSpeed == 0 || elapsed <= runtime->movementCustomJumpSpinElapsedFrames[slot]) {
        return;
    }

    runtime->movementCustomJumpSpinElapsedFrames[slot] = (u16)elapsed;
    if (runtime->movementCustomJumpSpinTimers[slot] > 1) {
        runtime->movementCustomJumpSpinTimers[slot]--;
        return;
    }

    runtime->movementCustomJumpSpinTimers[slot] = spinSpeed;
    spinStep = (runtime->movementCustomJumpSpinSteps[slot] + 1) & 3;
    runtime->movementCustomJumpSpinSteps[slot] = spinStep;
    OverworldWildSpawns_SetObjectFacing(
        object,
        OverworldWildSpawns_GetCustomJumpSpinDirection(spinStep));
}

static void OverworldWildSpawns_ApplyCustomJumpRenderOffset(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object,
    u32 elapsed,
    BOOL fieldContextAuthenticated)
{
    u32 total;
    s32 startX;
    s32 startY;
    s32 targetX;
    s32 targetY;
    s32 baseY;
    s32 arc;
    u32 curve;
    BOOL logicalTileChanged;
    OverworldWildOverlayRuntimeState *runtime;
    runtime = OW_WILD_RUNTIME(state);

    total = runtime->movementCustomJumpFrameCounts[slot];

    startX = OW_WILD_SPAWNER_TILE_CENTER_COORD_FX32(
        runtime->movementCustomJumpStartX[slot]);
    startY = OW_WILD_SPAWNER_TILE_CENTER_COORD_FX32(
        runtime->movementCustomJumpStartY[slot]);
    targetX = OW_WILD_SPAWNER_TILE_CENTER_COORD_FX32(
        runtime->movementCustomJumpTargetX[slot]);
    targetY = OW_WILD_SPAWNER_TILE_CENTER_COORD_FX32(
        runtime->movementCustomJumpTargetY[slot]);
    baseY = (s32)object->posVec[1];
    if (elapsed >= total
        || (runtime->movementCustomJumpTargetBaseY[slot] > baseY
            && elapsed + (total >> 2) >= total)) {
        baseY = runtime->movementCustomJumpTargetBaseY[slot];
    }

    object->posVec[0] = (u32)OverworldWildSpawns_LerpCustomJumpFx32(
        startX,
        targetX,
        elapsed,
        total);
    object->posVec[2] = (u32)OverworldWildSpawns_LerpCustomJumpFx32(
        startY,
        targetY,
        elapsed,
        total);
    if (elapsed >= total) {
        arc = 0;
    } else {
        curve = (4 * elapsed * (total - elapsed)) / total;
        arc = (s32)((OW_WILD_SPAWNER_CUSTOM_JUMP_ARC_HEIGHT_FX32 * curve) / total);
    }
    object->hCurr = (int)(baseY >> 15);
    object->posVec[1] = (u32)baseY;
    object->faceVec[0] = 0;
    object->faceVec[1] = (u32)arc;
    object->faceVec[2] = 0;
    object->unk88[1] = (u32)arc;
    object->unk94[1] = 0;
#if OW_WILD_SPAWNER_CUSTOM_JUMP_SYNC_LOGICAL_SECONDARY
    logicalTileChanged = OverworldWildSpawns_SyncCustomJumpLogicalTile(object);
#else
    logicalTileChanged = FALSE;
#endif
    if (logicalTileChanged
        && (fieldContextAuthenticated
            || OverworldWildSpawns_IsMovementFieldContextCurrent(
                state,
                state->movementFieldSystem))) {
        OverworldWildSpawns_ReconcileNativeShadow(
            state->movementFieldSystem,
            object);
    }
    OverworldWildSpawns_TickCustomJumpSpin(state, slot, object, elapsed);
    object->flags |= (BIT_JUMP_START | BIT_MOVE_START | MAPOBJECTFLAG_UNK13);
    MapObject_ClearBits(
        object,
        BIT_VANISH | MAPOBJECTFLAG_UNK4 | MAPOBJECTFLAG_UNK8 | MAPOBJECTFLAG_UNK22
            | MAPOBJECTFLAG_UNK30);
}

static void OverworldWildSpawns_CommitCustomJumpLanding(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object)
{
    OverworldWildOverlayRuntimeState *runtime;
    int targetX;
    int targetY;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || object == NULL
        || !OW_WILD_RUNTIME(state)->movementCustomJumpActive[slot]) {
        return;
    }

    runtime = OW_WILD_RUNTIME(state);
    targetX = runtime->movementCustomJumpTargetX[slot];
    targetY = runtime->movementCustomJumpTargetY[slot];

    OverworldWildSpawns_SetObjectLandingTile(
        state->movementFieldSystem,
        object,
        targetX,
        targetY);
    if (runtime->movementCustomJumpSpinSpeeds[slot] != 0
        && state->movementPendingDirections[slot] <= OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT) {
        OverworldWildSpawns_SetObjectFacing(object, state->movementPendingDirections[slot]);
    }
}

static BOOL OverworldWildSpawns_FinalizeCustomJumpAfterRestore(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object,
    BOOL customJumpLandingFinalized)
{
    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || object == NULL
        || !OW_WILD_RUNTIME(state)->movementCustomJumpActive[slot]) {
        return FALSE;
    }

    if (!customJumpLandingFinalized) {
        OverworldWildSpawns_ApplyCustomJumpRenderOffset(
            state,
            slot,
            object,
            OW_WILD_RUNTIME(state)->movementCustomJumpFrameCounts[slot],
            FALSE);
        OverworldWildSpawns_CommitCustomJumpLanding(state, slot, object);
    }
    if (OverworldWildSpawns_StartCustomJumpRenderSettle(state, slot, object)) {
        return TRUE;
    }
    OverworldWildSpawns_ClearCustomJump(state, slot);
    return FALSE;
}

static BOOL OverworldWildSpawns_UpdateCustomJumpLanding(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object,
    BOOL commandFinished,
    BOOL fieldContextAuthenticated)
{
    OverworldWildOverlayRuntimeState *runtime;
#if OW_WILD_SPAWNER_CUSTOM_JUMP_RENDER_OFFSET
    u32 elapsed;
    u32 total;
#endif

    if (slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || object == NULL) {
        return commandFinished;
    }
    runtime = OW_WILD_RUNTIME(state);
    if (!runtime->movementCustomJumpActive[slot]) {
        return commandFinished;
    }
    OverworldWildSpawns_RecordCustomJumpRamObject(
        state,
        slot,
        object,
        commandFinished ? 10 : 11);

#if OW_WILD_SPAWNER_CUSTOM_JUMP_RENDER_OFFSET
    total = runtime->movementCustomJumpFrameCounts[slot];
    elapsed = runtime->movementCustomJumpElapsedFrames[slot];
    if (elapsed < total) {
        elapsed++;
        runtime->movementCustomJumpElapsedFrames[slot] = (u16)elapsed;
        OverworldWildSpawns_ApplyCustomJumpRenderOffset(
            state,
            slot,
            object,
            elapsed,
            fieldContextAuthenticated);
    }
    if (elapsed < total) {
        return FALSE;
    }
#endif

    if (commandFinished
#if OW_WILD_SPAWNER_CUSTOM_JUMP_RENDER_OFFSET
        || elapsed >= total
#endif
    ) {
        if (!commandFinished && MapObject_IsSingleMovementActive(object)) {
            MapObject_ClearSingleMovementActive(object);
        }
#if OW_WILD_SPAWNER_CUSTOM_JUMP_VISIBLE_LEGS \
    && !OW_WILD_SPAWNER_CUSTOM_JUMP_RENDER_OFFSET \
    && !OW_WILD_SPAWNER_CUSTOM_JUMP_RENDER_SETTLE
        OverworldWildSpawns_ClearCustomJump(state, slot);
        sOverworldWildCustomJumpVisibleLegFinished[slot] = TRUE;
        OverworldWildSpawns_RecordCustomJumpRamObject(state, slot, object, 12);
#else
        OverworldWildSpawns_CommitCustomJumpLanding(state, slot, object);
#if !OW_WILD_SPAWNER_CUSTOM_JUMP_POST_RESTORE_FINALIZE
        OverworldWildSpawns_ClearCustomJump(state, slot);
#endif
        OverworldWildSpawns_RecordCustomJumpRamObject(state, slot, object, 13);
#endif
        return TRUE;
    }

    return FALSE;
}

static BOOL OverworldWildSpawns_UpdateSpawnerMovementCommandForSlot(
    OverworldWildSpawnState *state,
    int slot,
    BOOL fieldContextAuthenticated)
{
    LocalMapObject *object;
    BOOL commandFinished;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || state->spawns[slot].object == NULL) {
        return FALSE;
    }

    object = state->spawns[slot].object;
    if (OverworldWildSpawns_IsNativeHeldMovementOwned(state, slot)) {
        commandFinished = MapObject_ClearHeldMovementIfActive(object);
        if (commandFinished) {
            OW_WILD_RUNTIME(state)->movementNativeHeldMask &=
                ~OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot);
        }
    } else {
        commandFinished =
            OverworldWildSpawns_UpdateSpawnerMovementCommandSuppressingHopStartSound(
                state,
                object,
                slot);
    }
    if (commandFinished) {
        if (OW_WILD_RUNTIME(state)->movementHopStartSoundSuppressFrames[slot] != 0) {
            StopSE(OW_WILD_SPAWNER_HOP_START_SE);
        }
        OW_WILD_RUNTIME(state)->movementHopStartSoundSuppressFrames[slot] = 0;
        MapObject_ClearSingleMovementActive(object);
    } else {
        OverworldWildSpawns_TickHopStartSoundSuppression(state, slot);
    }
    return OverworldWildSpawns_UpdateCustomJumpLanding(
        state,
        slot,
        object,
        commandFinished,
        fieldContextAuthenticated);
}
#endif

#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
static void OverworldWildSpawns_TickFrameMovementDecisionCounter(void)
{
    sOverworldWildMovementFrameDecisionCounter++;
}

static u8 OverworldWildSpawns_GetFrameMovementDecisionIntervalForSpeed(u8 speed)
{
    switch (speed) {
    case OW_WILD_SPAWNER_MOVEMENT_SPEED_3:
        return OW_WILD_SPAWNER_MOVEMENT_SPEED_3_DECISION_INTERVAL;
    default:
        return OW_WILD_SPAWNER_MOVEMENT_FRAME_DECISION_INTERVAL;
    }
}

static BOOL OverworldWildSpawns_ShouldRunFrameMovementDecisionForSpeed(u8 speed)
{
    u8 interval = OverworldWildSpawns_GetFrameMovementDecisionIntervalForSpeed(speed);

    if (interval <= 1) {
        return TRUE;
    }
    if ((sOverworldWildMovementFrameDecisionCounter % interval) != 0) {
        return FALSE;
    }

    return TRUE;
}

static void OverworldWildSpawns_StopFrameMovementTask(void)
{
    sOverworldWildMovementFrameTaskExecuting = FALSE;
    if (sOverworldWildMovementFrameTask != NULL) {
        SysTask *task = sOverworldWildMovementFrameTask;

        sOverworldWildMovementFrameTask = NULL;
        DestroySysTask(task);
    }
}

static BOOL OverworldWildSpawns_QueueBattleScriptTask(FieldSystem *fieldSystem, LocalMapObject *object)
{
    if (fieldSystem == NULL
        || fieldSystem != gFieldSysPtr
        || fieldSystem->taskman != NULL
        || OverworldWildSpawns_IsPlayerBallProjectileActive()) {
        return FALSE;
    }

    EventSet_Script(fieldSystem, OVERWORLD_WILD_SPAWNS_BATTLE_SCRIPT, object);
    return TRUE;
}

static void OverworldWildSpawns_CancelDeferredBattleScript(void)
{
    OverworldWildSpawnState *state = sOverworldWildDeferredBattleState;

    if (sOverworldWildDeferredBattleScriptTask != NULL) {
        SysTask *task = sOverworldWildDeferredBattleScriptTask;

        sOverworldWildDeferredBattleScriptTask = NULL;
        sOverworldWildDeferredBattleFieldSystem = NULL;
        sOverworldWildDeferredBattleState = NULL;
        sOverworldWildDeferredBattleGeneration = 0;
        sOverworldWildDeferredBattleSlot = -1;
        DestroySysTask(task);
    } else {
        sOverworldWildDeferredBattleFieldSystem = NULL;
        sOverworldWildDeferredBattleState = NULL;
        sOverworldWildDeferredBattleGeneration = 0;
        sOverworldWildDeferredBattleSlot = -1;
    }

    if (state != NULL) {
        OverworldWildSpawns_ResetPendingBattle(state);
    }
}

static void OverworldWildSpawns_CleanupResidentTasks(void)
{
    int i;

    OverworldWildSpawns_CancelDeferredBattleScript();
    OverworldWildSpawns_StopFrameMovementTask();
    for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
        OverworldWildSpawns_ClearStagedHopMovementListTask(NULL, i);
    }
}

static void OverworldWildSpawns_DeferredBattleScriptTask(SysTask *task, void *data)
{
    FieldSystem *fieldSystem;
    const OverworldWildHelperOverlayEntry *helperEntry;
    OverworldWildSpawnState *state;
    LocalMapObject *object;
    u16 generation;
    int slot;

    (void)data;
    if (sOverworldWildMovementFrameTaskExecuting) {
        return;
    }

    fieldSystem = sOverworldWildDeferredBattleFieldSystem;
    if (fieldSystem == NULL
        || fieldSystem != gFieldSysPtr
        || fieldSystem->taskman != NULL) {
        OverworldWildSpawns_CancelDeferredBattleScript();
        return;
    }
    if (OverworldWildSpawns_IsPlayerBallProjectileActive()) {
        return;
    }

    state = sOverworldWildDeferredBattleState;
    generation = sOverworldWildDeferredBattleGeneration;
    slot = sOverworldWildDeferredBattleSlot;
    object = NULL;
    helperEntry = OverworldWildSpawns_GetHelperOverlayEntry();
    if (helperEntry != NULL
        && helperEntry->validateDeferredBattle != NULL
        && helperEntry->validateDeferredBattle(fieldSystem, state, slot, generation)) {
        object = state->spawns[slot].object;
    }
    sOverworldWildDeferredBattleScriptTask = NULL;
    sOverworldWildDeferredBattleFieldSystem = NULL;
    sOverworldWildDeferredBattleState = NULL;
    sOverworldWildDeferredBattleGeneration = 0;
    sOverworldWildDeferredBattleSlot = -1;

    OverworldWildSpawns_StopFrameMovementTask();
    if (fieldSystem != NULL && object != NULL) {
        OverworldWildSpawns_QueueBattleScriptTask(fieldSystem, object);
    } else if (state != NULL) {
        OverworldWildSpawns_ResetPendingBattle(state);
    }
    DestroySysTask(task);
}

static BOOL OverworldWildSpawns_DeferBattleScript(
    FieldSystem *fieldSystem,
    OverworldWildSpawnState *state,
    int slot)
{
    LocalMapObject *object;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || OverworldWildSpawns_IsPlayerBallProjectileActive()
        || !OverworldWildSpawns_IsCurrentSpawnObject(fieldSystem, &state->spawns[slot])) {
        return FALSE;
    }
    object = state->spawns[slot].object;
    if (fieldSystem != NULL
        && !sOverworldWildMovementFrameTaskExecuting
        && fieldSystem->taskman == NULL) {
        return OverworldWildSpawns_QueueBattleScriptTask(fieldSystem, object);
    }

    sOverworldWildDeferredBattleFieldSystem = fieldSystem;
    sOverworldWildDeferredBattleState = state;
    sOverworldWildDeferredBattleGeneration = state->spawns[slot].encounterGeneration;
    sOverworldWildDeferredBattleSlot = (s8)slot;
    if (sOverworldWildDeferredBattleScriptTask == NULL) {
        sOverworldWildDeferredBattleScriptTask = CreateSysTask(
            OverworldWildSpawns_DeferredBattleScriptTask,
            NULL,
            OW_WILD_SPAWNER_MOVEMENT_FRAME_TASK_PRIORITY + 1);
    }
    if (sOverworldWildDeferredBattleScriptTask != NULL) {
        return TRUE;
    }
    return FALSE;
}

static void OverworldWildSpawns_EnsureFrameMovementTask(OverworldWildSpawnState *state, FieldSystem *fieldSystem)
{
    if (state == NULL || fieldSystem == NULL) {
        return;
    }

    state->movementFieldSystem = fieldSystem;
    if (sOverworldWildMovementFrameTask == NULL) {
        sOverworldWildMovementFrameTask = CreateSysTask(
            OverworldWildSpawns_FrameMovementTask,
            state,
            OW_WILD_SPAWNER_MOVEMENT_FRAME_TASK_PRIORITY);
        if (sOverworldWildMovementFrameTask != NULL) {
            sOverworldWildMovementFrameDecisionCounter = 0;
        }
    }
}

static BOOL OverworldWildSpawns_TickPlayerBallProjectile(
    FieldSystem *fieldSystem,
    OverworldWildSpawnState *state)
{
    const OverworldWildHelperOverlayEntry *helperEntry;
    OverworldWildOverlayRuntimeState *runtime;
    LocalMapObject *ballObject;
    BOOL active;

    runtime = OverworldWildSpawns_EnsureRuntimeState(state);
    if (runtime == NULL) {
        return FALSE;
    }
    helperEntry = OverworldWildSpawns_GetHelperOverlayEntry();
    if (helperEntry == NULL
        || helperEntry->tickPlayerBallProjectile == NULL) {
        OverworldWildSpawns_ResetPlayerBallShadowTracking(state);
        return FALSE;
    }
    active = helperEntry->tickPlayerBallProjectile(
            fieldSystem,
            state,
            &runtime->spawnPresentations,
            &runtime->despawnTelemetry,
            OverworldWildSpawns_ResetSlotState,
            OverworldWildSpawns_PrepareSlotForCapture,
            OverworldWildSpawns_CalculatePlayerBallShakes,
            OverworldWildSpawns_FindCapturedPokemonDestination);
    ballObject = helperEntry->getPlayerBallProjectileObject != NULL
        ? helperEntry->getPlayerBallProjectileObject()
        : NULL;
    OverworldWildSpawns_SyncPlayerBallShadowObject(
        state,
        fieldSystem,
        ballObject);
    return active;
}

static void OverworldWildSpawns_TransitionPlayerBallProjectile(
    FieldSystem *fieldSystem,
    OverworldWildSpawnState *state,
    int command)
{
    const OverworldWildHelperOverlayEntry *helperEntry =
        OverworldWildSpawns_GetHelperOverlayEntry();

    if (helperEntry != NULL
        && helperEntry->normalizeThrowPresentation != NULL) {
        helperEntry->normalizeThrowPresentation(fieldSystem, state, command);
    }
    OverworldWildSpawns_ResetPlayerBallShadowTracking(state);
}

static LocalMapObject *OverworldWildSpawns_GetPlayerBallProjectileObject(void)
{
    const OverworldWildHelperOverlayEntry *helperEntry =
        OverworldWildSpawns_GetHelperOverlayEntry();

    return helperEntry != NULL
            && helperEntry->getPlayerBallProjectileObject != NULL
        ? helperEntry->getPlayerBallProjectileObject()
        : NULL;
}

static BOOL OverworldWildSpawns_IsPlayerBallProjectileActive(void)
{
    return OverworldWildSpawns_GetPlayerBallProjectileObject() != NULL;
}

static void OverworldWildSpawns_OverlayOnFieldBusy(
    FieldSystem *fieldSystem,
    OverworldWildSpawnState *state,
    OverworldWildResidentData *residentData)
{
    int i;

    residentData->pendingFlags |= OW_WILD_FIELD_IDLE_REARM_PENDING;
    if (state->movementRuntimeState != NULL
        && OW_WILD_RUNTIME(state)->playerStepMaintenanceState
            != OW_WILD_PLAYER_STEP_MAINTENANCE_NONE) {
        residentData->pendingFlags |=
            OW_WILD_FIELD_IDLE_ZERO_REFILL_PENDING;
    }
    if (IsOverlayLoaded(OVERLAY_OVERWORLD_WILD_HELPER)
        && OverworldWildSpawns_IsPlayerBallProjectileActive()) {
        OverworldWildSpawns_TransitionPlayerBallProjectile(
            fieldSystem,
            state,
            OW_WILD_HELPER_THROW_PRESENTATION_TRANSITION_SUSPEND);
    }
    OverworldWildSpawns_CancelNativeHeldMovement(state, fieldSystem);
    for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
        if (state->spawns[i].active
            && state->spawns[i].mapId == fieldSystem->location->mapId) {
            return;
        }
    }
    /* The frame task discards its interrupted staged refill while busy. */
    residentData->pendingFlags |=
        OW_WILD_FIELD_IDLE_ZERO_REFILL_PENDING;
}

static BOOL OverworldWildSpawns_OverlayOnPlayerFrame(
    FieldSystem *fieldSystem,
    OverworldWildSpawnState *state)
{
    int i;

    if (state != NULL
        && state->movementRuntimeState != NULL
        && OW_WILD_RUNTIME(state)->movementNativeShadowRestorePending) {
        OW_WILD_RUNTIME(state)->movementNativeShadowRestorePending = FALSE;
        if (OverworldWildSpawns_IsMovementFieldContextCurrent(
                state,
                fieldSystem)) {
            for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
                LocalMapObject *object = state->spawns[i].object;

                if (!state->spawns[i].active
                    || !OverworldWildSpawns_IsCurrentSpawnObject(
                        fieldSystem,
                        &state->spawns[i])) {
                    continue;
                }
                MapObject_ClearBits(
                    object,
                    MAPOBJECTFLAG_UNK15);
                OverworldWildSpawns_ReconcileNativeShadow(
                    fieldSystem,
                    object);
            }
        }
    }
    return OverworldWildSpawns_TickPlayerBallProjectile(fieldSystem, state)
        || (PAD_Read() & PAD_BUTTON_R) != 0;
}

static void OverworldWildSpawns_FrameMovementTask(SysTask *task, void *data)
{
    OverworldWildSpawnState *state = (OverworldWildSpawnState *)data;
    OverworldWildOverlayRuntimeState *runtime;
    FieldSystem *fieldSystem;
    BOOL playerStepMaintenanceRan = FALSE;
    BOOL spawnSetMayHaveChanged = FALSE;
    BOOL movementFinishedThisFrame = FALSE;
    u16 participantMaskAtFrameStart;
    u16 currentSpawnMask;
    u16 heavyWorkMask;
    u16 frameWorkMask;
    u16 selectedIdleMask;
    u32 frameWork;
#if OW_WILD_SPAWNER_IMMEDIATE_AI_AFTER_COMMAND_COMPLETION
    u16 movementInProgressBefore;
#endif
    int i;

    (void)task;
    OW_WILD_PERF_RESET_FRAME();

    if (state == NULL) {
        OverworldWildSpawns_StopFrameMovementTask();
        return;
    }

    sOverworldWildMovementFrameTaskExecuting = TRUE;
    runtime = OW_WILD_RUNTIME(state);
    fieldSystem = state->movementFieldSystem;
    if (fieldSystem != NULL && fieldSystem->taskman != NULL) {
        /* Native transitions own the live map objects once the field is busy. */
        if (runtime->playerStepMaintenanceState
                != OW_WILD_PLAYER_STEP_MAINTENANCE_NONE
            && runtime->residentData != NULL) {
            runtime->residentData->pendingFlags |=
                OW_WILD_FIELD_IDLE_ZERO_REFILL_PENDING;
        }
        runtime->playerStepMaintenanceState =
            OW_WILD_PLAYER_STEP_MAINTENANCE_NONE;
        OverworldWildSpawns_CancelNativeHeldMovement(state, fieldSystem);
        if (OverworldWildSpawns_IsPlayerBallProjectileActive()) {
            OverworldWildSpawns_TransitionPlayerBallProjectile(
                fieldSystem,
                state,
                OW_WILD_HELPER_THROW_PRESENTATION_TRANSITION_SUSPEND);
        }
        OverworldWildSpawns_StopFrameMovementTask();
        return;
    }
    if (!OverworldWildSpawns_IsMovementFieldContextCurrent(state, fieldSystem)) {
        state->presentationRestorePending = TRUE;
        OverworldWildSpawns_DetachAllMovementStateOnContextLoss(state, FALSE);
        return;
    }
    currentSpawnMask = OverworldWildSpawns_GetCurrentMovementSpawnMask(
        state,
        fieldSystem);
    if (state->presentationRestorePending
        || runtime->spawnPresentations.managerRestoreMask != 0) {
        sOverworldWildMovementFrameTaskExecuting = FALSE;
        return;
    }
    if ((runtime->playerStepMaintenanceState
            & OW_WILD_PLAYER_STEP_MAINTENANCE_PHASE_MASK)
        == OW_WILD_PLAYER_STEP_MAINTENANCE_QUEUED) {
        /* Never add maintenance to the exact task cycle that queued it. */
        runtime->playerStepMaintenanceState =
            (runtime->playerStepMaintenanceState
                & OW_WILD_PLAYER_STEP_MAINTENANCE_DIRTY)
            | OW_WILD_PLAYER_STEP_MAINTENANCE_DESPAWN;
    } else if ((runtime->playerStepMaintenanceState
            & OW_WILD_PLAYER_STEP_MAINTENANCE_PHASE_MASK)
        == OW_WILD_PLAYER_STEP_MAINTENANCE_DESPAWN) {
        runtime->playerStepMaintenanceState =
            (runtime->playerStepMaintenanceState
                & OW_WILD_PLAYER_STEP_MAINTENANCE_DIRTY)
            | OW_WILD_PLAYER_STEP_MAINTENANCE_REFILL;
        OverworldWildSpawns_DespawnFarMons(state, fieldSystem);
        playerStepMaintenanceRan = TRUE;
    } else if ((runtime->playerStepMaintenanceState
            & OW_WILD_PLAYER_STEP_MAINTENANCE_PHASE_MASK)
        == OW_WILD_PLAYER_STEP_MAINTENANCE_REFILL) {
        runtime->playerStepMaintenanceState =
            (runtime->playerStepMaintenanceState
                & OW_WILD_PLAYER_STEP_MAINTENANCE_DIRTY)
            | OW_WILD_PLAYER_STEP_MAINTENANCE_REVEAL;
        OverworldWildSpawns_TryRefill(state, fieldSystem);
        playerStepMaintenanceRan = TRUE;
    } else if ((runtime->playerStepMaintenanceState
            & OW_WILD_PLAYER_STEP_MAINTENANCE_PHASE_MASK)
        == OW_WILD_PLAYER_STEP_MAINTENANCE_REVEAL) {
        runtime->playerStepMaintenanceState =
            (runtime->playerStepMaintenanceState
                & OW_WILD_PLAYER_STEP_MAINTENANCE_DIRTY) != 0
            ? OW_WILD_PLAYER_STEP_MAINTENANCE_QUEUED
            : OW_WILD_PLAYER_STEP_MAINTENANCE_NONE;
        OverworldWildSpawns_RevealNonPhantomObjects(state, fieldSystem);
        playerStepMaintenanceRan = TRUE;
    }
    if (!playerStepMaintenanceRan
        && runtime->movementHelpSpawnParentSlotPlusOne != 0
        && runtime->movementHelpSpawnRemaining != 0) {
        OverworldWildSpawns_TickQueuedHelpChildren(state, fieldSystem);
        spawnSetMayHaveChanged = TRUE;
    }
    if (playerStepMaintenanceRan || spawnSetMayHaveChanged) {
        currentSpawnMask = OverworldWildSpawns_GetCurrentMovementSpawnMask(
            state,
            fieldSystem);
    }
    if (currentSpawnMask == 0) {
        if (runtime->playerStepMaintenanceState
            != OW_WILD_PLAYER_STEP_MAINTENANCE_NONE) {
            sOverworldWildMovementFrameTaskExecuting = FALSE;
            return;
        }
        OverworldWildSpawns_RestoreMankeyTreeTopLayerProbe();
        OverworldWildSpawns_ResetAllMovementCommands(state, TRUE, FALSE);
        OverworldWildSpawns_StopFrameMovementTask();
        return;
    }

    if (OverworldWildSpawns_TryStartCustomJumpRamProbe(state, fieldSystem)) {
        sOverworldWildMovementFrameTaskExecuting = FALSE;
        return;
    }

    participantMaskAtFrameStart = OverworldWildSpawns_GetThrowParticipantMask(state);
    frameWork = OverworldWildSpawns_GetFrameMovementWork(state);
    heavyWorkMask = (u16)(frameWork >> OW_WILD_SPAWNER_FRAME_WORK_HEAVY_SHIFT);
    frameWorkMask = ((u16)frameWork | participantMaskAtFrameStart)
        & currentSpawnMask;
    selectedIdleMask = OverworldWildSpawns_SelectIdleAiWork(
        state,
        currentSpawnMask
            & ~(u16)frameWork
            & ~participantMaskAtFrameStart);
    frameWorkMask |= selectedIdleMask;
    if (heavyWorkMask == 0) {
        OverworldWildSpawns_TickMovementParams(
            state,
            fieldSystem,
            TRUE,
            frameWorkMask
#if OW_WILD_SPAWNER_IMMEDIATE_AI_AFTER_COMMAND_COMPLETION
            , selectedIdleMask
#endif
        );
        sOverworldWildMovementFrameTaskExecuting = FALSE;
        return;
    }

#if OW_WILD_SPAWNER_IMMEDIATE_AI_AFTER_COMMAND_COMPLETION
    movementInProgressBefore = state->movementInProgressMask;
#endif

    for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
        BOOL currentSpawnObject =
            (currentSpawnMask & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(i)) != 0;

        if ((state->captureTargetMask
            & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(i)) != 0) {
            continue;
        }

        if (!currentSpawnObject
            && participantMaskAtFrameStart != 0
            && (OverworldWildSpawns_GetThrowParticipantMask(state)
                & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(i)) != 0) {
            OverworldWildSpawns_ClearThrowStateForSlot(state, i);
        }

        if ((heavyWorkMask & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(i)) == 0) {
            continue;
        }

        if (!currentSpawnObject) {
            OverworldWildSpawns_ResetSlotMovementCommand(state, i, FALSE);
            OverworldWildSpawns_ClearStagedHopTarget(state, i);
            OverworldWildSpawns_ClearSpawnRunState(state, i);
            continue;
        }

        OW_WILD_PERF_INC(sOverworldWildPerfActiveFrameSlotsThisFrame);
        if (OverworldWildSpawns_IsActivePhantomTeleportMove(state, i)) {
            /* The teleport-step visual owns this timer; avoid the generic flicker tick. */
        }
#if OW_WILD_SPAWNER_PHANTOM_STALK_ACTIVE_REAL_FLICKER
        else if (!OverworldWildSpawns_UpdateActivePhantomRealFlicker(state, i)) {
            OverworldWildSpawns_UpdatePhantomFlicker(state, i);
        }
#else
        else {
            OverworldWildSpawns_UpdatePhantomFlicker(state, i);
        }
#endif

        if (OverworldWildSpawns_IsMovementSlotInProgress(state, i)) {
            if (OverworldWildSpawns_TickStagedHopMovementListTask(state, fieldSystem, i, &movementFinishedThisFrame)) {
                continue;
            }
            if (OverworldWildSpawns_IsActivePhantomTeleportMove(state, i)) {
                if (OverworldWildSpawns_TickPhantomTeleportMovementCommand(
                        state,
                        i,
                        &movementFinishedThisFrame)) {
                    continue;
                }
            }
            if (OverworldWildSpawns_UpdateSpawnerMovementCommandForSlot(state, i, TRUE)) {
                OverworldWildSpawns_ClearMovementSlotInProgress(state, i);
                if (OverworldWildSpawns_HandleFinishedSpawnRunMovementCommand(state, fieldSystem, i)) {
                    movementFinishedThisFrame = TRUE;
                    continue;
                }
                if (OverworldWildSpawns_HandleFinishedStagedHopMovementCommand(
                        state,
                        fieldSystem,
                        i,
                        TRUE)) {
                    movementFinishedThisFrame = TRUE;
                    continue;
                }
                OverworldWildSpawns_HandleFinishedMovementCommand(state, i);
                movementFinishedThisFrame = TRUE;
            }
            continue;
        }

        if (OverworldWildSpawns_TickCustomJumpRenderSettle(state, fieldSystem, i)) {
            continue;
        }

        if (state->movementSpawnRunActive[i]) {
            OverworldWildSpawns_TryStartSpawnRunStep(state, fieldSystem, i);
            continue;
        }

        if (state->movementStagedHopPending[i]) {
            if (state->movementCooldowns[i] > 0) {
                state->movementCooldowns[i]--;
                continue;
            }
            OverworldWildSpawns_ContinuePendingStagedHop(state, fieldSystem, i);
            continue;
        }
    }

    if (movementFinishedThisFrame) {
        state->movementBattleSettleFrames = OW_WILD_SPAWNER_BATTLE_SETTLE_FRAMES;
    }

#if OW_WILD_SPAWNER_IMMEDIATE_AI_AFTER_COMMAND_COMPLETION
    frameWorkMask |= movementInProgressBefore & ~state->movementInProgressMask;
#endif
    OverworldWildSpawns_TickMovementParams(
        state,
        fieldSystem,
        TRUE,
        frameWorkMask
#if OW_WILD_SPAWNER_IMMEDIATE_AI_AFTER_COMMAND_COMPLETION
        , selectedIdleMask
            | (movementInProgressBefore & ~state->movementInProgressMask)
#endif
    );
    OverworldWildSpawns_UpdateMankeyTreeTopLayerProbe(state, fieldSystem);
    OverworldWildSpawns_UpdateMankeyTreeTopBubbleProbe(state, fieldSystem);
    OverworldWildSpawns_TickRamCrashShake(state, fieldSystem);
    if (movementFinishedThisFrame) {
        OverworldWildSpawns_RevealNonPhantomObjects(state, fieldSystem);
    }
    sOverworldWildMovementFrameTaskExecuting = FALSE;
}
#else
static void OverworldWildSpawns_CleanupResidentTasks(void)
{
}

static void OverworldWildSpawns_TickFrameMovementDecisionCounter(void)
{
}

static BOOL OverworldWildSpawns_ShouldRunFrameMovementDecisionForSpeed(u8 speed)
{
    (void)speed;
    return FALSE;
}
#endif

static BOOL OverworldWildSpawns_RequestBattleScript(
    FieldSystem *fieldSystem,
    OverworldWildSpawnState *state,
    int slot)
{
    if (fieldSystem == NULL
        || state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || OverworldWildSpawns_IsPlayerBallProjectileActive()
        || !OverworldWildSpawns_IsCurrentSpawnObject(fieldSystem, &state->spawns[slot])) {
        return FALSE;
    }

#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
    return OverworldWildSpawns_DeferBattleScript(fieldSystem, state, slot);
#else
    return OverworldWildSpawns_QueueBattleScriptTask(fieldSystem, state->spawns[slot].object);
#endif
}

static BOOL OverworldWildSpawns_IsSurfBehavior(u8 behavior)
{
    return behavior == 16 || behavior == 18 || behavior == 21 || behavior == 42;
}

static void OverworldWildSpawns_ResetSlotState(
    OverworldWildSpawnState *state,
    int slot,
    BOOL deleteAuxiliaryObjects)
{
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_PARAM_TICK
    OverworldWildSpawns_ResetSlotMovementCommand(state, slot, deleteAuxiliaryObjects);
#endif

    if (state->spawns[slot].active
        && state->spawns[slot].object != NULL
        && OverworldWildSpawns_IsCurrentSpawnObject(gFieldSysPtr, &state->spawns[slot])) {
        OverworldWildSpawns_RestoreMankeyTreeTopRenderOverride(slot, state->spawns[slot].object);
    }
#if !OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_PARAM_TICK
    OverworldWildSpawns_ClearThrowStateForSlot(state, slot);
#endif
    OverworldWildSpawns_ClearMankeyTreeTopProxyObject(state, slot, deleteAuxiliaryObjects);
    OverworldWildSpawns_ClearMankeyTreeTopCache(state, slot);
    OW_WILD_RUNTIME(state)->residentData->savedHp[slot] = 0;

    state->spawns[slot].object = NULL;
    state->spawns[slot].personality = 0;
    state->spawns[slot].mapId = MAP_NOTHING;
    state->spawns[slot].species = SPECIES_NONE;
    state->spawns[slot].form = 0;
    state->spawns[slot].level = 0;
    state->spawns[slot].terrain = 0;
    state->spawns[slot].shiny = FALSE;
    state->spawns[slot].active = FALSE;
    OW_WILD_RUNTIME(state)->playerBallCatchValues[slot] = 0;
    state->captureTargetMask &=
        (u16)~OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot);
    OW_WILD_RUNTIME(state)->spawnPresentations.managerRestoreMask &=
        (u16)~OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot);
    OW_WILD_RUNTIME(state)->spawnPresentations.farSamples[slot] = 0;
    state->movementBehaviorClasses[slot] = OW_WILD_BEHAVIOR_CLASS_DEFAULT;
    OW_WILD_RUNTIME(state)->movementBehaviorLimitKeys[slot] = OW_WILD_BEHAVIOR_CLASS_DEFAULT;
    OverworldWildSpawns_ClearCachedBehaviorProfile(state, slot);
    if (state->movementQueuedBattleSlot == slot) {
        state->movementQueuedBattleSlot = -1;
    }
    if (state->pendingSlot == slot) {
        OverworldWildSpawns_ResetPendingBattle(state);
    }
    state->movementSpawnRunTargetX[slot] = 0;
    state->movementSpawnRunTargetY[slot] = 0;
    state->movementSpawnRunActive[slot] = FALSE;
    state->movementMankeyTreeTopProxyObjects[slot] = NULL;
#if OW_WILD_SPAWNER_MANKEY_TREE_TOP_RENDER_OVERRIDE_SAVE_ENABLED
    sOverworldWildMankeyTreeTopPrioritySavedBits[slot] = 0;
    sOverworldWildMankeyTreeTopPriorityBitsSaved[slot] = FALSE;
#if OW_WILD_SPAWNER_MANKEY_TREE_TOP_DRAW_CALLBACK_OVERRIDE_ENABLED
    sOverworldWildMankeyTreeTopSavedDrawCallbacks[slot] = NULL;
    sOverworldWildMankeyTreeTopDrawCallbacksSaved[slot] = FALSE;
#endif
#endif
}

static void OverworldWildSpawns_ResetPendingBattle(OverworldWildSpawnState *state)
{
    state->pendingSlot = -1;
    state->pendingPersonality = 0;
    state->pendingSpecies = SPECIES_NONE;
    state->pendingLevel = 0;
    state->pendingShiny = FALSE;
    state->pendingMapGeneration = 0;
    state->pendingEncounterGeneration = 0;
}

#if OW_WILD_UPDATE_DIAGNOSTIC_SKIP_CLEAR
static void OverworldWildSpawns_ClearContextLite(OverworldWildSpawnState *state)
{
    int i;

    if (state == NULL) {
        return;
    }

    if (state->movementRuntimeState != NULL
        && OW_WILD_RUNTIME(state)->residentData != NULL) {
        for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
            if (state->spawns[i].active) {
                OverworldWildSpawns_ClearSlotAndSaveShiny(state, i, FALSE);
            }
        }
    } else {
        for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
            OverworldWildSpawns_TrySaveShinyReservation(state, &state->spawns[i]);
        }
        memset(state->spawns, 0, sizeof(state->spawns));
    }
    state->captureTargetMask = 0;

    state->justSpawned = FALSE;
    state->spawnCooldown = 0;
    state->headbuttSpawnCooldown = OW_WILD_HEADBUTT_REFILL_ATTEMPT_COOLDOWN;
    state->fishingSpawnCooldown = OW_WILD_FISHING_REFILL_ATTEMPT_COOLDOWN;
    state->battleGraceSteps = 0;
}
#endif

#if !OW_WILD_UPDATE_DIAGNOSTIC_SKIP_CLEAR
static void OverworldWildSpawns_Clear(OverworldWildSpawnState *state, BOOL deleteObjects)
{
    int i;

    OverworldWildSpawns_CancelDeferredBattleScript();
    OverworldWildSpawns_RestoreMankeyTreeTopLayerProbe();
    for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
        OverworldWildSpawns_ClearSlotAndSaveShiny(state, i, deleteObjects);
    }
    state->captureTargetMask = 0;

    state->justSpawned = FALSE;
    state->spawnCooldown = 0;
    state->headbuttSpawnCooldown = OW_WILD_HEADBUTT_REFILL_ATTEMPT_COOLDOWN;
    state->fishingSpawnCooldown = OW_WILD_FISHING_REFILL_ATTEMPT_COOLDOWN;
    OverworldWildSpawns_ResetAmbientCryCooldown(state);
    state->battleGraceSteps = 0;
    state->movementQueuedBattleSlot = -1;
    OverworldWildSpawns_ClearQueuedHelpChildren(state);
    OverworldWildSpawns_ResetPendingBattle(state);
}
#endif

static BOOL OverworldWildSpawns_TryGetEncounterDataId(FieldSystem *fieldSystem, int *encounterDataId)
{
    const OverworldWildHelperOverlayEntry *helperEntry;

    if (fieldSystem == NULL || fieldSystem->location == NULL || encounterDataId == NULL) {
        return FALSE;
    }

    helperEntry = OverworldWildSpawns_GetHelperOverlayEntry();
    return helperEntry != NULL
        && OverworldFieldService_TryGetEncounterDataIdForMap(
            fieldSystem->location->mapId,
            encounterDataId);
}

static BOOL OverworldWildSpawns_IsEnabledMap(FieldSystem *fieldSystem)
{
    int encounterDataId;

    return fieldSystem != NULL
        && fieldSystem->location != NULL
        && fieldSystem->mapObjectMan != NULL
        && fieldSystem->playerAvatar != NULL
        && OverworldWildSpawns_TryGetEncounterDataId(fieldSystem, &encounterDataId);
}

static BOOL OverworldWildSpawns_TryGetSpawnTerrain(FieldSystem *fieldSystem, int x, int y, OverworldWildSpawnTerrain *terrain)
{
    u8 behavior;

    if (fieldSystem == NULL || terrain == NULL || x < 0 || y < 0) {
        return FALSE;
    }

    behavior = GetMetatileBehaviorAt(fieldSystem, x, y);
    if (OverworldWildSpawns_IsSurfBehavior(behavior)) {
        *terrain = OW_WILD_SPAWN_TERRAIN_SURF;
    } else if (behavior == OW_WILD_TILE_ENCOUNTER_GRASS
        || behavior == OW_WILD_TILE_LONG_GRASS
        || behavior == 5
        || behavior == 8
        || behavior == 11
        || behavior == 37
        || behavior == 112
        || behavior == 119
        || behavior == 123
        || behavior == 163
        || behavior == 164) {
        *terrain = OW_WILD_SPAWN_TERRAIN_LAND;
    } else {
        return FALSE;
    }

    return TRUE;
}

static BOOL OverworldWildSpawns_TryGetFreeSlot(OverworldWildSpawnState *state, u8 start, u8 end, int *slot)
{
    u8 i;

    for (i = start; i < end; i++) {
        if (!state->spawns[i].active) {
            *slot = i;
            return TRUE;
        }
    }

    return FALSE;
}

static BOOL OverworldWildSpawns_IsTileOccupiedByObject(FieldSystem *fieldSystem, int x, int y)
{
    u32 i;
    MapObjectMan *mapObjectMan = (MapObjectMan *)fieldSystem->mapObjectMan;
    LocalMapObject *objects;

    OW_WILD_PERF_INC(sOverworldWildPerfTargetScansThisFrame);

    if (x == GetPlayerXCoord(fieldSystem->playerAvatar) && y == GetPlayerYCoord(fieldSystem->playerAvatar)) {
        return TRUE;
    }
    if (mapObjectMan == NULL || mapObjectMan->objects == NULL) {
        return FALSE;
    }

    objects = mapObjectMan->objects;
    for (i = 0; i < mapObjectMan->object_count; i++) {
        LocalMapObject *object = &objects[i];

        if ((object->flags & MAPOBJECTFLAG_ACTIVE) != 0
            && object != sOverworldWildCollisionIgnoredObject
            && !OverworldWildSpawns_IsIgnoredVisualObjectId(object->id)
            && (int)MapObject_GetCurrentX(object) == x
            && (int)MapObject_GetCurrentY(object) == y) {
            return TRUE;
        }
    }

    return FALSE;
}

static BOOL OverworldWildSpawns_IsTileOccupiedByNonPlayerObject(
    FieldSystem *fieldSystem,
    LocalMapObject *ignoredObject,
    int x,
    int y)
{
    MapObjectMan *mapObjectMan;
    LocalMapObject *playerObject;
    u32 i;

    if (fieldSystem == NULL || fieldSystem->mapObjectMan == NULL) {
        return FALSE;
    }
    mapObjectMan = (MapObjectMan *)fieldSystem->mapObjectMan;
    playerObject = fieldSystem->playerAvatar != NULL
        ? fieldSystem->playerAvatar->mapObject
        : NULL;
    if (mapObjectMan->objects == NULL) {
        return FALSE;
    }
    for (i = 0; i < mapObjectMan->object_count; i++) {
        LocalMapObject *object = &mapObjectMan->objects[i];

        if ((object->flags & MAPOBJECTFLAG_ACTIVE) != 0
            && object != ignoredObject
            && object != playerObject
            && object != sOverworldWildCollisionIgnoredObject
            && !OverworldWildSpawns_IsIgnoredVisualObjectId(object->id)
            && (int)MapObject_GetCurrentX(object) == x
            && (int)MapObject_GetCurrentY(object) == y) {
            return TRUE;
        }
    }
    return FALSE;
}

static BOOL OverworldWildSpawns_IsTileReservedByOtherWild(
    OverworldWildSpawnState *state,
    int movingSlot,
    int x,
    int y)
{
    OverworldWildOverlayRuntimeState *runtime;
    int i;

    if (state == NULL || x < 0 || y < 0) {
        return FALSE;
    }
    runtime = (OverworldWildOverlayRuntimeState *)state->movementRuntimeState;
    for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
        LocalMapObject *object;

        if (i == movingSlot || !state->spawns[i].active) {
            continue;
        }
        object = state->spawns[i].object;
        if (object != NULL && object == sOverworldWildCollisionIgnoredObject) {
            continue;
        }
        if (object != NULL
            && (int)MapObject_GetCurrentX(object) == x
            && (int)MapObject_GetCurrentY(object) == y) {
            return TRUE;
        }
        if (runtime != NULL
            && runtime->movementCustomJumpActive[i]
            && runtime->movementCustomJumpTargetX[i] == x
            && runtime->movementCustomJumpTargetY[i] == y) {
            return TRUE;
        }
        if (state->movementPhantomTeleportHasTarget[i]
            && state->movementPhantomTeleportTargetX[i] == x
            && state->movementPhantomTeleportTargetY[i] == y) {
            return TRUE;
        }
        if (state->movementStagedHopPending[i]
            && state->movementStagedHopTargetX[i] == x
            && state->movementStagedHopTargetY[i] == y) {
            return TRUE;
        }
        if (state->movementSpawnRunActive[i]
            && state->movementSpawnRunTargetX[i] == x
            && state->movementSpawnRunTargetY[i] == y) {
            return TRUE;
        }
        if ((state->movementInProgressMask
                & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(i)) != 0
            && state->movementPreviousTileLocked[i]
            && state->movementPendingDistances[i] != 0
            && state->movementPreviousTileX[i]
                    + OverworldWildSpawns_MovementDirectionDeltaX(
                        state->movementPendingDirections[i])
                        * state->movementPendingDistances[i] == x
            && state->movementPreviousTileY[i]
                    + OverworldWildSpawns_MovementDirectionDeltaY(
                        state->movementPendingDirections[i])
                        * state->movementPendingDistances[i] == y) {
            return TRUE;
        }
    }
    return FALSE;
}

static BOOL OverworldWildSpawns_IsWalkableLandTile(FieldSystem *fieldSystem, int x, int y)
{
    u8 behavior;

    if (x < 0 || y < 0) {
        return FALSE;
    }
    if (OverworldWildSpawns_IsTileOccupiedByObject(fieldSystem, x, y)) {
        return FALSE;
    }
    if (IsMetatileBlockedAt(fieldSystem, x, y)) {
        return FALSE;
    }

    behavior = GetMetatileBehaviorAt(fieldSystem, x, y);
    if (behavior == OW_WILD_TILE_HEADBUTT || OverworldWildSpawns_IsSurfBehavior(behavior)) {
        return FALSE;
    }

    return TRUE;
}

#if OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_SINGLE_JUMP_PROBE \
    || OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_JUMP2_PROBE \
    || OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_CHAINED_JUMP2_PROBE \
    || OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_CHAINED_JUMP2_NO_MIDPOINT_NORMALIZE_PROBE \
    || OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_CHAINED_JUMP2_LOGICAL_MIDPOINT_PROBE \
    || OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_WRAPPED_JUMP2_PROBE \
    || OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_WRAPPED_CHAINED_JUMP2_PROBE \
    || OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_SINGLE_WRAPPER_CHAINED_JUMP2_PROBE \
    || OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_PARTNER_PREPPED_INTERNAL_JUMP_PROBE
static BOOL OverworldWildSpawns_IsCanopyPathTile(FieldSystem *fieldSystem, int x, int y)
{
    return OverworldWildSpawns_IsWalkableLandTile(fieldSystem, x, y);
}
#endif

static int OverworldWildSpawns_GetCustomJumpDistance(int dx, int dy)
{
    return OverworldWildSpawns_Max(
        OverworldWildSpawns_Abs(dx),
        OverworldWildSpawns_Abs(dy));
}

static BOOL OverworldWildSpawns_IsCustomJumpVectorShape(int dx, int dy)
{
    int absDx = OverworldWildSpawns_Abs(dx);
    int absDy = OverworldWildSpawns_Abs(dy);

    if (absDx == 0 && absDy == 0) {
        return FALSE;
    }

    if (absDx == 0 || absDy == 0) {
        return TRUE;
    }

#if OW_WILD_SPAWNER_CUSTOM_JUMP_ALLOW_DIAGONAL
    return absDx == absDy;
#else
    return FALSE;
#endif
}

static BOOL OverworldWildSpawns_TryGetCustomJumpVector(
    int dx,
    int dy,
    u8 *direction,
    u8 *distance)
{
    int jumpDistance;
    u8 directions[OW_WILD_SPAWNER_MOVEMENT_MAX_DIRECTIONS];

    if (!OverworldWildSpawns_IsCustomJumpVectorShape(dx, dy)
        || OverworldWildSpawns_DiagnosticBuildDirections(dx, dy, directions) == 0) {
        return FALSE;
    }

    jumpDistance = OverworldWildSpawns_GetCustomJumpDistance(dx, dy);
    if (jumpDistance < OW_WILD_SPAWNER_CUSTOM_JUMP_MIN_TILES
        || jumpDistance > OW_WILD_SPAWNER_CUSTOM_JUMP_MAX_TILES) {
        return FALSE;
    }

    *direction = directions[0];
    *distance = (u8)jumpDistance;
    return TRUE;
}

static u8 OverworldWildSpawns_GetBehaviorHopMinDistance(
    const OverworldWildBehaviorProfile *profile,
    u8 spotState)
{
    u8 minDistance;

    if (profile == NULL) {
        return OW_WILD_SPAWNER_CUSTOM_JUMP_MIN_TILES;
    }
    minDistance = OverworldWildSpawns_SelectStateMovementByte(
        spotState,
        profile->hopMinDistance,
        profile->attentiveHopMinDistance,
        profile->tiredHopMinDistance);
    if (minDistance == 0) {
        return OW_WILD_SPAWNER_CUSTOM_JUMP_MIN_TILES;
    }
    return minDistance;
}

static u8 OverworldWildSpawns_GetBehaviorHopMaxDistance(
    const OverworldWildBehaviorProfile *profile,
    u8 spotState)
{
    u8 minDistance = OverworldWildSpawns_GetBehaviorHopMinDistance(profile, spotState);
    u8 maxDistance;

    if (profile == NULL) {
        return OW_WILD_SPAWNER_CUSTOM_JUMP_MAX_TILES;
    }
    maxDistance = OverworldWildSpawns_SelectStateMovementByte(
        spotState,
        profile->hopMaxDistance,
        profile->attentiveHopMaxDistance,
        profile->tiredHopMaxDistance);
    if (maxDistance == 0) {
        return OW_WILD_SPAWNER_CUSTOM_JUMP_MAX_TILES;
    }
    if (maxDistance < minDistance) {
        return minDistance;
    }
    return maxDistance;
}

static BOOL OverworldWildSpawns_IsBehaviorHopVectorShape(
    const OverworldWildBehaviorProfile *profile,
    u8 spotState,
    int dx,
    int dy)
{
    int absDx = OverworldWildSpawns_Abs(dx);
    int absDy = OverworldWildSpawns_Abs(dy);

    if (absDx == 0 && absDy == 0) {
        return FALSE;
    }

    if (absDx == 0 || absDy == 0) {
        return TRUE;
    }

    return profile != NULL
        && OverworldWildSpawns_SelectStateMovementByte(
            spotState,
            profile->hopAllowNonCardinal,
            profile->attentiveHopAllowNonCardinal,
            profile->tiredHopAllowNonCardinal) == OW_WILD_BEHAVIOR_BOOL_YES
        && absDx == absDy;
}

static BOOL OverworldWildSpawns_TryGetBehaviorHopVector(
    const OverworldWildBehaviorProfile *profile,
    u8 spotState,
    int dx,
    int dy,
    u8 *direction,
    u8 *distance)
{
    int jumpDistance;
    u8 directions[OW_WILD_SPAWNER_MOVEMENT_MAX_DIRECTIONS];

    if (!OverworldWildSpawns_IsBehaviorHopVectorShape(profile, spotState, dx, dy)
        || OverworldWildSpawns_DiagnosticBuildDirections(dx, dy, directions) == 0) {
        return FALSE;
    }

    jumpDistance = OverworldWildSpawns_GetCustomJumpDistance(dx, dy);
    if (jumpDistance < OverworldWildSpawns_GetBehaviorHopMinDistance(profile, spotState)
        || jumpDistance > OverworldWildSpawns_GetBehaviorHopMaxDistance(profile, spotState)) {
        return FALSE;
    }

    if (direction != NULL) {
        *direction = directions[0];
    }
    if (distance != NULL) {
        *distance = (u8)jumpDistance;
    }
    return TRUE;
}

static const OverworldWildHelperOverlayEntry *OverworldWildSpawns_GetHelperOverlayEntry(void)
{
    const OverworldWildHelperOverlayEntry *entry;

    if (sOverworldWildHelperOverlayReady) {
        if (IsOverlayLoaded(OVERLAY_OVERWORLD_WILD_HELPER)
            && OVERWORLD_WILD_HELPER_OVERLAY_VALIDATE(
                OVERWORLD_WILD_HELPER_OWNED_BEHAVIOR)) {
            entry = OVERWORLD_WILD_HELPER_OVERLAY_ENTRY;
            return entry;
        }
        /* Never reload or unload an owned behavior overlay under live users. */
        return NULL;
    }
    if (!IsOverlayLoaded(OVERLAY_OVERWORLD_WILD_HELPER)) {
        if (!HandleLoadOverlay(OVERLAY_OVERWORLD_WILD_HELPER, 0)) {
            return NULL;
        }
    }
    entry = OVERWORLD_WILD_HELPER_OVERLAY_ENTRY;

    if (!OVERWORLD_WILD_HELPER_OVERLAY_VALIDATE(
            OVERWORLD_WILD_HELPER_ENSURE_BEHAVIOR)) {
        return NULL;
    }

    sOverworldWildHelperOverlayReady = TRUE;
    return entry;
}

static void OverworldWildSpawns_BuildBehaviorHopHelperConfig(
    const OverworldWildBehaviorProfile *profile,
    u8 spotState,
    int objectX,
    int objectY,
    int targetX,
    int targetY,
    const u8 *directions,
    int directionCount,
    BOOL stopOneHopAway,
    OverworldWildHelperHopConfig *config)
{
    int i;
    int helperDirectionCount = directionCount;

    if (config == NULL) {
        return;
    }

    memset(config, 0, sizeof(*config));
    config->objectX = objectX;
    config->objectY = objectY;
    config->targetX = targetX;
    config->targetY = targetY;
    config->minDistance = OverworldWildSpawns_GetBehaviorHopMinDistance(
        profile,
        spotState);
    config->maxDistance = OverworldWildSpawns_GetBehaviorHopMaxDistance(
        profile,
        spotState);
    config->allowNonCardinal =
        OverworldWildSpawns_SelectStateMovementByte(
            spotState,
            profile->hopAllowNonCardinal,
            profile->attentiveHopAllowNonCardinal,
            profile->tiredHopAllowNonCardinal) == OW_WILD_BEHAVIOR_BOOL_YES;
    config->stopOneHopAway = stopOneHopAway;

    if (helperDirectionCount > OW_WILD_HELPER_HOP_PLAN_MAX_DIRECTIONS) {
        helperDirectionCount = OW_WILD_HELPER_HOP_PLAN_MAX_DIRECTIONS;
    }
    if (helperDirectionCount < 0 || directions == NULL) {
        helperDirectionCount = 0;
    }

    config->directionCount = (u8)helperDirectionCount;
    for (i = 0; i < helperDirectionCount; i++) {
        config->directions[i] = directions[i];
    }
}

static BOOL OverworldWildSpawns_ValidateBehaviorHopLanding(
    int landingX,
    int landingY,
    int targetX,
    int targetY,
    void *context)
{
    OverworldWildBehaviorHopValidationContext *validationContext =
        (OverworldWildBehaviorHopValidationContext *)context;

    if (validationContext == NULL
        || validationContext->fieldSystem == NULL
        || !OverworldWildSpawns_IsBehaviorAllowedHopLandingTile(
            validationContext->state,
            validationContext->slot,
            validationContext->fieldSystem,
            validationContext->allowedTile,
            landingX,
            landingY,
            validationContext->blockPlayerUnlessFinal ? targetX : -1,
            validationContext->blockPlayerUnlessFinal ? targetY : -1)) {
        return FALSE;
    }

    if (validationContext->blockPlayerUnlessFinal
        && (landingX != targetX || landingY != targetY)
        && OverworldWildSpawns_IsPlayerTile(
            validationContext->fieldSystem,
            landingX,
            landingY)) {
        return FALSE;
    }
    return TRUE;
}

static BOOL OverworldWildSpawns_TryStartBehaviorHopToPlannedTileCommand(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    const OverworldWildBehaviorProfile *profile,
    u8 allowedTile,
    int landingX,
    int landingY,
    int finalTargetX,
    int finalTargetY)
{
    LocalMapObject *object;
    u8 direction;
    u8 distance;
    int objectX;
    int objectY;
    u8 spotState;

    if (!OverworldWildSpawns_IsBehaviorAllowedHopLandingTile(
            state,
            slot,
            fieldSystem,
            allowedTile,
            landingX,
            landingY,
            finalTargetX,
            finalTargetY)) {
        return FALSE;
    }

    object = state->spawns[slot].object;
    if (object == NULL
        || MapObject_IsSingleMovementActive(object)
        || OverworldWildSpawns_IsMovementSlotInProgress(state, slot)) {
        return FALSE;
    }

    objectX = MapObject_GetCurrentX(object);
    objectY = MapObject_GetCurrentY(object);
    spotState = state->movementSpotStates[slot];
    if (!OverworldWildSpawns_TryGetBehaviorHopVector(
            profile,
            spotState,
            landingX - objectX,
            landingY - objectY,
            &direction,
            &distance)) {
        return FALSE;
    }

    state->movementStagedHopOriginX[slot] = (s16)objectX;
    state->movementStagedHopOriginY[slot] = (s16)objectY;
    state->movementStagedHopTargetX[slot] = (s16)finalTargetX;
    state->movementStagedHopTargetY[slot] = (s16)finalTargetY;
    state->movementStagedHopDistances[slot] = distance;
    state->movementStagedHopFinishWithTired[slot] = FALSE;
    state->movementStagedHopPending[slot] = TRUE;
    if (OverworldWildSpawns_StartPreparedCustomJumpCommand(
            state,
            fieldSystem,
            slot,
            object,
            direction,
            distance,
            landingX,
            landingY,
            profile,
            FALSE)) {
        return TRUE;
    }

    OverworldWildSpawns_ClearStagedHopTarget(state, slot);
    return FALSE;
}

static BOOL OverworldWildSpawns_TryStartBehaviorHopPlanStepCommand(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    const OverworldWildBehaviorProfile *profile,
    u8 allowedTile,
    int targetX,
    int targetY,
    const u8 *directions,
    int directionCount,
    BOOL stopOneHopAway)
{
    const OverworldWildHelperOverlayEntry *helperEntry;
    OverworldWildHelperHopConfig config;
    OverworldWildHelperHopResult result;
    OverworldWildBehaviorHopValidationContext validationContext;
    LocalMapObject *object;
    int objectX;
    int objectY;
    u8 spotState;

    if (state == NULL
        || fieldSystem == NULL
        || profile == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS) {
        return FALSE;
    }

    object = state->spawns[slot].object;
    if (object == NULL) {
        return FALSE;
    }

    objectX = MapObject_GetCurrentX(object);
    objectY = MapObject_GetCurrentY(object);
    spotState = state->movementSpotStates[slot];
    helperEntry = OverworldWildSpawns_GetHelperOverlayEntry();
    if (helperEntry == NULL || helperEntry->planBehaviorHopStep == NULL) {
        return FALSE;
    }

    OverworldWildSpawns_BuildBehaviorHopHelperConfig(
        profile,
        spotState,
        objectX,
        objectY,
        targetX,
        targetY,
        directions,
        directionCount,
        stopOneHopAway,
        &config);
    validationContext.state = state;
    validationContext.fieldSystem = fieldSystem;
    validationContext.slot = slot;
    validationContext.allowedTile = allowedTile;
    validationContext.blockPlayerUnlessFinal = TRUE;
    if (!helperEntry->planBehaviorHopStep(
            &config,
            OverworldWildSpawns_ValidateBehaviorHopLanding,
            &validationContext,
            &result)) {
        return FALSE;
    }
    if (stopOneHopAway) {
        result.finalTargetX = result.landingX;
        result.finalTargetY = result.landingY;
    }

    return OverworldWildSpawns_TryStartBehaviorHopToPlannedTileCommand(
        state,
        fieldSystem,
        slot,
        profile,
        allowedTile,
        result.landingX,
        result.landingY,
        result.finalTargetX,
        result.finalTargetY);
}

static BOOL OverworldWildSpawns_TryStartRandomBehaviorHopCommand(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    const OverworldWildBehaviorProfile *profile)
{
    const OverworldWildHelperOverlayEntry *helperEntry;
    OverworldWildHelperHopConfig config;
    OverworldWildHelperHopResult result;
    OverworldWildBehaviorHopValidationContext validationContext;
    LocalMapObject *object;
    int objectX;
    int objectY;
    u8 allowedTile;
    u8 spotState;

    if (state == NULL
        || fieldSystem == NULL
        || profile == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !state->spawns[slot].active) {
        return FALSE;
    }

    object = state->spawns[slot].object;
    if (object == NULL || MapObject_IsSingleMovementActive(object)) {
        return FALSE;
    }

    objectX = MapObject_GetCurrentX(object);
    objectY = MapObject_GetCurrentY(object);
    spotState = state->movementSpotStates[slot];
    allowedTile = OverworldWildSpawns_GetAllowedTileForSpotState(
        profile,
        spotState);
    helperEntry = OverworldWildSpawns_GetHelperOverlayEntry();
    if (helperEntry == NULL || helperEntry->pickRandomBehaviorHop == NULL) {
        return FALSE;
    }

    OverworldWildSpawns_BuildBehaviorHopHelperConfig(
        profile,
        spotState,
        objectX,
        objectY,
        0,
        0,
        NULL,
        0,
        FALSE,
        &config);
    validationContext.state = state;
    validationContext.fieldSystem = fieldSystem;
    validationContext.slot = slot;
    validationContext.allowedTile = allowedTile;
    validationContext.blockPlayerUnlessFinal = FALSE;
    if (!helperEntry->pickRandomBehaviorHop(
            &config,
            OverworldWildSpawns_ValidateBehaviorHopLanding,
            &validationContext,
            &result)) {
        return FALSE;
    }

    return OverworldWildSpawns_TryStartBehaviorHopToPlannedTileCommand(
        state,
        fieldSystem,
        slot,
        profile,
        allowedTile,
        result.landingX,
        result.landingY,
        result.finalTargetX,
        result.finalTargetY);
}

static BOOL OverworldWildSpawns_TryStartDirectedBehaviorHopCommand(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    const OverworldWildBehaviorProfile *profile,
    int dx,
    int dy,
    const u8 *directions,
    int directionCount,
    BOOL stopOneHopAway)
{
    LocalMapObject *object;
    int objectX;
    int objectY;
    int targetX;
    int targetY;
    int distance;
    int i;
    int adjacentIndex;
    u8 minDistance;
    u8 maxDistance;
    u8 allowedTile;
    u8 spotState;

    if (state == NULL
        || fieldSystem == NULL
        || profile == NULL
        || directions == NULL
        || directionCount <= 0
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !state->spawns[slot].active) {
        return FALSE;
    }

    object = state->spawns[slot].object;
    if (object == NULL) {
        return FALSE;
    }

    objectX = MapObject_GetCurrentX(object);
    objectY = MapObject_GetCurrentY(object);
    targetX = objectX + dx;
    targetY = objectY + dy;
    spotState = state->movementSpotStates[slot];
    minDistance = OverworldWildSpawns_GetBehaviorHopMinDistance(profile, spotState);
    maxDistance = OverworldWildSpawns_GetBehaviorHopMaxDistance(profile, spotState);
    allowedTile = OverworldWildSpawns_GetAllowedTileForSpotState(
        profile,
        state->movementSpotStates[slot]);

    if (OverworldWildSpawns_TryStartBehaviorHopToPlannedTileCommand(
            state,
            fieldSystem,
            slot,
            profile,
            allowedTile,
            targetX,
            targetY,
            targetX,
            targetY)) {
        return TRUE;
    }

    if (OverworldWildSpawns_IsTileReservedByOtherWild(
            state,
            slot,
            targetX,
            targetY)
        || OverworldWildSpawns_IsTileOccupiedByNonPlayerObject(
            fieldSystem,
            object,
            targetX,
            targetY)) {
        for (adjacentIndex = 0;
             adjacentIndex < (int)NELEMS(sCustomJumpOffsets);
             adjacentIndex++) {
            int adjacentX = targetX + sCustomJumpOffsets[adjacentIndex].dx;
            int adjacentY = targetY + sCustomJumpOffsets[adjacentIndex].dy;

            if (OverworldWildSpawns_TryStartBehaviorHopToPlannedTileCommand(
                    state,
                    fieldSystem,
                    slot,
                    profile,
                    allowedTile,
                    adjacentX,
                    adjacentY,
                    adjacentX,
                    adjacentY)) {
                return TRUE;
            }
        }
    }

    if (!OverworldWildSpawns_IsBehaviorAllowedHopLandingTile(
            state,
            slot,
            fieldSystem,
            allowedTile,
            targetX,
            targetY,
            targetX,
            targetY)
        && (dx != 0 || dy != 0)
        && OverworldWildSpawns_TryGetBehaviorHopVector(
            profile,
            spotState,
            dx,
            dy,
            NULL,
            NULL)) {
        return FALSE;
    }

    if (stopOneHopAway
        && OverworldWildSpawns_TryStartBehaviorHopPlanStepCommand(
            state,
            fieldSystem,
            slot,
            profile,
            allowedTile,
            targetX,
            targetY,
            directions,
            directionCount,
            TRUE)) {
        return TRUE;
    }

    if (OverworldWildSpawns_TryStartBehaviorHopPlanStepCommand(
            state,
            fieldSystem,
            slot,
            profile,
            allowedTile,
            targetX,
            targetY,
            directions,
            directionCount,
            FALSE)) {
        return TRUE;
    }

    if (OverworldWildSpawns_SelectStateMovementByte(
            spotState,
            profile->hopAllowNonCardinal,
            profile->attentiveHopAllowNonCardinal,
            profile->tiredHopAllowNonCardinal) == OW_WILD_BEHAVIOR_BOOL_YES
        && dx != 0
        && dy != 0) {
        int stepX = dx > 0 ? 1 : -1;
        int stepY = dy > 0 ? 1 : -1;

        for (distance = maxDistance; distance >= minDistance; distance--) {
            if (OverworldWildSpawns_TryStartBehaviorHopToPlannedTileCommand(
                    state,
                    fieldSystem,
                    slot,
                    profile,
                    allowedTile,
                    objectX + stepX * distance,
                    objectY + stepY * distance,
                    objectX + stepX * distance,
                    objectY + stepY * distance)) {
                return TRUE;
            }
        }
    }

    for (i = 0; i < directionCount; i++) {
        u8 direction = directions[i];
        int stepX = OverworldWildSpawns_MovementDirectionDeltaX(direction);
        int stepY = OverworldWildSpawns_MovementDirectionDeltaY(direction);

        for (distance = maxDistance; distance >= minDistance; distance--) {
            if (OverworldWildSpawns_TryStartBehaviorHopToPlannedTileCommand(
                    state,
                    fieldSystem,
                    slot,
                    profile,
                    allowedTile,
                    objectX + stepX * distance,
                    objectY + stepY * distance,
                    objectX + stepX * distance,
                    objectY + stepY * distance)) {
                return TRUE;
            }
        }
    }

    return FALSE;
}

static BOOL OverworldWildSpawns_IsFishingShoreTile(FieldSystem *fieldSystem, int x, int y)
{
    return x >= 0
        && y >= 0
        && OverworldWildSpawns_IsSurfBehavior(GetMetatileBehaviorAt(fieldSystem, x, y))
        && !OverworldWildSpawns_IsTileOccupiedByObject(fieldSystem, x, y);
}

static BOOL OverworldWildSpawns_IsSpawnRunStartTile(
    FieldSystem *fieldSystem,
    OverworldWildSpawnTerrain terrain,
    int x,
    int y)
{
    OverworldWildSpawnTerrain resolvedTerrain;

    if (fieldSystem == NULL || x < 0 || y < 0) {
        return FALSE;
    }

    switch (terrain) {
    case OW_WILD_SPAWN_TERRAIN_HEADBUTT:
        return OverworldWildSpawns_IsWalkableLandTile(fieldSystem, x, y);
    case OW_WILD_SPAWN_TERRAIN_FISHING:
        return OverworldWildSpawns_IsFishingShoreTile(fieldSystem, x, y);
    case OW_WILD_SPAWN_TERRAIN_SURF:
    case OW_WILD_SPAWN_TERRAIN_LAND:
    default:
        if (OverworldWildSpawns_IsTileOccupiedByObject(fieldSystem, x, y)
            || IsMetatileBlockedAt(fieldSystem, x, y)
            || !OverworldWildSpawns_TryGetSpawnTerrain(fieldSystem, x, y, &resolvedTerrain)) {
            return FALSE;
        }
        return resolvedTerrain == terrain;
    }
}

static BOOL OverworldWildSpawns_TryPickSpawnRunStart(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    OverworldWildSpawnTerrain terrain,
    int targetX,
    int targetY,
    int *startX,
    int *startY)
{
    int playerX;
    int playerY;
    int baseDx;
    int baseDy;
    int attempt;
    int scanDistance;
    int scanDirection;
    int scanLateral;

    if (state == NULL
        || fieldSystem == NULL
        || fieldSystem->playerAvatar == NULL
        || startX == NULL
        || startY == NULL) {
        return FALSE;
    }

    playerX = GetPlayerXCoord(fieldSystem->playerAvatar);
    playerY = GetPlayerYCoord(fieldSystem->playerAvatar);
    baseDx = targetX - playerX;
    baseDy = targetY - playerY;
    if (OverworldWildSpawns_Abs(baseDx) >= OverworldWildSpawns_Abs(baseDy)) {
        baseDx = baseDx >= 0 ? 1 : -1;
        baseDy = 0;
    } else {
        baseDy = baseDy >= 0 ? 1 : -1;
        baseDx = 0;
    }

    for (attempt = 0; attempt < OW_WILD_SPAWNER_SPAWN_RUN_START_ATTEMPTS; attempt++) {
        int distance = OW_WILD_SPAWNER_SPAWN_RUN_OFFSCREEN_MIN_DISTANCE
            + (gf_rand()
               % (OW_WILD_SPAWNER_SPAWN_RUN_OFFSCREEN_MAX_DISTANCE
                  - OW_WILD_SPAWNER_SPAWN_RUN_OFFSCREEN_MIN_DISTANCE
                  + 1));
        int lateral = (int)(gf_rand() % (OW_WILD_SPAWNER_SPAWN_RUN_LATERAL_SPREAD * 2 + 1))
            - OW_WILD_SPAWNER_SPAWN_RUN_LATERAL_SPREAD;
        int candidateX = playerX + baseDx * distance + baseDy * lateral;
        int candidateY = playerY + baseDy * distance + baseDx * lateral;

        if (OverworldWildSpawns_Max(
                OverworldWildSpawns_Abs(candidateX - targetX),
                OverworldWildSpawns_Abs(candidateY - targetY))
                < OW_WILD_SPAWNER_SPAWN_RUN_MIN_TRAVEL_TILES
            || !OverworldWildSpawns_IsSpawnRunStartTile(fieldSystem, terrain, candidateX, candidateY)
            || OverworldWildSpawns_IsNearActiveSpawn(state, candidateX, candidateY, 0)) {
            continue;
        }

        *startX = candidateX;
        *startY = candidateY;
        return TRUE;
    }

    for (scanDistance = OW_WILD_DESPAWN_DISTANCE;
         scanDistance >= OW_WILD_SPAWNER_SPAWN_RUN_MIN_TRAVEL_TILES;
         scanDistance--) {
        for (scanDirection = 0; scanDirection < 4; scanDirection++) {
            int scanDx = 0;
            int scanDy = 0;
            int lateralDx;
            int lateralDy;

            if (scanDirection == 0) {
                scanDy = -1;
            } else if (scanDirection == 1) {
                scanDx = 1;
            } else if (scanDirection == 2) {
                scanDy = 1;
            } else {
                scanDx = -1;
            }
            lateralDx = scanDy;
            lateralDy = scanDx;

            for (scanLateral = -OW_WILD_SPAWNER_SPAWN_RUN_LATERAL_SPREAD;
                 scanLateral <= OW_WILD_SPAWNER_SPAWN_RUN_LATERAL_SPREAD;
                 scanLateral++) {
                int candidateX = targetX + scanDx * scanDistance + lateralDx * scanLateral;
                int candidateY = targetY + scanDy * scanDistance + lateralDy * scanLateral;

                if (OverworldWildSpawns_DistanceFromPlayer(fieldSystem, candidateX, candidateY)
                        > OW_WILD_DESPAWN_DISTANCE
                    || !OverworldWildSpawns_IsSpawnRunStartTile(fieldSystem, terrain, candidateX, candidateY)
                    || OverworldWildSpawns_IsNearActiveSpawn(state, candidateX, candidateY, 0)) {
                    continue;
                }

                *startX = candidateX;
                *startY = candidateY;
                return TRUE;
            }
        }
    }

    return FALSE;
}

static void OverworldWildSpawns_ClearSpawnRunState(OverworldWildSpawnState *state, int slot)
{
    if (state == NULL || slot < 0 || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    state->movementSpawnRunTargetX[slot] = 0;
    state->movementSpawnRunTargetY[slot] = 0;
    state->movementSpawnRunActive[slot] = FALSE;
}

static void OverworldWildSpawns_SetSpawnRunState(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    int targetX,
    int targetY)
{
    state->movementFieldSystem = fieldSystem;
    state->movementSpawnRunTargetX[slot] = (s16)targetX;
    state->movementSpawnRunTargetY[slot] = (s16)targetY;
    state->movementSpawnRunActive[slot] = TRUE;
    state->movementCooldowns[slot] = 0;
}

static void OverworldWildSpawns_StartSpawnRun(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    int targetX,
    int targetY)
{
    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !state->spawns[slot].active
        || state->spawns[slot].object == NULL) {
        return;
    }

    OverworldWildSpawns_SetSpawnRunState(state, fieldSystem, slot, targetX, targetY);
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
    OverworldWildSpawns_EnsureFrameMovementTask(state, fieldSystem);
#endif
}

static void OverworldWildSpawns_SetPostSpawnStartupCooldown(
    OverworldWildSpawnState *state,
    int slot)
{
    if (state == NULL || slot < 0 || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    if (state->movementSpotStates[slot] == OW_WILD_SPAWNER_SPOT_STATE_ACTIVE) {
        state->movementCooldowns[slot] = OW_WILD_SPAWNER_MOVEMENT_DECISION_COOLDOWN;
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
        if ((OW_WILD_RUNTIME(state)->movementFrameDrivenActiveMask
                & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot)) != 0) {
            OverworldWildSpawns_EnsureFrameMovementTask(state, state->movementFieldSystem);
        }
#endif
        return;
    }

    state->movementCooldowns[slot] = OW_WILD_SPAWNER_DEFAULT_MOVEMENT_PAUSE_FRAMES;
}

static BOOL OverworldWildSpawns_TryStartSpawnRunStep(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot)
{
    LocalMapObject *object;
    int objectX;
    int objectY;
    int targetX;
    int targetY;
    u8 directions[OW_WILD_SPAWNER_MOVEMENT_MAX_DIRECTIONS];
    int directionCount;

    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !state->movementSpawnRunActive[slot]
        || !state->spawns[slot].active) {
        return FALSE;
    }

    object = state->spawns[slot].object;
    if (object == NULL) {
        OverworldWildSpawns_ClearSpawnRunState(state, slot);
        return TRUE;
    }
    if (MapObject_IsSingleMovementActive(object)
        || OverworldWildSpawns_IsMovementSlotInProgress(state, slot)) {
        return TRUE;
    }
    objectX = MapObject_GetCurrentX(object);
    objectY = MapObject_GetCurrentY(object);
    targetX = state->movementSpawnRunTargetX[slot];
    targetY = state->movementSpawnRunTargetY[slot];
    if (objectX == targetX && objectY == targetY) {
        OverworldWildSpawns_SetObjectLandingTile(fieldSystem, object, targetX, targetY);
        OverworldWildSpawns_RefreshCanopyHopperVisualStateAtLanding(
            state,
            fieldSystem,
            slot,
            object);
        OverworldWildSpawns_ClearSpawnRunState(state, slot);
        OverworldWildSpawns_SetPostSpawnStartupCooldown(state, slot);
        return TRUE;
    }

    directionCount = OverworldWildSpawns_DiagnosticBuildDirections(
        targetX - objectX,
        targetY - objectY,
        directions);
    if (directionCount > 1 && (gf_rand() & 1) != 0) {
        u8 swap = directions[0];
        directions[0] = directions[1];
        directions[1] = swap;
    }
    if (directionCount == 0
        || !OverworldWildSpawns_TryStartSpawnerMovementCommand(
            state,
            fieldSystem,
            slot,
            directions,
            directionCount,
            NULL,
            NULL)) {
        OverworldWildSpawns_SetObjectLandingTile(fieldSystem, object, targetX, targetY);
        OverworldWildSpawns_RefreshCanopyHopperVisualStateAtLanding(
            state,
            fieldSystem,
            slot,
            object);
        OverworldWildSpawns_ClearSpawnRunState(state, slot);
        OverworldWildSpawns_SetPostSpawnStartupCooldown(state, slot);
        return TRUE;
    }
    return TRUE;
}

static BOOL OverworldWildSpawns_HandleFinishedSpawnRunMovementCommand(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot)
{
    LocalMapObject *object;
    int targetX;
    int targetY;
    BOOL customJumpWasActive;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !state->movementSpawnRunActive[slot]) {
        return FALSE;
    }

    object = state->spawns[slot].object;
    if (object == NULL || !state->spawns[slot].active) {
        OverworldWildSpawns_ClearSpawnRunState(state, slot);
        return TRUE;
    }

    OverworldWildSpawns_RecordFinishedMovementHistory(state, slot);
    targetX = state->movementSpawnRunTargetX[slot];
    targetY = state->movementSpawnRunTargetY[slot];
    customJumpWasActive = OW_WILD_RUNTIME(state)->movementCustomJumpPrepActive[slot]
        || OW_WILD_RUNTIME(state)->movementCustomJumpActive[slot];
    if (customJumpWasActive) {
        if (OW_WILD_RUNTIME(state)->movementCustomJumpPrepActive[slot]) {
            (void)OverworldWildSpawns_RunImmediateCanopyMovementCommand(
                object,
                OW_WILD_SPAWNER_CANOPY_HOPPER_FREEZE_COMMAND);
            (void)OverworldWildSpawns_RunImmediateCanopyMovementCommand(
                object,
                OW_WILD_SPAWNER_CANOPY_HOPPER_PARTNER_RESTORE_COMMAND);
            OW_WILD_RUNTIME(state)->movementCustomJumpPrepActive[slot] = FALSE;
        }
        OverworldWildSpawns_SetObjectLandingTile(fieldSystem, object, targetX, targetY);
        OverworldWildSpawns_ClearCustomJump(state, slot);
        MapObject_ClearBits(object, BIT_VANISH);
    }
    if ((int)MapObject_GetCurrentX(object) == targetX
        && (int)MapObject_GetCurrentY(object) == targetY) {
        object = OverworldWildSpawns_NormalizeSpawnHopLandingObject(
            state,
            fieldSystem,
            slot,
            object,
            targetX,
            targetY);
        OverworldWildSpawns_RefreshCanopyHopperVisualStateAtLanding(
            state,
            fieldSystem,
            slot,
            object);
        state->movementPendingDirections[slot] = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_NONE;
        state->movementPendingDistances[slot] = 0;
        state->movementStagedHopDistances[slot] = 0;
        OverworldWildSpawns_ClearSpawnRunState(state, slot);
        OverworldWildSpawns_SetPostSpawnStartupCooldown(state, slot);
    } else {
        state->movementCooldowns[slot] = 0;
    }
    return TRUE;
}

#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_PARAM_TICK
static BOOL OverworldWildSpawns_TryGetCanopyHopperPlayerFrontTarget(
    FieldSystem *fieldSystem,
    int *targetX,
    int *targetY)
{
    LocalMapObject *playerObject;
    u8 facing;
    int x;
    int y;

    if (fieldSystem == NULL
        || fieldSystem->playerAvatar == NULL
        || fieldSystem->playerAvatar->mapObject == NULL
        || targetX == NULL
        || targetY == NULL) {
        return FALSE;
    }

    playerObject = fieldSystem->playerAvatar->mapObject;
    if (!OverworldWildSpawns_IsCurrentMapObject(fieldSystem, playerObject)
        || (playerObject->flags & MAPOBJECTFLAG_ACTIVE) == 0) {
        return FALSE;
    }

    facing = playerObject->curFacing;
    if (facing > OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT) {
        return FALSE;
    }

    x = GetPlayerXCoord(fieldSystem->playerAvatar)
        + OverworldWildSpawns_MovementDirectionDeltaX(facing);
    y = GetPlayerYCoord(fieldSystem->playerAvatar)
        + OverworldWildSpawns_MovementDirectionDeltaY(facing);
    if (!OverworldWildSpawns_IsWalkableLandTile(fieldSystem, x, y)) {
        return FALSE;
    }

    *targetX = x;
    *targetY = y;
    return TRUE;
}

static BOOL OverworldWildSpawns_IsHeadbuttMapTile(FieldSystem *fieldSystem, int x, int y)
{
    if (fieldSystem == NULL || x < 0 || y < 0) {
        return FALSE;
    }

    return GetMetatileBehaviorAt(fieldSystem, x, y) == OW_WILD_TILE_HEADBUTT;
}

static BOOL OverworldWildSpawns_IsLandMapTile(FieldSystem *fieldSystem, int x, int y)
{
    u8 behavior;

    if (fieldSystem == NULL || x < 0 || y < 0) {
        return FALSE;
    }

    behavior = GetMetatileBehaviorAt(fieldSystem, x, y);
    if (behavior == OW_WILD_TILE_HEADBUTT || OverworldWildSpawns_IsSurfBehavior(behavior)) {
        return FALSE;
    }

    return !IsMetatileBlockedAt(fieldSystem, x, y);
}

#if OW_WILD_SPAWNER_CANOPY_SHIFT_SOUTH_LAND_ANCHORS
static BOOL OverworldWildSpawns_IsHeadbuttTreeTopSouthLandAnchor(FieldSystem *fieldSystem, int x, int y)
{
    return OverworldWildSpawns_IsHeadbuttMapTile(fieldSystem, x, y)
        && OverworldWildSpawns_IsLandMapTile(fieldSystem, x, y + 1);
}

static BOOL OverworldWildSpawns_IsShiftedSouthLandTreeTop(FieldSystem *fieldSystem, int x, int y)
{
    return OverworldWildSpawns_IsHeadbuttMapTile(fieldSystem, x, y)
        && OverworldWildSpawns_IsHeadbuttTreeTopSouthLandAnchor(
            fieldSystem,
            x,
            y + OW_WILD_SPAWNER_CANOPY_SOUTH_LAND_SHIFT_TILES);
}

static BOOL OverworldWildSpawns_TryGetShiftedSouthLandTreeTop(
    FieldSystem *fieldSystem,
    int anchorX,
    int anchorY,
    int *targetX,
    int *targetY)
{
    int shiftedY;

    if (targetX == NULL
        || targetY == NULL
        || !OverworldWildSpawns_IsHeadbuttTreeTopSouthLandAnchor(fieldSystem, anchorX, anchorY)) {
        return FALSE;
    }

    shiftedY = anchorY - OW_WILD_SPAWNER_CANOPY_SOUTH_LAND_SHIFT_TILES;
    if (!OverworldWildSpawns_IsShiftedSouthLandTreeTop(fieldSystem, anchorX, shiftedY)) {
        return FALSE;
    }

    *targetX = anchorX;
    *targetY = shiftedY;
    return TRUE;
}

static BOOL OverworldWildSpawns_IsShiftedSouthLandAnchorHop(
    FieldSystem *fieldSystem,
    int startX,
    int startY,
    int targetX,
    int targetY)
{
    int shiftedX;
    int shiftedY;

    return OverworldWildSpawns_TryGetShiftedSouthLandTreeTop(
            fieldSystem,
            startX,
            startY,
            &shiftedX,
            &shiftedY)
        && shiftedX == targetX
        && shiftedY == targetY;
}
#endif

#if OW_WILD_SPAWNER_CANOPY_SHIFT_NORTH_LAND_ANCHORS
static BOOL OverworldWildSpawns_IsHeadbuttTreeTopNorthLandAnchor(FieldSystem *fieldSystem, int x, int y)
{
    return OverworldWildSpawns_IsHeadbuttMapTile(fieldSystem, x, y)
        && OverworldWildSpawns_IsLandMapTile(fieldSystem, x, y - 1);
}

static BOOL OverworldWildSpawns_IsShiftedNorthLandTreeTop(FieldSystem *fieldSystem, int x, int y)
{
    return OverworldWildSpawns_IsHeadbuttMapTile(fieldSystem, x, y)
        && OverworldWildSpawns_IsHeadbuttTreeTopNorthLandAnchor(
            fieldSystem,
            x,
            y - OW_WILD_SPAWNER_CANOPY_NORTH_LAND_SHIFT_TILES);
}

static BOOL OverworldWildSpawns_TryGetShiftedNorthLandTreeTop(
    FieldSystem *fieldSystem,
    int anchorX,
    int anchorY,
    int *targetX,
    int *targetY)
{
    int shiftedY;

    if (targetX == NULL
        || targetY == NULL
        || !OverworldWildSpawns_IsHeadbuttTreeTopNorthLandAnchor(fieldSystem, anchorX, anchorY)) {
        return FALSE;
    }

    shiftedY = anchorY + OW_WILD_SPAWNER_CANOPY_NORTH_LAND_SHIFT_TILES;
    if (!OverworldWildSpawns_IsShiftedNorthLandTreeTop(fieldSystem, anchorX, shiftedY)) {
        return FALSE;
    }

    *targetX = anchorX;
    *targetY = shiftedY;
    return TRUE;
}

static BOOL OverworldWildSpawns_IsShiftedNorthLandAnchorHop(
    FieldSystem *fieldSystem,
    int startX,
    int startY,
    int targetX,
    int targetY)
{
    int shiftedX;
    int shiftedY;

    return OverworldWildSpawns_TryGetShiftedNorthLandTreeTop(
            fieldSystem,
            startX,
            startY,
            &shiftedX,
            &shiftedY)
        && shiftedX == targetX
        && shiftedY == targetY;
}
#endif

static BOOL OverworldWildSpawns_IsHeadbuttTreeTopSurfaceTile(FieldSystem *fieldSystem, int x, int y)
{
    if (!OverworldWildSpawns_IsHeadbuttMapTile(fieldSystem, x, y)) {
        return FALSE;
    }

#if OW_WILD_SPAWNER_CANOPY_SHIFT_SOUTH_LAND_ANCHORS
    if (OverworldWildSpawns_IsShiftedSouthLandTreeTop(fieldSystem, x, y)) {
        return TRUE;
    }
#endif

#if OW_WILD_SPAWNER_CANOPY_SHIFT_NORTH_LAND_ANCHORS
    if (OverworldWildSpawns_IsShiftedNorthLandTreeTop(fieldSystem, x, y)) {
        return TRUE;
    }
#endif

#if OW_WILD_SPAWNER_CANOPY_SHIFT_SOUTH_LAND_ANCHORS
    if (OverworldWildSpawns_IsHeadbuttTreeTopSouthLandAnchor(fieldSystem, x, y)) {
        return FALSE;
    }
#endif

#if OW_WILD_SPAWNER_CANOPY_SHIFT_NORTH_LAND_ANCHORS
    if (OverworldWildSpawns_IsHeadbuttTreeTopNorthLandAnchor(fieldSystem, x, y)) {
        return FALSE;
    }
#endif

    /* This is the canonical locator used by both spawn and movement scans. */
    if (OverworldWildSpawns_IsHeadbuttMapTile(fieldSystem, x, y + 1)
        && OverworldWildSpawns_IsLandMapTile(fieldSystem, x, y + 2)) {
        return TRUE;
    }
    if (OverworldWildSpawns_IsHeadbuttMapTile(fieldSystem, x, y - 1)
        && OverworldWildSpawns_IsLandMapTile(fieldSystem, x, y - 2)) {
        return TRUE;
    }
    if (OverworldWildSpawns_IsLandMapTile(fieldSystem, x, y + 1)
        || OverworldWildSpawns_IsLandMapTile(fieldSystem, x, y - 1)) {
        return FALSE;
    }

    return OverworldWildSpawns_IsLandMapTile(fieldSystem, x - 1, y)
        || OverworldWildSpawns_IsLandMapTile(fieldSystem, x + 1, y);
}

static BOOL OverworldWildSpawns_InitHeadbuttTreeTopScan(
    FieldSystem *fieldSystem,
    OverworldWildHeadbuttTreeTopScan *scan,
    int centerX,
    int centerY,
    int radius)
{
    if (fieldSystem == NULL
        || scan == NULL
        || centerX < 0
        || centerY < 0
        || radius < 0) {
        return FALSE;
    }

    memset(scan, 0, sizeof(*scan));
    scan->centerX = centerX;
    scan->centerY = centerY;
    scan->radius = radius;
    scan->minX = centerX - radius;
    scan->maxX = centerX + radius;
    scan->maxY = centerY + radius;
    scan->nextX = scan->minX;
    scan->nextY = centerY - radius;
    if (scan->minX < 0) {
        scan->minX = 0;
    }
    if (scan->nextX < 0) {
        scan->nextX = 0;
    }
    if (scan->nextY < 0) {
        scan->nextY = 0;
    }
    return TRUE;
}

static BOOL OverworldWildSpawns_NextHeadbuttTreeTopCandidate(
    FieldSystem *fieldSystem,
    OverworldWildHeadbuttTreeTopScan *scan,
    int *x,
    int *y)
{
    if (fieldSystem == NULL || scan == NULL || x == NULL || y == NULL) {
        return FALSE;
    }

    while (scan->nextY <= scan->maxY
        && scan->emitted < OW_WILD_HEADBUTT_TREE_TOPS_MAX_LOCATIONS) {
        int candidateX = scan->nextX;
        int candidateY = scan->nextY;

        scan->nextX++;
        if (scan->nextX > scan->maxX) {
            scan->nextX = scan->minX;
            scan->nextY++;
        }
        if (candidateX < 0 || candidateY < 0) {
            continue;
        }
        if (OverworldWildSpawns_Max(
                OverworldWildSpawns_Abs(candidateX - scan->centerX),
                OverworldWildSpawns_Abs(candidateY - scan->centerY)) > scan->radius) {
            continue;
        }
        if (!OverworldWildSpawns_IsHeadbuttTreeTopLocation(fieldSystem, candidateX, candidateY)) {
            continue;
        }

        scan->emitted++;
        *x = candidateX;
        *y = candidateY;
        return TRUE;
    }

    return FALSE;
}

static BOOL OverworldWildSpawns_IsHeadbuttTreeTopLocation(FieldSystem *fieldSystem, int x, int y)
{
    return OverworldWildSpawns_IsHeadbuttTreeTopSurfaceTile(fieldSystem, x, y);
}

static BOOL OverworldWildSpawns_IsPlayerTile(FieldSystem *fieldSystem, int x, int y)
{
    return fieldSystem != NULL
        && fieldSystem->playerAvatar != NULL
        && x == GetPlayerXCoord(fieldSystem->playerAvatar)
        && y == GetPlayerYCoord(fieldSystem->playerAvatar);
}

static BOOL OverworldWildSpawns_IsPlayerFrontTile(FieldSystem *fieldSystem, int x, int y)
{
    LocalMapObject *playerObject;
    u8 playerFacing;

    if (fieldSystem == NULL || fieldSystem->playerAvatar == NULL) {
        return FALSE;
    }

    playerObject = fieldSystem->playerAvatar->mapObject;
    if (playerObject == NULL
        || !OverworldWildSpawns_IsCurrentMapObject(fieldSystem, playerObject)
        || (playerObject->flags & MAPOBJECTFLAG_ACTIVE) == 0) {
        return FALSE;
    }

    playerFacing = playerObject->curFacing;
    if (playerFacing > OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT) {
        return FALSE;
    }

    return x == (int)MapObject_GetCurrentX(playerObject)
            + OverworldWildSpawns_MovementDirectionDeltaX(playerFacing)
        && y == (int)MapObject_GetCurrentY(playerObject)
            + OverworldWildSpawns_MovementDirectionDeltaY(playerFacing);
}

static BOOL OverworldWildSpawns_DoesAllowedTileMatch(
    FieldSystem *fieldSystem,
    u8 allowedTile,
    u8 behavior,
    int x,
    int y,
    BOOL *allowPlayerOccupancy)
{
    switch (allowedTile) {
    case OW_WILD_BEHAVIOR_ALLOWED_TILE_WATER:
        return OverworldWildSpawns_IsSurfBehavior(behavior);
    case OW_WILD_BEHAVIOR_ALLOWED_TILE_CANOPY:
        return OverworldWildSpawns_IsHeadbuttTreeTopLocation(fieldSystem, x, y);
    case OW_WILD_BEHAVIOR_ALLOWED_TILE_GRASS:
        return behavior == OW_WILD_TILE_ENCOUNTER_GRASS
            || behavior == OW_WILD_TILE_LONG_GRASS;
    case OW_WILD_BEHAVIOR_ALLOWED_TILE_PLAYER:
        if (!OverworldWildSpawns_IsPlayerTile(fieldSystem, x, y)) {
            return FALSE;
        }
        if (allowPlayerOccupancy != NULL) {
            *allowPlayerOccupancy = TRUE;
        }
        return TRUE;
    case OW_WILD_BEHAVIOR_ALLOWED_TILE_PLAYER_FRONT:
        return OverworldWildSpawns_IsPlayerFrontTile(fieldSystem, x, y);
    case OW_WILD_BEHAVIOR_ALLOWED_TILE_NONE:
        return FALSE;
    case OW_WILD_BEHAVIOR_ALLOWED_TILE_LAND:
    default:
        return OverworldWildSpawns_IsLandMapTile(fieldSystem, x, y);
    }
}

static BOOL OverworldWildSpawns_IsBehaviorAllowedMovementTile(
    FieldSystem *fieldSystem,
    u8 allowedTile,
    int x,
    int y)
{
    return OverworldWildSpawns_IsBehaviorAllowedHopLandingTile(
        NULL,
        -1,
        fieldSystem,
        allowedTile,
        x,
        y,
        -1,
        -1);
}

static BOOL OverworldWildSpawns_IsBehaviorAllowedHopLandingTile(
    OverworldWildSpawnState *state,
    int slot,
    FieldSystem *fieldSystem,
    u8 allowedTile,
    int x,
    int y,
    int finalTargetX,
    int finalTargetY)
{
    u8 behavior;
    u8 primaryAllowedTile;
    u8 secondaryAllowedTile;
    BOOL tileAllowed = FALSE;
    BOOL allowPlayerOccupancy = FALSE;
    LocalMapObject *movingObject = NULL;

    if (fieldSystem == NULL || x < 0 || y < 0) {
        return FALSE;
    }

    primaryAllowedTile = OW_WILD_BEHAVIOR_ALLOWED_TILE_PRIMARY(allowedTile);
    secondaryAllowedTile = OW_WILD_BEHAVIOR_ALLOWED_TILE_SECONDARY(allowedTile);
    behavior = GetMetatileBehaviorAt(fieldSystem, x, y);
    tileAllowed = OverworldWildSpawns_DoesAllowedTileMatch(
        fieldSystem,
        primaryAllowedTile,
        behavior,
        x,
        y,
        &allowPlayerOccupancy);
    tileAllowed = OverworldWildSpawns_DoesAllowedTileMatch(
            fieldSystem,
            secondaryAllowedTile,
            behavior,
            x,
            y,
            &allowPlayerOccupancy)
        || tileAllowed;

    if (!tileAllowed
        || OverworldWildSpawns_IsTileReservedByOtherWild(state, slot, x, y)) {
        return FALSE;
    }
    if (state != NULL
        && slot >= 0
        && slot < OW_WILD_MAX_SPAWNS
        && state->spawns[slot].active) {
        movingObject = state->spawns[slot].object;
    }
    if (allowPlayerOccupancy
        || (x == finalTargetX
            && y == finalTargetY
            && OverworldWildSpawns_IsPlayerTile(fieldSystem, x, y))) {
        return !OverworldWildSpawns_IsTileOccupiedByNonPlayerObject(
            fieldSystem,
            movingObject,
            x,
            y);
    }
    return !OverworldWildSpawns_IsTileOccupiedByObject(fieldSystem, x, y);
}

static BOOL OverworldWildSpawns_IsMankeyOnHeadbuttTreeTopTile(FieldSystem *fieldSystem, int x, int y)
{
    return OverworldWildSpawns_IsHeadbuttTreeTopLocation(fieldSystem, x, y);
}

static BOOL OverworldWildSpawns_IsMankeyOnHeadbuttTreeTopTileCached(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    int x,
    int y)
{
    BOOL result;

    if (state == NULL
        || fieldSystem == NULL
        || fieldSystem->location == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || x < 0
        || y < 0
        || !state->spawns[slot].active
        || !OverworldWildSpawns_IsCanopyHopperTreeTopSlot(state, slot)) {
        return FALSE;
    }

    if (OW_WILD_RUNTIME(state)->movementMankeyTreeTopCacheValid[slot]
        && OW_WILD_RUNTIME(state)->movementMankeyTreeTopCacheMapId[slot] == fieldSystem->location->mapId
        && OW_WILD_RUNTIME(state)->movementMankeyTreeTopCacheX[slot] == x
        && OW_WILD_RUNTIME(state)->movementMankeyTreeTopCacheY[slot] == y) {
        return OW_WILD_RUNTIME(state)->movementMankeyTreeTopCacheResult[slot];
    }

    result = OverworldWildSpawns_IsMankeyOnHeadbuttTreeTopTile(fieldSystem, x, y);
    OW_WILD_RUNTIME(state)->movementMankeyTreeTopCacheValid[slot] = TRUE;
    OW_WILD_RUNTIME(state)->movementMankeyTreeTopCacheResult[slot] = result;
    OW_WILD_RUNTIME(state)->movementMankeyTreeTopCacheX[slot] = (s16)x;
    OW_WILD_RUNTIME(state)->movementMankeyTreeTopCacheY[slot] = (s16)y;
    OW_WILD_RUNTIME(state)->movementMankeyTreeTopCacheMapId[slot] = fieldSystem->location->mapId;
    return result;
}

#if OW_WILD_SPAWNER_MANKEY_TREE_TOP_LATE_DRAW_EFFECT_ENABLED
static BOOL OverworldWildSpawns_IsMankeyTreeTopLateDrawEffectValid(
    const OverworldWildMankeyTreeTopLateDrawEffectWork *work)
{
    int x;
    int y;

    if (work == NULL
        || work->state == NULL
        || work->fieldSystem == NULL
        || work->object == NULL
        || work->slot < 0
        || work->slot >= OW_WILD_MAX_SPAWNS
        || !OverworldWildSpawns_IsMovementFieldContextCurrent(work->state, work->fieldSystem)
        || !OverworldWildSpawns_IsCurrentMapObject(work->fieldSystem, work->object)
        || !work->state->spawns[work->slot].active
        || !OverworldWildSpawns_IsCanopyHopperTreeTopSlot(work->state, work->slot)
        || work->state->spawns[work->slot].object != work->object
        || !OW_WILD_RUNTIME(state)->movementMankeyTreeTopSettled[work->slot]) {
        return FALSE;
    }

    x = MapObject_GetCurrentX(work->object);
    y = MapObject_GetCurrentY(work->object);
    return OverworldWildSpawns_IsMankeyOnHeadbuttTreeTopTileCached(
        work->state,
        work->fieldSystem,
        work->slot,
        x,
        y);
}

static BOOL OverworldWildSpawns_MankeyTreeTopLateDrawEffectInit(void *effect, void *work)
{
    const OverworldWildMankeyTreeTopLateDrawEffectInit *init;
    OverworldWildMankeyTreeTopLateDrawEffectWork *effectWork = work;

    (void)effect;
    if (effectWork == NULL) {
        return FALSE;
    }

    init = FieldEffect_GetInitData(effect);
    if (init == NULL
        || init->slot < 0
        || init->slot >= OW_WILD_MAX_SPAWNS
        || init->state == NULL
        || init->fieldSystem == NULL
        || init->object == NULL) {
        effectWork->state = NULL;
        effectWork->fieldSystem = NULL;
        effectWork->object = NULL;
        effectWork->slot = -1;
        return FALSE;
    }

    effectWork->state = init->state;
    effectWork->fieldSystem = init->fieldSystem;
    effectWork->object = init->object;
    effectWork->slot = init->slot;
    effectWork->markerTimer = OW_WILD_SPAWNER_MANKEY_TREE_TOP_EFFECT_MARKER_FRAMES;
    return TRUE;
}

static void OverworldWildSpawns_MankeyTreeTopLateDrawEffectDestroy(void *effect, void *work)
{
    OverworldWildMankeyTreeTopLateDrawEffectWork *effectWork = work;
    int slot;

    (void)effect;
    if (effectWork == NULL) {
        return;
    }

    slot = effectWork->slot;
    if (slot >= 0
        && slot < OW_WILD_MAX_SPAWNS
        && sOverworldWildMankeyTreeTopLateDrawEffects[slot] == effect) {
        sOverworldWildMankeyTreeTopLateDrawEffects[slot] = NULL;
    }

    effectWork->state = NULL;
    effectWork->fieldSystem = NULL;
    effectWork->object = NULL;
    effectWork->slot = -1;
    effectWork->markerTimer = 0;
}

static void OverworldWildSpawns_MankeyTreeTopLateDrawEffectUpdate(void *effect, void *work)
{
    OverworldWildMankeyTreeTopLateDrawEffectWork *effectWork = work;

    if (!OverworldWildSpawns_IsMankeyTreeTopLateDrawEffectValid(effectWork)) {
        if (effectWork != NULL
            && effectWork->slot >= 0
            && effectWork->slot < OW_WILD_MAX_SPAWNS
            && sOverworldWildMankeyTreeTopLateDrawEffects[effectWork->slot] == effect) {
            sOverworldWildMankeyTreeTopLateDrawEffects[effectWork->slot] = NULL;
        }
        ov01_021F1640(effect);
        return;
    }

    effectWork->markerTimer++;
    if (effectWork->markerTimer >= OW_WILD_SPAWNER_MANKEY_TREE_TOP_EFFECT_MARKER_FRAMES) {
        effectWork->markerTimer = 0;
        OverworldWildSpawns_ShowBubble(
            effectWork->object,
            OW_WILD_SPAWNER_MANKEY_TREE_TOP_EFFECT_MARKER_ID);
    }
}

static void OverworldWildSpawns_MankeyTreeTopLateDrawEffectRender(void *effect, void *work)
{
    OverworldWildMankeyTreeTopLateDrawEffectWork *effectWork = work;

    (void)effect;
    if (!OverworldWildSpawns_IsMankeyTreeTopLateDrawEffectValid(effectWork)) {
        return;
    }

    (void)effectWork;
}

static void OverworldWildSpawns_ClearMankeyTreeTopLateDrawEffect(int slot)
{
    void *effect;

    if (slot < 0 || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    effect = sOverworldWildMankeyTreeTopLateDrawEffects[slot];
    sOverworldWildMankeyTreeTopLateDrawEffects[slot] = NULL;
    if (effect != NULL) {
        ov01_021F1640(effect);
    }
}
#else
static void OverworldWildSpawns_ClearMankeyTreeTopLateDrawEffect(int slot)
{
    (void)slot;
}
#endif

static void OverworldWildSpawns_EnsureMankeyTreeTopLateDrawEffect(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    LocalMapObject *object)
{
#if !OW_WILD_SPAWNER_MANKEY_TREE_TOP_LATE_DRAW_EFFECT_ENABLED
    (void)state;
    (void)fieldSystem;
    (void)slot;
    (void)object;
#else
    OverworldWildMankeyTreeTopLateDrawEffectInit init;
    VecFx32 position;
    void *effectContext;
    void *effect;

    if (state == NULL
        || fieldSystem == NULL
        || object == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || sOverworldWildMankeyTreeTopLateDrawEffects[slot] != NULL
        || !OW_WILD_RUNTIME(state)->movementMankeyTreeTopSettled[slot]) {
        return;
    }

    effectContext = ov01_021F146C(object);
    if (effectContext == NULL) {
        return;
    }

    init.state = state;
    init.fieldSystem = fieldSystem;
    init.object = object;
    init.slot = slot;
    position.x = (fx32)object->posVec[0];
    position.y = (fx32)object->posVec[1];
    position.z = (fx32)object->posVec[2];

    effect = ov01_021F1620(
        effectContext,
        &sOverworldWildMankeyTreeTopLateDrawEffectDescriptor,
        &position,
        slot,
        &init,
        0);
    if (effect != NULL) {
        sOverworldWildMankeyTreeTopLateDrawEffects[slot] = effect;
    }
#endif
}

static void OverworldWildSpawns_UpdateMankeyTreeTopPriorityBits(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    LocalMapObject *object)
{
    int objectX;
    int objectY;

    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !state->spawns[slot].active
        || !OverworldWildSpawns_IsCanopyHopperTreeTopSlot(state, slot)
        || object == NULL) {
        return;
    }

    objectX = MapObject_GetCurrentX(object);
    objectY = MapObject_GetCurrentY(object);
    if (OverworldWildSpawns_IsMankeyOnHeadbuttTreeTopTileCached(state, fieldSystem, slot, objectX, objectY)) {
        OverworldWildSpawns_ApplyMankeyTreeTopRenderOverride(slot, object);
        MapObject_ClearBits(object, BIT_VANISH);
        OverworldWildSpawns_ClearMankeyTreeTopProxyObject(state, slot, TRUE);
        OverworldWildSpawns_EnsureMankeyTreeTopLateDrawEffect(state, fieldSystem, slot, object);
    } else {
        OverworldWildSpawns_RestoreMankeyTreeTopRenderOverride(slot, object);
        OverworldWildSpawns_ClearMankeyTreeTopProxyObject(state, slot, TRUE);
        OverworldWildSpawns_ClearMankeyTreeTopLateDrawEffect(slot);
    }
}

#if OW_WILD_SPAWNER_MANKEY_TREE_TOP_LAYER_PROBE_ENABLED
static void OverworldWildSpawns_SetMankeyTreeTopLayerProbePhase(u8 phase)
{
    GX_EngineAToggleLayers(OW_WILD_SPAWNER_MANKEY_TREE_TOP_LAYER_MASK_2, phase != 0);
    GX_EngineAToggleLayers(OW_WILD_SPAWNER_MANKEY_TREE_TOP_LAYER_MASK_4, phase != 1);
    GX_EngineAToggleLayers(OW_WILD_SPAWNER_MANKEY_TREE_TOP_LAYER_MASK_8, phase != 2);
}

static void OverworldWildSpawns_RestoreMankeyTreeTopLayerProbe(void)
{
    GX_EngineAToggleLayers(OW_WILD_SPAWNER_MANKEY_TREE_TOP_LAYER_MASK_2, TRUE);
    GX_EngineAToggleLayers(OW_WILD_SPAWNER_MANKEY_TREE_TOP_LAYER_MASK_4, TRUE);
    GX_EngineAToggleLayers(OW_WILD_SPAWNER_MANKEY_TREE_TOP_LAYER_MASK_8, TRUE);
    sOverworldWildMankeyTreeTopLayerProbeActive = FALSE;
    sOverworldWildMankeyTreeTopLayerProbeTimer = 0;
    sOverworldWildMankeyTreeTopLayerProbePhase = 0;
}

static BOOL OverworldWildSpawns_AnyMankeyOnHeadbuttTreeTopTile(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem)
{
    int i;

    if (state == NULL
        || fieldSystem == NULL
        || !OverworldWildSpawns_IsMovementFieldContextCurrent(state, fieldSystem)) {
        return FALSE;
    }

    for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
        LocalMapObject *object = state->spawns[i].object;

        if (!state->spawns[i].active
            || !OverworldWildSpawns_IsCanopyHopperTreeTopSlot(state, i)
            || object == NULL
            || !OverworldWildSpawns_IsCurrentMapObject(fieldSystem, object)) {
            continue;
        }

        if (OverworldWildSpawns_IsMankeyOnHeadbuttTreeTopTileCached(
                state,
                fieldSystem,
                i,
                MapObject_GetCurrentX(object),
                MapObject_GetCurrentY(object))) {
            return TRUE;
        }
    }

    return FALSE;
}
#else
static void OverworldWildSpawns_RestoreMankeyTreeTopLayerProbe(void)
{
}
#endif

static void OverworldWildSpawns_UpdateMankeyTreeTopLayerProbe(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem)
{
#if !OW_WILD_SPAWNER_MANKEY_TREE_TOP_LAYER_PROBE_ENABLED
    (void)state;
    (void)fieldSystem;
    return;
#else
    if (!OverworldWildSpawns_AnyMankeyOnHeadbuttTreeTopTile(state, fieldSystem)) {
        if (sOverworldWildMankeyTreeTopLayerProbeActive) {
            OverworldWildSpawns_RestoreMankeyTreeTopLayerProbe();
        }
        return;
    }

    if (!sOverworldWildMankeyTreeTopLayerProbeActive) {
        sOverworldWildMankeyTreeTopLayerProbeActive = TRUE;
        sOverworldWildMankeyTreeTopLayerProbeTimer = 0;
        sOverworldWildMankeyTreeTopLayerProbePhase = 0;
        OverworldWildSpawns_SetMankeyTreeTopLayerProbePhase(
            sOverworldWildMankeyTreeTopLayerProbePhase);
        return;
    }

    sOverworldWildMankeyTreeTopLayerProbeTimer++;
    if (sOverworldWildMankeyTreeTopLayerProbeTimer < OW_WILD_SPAWNER_MANKEY_TREE_TOP_LAYER_PROBE_FRAMES) {
        return;
    }

    sOverworldWildMankeyTreeTopLayerProbeTimer = 0;
    sOverworldWildMankeyTreeTopLayerProbePhase++;
    if (sOverworldWildMankeyTreeTopLayerProbePhase >= OW_WILD_SPAWNER_MANKEY_TREE_TOP_LAYER_PROBE_PHASES) {
        sOverworldWildMankeyTreeTopLayerProbePhase = 0;
    }
    OverworldWildSpawns_SetMankeyTreeTopLayerProbePhase(
        sOverworldWildMankeyTreeTopLayerProbePhase);
#endif
}

static void OverworldWildSpawns_UpdateMankeyTreeTopBubbleProbe(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem)
{
#if !OW_WILD_SPAWNER_MANKEY_TREE_TOP_BUBBLE_PROBE_ENABLED
    (void)state;
    (void)fieldSystem;
    return;
#else
    int i;
    BOOL foundMankey = FALSE;

    if (state == NULL
        || fieldSystem == NULL
        || !OverworldWildSpawns_IsMovementFieldContextCurrent(state, fieldSystem)) {
        sOverworldWildMankeyTreeTopBubbleProbeTimer = 0;
        return;
    }

    for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
        LocalMapObject *object = state->spawns[i].object;

        if (!state->spawns[i].active
            || !OverworldWildSpawns_IsCanopyHopperTreeTopSlot(state, i)
            || !OW_WILD_RUNTIME(state)->movementMankeyTreeTopSettled[i]
            || object == NULL
            || !OverworldWildSpawns_IsCurrentMapObject(fieldSystem, object)
            || !OverworldWildSpawns_IsMankeyOnHeadbuttTreeTopTileCached(
                state,
                fieldSystem,
                i,
                MapObject_GetCurrentX(object),
                MapObject_GetCurrentY(object))) {
            continue;
        }

        foundMankey = TRUE;
        break;
    }

    if (!foundMankey) {
        sOverworldWildMankeyTreeTopBubbleProbeTimer = 0;
        return;
    }

    sOverworldWildMankeyTreeTopBubbleProbeTimer++;
    if (sOverworldWildMankeyTreeTopBubbleProbeTimer < OW_WILD_SPAWNER_MANKEY_TREE_TOP_BUBBLE_PROBE_FRAMES) {
        return;
    }

    sOverworldWildMankeyTreeTopBubbleProbeTimer = 0;
    OverworldWildSpawns_ShowBubble(
        state->spawns[i].object,
        OW_WILD_SPAWNER_MANKEY_TREE_TOP_BUBBLE_PROBE_ID);
#endif
}

static BOOL OverworldWildSpawns_StageHopTarget(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    LocalMapObject *object,
    const OverworldWildBehaviorProfile *profile,
    int targetX,
    int targetY,
    u8 direction,
    u8 distance,
    BOOL finishWithTired)
{
    int objectX;
    int objectY;
    int dx;
    int dy;
    u8 resolvedDistance;
    u8 resolvedDirection;
    u8 spotState;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || object == NULL) {
        return FALSE;
    }

    objectX = MapObject_GetCurrentX(object);
    objectY = MapObject_GetCurrentY(object);
    dx = targetX - objectX;
    dy = targetY - objectY;
    spotState = state->movementSpotStates[slot];
#if OW_WILD_SPAWNER_CANOPY_SHIFT_SOUTH_LAND_ANCHORS
    if (OverworldWildSpawns_IsShiftedSouthLandAnchorHop(
            fieldSystem,
            objectX,
            objectY,
            targetX,
            targetY)) {
        resolvedDirection = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_UP;
        resolvedDistance = OW_WILD_SPAWNER_CANOPY_SOUTH_LAND_SHIFT_TILES;
    } else
#endif
#if OW_WILD_SPAWNER_CANOPY_SHIFT_NORTH_LAND_ANCHORS
    if (OverworldWildSpawns_IsShiftedNorthLandAnchorHop(
            fieldSystem,
            objectX,
            objectY,
            targetX,
            targetY)) {
        resolvedDirection = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN;
        resolvedDistance = OW_WILD_SPAWNER_CANOPY_NORTH_LAND_SHIFT_TILES;
    } else
#endif
    if (!OverworldWildSpawns_TryGetBehaviorHopVector(
            profile,
            spotState,
            dx,
            dy,
            &resolvedDirection,
            &resolvedDistance)) {
        MapObject_ClearBits(object, BIT_VANISH);
        return FALSE;
    }
    direction = resolvedDirection;
    distance = resolvedDistance;

    state->movementStagedHopOriginX[slot] = (s16)objectX;
    state->movementStagedHopOriginY[slot] = (s16)objectY;
    state->movementStagedHopTargetX[slot] = (s16)targetX;
    state->movementStagedHopTargetY[slot] = (s16)targetY;
    state->movementStagedHopDistances[slot] = distance;
    state->movementStagedHopFinishWithTired[slot] = finishWithTired;
    state->movementStagedHopPending[slot] = TRUE;
    state->movementCooldowns[slot] = OW_WILD_SPAWNER_CANOPY_HOPPER_PRE_HOP_WAIT_FRAMES;
    OverworldWildSpawns_SetObjectFacing(object, direction);
    OverworldWildSpawns_ClearCanopyHopperVisualStateAtBoundary(state, fieldSystem, slot);
    OverworldWildSpawns_RecordCustomJumpRamObject(state, slot, object, 8);

#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
    OverworldWildSpawns_EnsureFrameMovementTask(state, fieldSystem);
#else
    (void)fieldSystem;
#endif
    return TRUE;
}

#if OW_WILD_SPAWNER_CUSTOM_JUMP_RAM_PROBE
static void OverworldWildSpawns_RecordCustomJumpRamObject(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object,
    int stage)
{
    sOverworldWildCustomJumpRamStage = stage;
    sOverworldWildCustomJumpRamSlot = slot;
    if (stage != 14 && stage != 15) {
        sOverworldWildCustomJumpRamFinalLanding = FALSE;
    }
    if (state != NULL && slot >= 0 && slot < OW_WILD_MAX_SPAWNS) {
        sOverworldWildCustomJumpRamPending = state->movementStagedHopPending[slot];
        sOverworldWildCustomJumpRamActive =
            OW_WILD_RUNTIME(state)->movementCustomJumpActive[slot];
#if OW_WILD_SPAWNER_CUSTOM_JUMP_VISIBLE_LEGS
        sOverworldWildCustomJumpRamVisibleLeg =
            sOverworldWildCustomJumpVisibleLegFinished[slot];
#else
        sOverworldWildCustomJumpRamVisibleLeg = FALSE;
#endif
        if (state->movementStagedHopPending[slot] || stage < 15) {
            sOverworldWildCustomJumpRamStartX = state->movementStagedHopOriginX[slot];
            sOverworldWildCustomJumpRamStartY = state->movementStagedHopOriginY[slot];
            sOverworldWildCustomJumpRamTargetX = state->movementStagedHopTargetX[slot];
            sOverworldWildCustomJumpRamTargetY = state->movementStagedHopTargetY[slot];
        }
    }
    if (object != NULL) {
        sOverworldWildCustomJumpRamCurrentX = MapObject_GetCurrentX(object);
        sOverworldWildCustomJumpRamCurrentY = MapObject_GetCurrentY(object);
        sOverworldWildCustomJumpRamRenderX =
            ((s32)object->posVec[0]) / OW_WILD_SPAWNER_TILE_FX32;
        sOverworldWildCustomJumpRamRenderY =
            ((s32)object->posVec[2]) / OW_WILD_SPAWNER_TILE_FX32;
        sOverworldWildCustomJumpRamRenderFx32X = (s32)object->posVec[0];
        sOverworldWildCustomJumpRamRenderFx32Y = (s32)object->posVec[2];
        sOverworldWildCustomJumpRamFlags = (int)object->flags;
    }
    if (slot >= 0 && slot < OW_WILD_MAX_SPAWNS) {
        sOverworldWildCustomJumpRamElapsedFrames =
            (int)OW_WILD_RUNTIME(state)->movementCustomJumpElapsedFrames[slot];
        sOverworldWildCustomJumpRamFrameCount =
            (int)OW_WILD_RUNTIME(state)->movementCustomJumpFrameCounts[slot];
    }
}

static BOOL OverworldWildSpawns_TryPickCustomJumpRamProbeTarget(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    LocalMapObject *object,
    int *targetX,
    int *targetY,
    u8 *direction,
    u8 *distance)
{
    int objectX;
    int objectY;

    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || object == NULL
        || targetX == NULL
        || targetY == NULL
        || direction == NULL
        || distance == NULL) {
        return FALSE;
    }

    objectX = MapObject_GetCurrentX(object);
    objectY = MapObject_GetCurrentY(object);
    *targetX = objectX + OW_WILD_SPAWNER_CUSTOM_JUMP_RAM_PROBE_MIN_TILES;
    *targetY = objectY + OW_WILD_SPAWNER_CUSTOM_JUMP_RAM_PROBE_MIN_TILES;
    return OverworldWildSpawns_TryGetCustomJumpVector(
        *targetX - objectX,
        *targetY - objectY,
        direction,
        distance);
}
#endif

#if OW_WILD_SPAWNER_CUSTOM_JUMP_RAM_PROBE
static BOOL OverworldWildSpawns_TryStartCustomJumpRamProbe(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem)
{
    u32 pad;
    int slot;

    if (state == NULL || fieldSystem == NULL) {
        return FALSE;
    }

    pad = PAD_Read();
    if ((pad & OW_WILD_SPAWNER_CUSTOM_JUMP_RAM_PROBE_KEYS)
        != OW_WILD_SPAWNER_CUSTOM_JUMP_RAM_PROBE_KEYS) {
        sOverworldWildCustomJumpRamProbeKeyDown = FALSE;
        return FALSE;
    }
    if (sOverworldWildCustomJumpRamProbeKeyDown) {
        return FALSE;
    }

    sOverworldWildCustomJumpRamProbeKeyDown = TRUE;
    sOverworldWildCustomJumpRamProbeTriggers++;
    sOverworldWildCustomJumpRamStage = 1;
    sOverworldWildCustomJumpRamProbeActiveSlots = 0;
    sOverworldWildCustomJumpRamProbeCurrentSlots = 0;
    sOverworldWildCustomJumpRamProbeMankeySlots = 0;
    sOverworldWildCustomJumpRamProbeObjectSlots = 0;
    sOverworldWildCustomJumpRamProbeLastSpecies = SPECIES_NONE;

    state->spawnCooldown = 0;
    state->headbuttSpawnCooldown = 0;
    state->fishingSpawnCooldown = 0;
    OverworldWildSpawns_TryRefill(state, fieldSystem);

    for (slot = 0; slot < OW_WILD_MAX_SPAWNS; slot++) {
        LocalMapObject *object;
        OverworldWildBehaviorProfile profile;
        int targetX;
        int targetY;
        u8 direction;
        u8 distance;

        if (!state->spawns[slot].active) {
            continue;
        }
        sOverworldWildCustomJumpRamProbeActiveSlots++;
        sOverworldWildCustomJumpRamProbeLastSpecies = state->spawns[slot].species;
        if (!OverworldWildSpawns_IsCurrentSpawnObject(fieldSystem, &state->spawns[slot])) {
            continue;
        }
        sOverworldWildCustomJumpRamProbeCurrentSlots++;
        if (!OverworldWildSpawns_IsCanopyHopperTreeTopSlot(state, slot)) {
            continue;
        }
        sOverworldWildCustomJumpRamProbeMankeySlots++;

        object = state->spawns[slot].object;
        if (object == NULL) {
            continue;
        }
        sOverworldWildCustomJumpRamProbeObjectSlots++;

        if (OverworldWildSpawns_IsMovementSlotInProgress(state, slot)) {
            OverworldWildSpawns_ClearMovementSlotInProgress(state, slot);
        }
        if (MapObject_IsSingleMovementActive(object)) {
            MapObject_ClearSingleMovementActive(object);
        }
        OverworldWildSpawns_ClearStagedHopTarget(state, slot);
        OverworldWildSpawns_RecordCustomJumpRamObject(state, slot, object, 2);
        if (!OverworldWildSpawns_TryPickCustomJumpRamProbeTarget(
                state,
                fieldSystem,
                slot,
                object,
                &targetX,
                &targetY,
                &direction,
                &distance)) {
            sOverworldWildCustomJumpRamProbeFailures++;
            OverworldWildSpawns_RecordCustomJumpRamObject(state, slot, object, 3);
            sOverworldWildCustomJumpRamProbeKeyDown = FALSE;
            return TRUE;
        }

        profile = OverworldWildSpawns_GetBehaviorProfile(state, slot);
        if (!OverworldWildSpawns_StageHopTarget(
                state,
                fieldSystem,
                slot,
                object,
                &profile,
                targetX,
                targetY,
                direction,
                distance,
                FALSE)) {
            sOverworldWildCustomJumpRamProbeFailures++;
            OverworldWildSpawns_RecordCustomJumpRamObject(state, slot, object, 4);
            sOverworldWildCustomJumpRamProbeKeyDown = FALSE;
            return TRUE;
        }

        sOverworldWildCustomJumpRamProbeStarts++;
        OverworldWildSpawns_RecordCustomJumpRamObject(state, slot, object, 5);
        if (OverworldWildSpawns_ExecutePendingStagedHop(
                state,
                fieldSystem,
                slot,
                object,
                &profile)) {
            return TRUE;
        }

        sOverworldWildCustomJumpRamProbeFailures++;
        OverworldWildSpawns_RecordCustomJumpRamObject(state, slot, object, 17);
        sOverworldWildCustomJumpRamProbeKeyDown = FALSE;
        return TRUE;
    }

    sOverworldWildCustomJumpRamProbeFailures++;
    sOverworldWildCustomJumpRamStage = 6;
    sOverworldWildCustomJumpRamProbeKeyDown = FALSE;
    return TRUE;
}
#endif

static BOOL OverworldWildSpawns_IsAtStagedHopTarget(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object)
{
    int objectX;
    int objectY;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || object == NULL
        || !state->movementStagedHopPending[slot]) {
        return FALSE;
    }

    objectX = MapObject_GetCurrentX(object);
    objectY = MapObject_GetCurrentY(object);
    return objectX == state->movementStagedHopTargetX[slot]
        && objectY == state->movementStagedHopTargetY[slot];
}

#if 0
static u8 OverworldWildSpawns_GetCanopyRenderHopDuration(u8 distance)
{
    u16 frames;

    if (distance == 0) {
        distance = OW_WILD_SPAWNER_MOVEMENT_DISTANCE_STEP;
    }

    frames = (u16)distance * OW_WILD_SPAWNER_CANOPY_HOPPER_RENDER_HOP_FRAMES_PER_TILE;
    if (frames < OW_WILD_SPAWNER_CANOPY_HOPPER_RENDER_HOP_MIN_FRAMES) {
        frames = OW_WILD_SPAWNER_CANOPY_HOPPER_RENDER_HOP_MIN_FRAMES;
    }
    if (frames > 255) {
        frames = 255;
    }

    return (u8)frames;
}

static s32 OverworldWildSpawns_LerpCanopyRenderHopValue(s32 start, s32 target, u8 elapsed, u8 total)
{
    if (elapsed >= total) {
        return target;
    }

    return start + (((target - start) * elapsed) / total);
}

static s32 OverworldWildSpawns_GetCanopyRenderHopArcHeight(u8 elapsed, u8 total)
{
    s32 height;

    if (elapsed >= total || total == 0) {
        return 0;
    }

    height = (4
        * OW_WILD_SPAWNER_CANOPY_HOPPER_RENDER_HOP_HEIGHT_FX32
        * elapsed
        * (total - elapsed))
        / (total * total);
    return height;
}
#endif

static BOOL OverworldWildSpawns_RunImmediateCanopyMovementCommand(LocalMapObject *object, u32 movementCommand)
{
    int i;

    if (object == NULL || MapObject_IsSingleMovementActive(object)) {
        return FALSE;
    }

    MapObject_StartMovementCommand(object, movementCommand);
    MapObject_SetSingleMovementActive(object);
    for (i = 0; i < OW_WILD_SPAWNER_MOVEMENT_BURST_UPDATE_STEPS; i++) {
        if (!MapObject_IsSingleMovementActive(object)) {
            return TRUE;
        }
        if (MapObject_UpdateMovementCommand(object)) {
            MapObject_ClearSingleMovementActive(object);
            return TRUE;
        }
    }

    if (MapObject_IsSingleMovementActive(object)) {
        MapObject_ClearSingleMovementActive(object);
    }
    return FALSE;
}

static void OverworldWildSpawns_StartCustomJump(
    OverworldWildSpawnState *state,
    int slot,
    int startX,
    int startY,
    int targetX,
    int targetY,
    s32 targetBaseY,
    u32 frameCount)
{
    OverworldWildOverlayRuntimeState *runtime;

    OverworldWildSpawns_ClearCustomJump(state, slot);
    runtime = OW_WILD_RUNTIME(state);

    runtime->movementCustomJumpActive[slot] = TRUE;
#if OW_WILD_SPAWNER_CUSTOM_JUMP_RAM_PROBE
    sOverworldWildCustomJumpRamFinalLanding = FALSE;
#endif
    runtime->movementCustomJumpStartX[slot] = (s16)startX;
    runtime->movementCustomJumpStartY[slot] = (s16)startY;
    runtime->movementCustomJumpTargetX[slot] = (s16)targetX;
    runtime->movementCustomJumpTargetY[slot] = (s16)targetY;
    runtime->movementCustomJumpTargetBaseY[slot] = targetBaseY;
    runtime->movementCustomJumpFrameCounts[slot] = (u16)frameCount;
    runtime->movementCustomJumpElapsedFrames[slot] = 0;
#if OW_WILD_SPAWNER_CUSTOM_JUMP_RAM_PROBE
    sOverworldWildCustomJumpRamStage = 7;
    sOverworldWildCustomJumpRamSlot = slot;
    sOverworldWildCustomJumpRamStartX = startX;
    sOverworldWildCustomJumpRamStartY = startY;
    sOverworldWildCustomJumpRamTargetX = targetX;
    sOverworldWildCustomJumpRamTargetY = targetY;
    sOverworldWildCustomJumpRamActive = TRUE;
#endif
}

static BOOL OverworldWildSpawns_StartPreparedCustomJumpCommand(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    LocalMapObject *object,
    u8 direction,
    u8 distance,
    int targetX,
    int targetY,
    const OverworldWildBehaviorProfile *profile,
    BOOL suppressHopStartSound)
{
    OverworldWildOverlayRuntimeState *runtime;
    u32 frameCount;
    u32 movementCommand;
    int objectX;
    int objectY;
    u8 framesPerTile;
    u8 spinSpeed;
    BOOL playHopStartSound;
    s32 targetBaseY;

    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || object == NULL
        || direction > OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT
        || OverworldWildSpawns_IsTileReservedByOtherWild(
            state,
            slot,
            targetX,
            targetY)) {
        return FALSE;
    }

    runtime = OW_WILD_RUNTIME(state);
    objectX = MapObject_GetCurrentX(object);
    objectY = MapObject_GetCurrentY(object);
    playHopStartSound = !suppressHopStartSound
        && OverworldWildSpawns_IsHeadbuttTreeTopLocation(fieldSystem, objectX, objectY);
    state->movementFieldSystem = fieldSystem;
    framesPerTile = profile->hopTime;
    if (framesPerTile == 0) {
        framesPerTile = OW_WILD_SPAWNER_CUSTOM_JUMP_FRAMES_PER_TILE;
    }
    if (distance < OW_WILD_SPAWNER_MOVEMENT_DISTANCE_STEP
        || distance > OW_WILD_SPAWNER_CUSTOM_JUMP_MAX_TILES) {
        return FALSE;
    }
    frameCount = framesPerTile + (u32)(distance - 1) * (framesPerTile - (framesPerTile >> 2));
    targetBaseY = OverworldWildSpawns_GetObjectGroundBaseYAt(object, targetX, targetY);
    OverworldWildSpawns_ClearStagedHopMovementListTask(state, slot);
    OverworldWildSpawns_ClearCanopyHopperVisualStateAtBoundary(state, fieldSystem, slot);
    OverworldWildSpawns_SetObjectFacing(object, direction);
    MapObject_ClearBits(object, BIT_VANISH | OW_WILD_SPAWNER_CUSTOM_JUMP_OWNED_BITS);
    if (!OverworldWildSpawns_RunImmediateCanopyMovementCommand(
            object,
            OW_WILD_SPAWNER_CANOPY_HOPPER_PARTNER_PREP_COMMAND)) {
        (void)OverworldWildSpawns_RunImmediateCanopyMovementCommand(
            object,
            OW_WILD_SPAWNER_CANOPY_HOPPER_PARTNER_RESTORE_COMMAND);
        return FALSE;
    }
    OverworldWildSpawns_StartCustomJump(
        state,
        slot,
        objectX,
        objectY,
        targetX,
        targetY,
        targetBaseY,
        frameCount);
    runtime->movementCustomJumpPrepActive[slot] = TRUE;
    OverworldWildSpawns_ReconcileNativeShadow(fieldSystem, object);
    spinSpeed = OverworldWildSpawns_GetBehaviorHopSpinSpeed(
        profile,
        state->movementSpotStates[slot]);
    runtime->movementCustomJumpSpinSpeeds[slot] = spinSpeed;
    runtime->movementCustomJumpSpinTimers[slot] = spinSpeed;
    runtime->movementCustomJumpSpinSteps[slot] =
        OverworldWildSpawns_GetCustomJumpSpinStep(direction);
    movementCommand = OW_WILD_SPAWNER_CANOPY_HOPPER_FREEZE_COMMAND;
    MapObject_StartMovementCommand(object, movementCommand);
    MapObject_SetSingleMovementActive(object);
    StopSE(OW_WILD_SPAWNER_SPOT_EMOTE_SE);
    if (playHopStartSound) {
        StopSE(OW_WILD_SPAWNER_HOP_START_SE);
        OverworldWildSpawns_PlayCanopyHopSE(state);
    } else {
        OverworldWildSpawns_StartHopStartSoundSuppression(state, slot);
    }

    OverworldWildSpawns_SetPreviousTile(state, slot, objectX, objectY);
    state->movementPendingDirections[slot] = direction;
    state->movementPendingDistances[slot] = distance;
    state->movementStagedHopDistances[slot] = distance;
    state->movementBattleSettleFrames = 0;
    OverworldWildSpawns_SetMovementSlotInProgress(state, slot);
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
    OverworldWildSpawns_EnsureFrameMovementTask(state, fieldSystem);
#endif
    OverworldWildSpawns_RecordCustomJumpRamObject(state, slot, object, 9);
    return TRUE;
}

static void OverworldWildSpawns_StartSpawnHop(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    int startX,
    int startY,
    int targetX,
    int targetY,
    u8 direction)
{
    LocalMapObject *object;
    OverworldWildBehaviorProfile profile;
    u8 resolvedDirection;
    u8 resolvedDistance;

    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !state->spawns[slot].active
        || state->spawns[slot].object == NULL
        || direction > OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT) {
        return;
    }

    object = state->spawns[slot].object;
    OverworldWildSpawns_SetObjectTile(object, startX, startY);
    profile = OverworldWildSpawns_GetBehaviorProfile(state, slot);
    profile.hopTime = profile.spawnHopTime;
    if (!OverworldWildSpawns_TryGetCustomJumpVector(
            targetX - MapObject_GetCurrentX(object),
            targetY - MapObject_GetCurrentY(object),
            &resolvedDirection,
            &resolvedDistance)) {
        object = OverworldWildSpawns_NormalizeSpawnHopLandingObject(
            state,
            fieldSystem,
            slot,
            object,
            targetX,
            targetY);
        OverworldWildSpawns_RefreshCanopyHopperVisualStateAtLanding(
            state,
            fieldSystem,
            slot,
            object);
        OverworldWildSpawns_SetPostSpawnStartupCooldown(state, slot);
        return;
    }

    if (!OverworldWildSpawns_StartPreparedCustomJumpCommand(
            state,
            fieldSystem,
            slot,
            object,
            resolvedDirection,
            resolvedDistance,
            targetX,
            targetY,
            &profile,
            TRUE)) {
        object = OverworldWildSpawns_NormalizeSpawnHopLandingObject(
            state,
            fieldSystem,
            slot,
            object,
            targetX,
            targetY);
        OverworldWildSpawns_RefreshCanopyHopperVisualStateAtLanding(
            state,
            fieldSystem,
            slot,
            object);
        state->movementPendingDirections[slot] = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_NONE;
        state->movementPendingDistances[slot] = 0;
        state->movementStagedHopDistances[slot] = 0;
        OverworldWildSpawns_SetPostSpawnStartupCooldown(state, slot);
        return;
    }

    OverworldWildSpawns_SetSpawnRunState(state, fieldSystem, slot, targetX, targetY);
}

static BOOL OverworldWildSpawns_TryStartNextStagedHopMovementCommand(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    LocalMapObject *object)
{
    int dx;
    int dy;
    int objectX;
    int objectY;
    int totalRemainingDistance;
    int targetX;
    int targetY;
    OverworldWildBehaviorProfile profile;
#if OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_CHAINED_JUMP2_PROBE \
    || OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_CHAINED_JUMP2_NO_MIDPOINT_NORMALIZE_PROBE \
    || OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_CHAINED_JUMP2_LOGICAL_MIDPOINT_PROBE
    u8 direction;
    u32 movementCommand;
#endif
    u8 behaviorDirection;
    u8 behaviorDistance;
    u8 plannedDirections[OW_WILD_SPAWNER_MOVEMENT_MAX_DIRECTIONS];
    int plannedDirectionCount;
    u8 allowedTile;

    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || object == NULL
        || !state->movementStagedHopPending[slot]) {
        return FALSE;
    }

    if (MapObject_IsSingleMovementActive(object)) {
        MapObject_ClearSingleMovementActive(object);
    }

    objectX = MapObject_GetCurrentX(object);
    objectY = MapObject_GetCurrentY(object);
    targetX = state->movementStagedHopTargetX[slot];
    targetY = state->movementStagedHopTargetY[slot];
    dx = targetX - objectX;
    dy = targetY - objectY;
    if (OverworldWildSpawns_IsAtStagedHopTarget(state, slot, object)) {
        return FALSE;
    }

    profile = OverworldWildSpawns_GetBehaviorProfile(state, slot);
    allowedTile = OverworldWildSpawns_GetAllowedTileForSpotState(
        &profile,
        state->movementSpotStates[slot]);
    totalRemainingDistance = OverworldWildSpawns_Max(
        OverworldWildSpawns_Abs(dx),
        OverworldWildSpawns_Abs(dy));
    if (totalRemainingDistance < OverworldWildSpawns_GetBehaviorHopMinDistance(
            &profile,
            state->movementSpotStates[slot])
        && totalRemainingDistance > OW_WILD_SPAWNER_MOVEMENT_DISTANCE_STEP
#if OW_WILD_SPAWNER_CANOPY_SHIFT_SOUTH_LAND_ANCHORS
        && !OverworldWildSpawns_IsShiftedSouthLandAnchorHop(
            fieldSystem,
            objectX,
            objectY,
            targetX,
            targetY)
#endif
#if OW_WILD_SPAWNER_CANOPY_SHIFT_NORTH_LAND_ANCHORS
        && !OverworldWildSpawns_IsShiftedNorthLandAnchorHop(
            fieldSystem,
            objectX,
            objectY,
            targetX,
            targetY)
#endif
        ) {
        OverworldWildSpawns_ClearStagedHopTarget(state, slot);
        MapObject_ClearBits(object, BIT_VANISH);
        state->movementCooldowns[slot] = OW_WILD_SPAWNER_DEFAULT_MOVEMENT_PAUSE_FRAMES;
        return FALSE;
    }

    if (OverworldWildSpawns_IsBehaviorAllowedHopLandingTile(
            state,
            slot,
            fieldSystem,
            allowedTile,
            targetX,
            targetY,
            targetX,
            targetY)
        && OverworldWildSpawns_TryGetBehaviorHopVector(
            &profile,
            state->movementSpotStates[slot],
            dx,
            dy,
            &behaviorDirection,
            &behaviorDistance)
        && OverworldWildSpawns_StartPreparedCustomJumpCommand(
            state,
            fieldSystem,
            slot,
            object,
            behaviorDirection,
            behaviorDistance,
            targetX,
            targetY,
            &profile,
            FALSE)) {
        return TRUE;
    }

    plannedDirectionCount = OverworldWildSpawns_DiagnosticBuildDirections(
        dx,
        dy,
        plannedDirections);
    if (plannedDirectionCount > 0
        && OverworldWildSpawns_TryStartBehaviorHopPlanStepCommand(
            state,
            fieldSystem,
            slot,
            &profile,
            allowedTile,
            targetX,
            targetY,
            plannedDirections,
            plannedDirectionCount,
            FALSE)) {
        return TRUE;
    }

    if (totalRemainingDistance <= OW_WILD_SPAWNER_MOVEMENT_DISTANCE_STEP
        && OverworldWildSpawns_IsBehaviorAllowedHopLandingTile(
            state,
            slot,
            fieldSystem,
            allowedTile,
            targetX,
            targetY,
            targetX,
            targetY)) {
        OverworldWildSpawns_SetObjectLandingTile(fieldSystem, object, targetX, targetY);
        MapObject_ClearBits(object, BIT_VANISH);
        OverworldWildSpawns_FinishPendingStagedHop(state, slot, object, &profile);
        OverworldWildSpawns_UpdateMankeyTreeTopPriorityBits(state, fieldSystem, slot, object);
        return TRUE;
    }
#if OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_CHAINED_JUMP2_PROBE \
    || OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_CHAINED_JUMP2_NO_MIDPOINT_NORMALIZE_PROBE \
    || OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_CHAINED_JUMP2_LOGICAL_MIDPOINT_PROBE
    if (totalRemainingDistance >= OW_WILD_SPAWNER_MOVEMENT_DISTANCE_LEDGE_JUMP
        && (dx == 0 || dy == 0)) {
        if (dx > 0) {
            direction = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT;
        } else if (dx < 0) {
            direction = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_LEFT;
        } else if (dy > 0) {
            direction = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN;
        } else {
            direction = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_UP;
        }

        movementCommand = MapObject_MovementCommandFromDirection(
            direction,
            OW_WILD_SPAWNER_CANOPY_HOPPER_JUMP_2_COMMAND);
        state->movementFieldSystem = fieldSystem;
        state->movementStagedHopDistances[slot] = OW_WILD_SPAWNER_MOVEMENT_DISTANCE_LEDGE_JUMP;
        OverworldWildSpawns_StartMovementCommandForSlot(
            state,
            slot,
            object,
            movementCommand,
            direction,
            OW_WILD_SPAWNER_MOVEMENT_DISTANCE_LEDGE_JUMP);
        return TRUE;
    }
#endif
    OverworldWildSpawns_ClearStagedHopTarget(state, slot);
    MapObject_ClearBits(object, BIT_VANISH);
    state->movementCooldowns[slot] = OW_WILD_SPAWNER_DEFAULT_MOVEMENT_PAUSE_FRAMES;
    return FALSE;
}

static void OverworldWildSpawns_FinishPendingStagedHop(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object,
    const OverworldWildBehaviorProfile *profile)
{
    OverworldWildOverlayRuntimeState *runtime;
    BOOL finishWithTired;
    BOOL treeTopLandingExpected;
    BOOL finalTreeTopLanding = FALSE;
    s16 avoidX;
    s16 avoidY;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || object == NULL
        || profile == NULL) {
        return;
    }
    runtime = OW_WILD_RUNTIME(state);

    finishWithTired = state->movementStagedHopFinishWithTired[slot];
    treeTopLandingExpected = runtime->movementMankeyTreeTopLandingExpected[slot];
    if (treeTopLandingExpected
        && OverworldWildSpawns_IsCanopyHopperTreeTopSlot(state, slot)
        && state->movementFieldSystem != NULL) {
        finalTreeTopLanding = OverworldWildSpawns_IsHeadbuttTreeTopLocation(
            state->movementFieldSystem,
            MapObject_GetCurrentX(object),
            MapObject_GetCurrentY(object));
    }
    avoidX = state->movementStagedHopOriginX[slot];
    avoidY = state->movementStagedHopOriginY[slot];
    OverworldWildSpawns_ClearStagedHopTarget(state, slot);
    runtime->movementMankeyTreeTopLandingExpected[slot] = FALSE;
    runtime->movementMankeyTreeTopSettled[slot] = finalTreeTopLanding;
    if (runtime->movementMankeyTreeTopSettled[slot]) {
        runtime->movementMankeyTreeTopSettledX[slot] = (s16)MapObject_GetCurrentX(object);
        runtime->movementMankeyTreeTopSettledY[slot] = (s16)MapObject_GetCurrentY(object);
    } else {
        runtime->movementMankeyTreeTopSettledX[slot] = OW_WILD_SPAWNER_PREVIOUS_TILE_NONE;
        runtime->movementMankeyTreeTopSettledY[slot] = OW_WILD_SPAWNER_PREVIOUS_TILE_NONE;
    }
    state->movementStagedHopAvoidX[slot] = avoidX;
    state->movementStagedHopAvoidY[slot] = avoidY;
    state->movementStagedHopAvoidValid[slot] = TRUE;

    if (finishWithTired
        && profile->stamina > 0
        && profile->tiredState != OW_WILD_BEHAVIOR_KIND_NONE) {
        state->movementActiveSteps[slot] = profile->stamina - 1;
    }
    OverworldWildSpawns_HandleFinishedMovementCommand(state, slot);
    if (state->movementCooldowns[slot] == 0
        && (state->movementSpotStates[slot] == OW_WILD_SPAWNER_SPOT_STATE_CHILL
            || state->movementSpotStates[slot] == OW_WILD_SPAWNER_SPOT_STATE_ACTIVE)) {
        state->movementCooldowns[slot] =
            OverworldWildSpawns_GetBehaviorHopPauseFrames(
                profile,
                state->movementSpotStates[slot]);
    }
}

#if 0
static BOOL OverworldWildSpawns_TickCanopyRenderHopMovementCommand(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    BOOL *finished)
{
    LocalMapObject *object;
    OverworldWildBehaviorProfile profile;
    u8 totalFrames;
    u8 remainingFrames;
    u8 elapsedFrames;
    s32 startX;
    s32 startZ;
    s32 targetX;
    s32 targetZ;
    s32 baseY;

    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || state->movementCanopyRenderHopTimers[slot] == 0) {
        return FALSE;
    }

    if (!OverworldWildSpawns_IsCurrentSpawnObject(fieldSystem, &state->spawns[slot])) {
        state->movementCanopyRenderHopTimers[slot] = 0;
        OverworldWildSpawns_ClearCanopyRenderHopObject(state, slot, TRUE);
        OverworldWildSpawns_ClearMovementSlotInProgress(state, slot);
        if (finished != NULL) {
            *finished = TRUE;
        }
        return TRUE;
    }

    object = state->spawns[slot].object;
    if (object == NULL) {
        state->movementCanopyRenderHopTimers[slot] = 0;
        OverworldWildSpawns_ClearCanopyRenderHopObject(state, slot, TRUE);
        OverworldWildSpawns_ClearMovementSlotInProgress(state, slot);
        if (finished != NULL) {
            *finished = TRUE;
        }
        return TRUE;
    }

    totalFrames = OverworldWildSpawns_GetCanopyRenderHopDuration(
        state->movementStagedHopDistances[slot]);
    remainingFrames = state->movementCanopyRenderHopTimers[slot];
    if (remainingFrames > totalFrames) {
        remainingFrames = totalFrames;
    }
    elapsedFrames = (u8)(totalFrames - remainingFrames + 1);
    startX = state->movementCanopyRenderHopStartX[slot];
    startZ = state->movementCanopyRenderHopStartZ[slot];
    targetX = state->movementCanopyRenderHopTargetX[slot];
    targetZ = state->movementCanopyRenderHopTargetZ[slot];
    baseY = state->movementCanopyRenderHopBaseY[slot];
    MapObject_ClearBits(object, BIT_VANISH);

    if (elapsedFrames >= totalFrames) {
        OverworldWildSpawns_SetObjectTile(
            object,
            targetX / OW_WILD_SPAWNER_FX32_ONE,
            targetZ / OW_WILD_SPAWNER_FX32_ONE);
        object->posVec[0] = (u32)targetX;
        object->posVec[1] = (u32)baseY;
        object->posVec[2] = (u32)targetZ;
        state->movementCanopyRenderHopTimers[slot] = 0;
        OverworldWildSpawns_ClearCanopyRenderHopObject(state, slot, TRUE);
        OverworldWildSpawns_ClearMovementSlotInProgress(state, slot);
        profile = OverworldWildSpawns_GetBehaviorProfile(state, slot);
        if (OverworldWildSpawns_ShouldRefreshCanopyObjectAtTile(
                state,
                fieldSystem,
                slot,
                targetX / OW_WILD_SPAWNER_FX32_ONE,
                targetZ / OW_WILD_SPAWNER_FX32_ONE,
                TRUE)) {
            object = OverworldWildSpawns_RecreateSpawnObjectAtTile(
                state,
                fieldSystem,
                slot,
                object,
                targetX / OW_WILD_SPAWNER_FX32_ONE,
                targetZ / OW_WILD_SPAWNER_FX32_ONE,
                OverworldWildSpawns_ShouldNormalizeCanopyRefreshAtTile(
                    fieldSystem,
                    targetX / OW_WILD_SPAWNER_FX32_ONE,
                    targetZ / OW_WILD_SPAWNER_FX32_ONE));
        } else {
            MapObject_ClearBits(object, BIT_VANISH);
        }
        OverworldWildSpawns_FinishPendingStagedHop(state, slot, object, &profile);
        if (finished != NULL) {
            *finished = TRUE;
        }
        return TRUE;
    }

    object->posVec[0] = (u32)OverworldWildSpawns_LerpCanopyRenderHopValue(
        startX,
        targetX,
        elapsedFrames,
        totalFrames);
    object->posVec[1] = (u32)(baseY
        + OverworldWildSpawns_GetCanopyRenderHopArcHeight(elapsedFrames, totalFrames));
    object->posVec[2] = (u32)OverworldWildSpawns_LerpCanopyRenderHopValue(
        startZ,
        targetZ,
        elapsedFrames,
        totalFrames);
    state->movementCanopyRenderHopTimers[slot] = (u8)(remainingFrames - 1);
    return TRUE;
}
#endif

static BOOL OverworldWildSpawns_TickStagedHopMovementListTask(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    BOOL *finished)
{
    SysTask *movementTask;

    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || sOverworldWildStagedHopMovementTasks[slot] == NULL) {
        return FALSE;
    }

    if (!OverworldWildSpawns_IsCurrentSpawnObject(fieldSystem, &state->spawns[slot])) {
        OverworldWildSpawns_ClearStagedHopMovementListTask(state, slot);
        OverworldWildSpawns_ClearMovementSlotInProgress(state, slot);
        if (finished != NULL) {
            *finished = TRUE;
        }
        return TRUE;
    }

    movementTask = sOverworldWildStagedHopMovementTasks[slot];
    if (!MapObject_MovementListTaskIsFinished(movementTask)) {
        return TRUE;
    }

    OverworldWildSpawns_ClearStagedHopMovementListTask(state, slot);
    OverworldWildSpawns_ClearMovementSlotInProgress(state, slot);
    if (!OverworldWildSpawns_HandleFinishedStagedHopMovementCommand(
            state,
            fieldSystem,
            slot,
            FALSE)) {
        OverworldWildSpawns_HandleFinishedMovementCommand(state, slot);
    }
    if (finished != NULL) {
        *finished = TRUE;
    }
    return TRUE;
}

static BOOL OverworldWildSpawns_HandleFinishedStagedHopMovementCommand(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    BOOL customJumpLandingFinalized)
{
    LocalMapObject *object;
    OverworldWildBehaviorProfile profile;
    BOOL finalLanding;
    BOOL skipFinalMankeyTreeTopRestore;
    BOOL customJumpVisibleLegFinished;
    BOOL customJumpRenderSettleActive = FALSE;
#if !OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_CHAINED_JUMP2_NO_MIDPOINT_NORMALIZE_PROBE
    int landingX;
    int landingY;
#endif

    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !state->movementStagedHopPending[slot]
        || !OverworldWildSpawns_IsCurrentSpawnObject(fieldSystem, &state->spawns[slot])) {
        return FALSE;
    }

    object = state->spawns[slot].object;
    if (object == NULL) {
        return FALSE;
    }
    profile = OverworldWildSpawns_GetBehaviorProfile(state, slot);
    finalLanding = OverworldWildSpawns_IsAtStagedHopTarget(state, slot, object);
#if OW_WILD_SPAWNER_CUSTOM_JUMP_VISIBLE_LEGS
    customJumpVisibleLegFinished =
        sOverworldWildCustomJumpVisibleLegFinished[slot];
#else
    customJumpVisibleLegFinished = FALSE;
#endif
#if OW_WILD_SPAWNER_CUSTOM_JUMP_RAM_PROBE
    sOverworldWildCustomJumpRamFinalLanding = finalLanding;
#endif
    OverworldWildSpawns_RecordCustomJumpRamObject(state, slot, object, 14);
    skipFinalMankeyTreeTopRestore =
        finalLanding
        && OverworldWildSpawns_IsCanopyHopperTreeTopSlot(state, slot)
        && OW_WILD_RUNTIME(state)->movementMankeyTreeTopLandingExpected[slot];
#if !OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_CHAINED_JUMP2_NO_MIDPOINT_NORMALIZE_PROBE
    landingX = MapObject_GetCurrentX(object);
    landingY = MapObject_GetCurrentY(object);
#endif
    if (!finalLanding) {
#if !OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_CHAINED_JUMP2_NO_MIDPOINT_NORMALIZE_PROBE
#if OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_CHAINED_JUMP2_LOGICAL_MIDPOINT_PROBE
        OverworldWildSpawns_SetObjectLogicalTileOnly(object, landingX, landingY);
#else
        if (customJumpVisibleLegFinished) {
            OverworldWildSpawns_SetObjectLogicalTileOnly(object, landingX, landingY);
        } else {
            OverworldWildSpawns_SetObjectTile(object, landingX, landingY);
        }
#endif
#endif
    }
#if OW_WILD_SPAWNER_CUSTOM_JUMP_VISIBLE_LEGS
    sOverworldWildCustomJumpVisibleLegFinished[slot] = FALSE;
#endif
    if (OW_WILD_RUNTIME(state)->movementCustomJumpPrepActive[slot]) {
        (void)OverworldWildSpawns_RunImmediateCanopyMovementCommand(
            object,
            OW_WILD_SPAWNER_CANOPY_HOPPER_FREEZE_COMMAND);
        if (!skipFinalMankeyTreeTopRestore) {
            (void)OverworldWildSpawns_RunImmediateCanopyMovementCommand(
                object,
                OW_WILD_SPAWNER_CANOPY_HOPPER_PARTNER_RESTORE_COMMAND);
        }
        OW_WILD_RUNTIME(state)->movementCustomJumpPrepActive[slot] = FALSE;
    }
    OverworldWildSpawns_ClearCanopyHopperVisualStateAtBoundary(state, fieldSystem, slot);
#if OW_WILD_SPAWNER_CUSTOM_JUMP_POST_RESTORE_FINALIZE
    customJumpRenderSettleActive =
        OverworldWildSpawns_FinalizeCustomJumpAfterRestore(
            state,
            slot,
            object,
            customJumpLandingFinalized);
#endif
    if (customJumpRenderSettleActive) {
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
        OverworldWildSpawns_EnsureFrameMovementTask(state, fieldSystem);
#endif
        OverworldWildSpawns_RecordCustomJumpRamObject(state, slot, object, 18);
        return TRUE;
    }
    if (finalLanding) {
        object = OverworldWildSpawns_NormalizeSpawnHopLandingObject(
            state,
            fieldSystem,
            slot,
            object,
            MapObject_GetCurrentX(object),
            MapObject_GetCurrentY(object));
        OverworldWildSpawns_FinishPendingStagedHop(state, slot, object, &profile);
        OverworldWildSpawns_RefreshCanopyHopperVisualStateAtLanding(state, fieldSystem, slot, object);
        OverworldWildSpawns_RecordCustomJumpRamObject(state, slot, object, 15);
    } else {
        state->movementCooldowns[slot] = OW_WILD_SPAWNER_CANOPY_HOPPER_SEGMENT_SETTLE_FRAMES;
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
        OverworldWildSpawns_EnsureFrameMovementTask(state, fieldSystem);
#endif
        OverworldWildSpawns_RecordCustomJumpRamObject(state, slot, object, 16);
    }
    return TRUE;
}

static BOOL OverworldWildSpawns_ExecutePendingStagedHop(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    LocalMapObject *object,
    const OverworldWildBehaviorProfile *profile)
{
    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || object == NULL
        || profile == NULL
        || !state->movementStagedHopPending[slot]) {
        return FALSE;
    }

    if (OverworldWildSpawns_IsAtStagedHopTarget(state, slot, object)) {
        OverworldWildSpawns_SetObjectLandingTile(
            fieldSystem,
            object,
            MapObject_GetCurrentX(object),
            MapObject_GetCurrentY(object));
        OverworldWildSpawns_FinishPendingStagedHop(state, slot, object, profile);
        return TRUE;
    }

    return OverworldWildSpawns_TryStartNextStagedHopMovementCommand(
        state,
        fieldSystem,
        slot,
        object);
}

#if OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_SINGLE_JUMP_PROBE \
    || OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_JUMP2_PROBE \
    || OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_CHAINED_JUMP2_PROBE \
    || OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_CHAINED_JUMP2_NO_MIDPOINT_NORMALIZE_PROBE \
    || OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_CHAINED_JUMP2_LOGICAL_MIDPOINT_PROBE \
    || OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_WRAPPED_JUMP2_PROBE \
    || OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_WRAPPED_CHAINED_JUMP2_PROBE \
    || OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_SINGLE_WRAPPER_CHAINED_JUMP2_PROBE \
    || OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_PARTNER_PREPPED_INTERNAL_JUMP_PROBE
static BOOL OverworldWildSpawns_TryPickStraightCanopyStepTarget(
    FieldSystem *fieldSystem,
    LocalMapObject *object,
    u8 distance,
    int *targetX,
    int *targetY,
    u8 *direction)
{
    u32 startIndex;
    u32 i;
    int objectX;
    int objectY;

    if (fieldSystem == NULL
        || object == NULL
        || distance == 0
        || targetX == NULL
        || targetY == NULL
        || direction == NULL) {
        return FALSE;
    }

    objectX = MapObject_GetCurrentX(object);
    objectY = MapObject_GetCurrentY(object);
    if (objectX < 0 || objectY < 0) {
        return FALSE;
    }

    startIndex = gf_rand() % NELEMS(sCustomJumpOffsets);
    for (i = 0; i < NELEMS(sCustomJumpOffsets); i++) {
        const OverworldWildCoordOffset *offset =
            &sCustomJumpOffsets[(startIndex + i) % NELEMS(sCustomJumpOffsets)];
        int candidateX = objectX + offset->dx * distance;
        int candidateY = objectY + offset->dy * distance;
        int stepDistance;
        u8 directions[OW_WILD_SPAWNER_MOVEMENT_MAX_DIRECTIONS];

        if (!OverworldWildSpawns_IsCanopyPathTile(fieldSystem, candidateX, candidateY)
            || OverworldWildSpawns_DiagnosticBuildDirections(
                   candidateX - objectX,
                   candidateY - objectY,
                   directions) == 0) {
            continue;
        }
        for (stepDistance = OW_WILD_SPAWNER_MOVEMENT_DISTANCE_LEDGE_JUMP;
             stepDistance < distance;
             stepDistance += OW_WILD_SPAWNER_MOVEMENT_DISTANCE_LEDGE_JUMP) {
            if (!OverworldWildSpawns_IsCanopyPathTile(
                    fieldSystem,
                    objectX + offset->dx * stepDistance,
                    objectY + offset->dy * stepDistance)) {
                break;
            }
        }
        if (stepDistance < distance) {
            continue;
        }

        *targetX = candidateX;
        *targetY = candidateY;
        *direction = directions[0];
        return TRUE;
    }

    return FALSE;
}
#endif

#if OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_WRAPPED_JUMP2_PROBE \
    || OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_WRAPPED_CHAINED_JUMP2_PROBE \
    || OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_SINGLE_WRAPPER_CHAINED_JUMP2_PROBE
static BOOL OverworldWildSpawns_StartWrappedCanopyJump2Probe(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    LocalMapObject *object,
    u8 direction,
    u8 segmentCount)
{
    u16 *movementList;
    SysTask *movementTask;
    u16 wrappedJumpCommand;
    u8 segment;
    u8 wordIndex = 0;
    int objectX;
    int objectY;
    BOOL useSingleWrapper =
        OW_WILD_SPAWNER_CANOPY_TREE_ANCHOR_SINGLE_WRAPPER_CHAINED_JUMP2_PROBE;

    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || object == NULL
        || segmentCount == 0
        || segmentCount > 2) {
        return FALSE;
    }

    objectX = MapObject_GetCurrentX(object);
    objectY = MapObject_GetCurrentY(object);
    wrappedJumpCommand = (u16)MapObject_MovementCommandFromDirection(
        direction,
        OW_WILD_SPAWNER_CANOPY_HOPPER_JUMP_2_COMMAND);

    OverworldWildSpawns_ClearStagedHopMovementListTask(state, slot);
    movementList = OverworldWildSpawns_AllocStagedHopMovementList(slot);
    if (movementList == NULL) {
        return FALSE;
    }

    if (useSingleWrapper) {
        movementList[wordIndex++] = OW_WILD_SPAWNER_CANOPY_HOPPER_PARTNER_PREP_COMMAND;
        movementList[wordIndex++] = 1;
    }
    for (segment = 0; segment < segmentCount; segment++) {
        if (!useSingleWrapper) {
            movementList[wordIndex++] = OW_WILD_SPAWNER_CANOPY_HOPPER_PARTNER_PREP_COMMAND;
            movementList[wordIndex++] = 1;
        }
        movementList[wordIndex++] = wrappedJumpCommand;
        movementList[wordIndex++] = 1;
        if (!useSingleWrapper) {
            movementList[wordIndex++] = OW_WILD_SPAWNER_CANOPY_HOPPER_FREEZE_COMMAND;
            movementList[wordIndex++] = 1;
            movementList[wordIndex++] = OW_WILD_SPAWNER_CANOPY_HOPPER_PARTNER_RESTORE_COMMAND;
            movementList[wordIndex++] = 1;
        }
    }
    if (useSingleWrapper) {
        movementList[wordIndex++] = OW_WILD_SPAWNER_CANOPY_HOPPER_FREEZE_COMMAND;
        movementList[wordIndex++] = 1;
        movementList[wordIndex++] = OW_WILD_SPAWNER_CANOPY_HOPPER_PARTNER_RESTORE_COMMAND;
        movementList[wordIndex++] = 1;
    }
    movementList[wordIndex++] = OW_WILD_SPAWNER_CANOPY_HOPPER_MOVEMENT_END_COMMAND;
    movementList[wordIndex++] = 0;

    OverworldWildSpawns_ClearCanopyHopperVisualStateAtBoundary(state, fieldSystem, slot);
    OverworldWildSpawns_SetObjectFacing(object, direction);
    MapObject_ClearBits(object, BIT_VANISH);
    movementTask = MapObject_StartMovementList(object, movementList);
    if (movementTask == NULL) {
        OverworldWildSpawns_ClearStagedHopMovementListTask(state, slot);
        return FALSE;
    }

    sOverworldWildStagedHopMovementTasks[slot] = movementTask;
    state->movementFieldSystem = fieldSystem;
    OverworldWildSpawns_SetPreviousTile(state, slot, objectX, objectY);
    state->movementPendingDirections[slot] = direction;
    state->movementPendingDistances[slot] =
        (u8)(OW_WILD_SPAWNER_MOVEMENT_DISTANCE_LEDGE_JUMP * segmentCount);
    state->movementStagedHopDistances[slot] =
        (u8)(OW_WILD_SPAWNER_MOVEMENT_DISTANCE_LEDGE_JUMP * segmentCount);
    state->movementBattleSettleFrames = 0;
    OverworldWildSpawns_SetMovementSlotInProgress(state, slot);
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
    OverworldWildSpawns_EnsureFrameMovementTask(state, fieldSystem);
#endif
    sOverworldWildMovementDiagnosticLookCommand = wrappedJumpCommand;
    sOverworldWildMovementDiagnosticWalkIssued = TRUE;
    return TRUE;
}
#endif

static BOOL OverworldWildSpawns_ContinuePendingStagedHop(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot)
{
    LocalMapObject *object;
    OverworldWildBehaviorProfile profile;
    OverworldWildBehaviorPrimitives primitives;

    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !state->spawns[slot].active
        || !state->movementStagedHopPending[slot]) {
        return FALSE;
    }

    object = state->spawns[slot].object;
    if (object == NULL
        || OverworldWildSpawns_IsMovementSlotInProgress(state, slot)) {
        return FALSE;
    }

    profile = OverworldWildSpawns_GetBehaviorProfile(state, slot);
    primitives = OverworldWildSpawns_ResolveBehaviorPrimitives(&profile);
    if (primitives.chillLocomotion != OW_WILD_BEHAVIOR_LOCOMOTION_HOP
        && primitives.attentiveLocomotion != OW_WILD_BEHAVIOR_LOCOMOTION_HOP) {
        return FALSE;
    }
    if (MapObject_IsSingleMovementActive(object)) {
        MapObject_ClearSingleMovementActive(object);
    }

    return OverworldWildSpawns_ExecutePendingStagedHop(
        state,
        fieldSystem,
        slot,
        object,
        &profile);
}
#endif

static u32 OverworldWildSpawns_GetSpriteID(u16 species, u8 form)
{
    return OVERWORLD_WILD_SPAWN_METADATA_ENTRY->getSpriteId(species, form);
}

static u8 OverworldWildSpawns_GetLegacyMovementBehavior(const OverworldWildBehaviorProfile *profile)
{
    OverworldWildBehaviorPrimitives primitives =
        OverworldWildSpawns_ResolveBehaviorPrimitives(profile);

    if (primitives.attentiveTarget == OW_WILD_BEHAVIOR_TARGET_AWAY_FROM_PLAYER) {
        return OW_WILD_MOVEMENT_BEHAVIOR_FLEE_PLAYER;
    }

    return OW_WILD_MOVEMENT_BEHAVIOR_CHASE_PLAYER;
}

static LocalMapObject *OverworldWildSpawns_CreateObject(
    FieldSystem *fieldSystem,
    const OverworldWildSpawnPosition *position,
    u32 spriteId,
    BOOL shiny,
    const OverworldWildBehaviorProfile *profile)
{
    LocalMapObject *object;

    OverworldWildCustomMovement_SetFieldSystem(fieldSystem);

    object = CreateSpecialFieldObjectWithParams(
        fieldSystem->mapObjectMan,
        position->startX,
        position->startY,
        1,
        spriteId,
        OW_WILD_SPAWNER_MOVEMENT_OBJECT_MOVEMENT,
        fieldSystem->location->mapId,
        0,
        OverworldWildSpawns_GetLegacyMovementBehavior(profile),
        OW_WILD_PAL_PARAM_ENABLE | (shiny ? OW_WILD_PAL_PARAM_SHINY : 0));
    if (object != NULL) {
        OW_WILD_PERF_INC(sOverworldWildPerfMapObjectCreatesThisFrame);
        object->flags = (object->flags & ~BIT_VANISH) | MAPOBJECTFLAG_KEEP;
    }

    return object;
}

static void OverworldWildSpawns_ApplyMovementRange(LocalMapObject *object, u8 range)
{
    if (range == 0) {
        range = OW_WILD_SPAWNER_MOVEMENT_RANGE;
    }

    MapObject_SetXRange(object, range);
    MapObject_SetYRange(object, range);
}

static void OverworldWildSpawns_ApplyPokemonRenderParams(
    LocalMapObject *object,
    u16 species,
    u8 form,
    u32 spriteId,
    BOOL shiny)
{
    OVERWORLD_WILD_SPAWN_METADATA_ENTRY->applyRenderParams(
        object,
        species,
        form,
        spriteId,
        shiny);
    if (shiny) {
        object->flags &= ~BIT_VANISH;
    }
}

static LocalMapObject *__attribute__((noinline)) OverworldWildSpawns_RecreateSpawnObjectAtTile(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    LocalMapObject *object,
    int x,
    int y)
{
    const OverworldWildHelperOverlayEntry *helperEntry;
    OverworldWildBehaviorProfile profile;
    LocalMapObject *replacement;
    BOOL oldObjectCurrent;
    BOOL managerRestore;
    int facing;

    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !state->spawns[slot].active
        || state->spawns[slot].species == SPECIES_NONE) {
        return object;
    }

    if (object == NULL) {
        object = state->spawns[slot].object;
    }
    if (!OverworldWildSpawns_IsMovementFieldContextCurrent(state, fieldSystem)
        || state->spawns[slot].mapId != fieldSystem->location->mapId) {
        return NULL;
    }
    oldObjectCurrent = object != NULL
        && object == state->spawns[slot].object
        && OverworldWildSpawns_IsCurrentSpawnObject(fieldSystem, &state->spawns[slot]);
    managerRestore = (OW_WILD_RUNTIME(state)->spawnPresentations.managerRestoreMask
        & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot)) != 0;
    if (!oldObjectCurrent && !managerRestore) {
        return NULL;
    }

    if (oldObjectCurrent && MapObject_IsSingleMovementActive(object)) {
        MapObject_ClearSingleMovementActive(object);
    }

    facing = oldObjectCurrent
        ? object->curFacing
        : OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN;
    if (facing < OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_UP
        || facing > OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT) {
        facing = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN;
    }

    profile = OverworldWildSpawns_GetBehaviorProfile(state, slot);
    if (oldObjectCurrent) {
        /*
         * Canonicalize every existing primary record in place so every owner
         * keeps the same object identity.  Only a genuinely missing record
         * after a verified manager replacement may be recreated below.
         */
        OverworldWildSpawns_SetObjectFacing(object, (u8)facing);
        OverworldWildSpawns_SetObjectLandingTile(fieldSystem, object, x, y);
        MapObject_ClearBits(object, MAPOBJECTFLAG_UNK8 | MAPOBJECTFLAG_UNK18);
        OverworldWildSpawns_ApplyMovementRange(object, profile.range);
        OverworldWildSpawns_ApplySpawnPassThroughFlag(state, slot, object);
        OW_WILD_RUNTIME(state)->spawnPresentations.lastKnownX[slot] = (s16)x;
        OW_WILD_RUNTIME(state)->spawnPresentations.lastKnownY[slot] = (s16)y;
        OverworldWildSpawns_ClearCanopyHopperVisualStateAtBoundary(state, fieldSystem, slot);
        return object;
    }

    helperEntry = OverworldWildSpawns_GetHelperOverlayEntry();
    replacement = helperEntry != NULL && helperEntry->createPresentationObject != NULL
        ? helperEntry->createPresentationObject(
        fieldSystem,
        state,
        slot,
        x,
        y,
        (u8)facing,
        OverworldWildSpawns_GetLegacyMovementBehavior(&profile),
        profile.range)
        : NULL;
    if (replacement == NULL) {
        return NULL;
    }

    OverworldWildSpawns_ApplySpawnPassThroughFlag(state, slot, replacement);

    state->spawns[slot].object = replacement;
    state->spawns[slot].objectId = OW_WILD_OBJECT_ID_START + slot;
    OW_WILD_RUNTIME(state)->spawnPresentations.lastKnownX[slot] = (s16)x;
    OW_WILD_RUNTIME(state)->spawnPresentations.lastKnownY[slot] = (s16)y;
    OverworldWildSpawns_ClearCanopyHopperVisualStateAtBoundary(state, fieldSystem, slot);
    return replacement;
}

static BOOL OverworldWildSpawns_TryPickSpawnPosition(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    OverworldWildSpawnTerrain requestedTerrain,
    OverworldWildSpawnPosition *position)
{
    int playerX;
    int playerY;
    u32 candidateCount = 0;
    int x;
    int y;

    if (state == NULL
        || fieldSystem == NULL
        || fieldSystem->playerAvatar == NULL
        || position == NULL
        || (requestedTerrain != OW_WILD_SPAWN_TERRAIN_LAND
            && requestedTerrain != OW_WILD_SPAWN_TERRAIN_SURF)) {
        return FALSE;
    }

    playerX = GetPlayerXCoord(fieldSystem->playerAvatar);
    playerY = GetPlayerYCoord(fieldSystem->playerAvatar);
    for (y = playerY - OW_WILD_SPAWN_MAX_DISTANCE; y <= playerY + OW_WILD_SPAWN_MAX_DISTANCE; y++) {
        for (x = playerX - OW_WILD_SPAWN_MAX_DISTANCE; x <= playerX + OW_WILD_SPAWN_MAX_DISTANCE; x++) {
            int dx = x - playerX;
            int dy = y - playerY;
            int distance = OverworldWildSpawns_Max(OverworldWildSpawns_Abs(dx), OverworldWildSpawns_Abs(dy));
            OverworldWildSpawnTerrain terrain;

            if (distance < OW_WILD_SPAWN_MIN_DISTANCE
                || distance > OW_WILD_SPAWN_MAX_DISTANCE
                || !OverworldWildSpawns_TryGetSpawnTerrain(fieldSystem, x, y, &terrain)
                || terrain != requestedTerrain
                || OverworldWildSpawns_IsTileOccupiedByObject(fieldSystem, x, y)
                || OverworldWildSpawns_IsNearActiveSpawn(state, x, y, OW_WILD_SPAWN_MIN_MON_DISTANCE)) {
                continue;
            }

            candidateCount++;
            if ((gf_rand() % candidateCount) == 0) {
                position->startX = x;
                position->startY = y;
            }
        }
    }

    return candidateCount != 0;
}

static BOOL OverworldWildSpawns_TryPickWalkableLandSpawnPosition(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    OverworldWildSpawnPosition *position)
{
    int playerX = GetPlayerXCoord(fieldSystem->playerAvatar);
    int playerY = GetPlayerYCoord(fieldSystem->playerAvatar);
    u32 candidateCount = 0;
    int x;
    int y;

    for (y = playerY - OW_WILD_SPAWN_MAX_DISTANCE; y <= playerY + OW_WILD_SPAWN_MAX_DISTANCE; y++) {
        for (x = playerX - OW_WILD_SPAWN_MAX_DISTANCE; x <= playerX + OW_WILD_SPAWN_MAX_DISTANCE; x++) {
            int dx = x - playerX;
            int dy = y - playerY;
            int distance = OverworldWildSpawns_Max(OverworldWildSpawns_Abs(dx), OverworldWildSpawns_Abs(dy));

            if (distance < OW_WILD_SPAWN_MIN_DISTANCE
                || distance > OW_WILD_SPAWN_MAX_DISTANCE
                || !OverworldWildSpawns_IsWalkableLandTile(fieldSystem, x, y)
                || OverworldWildSpawns_IsNearActiveSpawn(state, x, y, OW_WILD_SPAWN_MIN_MON_DISTANCE)) {
                continue;
            }

            candidateCount++;
            if ((gf_rand() % candidateCount) == 0) {
                position->startX = x;
                position->startY = y;
            }
        }
    }

    return candidateCount != 0;
}

static BOOL OverworldWildSpawns_TryPickFishingSpawnPosition(
    FieldSystem *fieldSystem,
    OverworldWildSpawnPosition *position)
{
    int playerX;
    int playerY;
    u32 start;
    u32 i;

    if (fieldSystem == NULL
        || fieldSystem->playerAvatar == NULL
        || position == NULL) {
        return FALSE;
    }

    playerX = GetPlayerXCoord(fieldSystem->playerAvatar);
    playerY = GetPlayerYCoord(fieldSystem->playerAvatar);
    start = gf_rand() & 3;
    for (i = 0; i < 4; i++) {
        const OverworldWildCoordOffset *offset = &sCustomJumpOffsets[(start + i) & 3];
        int x = playerX + offset->dx;
        int y = playerY + offset->dy;

        if (!OverworldWildSpawns_IsFishingShoreTile(fieldSystem, x, y)) {
            continue;
        }

        position->startX = x;
        position->startY = y;
        return TRUE;
    }

    return FALSE;
}

static BOOL OverworldWildSpawns_TryPickCanopyHopperTreeTopSpawnPosition(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    OverworldWildSpawnPosition *position)
{
    OverworldWildHeadbuttTreeTopScan scan;
    u32 candidateCount = 0;
    int playerX;
    int playerY;
    int treeX;
    int treeY;

    if (state == NULL
        || fieldSystem == NULL
        || fieldSystem->playerAvatar == NULL
        || position == NULL) {
        return FALSE;
    }

    playerX = GetPlayerXCoord(fieldSystem->playerAvatar);
    playerY = GetPlayerYCoord(fieldSystem->playerAvatar);
    if (!OverworldWildSpawns_InitHeadbuttTreeTopScan(
            fieldSystem,
            &scan,
            playerX,
            playerY,
            OW_WILD_SPAWN_MAX_DISTANCE)) {
        return FALSE;
    }

    while (OverworldWildSpawns_NextHeadbuttTreeTopCandidate(fieldSystem, &scan, &treeX, &treeY)) {
        int distance = OverworldWildSpawns_Max(
            OverworldWildSpawns_Abs(treeX - playerX),
            OverworldWildSpawns_Abs(treeY - playerY));

        if (distance < OW_WILD_SPAWN_MIN_DISTANCE
            || distance > OW_WILD_SPAWN_MAX_DISTANCE
            || OverworldWildSpawns_IsTileOccupiedByObject(fieldSystem, treeX, treeY)
            || OverworldWildSpawns_IsNearActiveSpawn(state, treeX, treeY, OW_WILD_SPAWN_MIN_MON_DISTANCE)) {
            continue;
        }

        candidateCount++;
        if ((gf_rand() % candidateCount) == 0) {
            position->startX = treeX;
            position->startY = treeY;
        }
    }

    return candidateCount != 0;
}

static BOOL OverworldWildSpawns_TryGetPlayerRelativeSpawnDestination(
    OverworldWildSpawnDestination destination,
    int *directionScale,
    int *distanceOut)
{
    if (directionScale == NULL || distanceOut == NULL) {
        return FALSE;
    }

    switch (destination) {
    case OW_WILD_SPAWN_DESTINATION_FRONT_OF_PLAYER:
        *directionScale = 1;
        *distanceOut = 1;
        return TRUE;
    case OW_WILD_SPAWN_DESTINATION_TWO_TILES_FRONT_OF_PLAYER:
        *directionScale = 1;
        *distanceOut = 2;
        return TRUE;
    case OW_WILD_SPAWN_DESTINATION_THREE_TILES_FRONT_OF_PLAYER:
        *directionScale = 1;
        *distanceOut = 3;
        return TRUE;
    case OW_WILD_SPAWN_DESTINATION_FOUR_TILES_FRONT_OF_PLAYER:
        *directionScale = 1;
        *distanceOut = 4;
        return TRUE;
    case OW_WILD_SPAWN_DESTINATION_FIVE_TILES_FRONT_OF_PLAYER:
        *directionScale = 1;
        *distanceOut = 5;
        return TRUE;
    case OW_WILD_SPAWN_DESTINATION_ONE_TILE_BEHIND_PLAYER:
        *directionScale = -1;
        *distanceOut = 1;
        return TRUE;
    case OW_WILD_SPAWN_DESTINATION_TWO_TILES_BEHIND_PLAYER:
        *directionScale = -1;
        *distanceOut = 2;
        return TRUE;
    case OW_WILD_SPAWN_DESTINATION_THREE_TILES_BEHIND_PLAYER:
        *directionScale = -1;
        *distanceOut = 3;
        return TRUE;
    case OW_WILD_SPAWN_DESTINATION_FOUR_TILES_BEHIND_PLAYER:
        *directionScale = -1;
        *distanceOut = 4;
        return TRUE;
    case OW_WILD_SPAWN_DESTINATION_FIVE_TILES_BEHIND_PLAYER:
        *directionScale = -1;
        *distanceOut = 5;
        return TRUE;
    default:
        return FALSE;
    }
}

static void OverworldWildSpawns_GetPlayerRelativeSpawnDistanceRange(
    const OverworldWildBehaviorProfile *profile,
    int *minDistanceOut,
    int *maxDistanceOut)
{
    int minDistance = OW_WILD_PLAYER_RELATIVE_SPAWN_MIN_DISTANCE;
    int maxDistance = OW_WILD_PLAYER_RELATIVE_SPAWN_MIN_DISTANCE;

    if (profile != NULL) {
        minDistance = profile->spawnDestinationMinDistance;
        maxDistance = profile->spawnDestinationMaxDistance;
    }
    if (minDistance < OW_WILD_PLAYER_RELATIVE_SPAWN_MIN_DISTANCE) {
        minDistance = OW_WILD_PLAYER_RELATIVE_SPAWN_MIN_DISTANCE;
    } else if (minDistance > OW_WILD_PLAYER_RELATIVE_SPAWN_MAX_DISTANCE) {
        minDistance = OW_WILD_PLAYER_RELATIVE_SPAWN_MAX_DISTANCE;
    }
    if (maxDistance < OW_WILD_PLAYER_RELATIVE_SPAWN_MIN_DISTANCE) {
        maxDistance = OW_WILD_PLAYER_RELATIVE_SPAWN_MAX_DISTANCE;
    } else if (maxDistance > OW_WILD_PLAYER_RELATIVE_SPAWN_MAX_DISTANCE) {
        maxDistance = OW_WILD_PLAYER_RELATIVE_SPAWN_MAX_DISTANCE;
    }
    if (maxDistance < minDistance) {
        maxDistance = minDistance;
    }

    *minDistanceOut = minDistance;
    *maxDistanceOut = maxDistance;
}

static BOOL OverworldWildSpawns_IsUsablePlayerRangeSpawnTile(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int x,
    int y)
{
    OverworldWildSpawnTerrain terrain;

    if (x < 0 || y < 0) {
        return FALSE;
    }
    if (OverworldWildSpawns_IsTileOccupiedByObject(fieldSystem, x, y)
        || OverworldWildSpawns_IsNearActiveSpawn(state, x, y, OW_WILD_SPAWN_MIN_MON_DISTANCE)) {
        return FALSE;
    }
    if (OverworldWildSpawns_IsWalkableLandTile(fieldSystem, x, y)) {
        return TRUE;
    }
    return OverworldWildSpawns_TryGetSpawnTerrain(fieldSystem, x, y, &terrain)
        && terrain == OW_WILD_SPAWN_TERRAIN_SURF;
}

static BOOL OverworldWildSpawns_TryPickPlayerRelativeSpawnPosition(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    const OverworldWildBehaviorProfile *profile,
    OverworldWildSpawnDestination destination,
    OverworldWildSpawnPosition *position)
{
    LocalMapObject *playerObject;
    int playerX;
    int playerY;
    int x;
    int y;
    int directionScale;
    int exactDistance;
    int distance;
    int minDistance;
    int maxDistance;
    int candidateCount = 0;
    u8 facing;

    if (state == NULL
        || fieldSystem == NULL
        || fieldSystem->playerAvatar == NULL
        || position == NULL) {
        return FALSE;
    }

    playerObject = fieldSystem->playerAvatar->mapObject;
    if (playerObject == NULL
        || !OverworldWildSpawns_IsCurrentMapObject(fieldSystem, playerObject)
        || (playerObject->flags & MAPOBJECTFLAG_ACTIVE) == 0) {
        return FALSE;
    }

    facing = playerObject->curFacing;
    if (facing > OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT) {
        return FALSE;
    }

    playerX = MapObject_GetCurrentX(playerObject);
    playerY = MapObject_GetCurrentY(playerObject);
    if (!OverworldWildSpawns_TryGetPlayerRelativeSpawnDestination(
            destination,
            &directionScale,
            &exactDistance)) {
        return FALSE;
    }
    if (exactDistance > 0) {
        minDistance = exactDistance;
        maxDistance = exactDistance;
    } else {
        OverworldWildSpawns_GetPlayerRelativeSpawnDistanceRange(profile, &minDistance, &maxDistance);
    }

    for (distance = minDistance; distance <= maxDistance; distance++) {
        x = playerX + OverworldWildSpawns_MovementDirectionDeltaX(facing)
            * directionScale
            * distance;
        y = playerY + OverworldWildSpawns_MovementDirectionDeltaY(facing)
            * directionScale
            * distance;

        if (!OverworldWildSpawns_IsUsablePlayerRangeSpawnTile(state, fieldSystem, x, y)) {
            continue;
        }

        candidateCount++;
        if ((gf_rand() % candidateCount) == 0) {
            position->startX = x;
            position->startY = y;
        }
    }

    return candidateCount != 0;
}

static BOOL OverworldWildSpawns_TryPickNextToPlayerSpawnPosition(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    const OverworldWildBehaviorProfile *profile,
    OverworldWildSpawnPosition *position)
{
    LocalMapObject *playerObject;
    int playerX;
    int playerY;
    int x;
    int y;
    int dx;
    int dy;
    int distance;
    int minDistance;
    int maxDistance;
    int candidateCount = 0;

    if (state == NULL
        || fieldSystem == NULL
        || fieldSystem->playerAvatar == NULL
        || position == NULL) {
        return FALSE;
    }

    playerObject = fieldSystem->playerAvatar->mapObject;
    if (playerObject == NULL
        || !OverworldWildSpawns_IsCurrentMapObject(fieldSystem, playerObject)
        || (playerObject->flags & MAPOBJECTFLAG_ACTIVE) == 0) {
        return FALSE;
    }

    playerX = MapObject_GetCurrentX(playerObject);
    playerY = MapObject_GetCurrentY(playerObject);
    OverworldWildSpawns_GetPlayerRelativeSpawnDistanceRange(profile, &minDistance, &maxDistance);

    for (dy = -maxDistance; dy <= maxDistance; dy++) {
        for (dx = -maxDistance; dx <= maxDistance; dx++) {
            distance = OverworldWildSpawns_Max(
                OverworldWildSpawns_Abs(dx),
                OverworldWildSpawns_Abs(dy));
            if (distance < minDistance || distance > maxDistance) {
                continue;
            }

            x = playerX + dx;
            y = playerY + dy;
            if (!OverworldWildSpawns_IsUsablePlayerRangeSpawnTile(state, fieldSystem, x, y)) {
                continue;
            }

            candidateCount++;
            if ((gf_rand() % candidateCount) == 0) {
                position->startX = x;
                position->startY = y;
            }
        }
    }

    return candidateCount != 0;
}

static BOOL OverworldWildSpawns_TryApplySpawnDestination(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    OverworldWildSpawnTerrain terrain,
    const OverworldWildBehaviorProfile *profile,
    OverworldWildSpawnPosition *position)
{
    OverworldWildSpawnDestination destination;

    if (profile == NULL || position == NULL) {
        return FALSE;
    }

    destination = terrain == OW_WILD_SPAWN_TERRAIN_HEADBUTT
        ? OW_WILD_SPAWN_DESTINATION_CANOPY
        : (OverworldWildSpawnDestination)profile->spawnDestination;
    if (destination == OW_WILD_SPAWN_DESTINATION_POOL) {
        if (OverworldWildSpawns_IsCanopyHopperTreeTopProfile(profile)) {
            destination = OW_WILD_SPAWN_DESTINATION_CANOPY;
        } else {
            return TRUE;
        }
    }

    switch (destination) {
    case OW_WILD_SPAWN_DESTINATION_CANOPY:
        return OverworldWildSpawns_TryPickCanopyHopperTreeTopSpawnPosition(state, fieldSystem, position);
    case OW_WILD_SPAWN_DESTINATION_LAND:
        return OverworldWildSpawns_TryPickWalkableLandSpawnPosition(state, fieldSystem, position);
    case OW_WILD_SPAWN_DESTINATION_GRASS:
        return OverworldWildSpawns_TryPickSpawnPosition(state, fieldSystem, OW_WILD_SPAWN_TERRAIN_LAND, position);
    case OW_WILD_SPAWN_DESTINATION_SHORE:
        return OverworldWildSpawns_TryPickFishingSpawnPosition(fieldSystem, position);
    case OW_WILD_SPAWN_DESTINATION_WATER:
        return OverworldWildSpawns_TryPickSpawnPosition(state, fieldSystem, OW_WILD_SPAWN_TERRAIN_SURF, position);
    case OW_WILD_SPAWN_DESTINATION_FRONT_OF_PLAYER:
    case OW_WILD_SPAWN_DESTINATION_TWO_TILES_FRONT_OF_PLAYER:
    case OW_WILD_SPAWN_DESTINATION_THREE_TILES_FRONT_OF_PLAYER:
    case OW_WILD_SPAWN_DESTINATION_FOUR_TILES_FRONT_OF_PLAYER:
    case OW_WILD_SPAWN_DESTINATION_FIVE_TILES_FRONT_OF_PLAYER:
    case OW_WILD_SPAWN_DESTINATION_ONE_TILE_BEHIND_PLAYER:
    case OW_WILD_SPAWN_DESTINATION_TWO_TILES_BEHIND_PLAYER:
    case OW_WILD_SPAWN_DESTINATION_THREE_TILES_BEHIND_PLAYER:
    case OW_WILD_SPAWN_DESTINATION_FOUR_TILES_BEHIND_PLAYER:
    case OW_WILD_SPAWN_DESTINATION_FIVE_TILES_BEHIND_PLAYER:
        return OverworldWildSpawns_TryPickPlayerRelativeSpawnPosition(
            state,
            fieldSystem,
            profile,
            destination,
            position);
    case OW_WILD_SPAWN_DESTINATION_NEXT_TO_PLAYER:
        return OverworldWildSpawns_TryPickNextToPlayerSpawnPosition(
            state,
            fieldSystem,
            profile,
            position);
    case OW_WILD_SPAWN_DESTINATION_POOL:
    default:
        return TRUE;
    }
}

static BOOL OverworldWildSpawns_PrepareSpawnHopStart(
    FieldSystem *fieldSystem,
    OverworldWildSpawnStartup *startup)
{
    int targetX;
    int targetY;
    int startX;
    int startY;
    int dx;
    int dy;
    u8 direction;

    if (fieldSystem == NULL
        || fieldSystem->playerAvatar == NULL
        || startup == NULL) {
        return FALSE;
    }

    targetX = startup->targetX;
    targetY = startup->targetY;
    dx = targetX - GetPlayerXCoord(fieldSystem->playerAvatar);
    dy = targetY - GetPlayerYCoord(fieldSystem->playerAvatar);

    if (OverworldWildSpawns_Abs(dx) >= OverworldWildSpawns_Abs(dy)) {
        direction = dx < 0
            ? OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT
            : OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_LEFT;
    } else {
        direction = dy < 0
            ? OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN
            : OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_UP;
    }

    startX = targetX - OverworldWildSpawns_MovementDirectionDeltaX(direction)
        * OW_WILD_SPAWNER_SPAWN_HOP_DISTANCE;
    startY = targetY - OverworldWildSpawns_MovementDirectionDeltaY(direction)
        * OW_WILD_SPAWNER_SPAWN_HOP_DISTANCE;
    if (startX < 0
        || startY < 0
        || GetMetatileBehaviorAt(fieldSystem, startX, startY) == 0xFF) {
        return FALSE;
    }

    startup->startX = (s16)startX;
    startup->startY = (s16)startY;
    startup->locomotion = OW_WILD_BEHAVIOR_LOCOMOTION_HOP_FROM_OFF_SCREEN;
    startup->hopDirection = direction;
    return TRUE;
}

static void OverworldWildSpawns_PrepareSpawnStartup(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    OverworldWildSpawnTerrain terrain,
    const OverworldWildBehaviorProfile *profile,
    OverworldWildSpawnPosition *position,
    OverworldWildSpawnStartup *startup)
{
    OverworldWildBehaviorPrimitives primitives =
        OverworldWildSpawns_ResolveBehaviorPrimitives(profile);

    startup->targetX = (s16)position->startX;
    startup->targetY = (s16)position->startY;
    startup->startX = startup->targetX;
    startup->startY = startup->targetY;
    startup->locomotion = OW_WILD_BEHAVIOR_LOCOMOTION_NONE;
    startup->hopDirection = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_NONE;

    if (primitives.spawnLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_RUN_FROM_OFF_SCREEN) {
        if (OverworldWildSpawns_TryPickSpawnRunStart(
                state,
                fieldSystem,
                terrain,
                startup->targetX,
                startup->targetY,
                &position->startX,
                &position->startY)) {
            startup->locomotion = OW_WILD_BEHAVIOR_LOCOMOTION_RUN_FROM_OFF_SCREEN;
        }
    } else if (primitives.spawnLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_HOP_FROM_OFF_SCREEN) {
        (void)OverworldWildSpawns_PrepareSpawnHopStart(
            fieldSystem,
            startup);
    } else if (primitives.spawnLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_APPEAR_HOP) {
        startup->locomotion = OW_WILD_BEHAVIOR_LOCOMOTION_APPEAR_HOP;
    }
}

static OverworldWildBehaviorProfile OverworldWildSpawns_ResolveSpawnBehaviorProfile(
    const OverworldWildRolledEncounter *encounter,
    OverworldWildSpawnTerrain terrain,
    BOOL shiny,
    u8 *behaviorClass,
    u8 *behaviorLimitKey,
    u8 *playerBallCatchValue)
{
    OverworldWildBehaviorContext behaviorContext;
    OverworldWildSpawnMetadata metadata;
    u8 personal[OW_WILD_PLAYER_BALL_PERSONAL_RECORD_SIZE];
    u8 type1;
    u8 type2;

    if (OVERWORLD_WILD_SPAWN_METADATA_ENTRY->tryGet(
            encounter->species,
            encounter->form,
            &metadata)) {
        type1 = metadata.type1;
        type2 = metadata.type2;
        *playerBallCatchValue = metadata.catchValue;
    } else {
        ArchiveDataLoadOfs(
            personal,
            ARC_PERSONAL,
            PokeOtherFormMonsNoGet(encounter->species, encounter->form),
            0,
            sizeof(personal));
        type1 = personal[OW_WILD_PLAYER_BALL_PERSONAL_TYPE_1_OFFSET];
        type2 = personal[OW_WILD_PLAYER_BALL_PERSONAL_TYPE_2_OFFSET];
        *playerBallCatchValue = OverworldWildSpawns_GetPlayerBallCatchValue(
            personal[OW_WILD_PLAYER_BALL_PERSONAL_CATCH_RATE_OFFSET]);
    }
    behaviorContext.species = encounter->species;
    behaviorContext.groupFlags = OverworldWildSpawns_GetBehaviorGroupFlagsForTypes(
        encounter->species,
        type1,
        type2);
    behaviorContext.level = encounter->level;
    behaviorContext.terrain = terrain;
    behaviorContext.shiny = shiny;
    behaviorContext.behaviorClass = OW_WILD_BEHAVIOR_CLASS_DEFAULT;
    *behaviorClass = OverworldWildSpawns_GetBehaviorClassForContext(
        &behaviorContext);
    behaviorContext.behaviorClass = *behaviorClass;
    return OverworldWildSpawns_ResolveBehaviorProfileForContext(&behaviorContext, behaviorLimitKey);
}

static BOOL OverworldWildSpawns_FinalizePreparedSpawn(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    OverworldWildSpawnTerrain terrain,
    OverworldWildPreparedSpawn *prepared)
{
    struct PlayerProfile *profile;

    if (state == NULL
        || fieldSystem == NULL
        || fieldSystem->savedata == NULL
        || prepared == NULL
        || prepared->encounter.species == SPECIES_NONE
        || prepared->encounter.level == 0) {
        return FALSE;
    }

    prepared->behaviorProfile = OverworldWildSpawns_ResolveSpawnBehaviorProfile(
        &prepared->encounter,
        terrain,
        prepared->shiny,
        &prepared->behaviorClass,
        &prepared->behaviorLimitKey,
        &prepared->playerBallCatchValue);
    if (OverworldWildSpawns_IsBehaviorLimitKeyAtOverworldLimit(
            state,
            prepared->behaviorLimitKey,
            &prepared->behaviorProfile)) {
        return FALSE;
    }
    if (!OverworldWildSpawns_TryApplySpawnDestination(
            state,
            fieldSystem,
            terrain,
            &prepared->behaviorProfile,
            &prepared->position)) {
        return FALSE;
    }
    OverworldWildSpawns_PrepareSpawnStartup(
        state,
        fieldSystem,
        terrain,
        &prepared->behaviorProfile,
        &prepared->position,
        &prepared->startup);
    profile = Sav2_PlayerData_GetProfileAddr(fieldSystem->savedata);
    if (profile == NULL) {
        return FALSE;
    }
    prepared->encounter.personality =
        OVERWORLD_WILD_CAPTURE_UTILITIES_ENTRY->finalizePersonality(
            prepared->encounter.personality,
            profile->id,
            prepared->shiny);
    return TRUE;
}

static BOOL OverworldWildSpawns_HelperGetPlayerState(
    void *context,
    OverworldWildHelperPlayerState *playerState)
{
    OverworldWildSpawnPrepContext *prepContext = (OverworldWildSpawnPrepContext *)context;
    FieldSystem *fieldSystem;
    LocalMapObject *playerObject;

    if (prepContext == NULL
        || prepContext->fieldSystem == NULL
        || prepContext->fieldSystem->playerAvatar == NULL
        || playerState == NULL) {
        return FALSE;
    }

    fieldSystem = prepContext->fieldSystem;
    playerState->playerX = GetPlayerXCoord(fieldSystem->playerAvatar);
    playerState->playerY = GetPlayerYCoord(fieldSystem->playerAvatar);
    playerState->objectX = playerState->playerX;
    playerState->objectY = playerState->playerY;
    playerState->facing = OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_NONE;
    playerState->hasObject = FALSE;
    playerState->reserved[0] = 0;
    playerState->reserved[1] = 0;

    playerObject = fieldSystem->playerAvatar->mapObject;
    if (playerObject != NULL
        && OverworldWildSpawns_IsCurrentMapObject(fieldSystem, playerObject)
        && (playerObject->flags & MAPOBJECTFLAG_ACTIVE) != 0) {
        playerState->objectX = MapObject_GetCurrentX(playerObject);
        playerState->objectY = MapObject_GetCurrentY(playerObject);
        playerState->facing = playerObject->curFacing;
        playerState->hasObject = TRUE;
    }

    return TRUE;
}

static BOOL OverworldWildSpawns_HelperTryGetSpawnTerrain(
    void *context,
    int x,
    int y,
    OverworldWildSpawnTerrain *terrain)
{
    OverworldWildSpawnPrepContext *prepContext = (OverworldWildSpawnPrepContext *)context;

    return OverworldWildSpawns_TryGetSpawnTerrain(
        prepContext->fieldSystem,
        x,
        y,
        terrain);
}

static BOOL OverworldWildSpawns_HelperIsTileOccupied(void *context, int x, int y)
{
    OverworldWildSpawnPrepContext *prepContext = (OverworldWildSpawnPrepContext *)context;

    return OverworldWildSpawns_IsTileOccupiedByObject(prepContext->fieldSystem, x, y);
}

static BOOL OverworldWildSpawns_HelperIsNearActiveSpawn(
    void *context,
    int x,
    int y,
    int radius)
{
    OverworldWildSpawnPrepContext *prepContext = (OverworldWildSpawnPrepContext *)context;

    return OverworldWildSpawns_IsNearActiveSpawn(prepContext->state, x, y, radius);
}

static BOOL OverworldWildSpawns_HelperGetMapId(void *context, u16 *mapId)
{
    OverworldWildSpawnPrepContext *prepContext = (OverworldWildSpawnPrepContext *)context;

    if (prepContext == NULL
        || prepContext->fieldSystem == NULL
        || prepContext->fieldSystem->location == NULL
        || mapId == NULL) {
        return FALSE;
    }

    *mapId = prepContext->fieldSystem->location->mapId;
    return TRUE;
}

static BOOL OverworldWildSpawns_HelperLoadArchiveData(
    void *context,
    int arcId,
    int datId,
    int offset,
    void *dest,
    int size)
{
    OverworldWildSpawnPrepContext *prepContext = (OverworldWildSpawnPrepContext *)context;

    if (dest == NULL || size <= 0) {
        return FALSE;
    }

    if (arcId == ARC_ENCOUNTERS
        && offset == 0
        && size == MAP_EVENTS_WILD_ENCOUNTERS_SIZE
        && prepContext != NULL
        && prepContext->fieldSystem != NULL
        && prepContext->fieldSystem->location != NULL
        && prepContext->fieldSystem->map_events != NULL) {
        u16 mapId = prepContext->fieldSystem->location->mapId;

        if (MapHeader_HasWildEncounters(mapId)
            && datId == MapHeader_GetWildEncounterBank(mapId)) {
            memcpy(
                dest,
                prepContext->fieldSystem->map_events->wildEncounters,
                MAP_EVENTS_WILD_ENCOUNTERS_SIZE);
            return TRUE;
        }
    }

    ArchiveDataLoadOfs(dest, arcId, datId, offset, size);
    return TRUE;
}

static BOOL OverworldWildSpawns_HelperTryGetEncounterDataId(
    void *context,
    int *encounterDataId)
{
    OverworldWildSpawnPrepContext *prepContext = (OverworldWildSpawnPrepContext *)context;

    return OverworldWildSpawns_TryGetEncounterDataId(
        prepContext->fieldSystem,
        encounterDataId);
}

static int OverworldWildSpawns_HelperFindSavedShiny(
    void *context,
    OverworldWildSpawnTerrain terrain)
{
    OverworldWildSpawnPrepContext *prepContext = (OverworldWildSpawnPrepContext *)context;

    if (prepContext == NULL
        || prepContext->fieldSystem == NULL
        || prepContext->fieldSystem->location == NULL) {
        return -1;
    }

    return OverworldWildSpawns_FindSavedShiny(
        prepContext->state,
        prepContext->fieldSystem->location->mapId,
        terrain);
}

static void OverworldWildSpawns_HelperLoadSavedShiny(
    void *context,
    int savedShinySlot,
    OverworldWildRolledEncounter *encounter)
{
    OverworldWildSpawnPrepContext *prepContext = (OverworldWildSpawnPrepContext *)context;

    if (prepContext == NULL || encounter == NULL) {
        return;
    }

    OverworldWildSpawns_LoadSavedShinyEncounter(prepContext->state, savedShinySlot, encounter);
}

static const OverworldWildHelperSpawnCallbacks sOverworldWildSpawnPrepCallbacks = {
    OverworldWildSpawns_HelperGetPlayerState,
    OverworldWildSpawns_HelperTryGetSpawnTerrain,
    OverworldWildSpawns_HelperIsTileOccupied,
    OverworldWildSpawns_HelperIsNearActiveSpawn,
    OverworldWildSpawns_HelperGetMapId,
    OverworldWildSpawns_HelperLoadArchiveData,
    OverworldWildSpawns_HelperTryGetEncounterDataId,
    OverworldWildSpawns_HelperFindSavedShiny,
    OverworldWildSpawns_HelperLoadSavedShiny,
};

 static BOOL OverworldWildSpawns_TryPrepareSpawnWithHelper(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    OverworldWildSpawnTerrain terrain,
    int slot,
    OverworldWildPreparedSpawn *prepared)
{
    const OverworldWildHelperOverlayEntry *helperEntry;
    OverworldWildSpawnPrepContext prepContext = { 0 };

    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || prepared == NULL) {
        return FALSE;
    }

    helperEntry = OverworldWildSpawns_GetHelperOverlayEntry();
    if (helperEntry == NULL || helperEntry->tryPrepareSpawn == NULL) {
        return FALSE;
    }

    prepContext.state = state;
    prepContext.fieldSystem = fieldSystem;
    prepContext.terrain = terrain;
    if (!helperEntry->tryPrepareSpawn(
            &sOverworldWildSpawnPrepCallbacks,
            &prepContext,
            terrain,
            slot,
            state->shinySpawned,
            OVERWORLD_WILD_SHINY_BASE_ODDS,
            prepared)) {
        return FALSE;
    }

    if (!OverworldWildSpawns_FinalizePreparedSpawn(state, fieldSystem, terrain, prepared)) {
        return FALSE;
    }

    return TRUE;
}

static BOOL OverworldWildSpawns_TryPrepareEncounterSpawnWithHelper(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    OverworldWildSpawnTerrain terrain,
    int slot,
    const OverworldWildRolledEncounter *encounter,
    BOOL shiny,
    int savedShinySlot,
    BOOL rollPersonality,
    OverworldWildPreparedSpawn *prepared)
{
    const OverworldWildHelperOverlayEntry *helperEntry;
    OverworldWildSpawnPrepContext prepContext = { 0 };

    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || encounter == NULL
        || prepared == NULL) {
        return FALSE;
    }

    helperEntry = OverworldWildSpawns_GetHelperOverlayEntry();
    if (helperEntry == NULL || helperEntry->tryPrepareEncounterSpawn == NULL) {
        return FALSE;
    }

    prepContext.state = state;
    prepContext.fieldSystem = fieldSystem;
    prepContext.terrain = terrain;
    if (!helperEntry->tryPrepareEncounterSpawn(
            &sOverworldWildSpawnPrepCallbacks,
            &prepContext,
            terrain,
            slot,
            encounter,
            shiny,
            savedShinySlot,
            rollPersonality,
            prepared)) {
        return FALSE;
    }

    return OverworldWildSpawns_FinalizePreparedSpawn(state, fieldSystem, terrain, prepared);
}

static void OverworldWildSpawns_ApplySpawnObjectSetup(
    int slot,
    LocalMapObject *object,
    const OverworldWildRolledEncounter *encounter,
    u32 spriteId,
    BOOL shiny,
    const OverworldWildBehaviorProfile *profile)
{
    OverworldWildSpawns_SetObjectPassThrough(object, FALSE);
    MapObject_SetID(object, OW_WILD_OBJECT_ID_START + slot);
    MapObject_SetScript(object, OVERWORLD_WILD_SPAWNS_BATTLE_SCRIPT);
    OverworldWildSpawns_ApplyMovementRange(object, profile->range);
    OverworldWildSpawns_ApplyPokemonRenderParams(
        object,
        encounter->species,
        encounter->form,
        spriteId,
        shiny);
    if (OverworldWildSpawns_IsCanopyHopperProfile(profile)) {
        MapObject_ClearBits(object, BIT_VANISH);
    }
}

static void OverworldWildSpawns_InitSpawnSlotState(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    OverworldWildSpawnTerrain terrain,
    int slot,
    LocalMapObject *object,
    const OverworldWildRolledEncounter *encounter,
    BOOL shiny,
    u8 behaviorClass,
    u8 behaviorLimitKey,
    u8 playerBallCatchValue)
{
    u16 encounterGeneration = state->spawns[slot].encounterGeneration + 1;
    OverworldWildSpawn spawn = {
        object,
        encounter->personality,
        fieldSystem->location->mapId,
        encounter->species,
        encounter->form,
        encounter->level,
        terrain,
        shiny,
        TRUE,
        0,
        0
    };

    if (encounterGeneration == 0) {
        encounterGeneration = 1;
    }
    spawn.objectId = OW_WILD_OBJECT_ID_START + slot;
    spawn.encounterGeneration = encounterGeneration;
    state->spawns[slot] = spawn;
    OW_WILD_RUNTIME(state)->playerBallCatchValues[slot] = playerBallCatchValue;
    OW_WILD_RUNTIME(state)->spawnPresentations.lastKnownX[slot] = (s16)MapObject_GetCurrentX(object);
    OW_WILD_RUNTIME(state)->spawnPresentations.lastKnownY[slot] = (s16)MapObject_GetCurrentY(object);
    OW_WILD_RUNTIME(state)->spawnPresentations.managerRestoreMask &=
        (u16)~OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot);
    OW_WILD_RUNTIME(state)->spawnPresentations.farSamples[slot] = 0;
    OverworldWildSpawns_ResetSlotSpotState(state, slot);
    state->movementBehaviorClasses[slot] = behaviorClass;
    OW_WILD_RUNTIME(state)->movementBehaviorLimitKeys[slot] = behaviorLimitKey;
    OverworldWildSpawns_ClearCachedBehaviorProfile(state, slot);
    OverworldWildSpawns_ApplySpawnPassThroughFlag(state, slot, object);
#if OW_WILD_SPAWNER_MANKEY_TREE_TOP_RENDER_OVERRIDE_SAVE_ENABLED
    sOverworldWildMankeyTreeTopPrioritySavedBits[slot] = 0;
    sOverworldWildMankeyTreeTopPriorityBitsSaved[slot] = FALSE;
#if OW_WILD_SPAWNER_MANKEY_TREE_TOP_DRAW_CALLBACK_OVERRIDE_ENABLED
    sOverworldWildMankeyTreeTopSavedDrawCallbacks[slot] = NULL;
    sOverworldWildMankeyTreeTopDrawCallbacksSaved[slot] = FALSE;
#endif
#endif
    OverworldWildSpawns_ClearMankeyTreeTopCache(state, slot);
    OverworldWildSpawns_UpdateMankeyTreeTopPriorityBits(state, fieldSystem, slot, object);
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_PARAM_TICK
    OverworldWildSpawns_ClearMovementSlotInProgress(state, slot);
#endif
}

static void OverworldWildSpawns_StartSpawnAppearHop(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot)
{
    LocalMapObject *object;
    u8 direction;

    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !state->spawns[slot].active
        || state->spawns[slot].object == NULL) {
        return;
    }

    object = state->spawns[slot].object;
    if (MapObject_IsSingleMovementActive(object)) {
        return;
    }

    direction = object->curFacing <= OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_RIGHT
        ? object->curFacing
        : OW_WILD_MOVEMENT_DIAGNOSTIC_DIRECTION_DOWN;
    state->movementFieldSystem = fieldSystem;
    OverworldWildSpawns_SetObjectFacing(object, direction);
    MapObject_ClearBits(object, BIT_VANISH);
    (void)OverworldWildSpawns_TryStartManualHopEmote(
        state,
        slot,
        object,
        OW_WILD_SPAWNER_SPOT_STATE_CHILL,
        OW_WILD_SPAWNER_SPOT_STATE_CHILL,
        direction,
        OW_WILD_SPAWNER_SPOT_EMOTE_JUMPS_DEFAULT,
        OW_WILD_SPAWNER_SPOT_EMOTE_FRAMES_PER_JUMP
            * OW_WILD_SPAWNER_SPOT_EMOTE_JUMPS_DEFAULT,
        OW_WILD_SPAWNER_BUBBLE_ID_NONE,
        FALSE,
        FALSE);
}

static void OverworldWildSpawns_StartSpawnStartup(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    const OverworldWildSpawnStartup *startup)
{
    if (startup->locomotion == OW_WILD_BEHAVIOR_LOCOMOTION_RUN_FROM_OFF_SCREEN) {
        OverworldWildSpawns_StartSpawnRun(state, fieldSystem, slot, startup->targetX, startup->targetY);
    } else if (startup->locomotion == OW_WILD_BEHAVIOR_LOCOMOTION_HOP_FROM_OFF_SCREEN) {
        OverworldWildSpawns_StartSpawnHop(
            state,
            fieldSystem,
            slot,
            startup->startX,
            startup->startY,
            startup->targetX,
            startup->targetY,
            startup->hopDirection);
    } else if (startup->locomotion == OW_WILD_BEHAVIOR_LOCOMOTION_APPEAR_HOP) {
        OverworldWildSpawns_StartSpawnAppearHop(state, fieldSystem, slot);
    }
}

static u8 OverworldWildSpawns_CountActiveBehaviorLimitKey(
    OverworldWildSpawnState *state,
    u8 behaviorLimitKey)
{
    u8 count = 0;
    int i;

    if (state == NULL) {
        return 0;
    }

    for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
        if (state->spawns[i].active
            && OW_WILD_RUNTIME(state)->movementBehaviorLimitKeys[i] == behaviorLimitKey) {
            count++;
        }
    }

    return count;
}

static BOOL OverworldWildSpawns_IsBehaviorLimitKeyAtOverworldLimit(
    OverworldWildSpawnState *state,
    u8 behaviorLimitKey,
    const OverworldWildBehaviorProfile *profile)
{
    if (profile == NULL || profile->overworldLimit == 0) {
        return FALSE;
    }

    return OverworldWildSpawns_CountActiveBehaviorLimitKey(state, behaviorLimitKey) >= profile->overworldLimit;
}

static BOOL OverworldWildSpawns_SpawnPreparedEncounter(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    OverworldWildSpawnTerrain terrain,
    int slot,
    const OverworldWildPreparedSpawn *prepared)
{
    LocalMapObject *object;
    OverworldWildBehaviorPrimitives behaviorPrimitives;
    u32 spriteId;
    u16 slotMask;
    const OverworldWildRolledEncounter *encounter;
    const OverworldWildBehaviorProfile *behaviorProfile;
    OverworldWildSpawnPosition createPosition;

    if (state == NULL
        || fieldSystem == NULL
        || prepared == NULL
        || prepared->encounter.species == SPECIES_NONE
        || prepared->encounter.level == 0) {
        return FALSE;
    }

    state->movementFieldSystem = fieldSystem;
    encounter = &prepared->encounter;
    behaviorProfile = &prepared->behaviorProfile;
    if (encounter == NULL
        || encounter->species == SPECIES_NONE
        || encounter->level == 0) {
        return FALSE;
    }
    spriteId = OverworldWildSpawns_GetSpriteID(encounter->species, encounter->form);
    createPosition = prepared->position;
    if (prepared->startup.locomotion == OW_WILD_BEHAVIOR_LOCOMOTION_HOP_FROM_OFF_SCREEN) {
        createPosition.startX = prepared->startup.startX;
        createPosition.startY = prepared->startup.startY;
    }
    object = OverworldWildSpawns_CreateObject(
        fieldSystem,
        &createPosition,
        spriteId,
        prepared->shiny,
        behaviorProfile);
    if (object == NULL) {
        return FALSE;
    }

    OverworldWildSpawns_ApplySpawnObjectSetup(
        slot,
        object,
        encounter,
        spriteId,
        prepared->shiny,
        behaviorProfile);

    if (prepared->savedShinySlot >= 0) {
        OverworldWildSpawns_ClearSavedShiny(state, prepared->savedShinySlot);
    }

    OverworldWildSpawns_InitSpawnSlotState(
        state,
        fieldSystem,
        terrain,
        slot,
        object,
        encounter,
        prepared->shiny,
        prepared->behaviorClass,
        prepared->behaviorLimitKey,
        prepared->playerBallCatchValue);
    behaviorPrimitives = OverworldWildSpawns_ResolveBehaviorPrimitives(behaviorProfile);
    slotMask = OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot);
    if (behaviorProfile->alertSpecialAction == OW_WILD_BEHAVIOR_ALERT_SPECIAL_PICKUP_THROW
        || (OverworldWildSpawns_IsCanopyHopperTreeTopProfile(behaviorProfile)
            && behaviorPrimitives.chillLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_HOP)
        || behaviorPrimitives.chillLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_RAM) {
        OW_WILD_RUNTIME(state)->movementFrameDrivenChillMask |= slotMask;
    } else {
        OW_WILD_RUNTIME(state)->movementFrameDrivenChillMask &= ~slotMask;
    }
    if (OverworldWildSpawns_IsFrameDrivenActiveLocomotion(behaviorPrimitives.attentiveLocomotion)) {
        OW_WILD_RUNTIME(state)->movementFrameDrivenActiveMask |= slotMask;
    } else {
        OW_WILD_RUNTIME(state)->movementFrameDrivenActiveMask &= ~slotMask;
    }
    if (behaviorPrimitives.chillLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_PHANTOM_TELEPORT) {
        OW_WILD_RUNTIME(state)->movementChillPhantomMask |= slotMask;
    } else {
        OW_WILD_RUNTIME(state)->movementChillPhantomMask &= ~slotMask;
    }
    if (behaviorPrimitives.attentiveLocomotion == OW_WILD_BEHAVIOR_LOCOMOTION_PHANTOM_TELEPORT) {
        OW_WILD_RUNTIME(state)->movementActivePhantomMask |= slotMask;
    } else {
        OW_WILD_RUNTIME(state)->movementActivePhantomMask &= ~slotMask;
    }

    if (prepared->shiny) {
        state->shinySpawned = TRUE;
        PlaySE(SEQ_SE_PL_KIRAKIRA);
    }
    OverworldWildSpawns_StartSpawnStartup(state, fieldSystem, slot, &prepared->startup);

    return TRUE;
}

static BOOL OverworldWildSpawns_SpawnOne(OverworldWildSpawnState *state, FieldSystem *fieldSystem, OverworldWildSpawnTerrain terrain, int slot)
{
    OverworldWildPreparedSpawn prepared;

    if (!OverworldWildSpawns_TryPrepareSpawnWithHelper(
            state,
            fieldSystem,
            terrain,
            slot,
            &prepared)) {
        return FALSE;
    }

    return OverworldWildSpawns_SpawnPreparedEncounter(
        state,
        fieldSystem,
        terrain,
        slot,
        &prepared);
}

static void OverworldWildSpawns_ApplyHelpChildSpawnState(
    OverworldWildSpawnState *state,
    int slot,
    LocalMapObject *object)
{
    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !state->spawns[slot].active) {
        return;
    }

    if (object == NULL) {
        object = state->spawns[slot].object;
    }

    if (state->movementSpotStates[slot] == OW_WILD_SPAWNER_SPOT_STATE_EMOTING) {
        state->movementEmoteEndStates[slot] = OW_WILD_SPAWNER_SPOT_STATE_ACTIVE;
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
        OverworldWildSpawns_EnsureFrameMovementTask(state, state->movementFieldSystem);
#endif
        return;
    }

    state->movementSpotStates[slot] = OW_WILD_SPAWNER_SPOT_STATE_ACTIVE;
    state->movementRamStepCounters[slot] = 0;
    if (!state->movementSpawnRunActive[slot]
        && !OverworldWildSpawns_IsMovementSlotInProgress(state, slot)
        && (object == NULL || !MapObject_IsSingleMovementActive(object))) {
        OverworldWildSpawns_EnterActiveStateFromGenericAlert(state, slot, object);
    }

#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
    OverworldWildSpawns_EnsureFrameMovementTask(state, state->movementFieldSystem);
#endif
}

static void OverworldWildSpawns_ClearQueuedHelpChildren(OverworldWildSpawnState *state)
{
    OverworldWildOverlayRuntimeState *runtime;

    if (state->movementRuntimeState == NULL) {
        return;
    }

    runtime = OW_WILD_RUNTIME(state);
    runtime->movementHelpSpawnParentSlotPlusOne = 0;
    runtime->movementHelpSpawnRemaining = 0;
    runtime->movementHelpSpawnDelay = 0;
}

static BOOL OverworldWildSpawns_TrySpawnOneHelpChild(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int parentSlot)
{
    int childSlot;
    OverworldWildRolledEncounter encounter = { 0 };
    OverworldWildPreparedSpawn prepared;
    OverworldWildSpawnTerrain terrain;

    if (state == NULL
        || fieldSystem == NULL
        || parentSlot < 0
        || parentSlot >= OW_WILD_MAX_SPAWNS
        || !OverworldWildSpawns_IsMovementFieldContextCurrent(state, fieldSystem)
        || !OverworldWildSpawns_IsCurrentSpawnObject(
            fieldSystem,
            &state->spawns[parentSlot])) {
        return FALSE;
    }
    if (!OverworldWildSpawns_TryGetFreeSlot(state, 0, OW_WILD_MAX_SPAWNS, &childSlot)) {
        return FALSE;
    }

    terrain = (OverworldWildSpawnTerrain)state->spawns[parentSlot].terrain;
    encounter.species = state->spawns[parentSlot].species;
    encounter.form = state->spawns[parentSlot].form;
    encounter.level = state->spawns[parentSlot].level;
    if (!OverworldWildSpawns_TryPrepareEncounterSpawnWithHelper(
            state,
            fieldSystem,
            terrain,
            childSlot,
            &encounter,
            FALSE,
            -1,
            TRUE,
            &prepared)
        || !OverworldWildSpawns_SpawnPreparedEncounter(
            state,
            fieldSystem,
            terrain,
            childSlot,
            &prepared)) {
        return FALSE;
    }

    OverworldWildSpawns_ApplyHelpChildSpawnState(
        state,
        childSlot,
        state->spawns[childSlot].object);
    return TRUE;
}

static void OverworldWildSpawns_TickQueuedHelpChildren(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem)
{
    OverworldWildOverlayRuntimeState *runtime;
    int parentSlot;

    if (state->movementRuntimeState == NULL) {
        return;
    }

    runtime = OW_WILD_RUNTIME(state);
    if (runtime->movementHelpSpawnParentSlotPlusOne == 0
        || runtime->movementHelpSpawnRemaining == 0) {
        return;
    }
    if (runtime->movementHelpSpawnDelay != 0) {
        runtime->movementHelpSpawnDelay--;
        return;
    }

    parentSlot = runtime->movementHelpSpawnParentSlotPlusOne - 1;
    if (!OverworldWildSpawns_TrySpawnOneHelpChild(state, fieldSystem, parentSlot)) {
        OverworldWildSpawns_ClearQueuedHelpChildren(state);
        return;
    }

    runtime->movementHelpSpawnRemaining--;
    if (runtime->movementHelpSpawnRemaining == 0) {
        OverworldWildSpawns_ClearQueuedHelpChildren(state);
        return;
    }

    runtime->movementHelpSpawnDelay = OW_WILD_SPAWNER_HELP_SPAWN_FRAME_GAP;
}

static void OverworldWildSpawns_TrySpawnHelpChildren(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot,
    const OverworldWildBehaviorProfile *profile)
{
    OverworldWildOverlayRuntimeState *runtime;
    u8 spawnCount = OW_WILD_SPAWNER_SWARM_MAX_EXTRA_SPAWNS;
    u8 activeCount;

    if (!OverworldWildSpawns_IsMovementFieldContextCurrent(state, fieldSystem)) {
        return;
    }

    runtime = OverworldWildSpawns_EnsureRuntimeState(state);
    if (runtime == NULL) {
        return;
    }
    if (runtime->movementHelpSpawnRemaining != 0) {
        return;
    }

    if (profile->overworldLimit != 0) {
        activeCount = OverworldWildSpawns_CountActiveBehaviorLimitKey(
            state,
            runtime->movementBehaviorLimitKeys[slot]);
        if (activeCount >= profile->overworldLimit) {
            return;
        }
        spawnCount = profile->overworldLimit - activeCount;
    }

    runtime->movementHelpSpawnParentSlotPlusOne = (u8)(slot + 1);
    runtime->movementHelpSpawnRemaining = spawnCount;
    runtime->movementHelpSpawnDelay = 0;

#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
    OverworldWildSpawns_EnsureFrameMovementTask(state, fieldSystem);
#endif
}

static void OverworldWildSpawns_DespawnFarMons(OverworldWildSpawnState *state, FieldSystem *fieldSystem)
{
    OverworldWildOverlayRuntimeState *runtime = OW_WILD_RUNTIME(state);
    const OverworldWildHelperOverlayEntry *helperEntry =
        OverworldWildSpawns_GetHelperOverlayEntry();
    u16 movementProtectedMask =
        OverworldWildSpawns_GetThrowParticipantMask(state) | state->movementInProgressMask;

    if (runtime->throwState.targetMask != 0) {
        movementProtectedMask = 0xFFFF;
    }

    if (helperEntry != NULL && helperEntry->despawnFarEncounters != NULL) {
        helperEntry->despawnFarEncounters(
            fieldSystem,
            state,
            &OW_WILD_RUNTIME(state)->spawnPresentations,
            &OW_WILD_RUNTIME(state)->despawnTelemetry,
            movementProtectedMask,
            OverworldWildSpawns_ResetSlotState);
    }
}

static void OverworldWildSpawns_TryRefill(OverworldWildSpawnState *state, FieldSystem *fieldSystem)
{
    int slot;
    u8 rollMask = 0;
    BOOL spawned = FALSE;

    if (state->spawnCooldown != 0) {
        state->spawnCooldown--;
        return;
    }

    if (state->headbuttSpawnCooldown != 0) {
        state->headbuttSpawnCooldown--;
    } else {
        state->headbuttSpawnCooldown = OW_WILD_HEADBUTT_REFILL_ATTEMPT_COOLDOWN;
        if ((gf_rand() % 100) < OW_WILD_HEADBUTT_SPAWN_CHANCE_PERCENT) {
            rollMask |= 1;
        }
    }

    if (state->fishingSpawnCooldown != 0) {
        state->fishingSpawnCooldown--;
    } else {
        state->fishingSpawnCooldown = OW_WILD_FISHING_REFILL_ATTEMPT_COOLDOWN;
        if ((gf_rand() % 100) < OW_WILD_FISHING_SPAWN_CHANCE_PERCENT) {
            rollMask |= 2;
        }
    }

    if ((rollMask & 1)
        && OverworldWildSpawns_TryGetFreeSlot(state, OW_WILD_HEADBUTT_SLOT_START, OW_WILD_FISH_SLOT_START, &slot)) {
        spawned = OverworldWildSpawns_SpawnOne(
            state,
            fieldSystem,
            OW_WILD_SPAWN_TERRAIN_HEADBUTT,
            slot);
    }

    if (!spawned
        && (rollMask & 2)
        && OverworldWildSpawns_TryGetFreeSlot(state, OW_WILD_FISH_SLOT_START, OW_WILD_MAX_SPAWNS, &slot)) {
        spawned = OverworldWildSpawns_SpawnOne(
            state,
            fieldSystem,
            OW_WILD_SPAWN_TERRAIN_FISHING,
            slot);
    }

    if (!spawned && OverworldWildSpawns_TryGetFreeSlot(state, 0, OW_WILD_GRASS_MAX_SPAWNS, &slot)) {
        spawned = OverworldWildSpawns_SpawnOne(state, fieldSystem, OW_WILD_SPAWN_TERRAIN_LAND, slot);
    }

    if (!spawned && OverworldWildSpawns_TryGetFreeSlot(state, OW_WILD_GRASS_MAX_SPAWNS, OW_WILD_GRASS_MAX_SPAWNS + OW_WILD_SURF_MAX_SPAWNS, &slot)) {
        spawned = OverworldWildSpawns_SpawnOne(state, fieldSystem, OW_WILD_SPAWN_TERRAIN_SURF, slot);
    }

    if (spawned) {
        state->justSpawned = TRUE;
        state->spawnCooldown = OW_WILD_REFILL_COOLDOWN_STEPS;
    }
}

static BOOL OverworldWildSpawns_IsTouchingPlayer(OverworldWildSpawnState *state, FieldSystem *fieldSystem, int slot)
{
    const OverworldWildSpawn *spawn = &state->spawns[slot];
    int dx;
    int dy;

    if (!spawn->active || spawn->object == NULL) {
        return FALSE;
    }

#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_PARAM_TICK
    if (OverworldWildSpawns_GetThrowParticipantMask(state) & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot)) {
        return FALSE;
    }
    if (state->movementSpawnRunActive[slot]) {
        return FALSE;
    }
    if (state->movementSpotStates[slot] == OW_WILD_SPAWNER_SPOT_STATE_CHILL
        && state->movementSpotCooldowns[slot] != 0) {
        return FALSE;
    }
    if (OverworldWildSpawns_IsMovementSlotInProgress(state, slot)
        || MapObject_IsSingleMovementActive(spawn->object)) {
        return FALSE;
    }
#endif

    dx = (int)MapObject_GetCurrentX(spawn->object) - GetPlayerXCoord(fieldSystem->playerAvatar);
    dy = (int)MapObject_GetCurrentY(spawn->object) - GetPlayerYCoord(fieldSystem->playerAvatar);

    if (dx < 0) {
        dx = -dx;
    }
    if (dy < 0) {
        dy = -dy;
    }

    return (dx + dy) == 1;
}

static BOOL OverworldWildSpawns_HasPendingBattle(OverworldWildSpawnState *state)
{
    return state->pendingSlot >= 0;
}

static BOOL OverworldWildSpawns_HasQueuedBattle(OverworldWildSpawnState *state)
{
    return state->movementQueuedBattleSlot >= 0
        && state->movementQueuedBattleSlot < OW_WILD_MAX_SPAWNS;
}

static u8 OverworldWildSpawns_GetBattleTriggerForSpotState(
    const OverworldWildBehaviorProfile *profile,
    u8 spotState)
{
    if (profile == NULL) {
        return OW_WILD_BEHAVIOR_BATTLE_TRIGGER_NONE;
    }

    if (spotState == OW_WILD_SPAWNER_SPOT_STATE_ACTIVE) {
        return profile->attentiveBattle;
    }

    return OW_WILD_BEHAVIOR_BATTLE_TRIGGER_NONE;
}

static BOOL OverworldWildSpawns_IsSlotStableForBattle(OverworldWildSpawnState *state, int slot)
{
    OverworldWildBehaviorProfile profile;
    OverworldWildBehaviorPrimitives primitives;
    LocalMapObject *object;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !state->spawns[slot].active
        || state->spawns[slot].object == NULL) {
        return FALSE;
    }

    object = state->spawns[slot].object;
    if ((OverworldWildSpawns_GetThrowParticipantMask(state) & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot)) != 0
        || state->movementSpawnRunActive[slot]
        || state->movementStagedHopPending[slot]
        || state->movementRamCrashShakeTimers[slot] != 0
        || state->movementSpotStates[slot] == OW_WILD_SPAWNER_SPOT_STATE_EMOTING
        || state->movementSpotStates[slot] == OW_WILD_SPAWNER_SPOT_STATE_TIRED
        || OverworldWildSpawns_IsMovementSlotInProgress(state, slot)
        || MapObject_IsSingleMovementActive(object)) {
        return FALSE;
    }
    profile = OverworldWildSpawns_GetBehaviorProfile(state, slot);
    primitives = OverworldWildSpawns_ResolveBehaviorPrimitives(&profile);
    if (primitives.attentiveLocomotion != OW_WILD_BEHAVIOR_LOCOMOTION_PHANTOM_TELEPORT) {
        return TRUE;
    }

    if (state->movementPhantomHidden[slot]
        || state->movementPhantomFlickerTimers[slot] != 0
        || state->movementPhantomTeleportHasTarget[slot]) {
        return FALSE;
    }

    return TRUE;
}

static void OverworldWildSpawns_PrepareSlotForBattle(OverworldWildSpawnState *state, int slot)
{
    OverworldWildBehaviorProfile profile;
    OverworldWildBehaviorPrimitives primitives;

    if (state == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || !state->spawns[slot].active) {
        return;
    }

    profile = OverworldWildSpawns_GetBehaviorProfile(state, slot);
    primitives = OverworldWildSpawns_ResolveBehaviorPrimitives(&profile);
    if (primitives.attentiveLocomotion != OW_WILD_BEHAVIOR_LOCOMOTION_PHANTOM_TELEPORT) {
        return;
    }

    OverworldWildSpawns_RevealPhantomStalker(state, slot, state->spawns[slot].object);
}

static void OverworldWildSpawns_PrimePendingBattleForSlot(OverworldWildSpawnState *state, int slot)
{
    if (state == NULL || slot < 0 || slot >= OW_WILD_MAX_SPAWNS) {
        return;
    }

    state->movementBattleSettleFrames = 0;
    state->movementQueuedBattleSlot = -1;
    state->pendingPersonality = state->spawns[slot].personality;
    state->pendingSpecies = state->spawns[slot].species | (state->spawns[slot].form << OW_WILD_FORM_SHIFT);
    state->pendingLevel = state->spawns[slot].level;
    state->pendingShiny = state->spawns[slot].shiny;
    state->pendingSlot = slot;
    state->pendingMapGeneration = state->mapGeneration;
    state->pendingEncounterGeneration = state->spawns[slot].encounterGeneration;
    state->spawnCooldown = OW_WILD_REFILL_COOLDOWN_STEPS;
    state->battleGraceSteps = 0;
}

static BOOL OverworldWildSpawns_IsPlayerStableForBattle(FieldSystem *fieldSystem)
{
    return fieldSystem->playerAvatar->mapObject->movementStep == 0;
}

static BOOL OverworldWildSpawns_TryStartBattleForSlot(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot)
{
    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || OverworldWildSpawns_HasPendingBattle(state)
        || OverworldWildSpawns_IsPlayerBallProjectileActive()
        || OW_WILD_RUNTIME(state)->throwState.targetMask != 0
        || state->movementInProgressMask != 0
        || !OverworldWildSpawns_IsPlayerStableForBattle(fieldSystem)
        || !OverworldWildSpawns_IsCurrentSpawnObject(fieldSystem, &state->spawns[slot])) {
        return FALSE;
    }

    if (!OverworldWildSpawns_IsSlotStableForBattle(state, slot)) {
        return FALSE;
    }
    OverworldWildSpawns_PrepareSlotForBattle(state, slot);
    OverworldWildSpawns_ResetAllMovementCommands(state, TRUE, FALSE);
    state->movementFieldSystem = fieldSystem;

    OverworldWildSpawns_PrimePendingBattleForSlot(state, slot);

    if (!OverworldWildSpawns_RequestBattleScript(fieldSystem, state, slot)) {
        OverworldWildSpawns_ResetPendingBattle(state);
        return FALSE;
    }
    return TRUE;
}

static BOOL OverworldWildSpawns_QueueBattleForSlot(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot)
{
    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || OverworldWildSpawns_HasPendingBattle(state)
        || OverworldWildSpawns_IsPlayerBallProjectileActive()
        || OW_WILD_RUNTIME(state)->throwState.targetMask != 0
        || (OverworldWildSpawns_GetThrowParticipantMask(state) & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot)) != 0
        || !OverworldWildSpawns_IsMovementFieldContextCurrent(state, fieldSystem)
        || !OverworldWildSpawns_IsCurrentSpawnObject(fieldSystem, &state->spawns[slot])) {
        return FALSE;
    }

    state->movementQueuedBattleSlot = slot;
    state->movementBattleSettleFrames = OW_WILD_SPAWNER_BATTLE_SETTLE_FRAMES;
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
    OverworldWildSpawns_EnsureFrameMovementTask(state, fieldSystem);
#endif
    return TRUE;
}

static BOOL OverworldWildSpawns_TryStartBattleForSlotOrQueue(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot)
{
    if (OverworldWildSpawns_TryStartBattleForSlot(state, fieldSystem, slot)) {
        return TRUE;
    }

    return OverworldWildSpawns_QueueBattleForSlot(state, fieldSystem, slot);
}

static BOOL OverworldWildSpawns_TryStartQueuedBattle(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem)
{
    int slot;

    if (OverworldWildSpawns_HasPendingBattle(state)
        || OverworldWildSpawns_IsPlayerBallProjectileActive()
        || !OverworldWildSpawns_HasQueuedBattle(state)) {
        return FALSE;
    }

    slot = state->movementQueuedBattleSlot;
    if (!OverworldWildSpawns_IsMovementFieldContextCurrent(state, fieldSystem)
        || !OverworldWildSpawns_IsCurrentSpawnObject(fieldSystem, &state->spawns[slot])) {
        state->movementQueuedBattleSlot = -1;
        return FALSE;
    }
    if (state->movementBattleSettleFrames != 0) {
        state->movementBattleSettleFrames--;
        return FALSE;
    }

    if (!OverworldWildSpawns_IsSlotStableForBattle(state, slot)) {
        state->movementBattleSettleFrames = OW_WILD_SPAWNER_BATTLE_SETTLE_FRAMES;
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
        OverworldWildSpawns_EnsureFrameMovementTask(state, fieldSystem);
#endif
        return FALSE;
    }

    return OverworldWildSpawns_TryStartBattleForSlot(state, fieldSystem, slot);
}

static BOOL OverworldWildSpawns_TryPrimeBattleFromTalkSlot(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem,
    int slot)
{
    if (state == NULL
        || fieldSystem == NULL
        || slot < 0
        || slot >= OW_WILD_MAX_SPAWNS
        || OverworldWildSpawns_HasPendingBattle(state)
        || OverworldWildSpawns_IsPlayerBallProjectileActive()
        || OW_WILD_RUNTIME(state)->throwState.targetMask != 0
        || (OverworldWildSpawns_GetThrowParticipantMask(state) & OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(slot)) != 0
        || !OverworldWildSpawns_IsCurrentSpawnObject(fieldSystem, &state->spawns[slot])) {
        return FALSE;
    }

    if (!OverworldWildSpawns_IsPlayerStableForBattle(fieldSystem)) {
        return FALSE;
    }

    OverworldWildSpawns_PrepareSlotForBattle(state, slot);
    OverworldWildSpawns_ResetAllMovementCommands(state, TRUE, FALSE);
    state->movementFieldSystem = fieldSystem;
    OverworldWildSpawns_PrimePendingBattleForSlot(state, slot);
    return TRUE;
}

static int OverworldWildSpawns_FindBattleTalkSlot(
    FieldSystem *fieldSystem,
    OverworldWildSpawnState *state,
    LocalMapObject *talkedObject)
{
    if (state == NULL || state->movementRuntimeState == NULL) {
        return -1;
    }
    {
        const OverworldWildHelperOverlayEntry *helperEntry =
            OverworldWildSpawns_GetHelperOverlayEntry();

        if (helperEntry == NULL) {
            return -1;
        }
        return helperEntry->findBattleTalkSlot(
            fieldSystem,
            state,
            talkedObject,
            OverworldWildSpawns_GetThrowParticipantMask(state));
    }
}

static BOOL OverworldWildSpawns_OverlayTryPrimeBattleFromTalk(
    FieldSystem *fieldSystem,
    OverworldWildSpawnState *state,
    LocalMapObject *talkedObject)
{
    int slot = OverworldWildSpawns_FindBattleTalkSlot(fieldSystem, state, talkedObject);

    if (slot < 0) {
        return FALSE;
    }

    return OverworldWildSpawns_TryPrimeBattleFromTalkSlot(state, fieldSystem, slot);
}

static BOOL OverworldWildSpawns_TryStartBattleFromAButton(
    OverworldWildSpawnState *state,
    FieldSystem *fieldSystem)
{
    LocalMapObject *facingObject = NULL;
    int slot;

    if ((PAD_Read() & PAD_BUTTON_A) == 0) {
        state->movementAButtonDown = FALSE;
        return FALSE;
    }
    if (state->movementAButtonDown) {
        return FALSE;
    }

    slot = OverworldWildSpawns_FindBattleTalkSlot(fieldSystem, state, NULL);
    if (slot < 0) {
        return FALSE;
    }

    if (sub_0203DC64(fieldSystem, &facingObject)
        && facingObject != state->spawns[slot].object) {
        state->movementAButtonDown = TRUE;
        return FALSE;
    }

    if (!OverworldWildSpawns_TryStartBattleForSlotOrQueue(state, fieldSystem, slot)) {
        return FALSE;
    }

    state->movementAButtonDown = TRUE;
    return TRUE;
}

static BOOL OverworldWildSpawns_TryStartBattle(OverworldWildSpawnState *state, FieldSystem *fieldSystem, BOOL decrementBattleGrace)
{
    int i;

    if (state->battleGraceSteps != 0) {
        if (decrementBattleGrace) {
            state->battleGraceSteps--;
        }
        return FALSE;
    }

    if (OverworldWildSpawns_HasPendingBattle(state)) {
        return FALSE;
    }

    if (state->justSpawned) {
        state->justSpawned = FALSE;
        return FALSE;
    }

    for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
        OverworldWildBehaviorProfile profile;
        u8 battleTrigger;

        if (!state->spawns[i].active) {
            continue;
        }
        if (!OverworldWildSpawns_IsCurrentSpawnObject(
                fieldSystem,
                &state->spawns[i])) {
            state->presentationRestorePending = TRUE;
            continue;
        }

        if (!OverworldWildSpawns_IsTouchingPlayer(state, fieldSystem, i)) {
            continue;
        }

        profile = OverworldWildSpawns_GetMovementTickProfile(state, i);
        battleTrigger = OverworldWildSpawns_GetBattleTriggerForSpotState(
            &profile,
            state->movementSpotStates[i]);
        if (battleTrigger == OW_WILD_BEHAVIOR_BATTLE_TRIGGER_CONTACT) {
            return OverworldWildSpawns_TryStartBattleForSlotOrQueue(state, fieldSystem, i);
        }
    }

    return FALSE;
}

static u8 OverworldWildSpawns_OverlayCleanupPendingBattle(
    FieldSystem *fieldSystem,
    OverworldWildSpawnState *state,
    u16 battleResult)
{
    const OverworldWildHelperOverlayEntry *helperEntry;
    u8 disposition = OW_WILD_BATTLE_DISPOSITION_RETAIN;

    if (OverworldWildSpawns_EnsureRuntimeState(state) == NULL) {
        if (state != NULL) {
            OverworldWildSpawns_ResetPendingBattle(state);
            state->movementQueuedBattleSlot = -1;
            state->movementBattleSettleFrames = 0;
        }
        return OW_WILD_BATTLE_DISPOSITION_RETAIN;
    }
    helperEntry = OverworldWildSpawns_GetHelperOverlayEntry();
    if (helperEntry != NULL && helperEntry->finishBattle != NULL) {
        disposition = helperEntry->finishBattle(
            fieldSystem,
            state,
            &OW_WILD_RUNTIME(state)->spawnPresentations,
            &OW_WILD_RUNTIME(state)->despawnTelemetry,
            battleResult,
            OverworldWildSpawns_ResetSlotState);
    }

    if (state->pendingSlot >= 0 && state->pendingSlot < OW_WILD_MAX_SPAWNS) {
        int slot = state->pendingSlot;

        if (disposition == OW_WILD_BATTLE_DISPOSITION_FLED
            || disposition == OW_WILD_BATTLE_DISPOSITION_RETAIN) {
            state->battleGraceSteps = OW_WILD_FLEE_GRACE_STEPS;
            if (disposition == OW_WILD_BATTLE_DISPOSITION_FLED
                && OverworldWildSpawns_IsMovementFieldContextCurrent(state, fieldSystem)
                && OverworldWildSpawns_IsCurrentSpawnObject(fieldSystem, &state->spawns[slot])) {
                state->movementFieldSystem = fieldSystem;
                OverworldWildSpawns_StartTiredEmote(state, slot);
            }
        }
    }

    OverworldWildSpawns_ResetPendingBattle(state);
    state->movementAButtonDown = (PAD_Read() & PAD_BUTTON_A) != 0;

#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_PARAM_TICK && OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
    if (fieldSystem != NULL
        && fieldSystem->taskman == NULL
        && OverworldWildSpawns_IsMovementFieldContextCurrent(state, fieldSystem)
        && OverworldWildSpawns_GetCurrentMovementSpawnMask(state, fieldSystem) != 0) {
        OverworldWildSpawns_EnsureFrameMovementTask(state, fieldSystem);
    }
#endif
    return disposition;
}

static BOOL OverworldWildSpawns_UpdateMapState(FieldSystem *fieldSystem, OverworldWildSpawnState *state)
{
    MapObjectMan *mapObjectMan = (MapObjectMan *)fieldSystem->mapObjectMan;
    void *mapObjects = mapObjectMan != NULL ? mapObjectMan->objects : NULL;

#if OW_WILD_UPDATE_DIAGNOSTIC_READ_ONLY
    (void)state;
    sOverworldWildDiagnosticMapObjectMan = mapObjectMan;
    sOverworldWildDiagnosticMapObjects = mapObjects;
    return OverworldWildSpawns_IsEnabledMap(fieldSystem);
#endif

#if OW_WILD_UPDATE_DIAGNOSTIC_STATE_READ_ONLY
    sOverworldWildDiagnosticMapObjectMan = mapObjectMan;
    sOverworldWildDiagnosticMapObjects = mapObjects;
    sOverworldWildDiagnosticStateMapId = state != NULL ? state->mapId : MAP_NOTHING;
    sOverworldWildDiagnosticStateMapObjectMan = state != NULL ? state->mapObjectMan : NULL;
    sOverworldWildDiagnosticStateMapObjects = state != NULL ? state->mapObjects : NULL;
    return OverworldWildSpawns_IsEnabledMap(fieldSystem);
#endif

#if OW_WILD_UPDATE_DIAGNOSTIC_SETTER_ONLY
    (void)state;
    sOverworldWildDiagnosticMapObjectMan = mapObjectMan;
    sOverworldWildDiagnosticMapObjects = mapObjects;
    OverworldWildCustomMovement_SetFieldSystem(fieldSystem);
    return OverworldWildSpawns_IsEnabledMap(fieldSystem);
#endif

    OverworldWildSpawns_LoadSavedShinies(state, fieldSystem);

    if (!OverworldWildSpawns_IsEnabledMap(fieldSystem)) {
        OverworldWildCustomMovement_SetFieldSystem(NULL);
        OverworldWildSpawns_DetachAllMovementStateOnContextLoss(state, FALSE);
#if OW_WILD_UPDATE_DIAGNOSTIC_SKIP_CLEAR
        OverworldWildSpawns_ClearContextLite(state);
#endif
        state->mapId = MAP_NOTHING;
        state->mapObjectMan = NULL;
        state->mapObjects = NULL;
        state->presentationRestorePending = FALSE;
        return FALSE;
    }

    if (state->mapId != fieldSystem->location->mapId) {
#if OW_WILD_UPDATE_DIAGNOSTIC_SKIP_CLEAR
        OverworldWildSpawns_ClearContextLite(state);
#endif
        OverworldWildSpawns_DetachAllMovementStateOnContextLoss(state, FALSE);
#if OW_WILD_UPDATE_DIAGNOSTIC_SKIP_CLEAR
        state->mapGeneration++;
        if (state->mapGeneration == 0) {
            state->mapGeneration = 1;
        }
        state->mapId = fieldSystem->location->mapId;
        state->mapObjectMan = mapObjectMan;
        state->mapObjects = mapObjects;
#else
        OverworldWildSpawns_Clear(state, FALSE);
        state->mapId = fieldSystem->location->mapId;
        state->mapObjectMan = mapObjectMan;
        state->mapObjects = mapObjects;
#endif
    }

    if (state->mapObjectMan != mapObjectMan || state->mapObjects != mapObjects) {
        int i;

        OverworldWildSpawns_DetachAllMovementStateOnContextLoss(state, FALSE);
        state->presentationRestorePending = TRUE;
        for (i = 0; i < OW_WILD_MAX_SPAWNS; i++) {
            if (state->spawns[i].active) {
                OW_WILD_RUNTIME(state)->spawnPresentations.managerRestoreMask |=
                    OW_WILD_SPAWNER_MOVEMENT_SLOT_MASK(i);
                OW_WILD_RUNTIME(state)->spawnPresentations.farSamples[i] = 0;
                state->spawns[i].object = NULL;
            }
        }
        state->mapObjectMan = mapObjectMan;
        state->mapObjects = mapObjects;
    }

    OverworldWildCustomMovement_SetFieldSystem(fieldSystem);
    return TRUE;
}

static BOOL OverworldWildSpawns_OverlayOnPlayerStep(
    FieldSystem *fieldSystem,
    OverworldWildSpawnState *state,
    OverworldWildResidentData *residentData)
{
    MapObjectMan *mapObjectMan;
    u16 currentSpawnMask;
    BOOL resumeOnly = residentData->pendingFlags
        == OW_WILD_FIELD_IDLE_REARM_PENDING;

#if OW_WILD_STEP_DIAGNOSTIC_ENTRY_ONLY
    (void)fieldSystem;
    (void)state;
    return FALSE;
#endif

    if (OverworldWildSpawns_EnsureRuntimeState(state) == NULL) {
        return FALSE;
    }
    OW_WILD_RUNTIME(state)->residentData = residentData;

    mapObjectMan = (MapObjectMan *)fieldSystem->mapObjectMan;
    if (resumeOnly
        && state->mapId == fieldSystem->location->mapId
        && (state->mapObjectMan != mapObjectMan
            || mapObjectMan == NULL
            || state->mapObjects != mapObjectMan->objects)) {
        residentData->pendingFlags |=
            OW_WILD_FIELD_IDLE_ZERO_REFILL_PENDING;
        resumeOnly = FALSE;
        state->battleGraceSteps = OW_WILD_FIELD_READY_DELAY_FRAMES;
    }

    if (!OverworldWildSpawns_UpdateMapState(fieldSystem, state)) {
        if (fieldSystem->mapObjectMan != NULL
            && fieldSystem->playerAvatar != NULL
            && IsOverlayLoaded(OVERLAY_OVERWORLD_WILD_HELPER)) {
            residentData->pendingFlags = 0;
        }
        return FALSE;
    }
#if OW_WILD_STEP_DIAGNOSTIC_UPDATE_ONLY
    return FALSE;
#endif

    if (state->battleGraceSteps != 0) {
        return FALSE;
    }
    currentSpawnMask = OverworldWildSpawns_GetCurrentMovementSpawnMask(
        state,
        fieldSystem);
    if (state->presentationRestorePending
        || OW_WILD_RUNTIME(state)->spawnPresentations.managerRestoreMask != 0
        || !sOverworldWildHelperOverlayReady) {
        if (!OverworldWildSpawns_ReconcileSpawnPresentations(state, fieldSystem)) {
            return FALSE;
        }
        currentSpawnMask = OverworldWildSpawns_GetCurrentMovementSpawnMask(
            state,
            fieldSystem);
    }
    if (resumeOnly) {
        goto ensure_movement_task;
    }

#if OW_WILD_STEP_DIAGNOSTIC_DROP_STALE_ONLY
    return FALSE;
#endif

    if (OverworldWildSpawns_TryStartCustomJumpRamProbe(state, fieldSystem)) {
        return TRUE;
    }

    if (OverworldWildSpawns_TryStartBattle(state, fieldSystem, TRUE)) {
        return TRUE;
    }

#if OW_WILD_STEP_DIAGNOSTIC_BATTLE_ONLY
    return FALSE;
#endif

#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_PARAM_TICK
#if !OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
    OverworldWildSpawns_TickMovementParams(
        state,
        fieldSystem,
        FALSE,
        0
#if OW_WILD_SPAWNER_IMMEDIATE_AI_AFTER_COMMAND_COMPLETION
        , 0
#endif
    );
#endif
    if (OverworldWildSpawns_HasPendingBattle(state)) {
        return TRUE;
    }
#endif

#if !OW_WILD_STEP_DIAGNOSTIC_SKIP_AMBIENT_CRY
    OverworldWildSpawns_TryPlayAmbientCry(state);
#endif

    if (OW_WILD_RUNTIME(state)->playerStepMaintenanceState
        == OW_WILD_PLAYER_STEP_MAINTENANCE_NONE) {
        OW_WILD_RUNTIME(state)->playerStepMaintenanceState =
            OW_WILD_PLAYER_STEP_MAINTENANCE_QUEUED;
    } else {
        OW_WILD_RUNTIME(state)->playerStepMaintenanceState |=
            OW_WILD_PLAYER_STEP_MAINTENANCE_DIRTY;
    }
ensure_movement_task:
#if OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_PARAM_TICK && OW_WILD_SPAWNER_MOVEMENT_DIAGNOSTIC_FRAME_TASK
    if (OW_WILD_RUNTIME(state)->playerStepMaintenanceState
            != OW_WILD_PLAYER_STEP_MAINTENANCE_NONE
        || currentSpawnMask != 0) {
        OverworldWildSpawns_EnsureFrameMovementTask(state, fieldSystem);
        if (sOverworldWildMovementFrameTask == NULL) {
            /* The root monitor will retry the intact queued maintenance. */
            residentData->pendingFlags =
                OW_WILD_FIELD_IDLE_REARM_PENDING;
            return FALSE;
        }
    }
#endif
    residentData->pendingFlags = 0;
    return FALSE;
}

#endif // IMPLEMENT_OVERWORLD_WILD_SPAWNS
