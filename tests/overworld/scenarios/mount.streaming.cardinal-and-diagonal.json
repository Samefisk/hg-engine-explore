{
  "schemaVersion": 1,
  "id": "mount.streaming.cardinal-and-diagonal",
  "title": "Mounted Walk streams every crossed cardinal and diagonal axis",
  "status": "active",
  "capabilities": ["world.streaming", "walk.diagonal"],
  "proofLevel": "S5",
  "costTier": 5,
  "fixture": {"rom": "test.nds", "save": {"path": "test.dsv", "kind": "dsv"}, "seed": 0},
  "events": [
    {"at": 0, "kind": "input", "value": "Hold cardinal Walk across multiple streaming boundaries"},
    {"at": 1, "kind": "input", "value": "Hold diagonal Walk across both loader axes"},
    {"at": 2, "kind": "input", "value": "Request dismount during the final stream drain"}
  ],
  "stop": {"frameBudget": 6000, "condition": "Both routes finish, loader drains, and detach completes"},
  "expect": {
    "requiredEvents": [],
    "forbiddenEvents": [],
    "invariants": ["Cardinal path advances do not skip stream anchors", "Each diagonal tile loads both required axes", "The player stream pointer is restored before detach", "No control lock remains"]
  },
  "capture": "failure",
  "adapter": {
    "kind": "command-sequence",
    "commands": [
      ["{python}", "scripts/verify_overworld_walk_runtime.py", "--scenario", "cyndaquil_streaming_stress"],
      ["{python}", "scripts/verify_overworld_walk_runtime.py", "--scenario", "mounted_diagonal_streaming"]
    ],
    "result": "json-passed"
  }
}
